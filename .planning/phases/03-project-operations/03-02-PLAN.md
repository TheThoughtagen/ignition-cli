---
phase: 03-project-operations
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - Cargo.toml
  - crates/ignition-core/src/client/mod.rs
  - crates/ignition-core/src/client/projects.rs
  - crates/ignition-core/src/error.rs
  - crates/ignition-core/src/actions/projects.rs
  - crates/ignition-core/tests/projects_contract.rs
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/src/render.rs
  - crates/ignition-cli/tests/contract_projects.rs
  - crates/ignition-core/tests/live_gateway.rs
  - README.md
autonomous: true

must_haves:
  truths:
    - "User exports a project: `ign project export NAME [-o FILE]` — the ZIP STREAMS to disk chunk-by-chunk (no Vec<u8> accumulation) under a 120 s per-request timeout; stdout stays data-only (never a raw ZIP)"
    - "User imports: `ign project import NAME --file PATH|--file -` — file or stdin is read fully, uploaded as raw bytes with Content-Type application/zip + known Content-Length + `overwrite=<bool>` query param, under a 300 s per-request timeout (Pitfall 3)"
    - "Default collision policy abort: importing over an existing project exits 6 with the additive `project_exists` slug and a hint naming `--collision-policy overwrite` — BEFORE any upload (find pre-check)"
    - "`--collision-policy overwrite` is destructive: refused without --yes (exit 2, profile null, pre-resolution) and its hint warns that overwrite REPLACES the entire project (replace, not merge — Pitfall 4)"
    - "Both export and import JSON data carry `scope: {includes, excludes}` naming tag-providers/tags/UDTs as excluded (roadmap success criterion 4)"
    - "A non-ZIP file (missing PK\\x03\\x04 magic) or >512 MB input refuses with exit 2 `invalid_import_file` before any network I/O"
  artifacts:
    - path: "crates/ignition-core/src/client/mod.rs"
      provides: "download_to_file streaming pipeline helper (classify → bytes_stream → tokio::fs write) + put/post raw-body plumbing for import"
      contains: "bytes_stream"
    - path: "crates/ignition-core/src/client/projects.rs"
      provides: "PROJECT_EXPORT_TIMEOUT (120 s) / PROJECT_IMPORT_TIMEOUT (300 s) consts, ExportMeta, ImportOutcome, project_export_to_file + project_import methods"
      contains: "from_secs(300)"
    - path: "crates/ignition-core/src/error.rs"
      provides: "ADDITIVE variants ProjectExists (exit 6, slug project_exists) and InvalidImportFile (exit 2, slug invalid_import_file) — exit-code table updated in the two places (error.rs + README)"
      contains: "project_exists"
    - path: "crates/ignition-core/src/actions/projects.rs"
      provides: "export/import actions + the static ExportScope (includes/excludes) carried in both results"
      min_lines: 100
  key_links:
    - from: "download_to_file"
      to: "reqwest bytes_stream + tokio::fs"
      via: "classify() FIRST (status/headers before body), then chunk loop → AsyncWriteExt::write_all + flush; requires workspace features reqwest `stream` + tokio `fs` — the dep gap research flagged"
      pattern: "bytes_stream"
    - from: "import dispatch arm (overwrite)"
      to: "require_confirmation guard"
      via: "guard BEFORE resolve_gateway_api — exit 2, profile null; abort-policy imports skip the guard (they fail safely server-side)"
      pattern: "require_confirmation\\("
    - from: "project_import"
      to: "POST /data/api/v1/projects/import/{name}?overwrite=<bool>"
      via: "raw Vec<u8> body (known Content-Length) + Content-Type: application/zip + 300 s RequestBuilder::timeout — synchronous, no job IDs (verified)"
      pattern: "application/zip"
    - from: "collision pre-check"
      to: "project_find (03-01)"
      via: "policy=Abort: find Ok(_) → ProjectExists BEFORE upload; policy=Overwrite: no pre-check (server is the authority)"
      pattern: "ProjectExists"
---

<objective>
Ship project export/import (PROJ-03, PROJ-04): streaming ZIP export to disk, buffered file/stdin import with collision policy, per-operation timeouts, and explicit scope metadata.

Purpose: Long imports are the classic default-timeout death (Pitfall 3) and big exports are the classic OOM (Pitfall 2) — this plan closes both with the streaming download helper (the dep additions research verified) and per-request timeout overrides. Collision policy proves the abort/overwrite conventions; scope metadata tells users tag providers are NOT in a project export (roadmap criterion 4).

Output: `download_to_file` + `project_export_to_file`/`project_import` client methods, 2 additive error slugs, export/import actions with scope, `ign project export/import`, goldens.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-project-operations/03-RESEARCH.md
@.planning/phases/03-project-operations/03-01-SUMMARY.md
@crates/ignition-core/src/client/mod.rs
@crates/ignition-core/src/client/logs.rs
@crates/ignition-cli/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: streaming download_to_file + export/import client methods</name>
  <files>Cargo.toml, crates/ignition-core/src/client/mod.rs, crates/ignition-core/src/client/projects.rs, crates/ignition-core/tests/projects_contract.rs</files>
  <action>
    **Cargo.toml (workspace deps)** — the research-flagged gap: reqwest features += `"stream"` (bytes_stream/Body::wrap_stream are feature-gated), tokio features += `"fs"` (and `"io-std"`, `"io-util"` for 03-02's stdin read); if `futures_util::StreamExt` needs a direct import path, add `futures-util` workspace-level (it already arrives transitively with reqwest's stream feature — verify at impl, research §Code Examples).

    **client/mod.rs — `download_to_file(path, out: &Path, timeout: Duration) -> Result<ExportMeta, CoreError>`** (a body-consumption site, classify-first like the others):
    - GET + `.timeout(timeout)` + apply_auth → `send_and_classify` — classify BEFORE any body consumption (an error ZIP-less response must classify, never stream).
    - Capture `Content-Disposition` filename (reuse `logs::filename_from_content_disposition`) + `Content-Type` into the meta.
    - `response.bytes_stream()` loop (requires the stream feature): each chunk → `CoreError::Network` on stream error; `tokio::fs::File::create(out)` + `AsyncWriteExt::write_all(&chunk)` + final `flush`; count bytes into the meta. **NO `Vec<u8>` accumulation anywhere in the signature or body (Pitfall 2's warning sign).**

    **client/projects.rs additions:**
    - `pub const PROJECT_EXPORT_TIMEOUT: Duration = Duration::from_secs(120);` / `pub const PROJECT_IMPORT_TIMEOUT: Duration = Duration::from_secs(300);` (logs-download 120 s precedent; NO second client, NO global change).
    - `ExportMeta { filename: String, bytes: u64, content_type: Option<String> }`; `ImportOutcome { response: serde_json::Value }` (opaque-success: parse the body if JSON, else fall back to `{"status":"success"}` — the mcp pattern; restart's literal `true` is the same family style).
    - `project_export_path(name)` → `/data/api/v1/projects/export/{enc}`; `project_import_path(name)` → `/data/api/v1/projects/import/{enc}` (encode_segment from 03-01).
    - Trait: `project_export_to_file(&self, name: &str, out: &Path) -> Result<ExportMeta, CoreError>` (120 s, via download_to_file) and `project_import(&self, name: &str, zip: Vec<u8>, overwrite: bool) -> Result<ImportOutcome, CoreError>` — POST + `.query(&[("overwrite", …)])` + `.timeout(PROJECT_IMPORT_TIMEOUT)` + `header(CONTENT_TYPE, "application/zip")` + `.body(zip)` (Vec<u8> ⇒ known Content-Length — sidesteps chunked-encoding entirely) → classify → opaque body parse.
    - **Doubles chore:** stub both new methods in all inline rigs (grep count from 03-01's SUMMARY).
  </action>
  <verify>
    tests/projects_contract.rs (wiremock): export — mock GET returns a ZIP fixture via **`set_body_raw(bytes, "application/zip")`** (gotcha: set_body_string forces text/plain) with a `Content-Disposition: attachment; filename="x.zip"` header; the tmp file's bytes equal the fixture byte-for-byte and the meta carries filename + byte count; import — **recorded-request proofs**: exact `/projects/import/My%20Proj` path with `overwrite=true` AND `overwrite=false` variants, `Content-Type: application/zip` header, body bytes == fixture; 401 HTML → Auth; `cargo test --workspace` green; clippy -D warnings + fmt clean.
  </verify>
  <done>
    Streaming export and buffered import exist as contract-pinned capabilities with both timeout constants on the wire.
  </done>
</task>

<task type="auto">
  <name>Task 2: additive error slugs + export/import actions with scope metadata</name>
  <files>crates/ignition-core/src/error.rs, crates/ignition-core/src/actions/projects.rs</files>
  <action>
    **error.rs — ADDITIVE ONLY (taxonomy 0–7 untouched):**
    - `ProjectExists { name: String, endpoint: Option<String> }` — target-state exit 6, slug `project_exists`, hint: names `--collision-policy overwrite` AND warns "overwrite replaces the ENTIRE project — resources absent from the ZIP are deleted; merge is Designer-only" (Pitfall 4). Follow the GatewayTooOld action-built-variant precedent.
    - `InvalidImportFile { reason: String }` — usage-class exit 2 (it names what the caller must fix), slug `invalid_import_file`; covers the missing `PK\x03\x04` magic (the cheap wrong-file guard — Don't-Hand-Roll table) and the >512 MB sanity guard (`pub const IMPORT_MAX_BYTES` in actions).
    - Wire both into `code()`/`exit_code()`/`hint()`/endpoint plumbing AND the README exit-code table (the two-places rule).

    **actions/projects.rs additions (serde-only, no printing):**
    - `ExportScope { includes: Vec<&'static str>, excludes: Vec<&'static str> }` from `pub const EXPORT_INCLUDES`/`EXPORT_EXCLUDES` — the research's exact static arrays (views, scripts, named-queries, … / tag-providers, tags, udts, gateway-config, …). Data, not prose — agents key off them (criterion 4).
    - `project_export(api, name, out: Option<&Path>) -> ExportResult` — data `{ project, file (resolved path), bytes, scope }`. Output-path resolution: given `-o` → use it; else the dispatch streams to a same-dir `<final>.part` file and atomically renames to the sanitized Content-Disposition basename (strip path components) or `{name}.zip` — the client returns the disposition in ExportMeta so the action/dispatch pair decides the final name; remove the `.part` on error (best-effort).
    - `project_import(api, name, zip: Vec<u8>, policy: CollisionPolicy) -> ImportResult` — order: magic/size guards (exit 2) → **policy=Abort: `project_find` pre-check, `Ok(_)` → `Err(ProjectExists)` BEFORE upload**; policy=Overwrite: no pre-check → `project_import(…, overwrite=matches!(policy, Overwrite))`. Data `{ name, collision_policy, bytes, scope, outcome }`.
    - Planner note (record in SUMMARY): research suggested an import-timeout hint "verify with `ign project list`"; the frozen error contract stays untouched — the verify-after guidance rides the README timeout paragraph instead.
  </action>
  <verify>
    Unit tests: PK-magic refusal + 512 MB refusal both exit 2 with `invalid_import_file`; Abort-policy over an existing project (FakeApi find→Ok) returns `project_exists` exit 6 with an overwrite-naming hint; scope consts contain `tag-providers` under excludes.
  </verify>
  <done>
    Collision policy, sanity guards, and scope metadata exist as pure action logic with the two additive slugs in the exit table.
  </done>
</task>

<task type="auto">
  <name>Task 3: `ign project export/import` dispatch + guards + goldens</name>
  <files>crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/main.rs, crates/ignition-cli/src/render.rs, crates/ignition-cli/tests/contract_projects.rs, crates/ignition-core/tests/live_gateway.rs, README.md</files>
  <action>
    **cli.rs** — extend `ProjectCommand`: `Export { name: String, #[arg(short, long, value_name = "FILE")] output: Option<PathBuf> }`; `Import { name: String, #[arg(long, value_name = "PATH")] file: String, #[arg(long, value_enum, default_value_t = CollisionPolicy::Abort)] collision_policy: CollisionPolicy }`. `CollisionPolicy` ValueEnum = `{ Abort, Overwrite }` ONLY — `merge` is not a value (clap's invalid-value error lists the two valid choices; README documents merge as Designer-only — the sanctioned rejection-with-hint shape).

    **main.rs dispatch:**
    - Export — non-destructive (writes a local file): standard `resolve_gateway_api`; human mode may print a one-line progress to **stderr** while streaming; the file is the artifact, JSON data carries `{project, file, bytes, scope}` — **no stdout exception needed**.
    - Import — read the byte source in the dispatch layer (`--file PATH` via `std::fs::read`; `--file -` via tokio stdin `read_to_end`), then call the action. **policy=Overwrite → `require_confirmation(cli.yes, "project import --collision-policy overwrite")` BEFORE `resolve_gateway_api`** (exit 2, profile null — the LOCKED shape); policy=Abort → no guard (fails safely).
    - New `ActionOutput::ProjectExport`/`ProjectImport` variants + render_json arms (declaration-order goldens).

    **render.rs (human)** — export: `exported <name> → <file> (<n> bytes)` + a scope summary line (`scope: includes views/scripts/… · excludes tag-providers/…`); import: `imported <name> (<n> bytes, policy <abort|overwrite>)`.

    **contract_projects.rs goldens:** export success (mock ZIP → tmp cwd, [..] path elisions) incl. `scope.includes`/`scope.excludes` in --json data; export `-o` explicit path; **import abort-collision: mock find 200 → exit 6 `project_exists` golden; import overwrite WITHOUT --yes → exit 2 confirmation golden; WITH --yes → success + recorded `overwrite=true`; stdin import (`--file -` with piped bytes); non-ZIP input → exit 2 `invalid_import_file`.**

    **live_gateway.rs** — append an `#[ignore]` export→import round-trip behind `IGNITION_LIVE_MUTATIONS=1` (timestamped scratch project, delete cleanup — the 03-03 e2e loop's preview; optional live truth for the MEDIUM response bodies).

    **README.md** — export/import rows; collision-policy semantics (default abort; overwrite = full replace; merge = Designer-only); the 120 s/300 s timeout notes + verify-with-`ign project list` guidance; scope-metadata explanation (tag providers are gateway config, not project export).
  </action>
  <verify>
    `cargo test --workspace` green; `SNAPSHOTS=overwrite` no-op; `ign project import x --file f.zip --collision-policy overwrite` without --yes exits 2 profile-null; abort-over-existing exits 6 `project_exists` with zero POSTs observed at the mock; clippy -D warnings + fmt clean.
  </verify>
  <done>
    PROJ-03 + PROJ-04 user-observable and roadmap criterion 4 (explicit scope) true; the collision-policy conventions are proven end-to-end.
  </done>
</task>

</tasks>

<verification>
- No `Vec<u8>`-buffered export anywhere (grep the export path for `bytes()`/`bytes_stream` — only the import side is buffered, by design).
- Timeout constants ride the wire via `RequestBuilder::timeout` (120 s export / 300 s import), not a second client or a global change.
- The abort collision fails BEFORE upload (mock observes zero import POSTs); overwrite requires the guard.
- Scope metadata is identical (same consts) in export and import JSON data.
</verification>

<success_criteria>
A user moves projects between gateways (or into version control) entirely from the CLI — export streams to disk without memory blowups, imports survive long uploads, collisions are explicit and safe by default, and every output states what the ZIP does and does not contain.
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-operations/03-02-SUMMARY.md`
</output>
