---
phase: 03-project-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/ignition-core/src/client/mod.rs
  - crates/ignition-core/src/client/projects.rs
  - crates/ignition-core/src/actions/mod.rs
  - crates/ignition-core/src/actions/projects.rs
  - crates/ignition-core/tests/projects_contract.rs
  - crates/ignition-core/tests/live_gateway.rs
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/src/render.rs
  - crates/ignition-cli/tests/contract_projects.rs
  - README.md
autonomous: true

must_haves:
  truths:
    - "User runs `ign project list` and sees every runnable project with name, title, description, enabled, parent, and inheritable — inheritance info comes from the list items themselves (PROJ-01)"
    - "User creates a project: `ign project new NAME [--title --description --parent --inheritable --disabled]` — only provided fields serialize; a bare `new` sends exactly {\"name\":…,\"enabled\":true} (wiremock body-proof — Pitfall 5)"
    - "User copies, renames, and sets projects (title/description/parent/enabled/inheritable) without --yes — non-destructive mutations; `set --parent` IS the inheritance move (PUT modify)"
    - "`ign project delete NAME` without --yes refuses with exit 2, confirmation_required slug, profile null BEFORE any profile/secret/network resolution; with --yes the DELETE carries `confirm=true` as a QUERY param (both guard layers — Pitfall 8)"
    - "Project names with spaces/mixed case ride the wire percent-encoded per segment (find/My%20Project exact-path recorded-request pin — Pitfall 6)"
  artifacts:
    - path: "crates/ignition-core/src/client/projects.rs"
      provides: "ProjectRecord/ProjectCreate/ProjectModify models + verified path constants + encode_segment + the 7 project-CRUD capability signatures"
      min_lines: 80
    - path: "crates/ignition-core/src/actions/projects.rs"
      provides: "list/new/copy/rename/set/delete actions (serde models only, no printing)"
      min_lines: 60
    - path: "crates/ignition-core/src/client/mod.rs"
      provides: "post_json/put_json pipeline helpers (classify-first, the only new body-consumption sites) + 7 trait methods in the single impl block"
      contains: "async fn project_delete"
    - path: "crates/ignition-cli/tests/contract_projects.rs"
      provides: "Goldens: list 3 modes, new, set, delete-without---yes exit 2, delete-with---yes, delete nonexistent exit 6"
      min_lines: 60
  key_links:
    - from: "project delete dispatch arm"
      to: "require_confirmation guard"
      via: "sessions-terminate shape VERBATIM: guard BEFORE resolve_gateway_api — refusal = exit 2, profile null, zero config/network work"
      pattern: "require_confirmation\\("
    - from: "client project_delete"
      to: "DELETE /data/api/v1/projects/{name}"
      via: "delete_with_query with confirm=true — the server's own guard rides the query string (both layers, wiremock recorded-request proof)"
      pattern: "confirm.*true"
    - from: "every {name} path builder"
      to: "encode_segment"
      via: "percent-encoding per segment (NON_ALPHANUMERIC set — over-encoding is safe, mirrors mcp's quote(name, safe='')); resource paths in 03-03 keep '/'"
      pattern: "percent_encode|encode_segment"
    - from: "all new trait methods"
      to: "the ~8 inline test doubles"
      via: "grep `impl GatewayApi for` — currently ConnectionsRig, FakeApi, SessionsRig, DoctorRig, HealthyRig, BrokenOverview, TailRig, AuthRig across src/actions/*; stub unreachable!(\"not part of this action\") so nothing compiles half-way"
      pattern: "impl GatewayApi for"
---

<objective>
Ship project CRUD (PROJ-01, PROJ-02): list with inheritance info, create, copy, rename, set (reparent), delete — the first project-family commands and the phase's `--yes` proof for destructive project operations.

Purpose: The native `/data/api/v1/projects/*` family is HIGH confidence (official 83-api collection + the working ignition-mcp client agree on every path), so this plan is the fast, contract-pinned foundation 03-02/03-03 build on: the models, the `post_json`/`put_json` pipeline helpers, and the delete dispatch pattern every later destructive project command inherits.

Output: 7 GatewayApi methods, `ign project list/new/copy/rename/set/delete`, goldens, README rows.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-project-operations/03-RESEARCH.md
@crates/ignition-core/src/client/mod.rs
@crates/ignition-core/src/client/query.rs
@crates/ignition-cli/src/main.rs
@crates/ignition-cli/src/cli.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: project CRUD client capabilities (wiremock-pinned) + pipeline helpers</name>
  <files>Cargo.toml, crates/ignition-core/src/client/projects.rs, crates/ignition-core/src/client/mod.rs, crates/ignition-core/tests/projects_contract.rs</files>
  <action>
    **Cargo.toml (workspace)** — add `percent-encoding = "2"` (already a transitive dep of url; declaring it makes the direct import legal, zero new code compiled).

    **client/projects.rs (new)** — models two-column LOCKED (wire-faithful, gateway-native camelCase):
    - `ProjectRecord` — `#[serde(rename_all = "camelCase")]` typed core `{ name: String, title: Option<String>, description: Option<String>, enabled: bool, parent: Option<String>, inheritable: Option<bool>, default_db: Option<String>, tag_provider: Option<String>, user_source: Option<String> }` + `#[serde(flatten)] extra: BTreeMap<String, serde_json::Value>` passthrough (the 02-02 ModuleInfo pattern — item shape is MEDIUM until live capture, research Open Question 2).
    - `ProjectCreate` — `name: String` + `enabled: bool` ALWAYS sent; every optional field `Option` with `skip_serializing_if = "Option::is_none"` (Pitfall 5 — never serialize empty-string references).
    - `ProjectModify` — the create fields MINUS `name` (the PUT body must not carry it), same skip-serializing discipline.
    - `ProjectCopy { from_name, to_name }` — serde-renamed to the official body keys `fromName`/`toName`. `ProjectRenameBody { name: String }` (official body key is `name`).
    - Path constants/builders: `PROJECTS_LIST_PATH = "/data/api/v1/projects/list"`, `PROJECTS_CREATE_PATH = "/data/api/v1/projects"`, `PROJECTS_COPY_PATH = "/data/api/v1/projects/copy"`, `project_find_path(name)` → `/projects/find/{enc}`, `project_rename_path(name)` → `/projects/rename/{enc}`, `project_modify_path(name)`/`project_delete_path(name)` → `/projects/{enc}` — every `{enc}` through one `pub(crate) fn encode_segment(&str) -> String` (NON_ALPHANUMERIC set).
    - **Trait growth (7 methods, bodies in the ONE impl block in client/mod.rs):**
      - `projects(&self, query: &ListQuery) -> Result<ListEnvelope<ProjectRecord>, CoreError>` — GET list, standard `ListQuery::default()` (limit=-1 UI convention).
      - `project_find(&self, name: &str) -> Result<ProjectRecord, CoreError>` — GET find (404 → `NotFound` via classify; this doubles as 03-02's collision pre-check).
      - `project_create(&self, body: &ProjectCreate) -> Result<(), CoreError>` — POST JSON. Ok classification IS the success contract (create's response body is unverified LOW — the restart `literal true` precedent; callers that want data re-`find`).
      - `project_copy(&self, from: &str, to: &str) -> Result<(), CoreError>`, `project_rename(&self, name: &str, new_name: &str) -> Result<(), CoreError>` — POST JSON bodies.
      - `project_modify(&self, name: &str, body: &ProjectModify) -> Result<(), CoreError>` — PUT JSON.
      - `project_delete(&self, name: &str) -> Result<(), CoreError>` — **`delete_with_query` with `("confirm", "true")`** — the server's own confirmation guard rides the query string (Pitfall 8: both layers, always).
    - **New pipeline helpers in mod.rs (the only body-consumption sites):** `post_json(path, &body) -> Result<Response>` and `put_json(path, &body) -> Result<()>` — build URL → apply auth → send (`Network` on transport) → `classify()` → consume. Mirrors `post_empty`/`delete_with_query`; token-auth POST/PUT need NO CSRF (verified Phase 2).
    - **The doubles chore:** stub all 7 methods `unreachable!("not part of this action")` into every `impl GatewayApi` rig (grep is the source of truth — 8 today).
  </action>
  <verify>
    tests/projects_contract.rs (wiremock + IgnitionMock): list parses plausible `{items:[{name,title,description,enabled,parent,inheritable,defaultDb,…}]}` incl. passthrough extras surviving; **recorded-request proofs**: find with a SPACED name hits exactly `/data/api/v1/projects/find/My%20Project`; a bare create body is exactly `{"name":"x","enabled":true}` (serde_json value equality on the recorded body) and optionals appear only when provided — never `"parent":""`; copy body exactly `{"fromName":"a","toName":"b"}`; rename body exactly `{"name":"new"}`; modify PUT body carries NO `name` key; DELETE carries `confirm=true` QUERY param + empty body. 401 Jetty HTML → Auth (exit 5); 404 find → not_found (exit 6); `cargo test --workspace` green; clippy -D warnings + fmt clean.
  </verify>
  <done>
    All six CRUD endpoints exist as typed, contract-pinned capabilities with encoding and body-shape proofs; the doubles compile.
  </done>
</task>

<task type="auto">
  <name>Task 2: project actions (serde-only verb layer)</name>
  <files>crates/ignition-core/src/actions/projects.rs, crates/ignition-core/src/actions/mod.rs</files>
  <action>
    **actions/projects.rs (new; register in actions/mod.rs)** — serde models only, NO printing (TUI rides this in Phase 6). Two-column: action results re-expose wire-faithful records under unit-explicit snake_case keys.
    - `projects(api) -> ProjectsResult` — data `{ projects: [ProjectSummary { name, title, description, enabled, parent, inheritable }] }`.
    - `project_new(api, name, opts: NewOptions) -> ProjectMutationResult` — create, then **`project_find` read-back** (validates + fills the result; data `{ name, title, description, enabled, parent, inheritable }`).
    - `project_copy(api, from, to)` — copy + find(to) read-back; data `{ from, …record-of-to }`.
    - `project_rename(api, old, new)` — rename + find(new) read-back; data `{ previous_name, …record-of-new }`.
    - `project_set(api, name, opts: SetOptions) -> ProjectMutationResult` — build `ProjectModify` from `Some`-fields ONLY (absent flag = don't touch — matches Pitfall 5); PUT + find read-back. This is the inheritance move (`--parent`).
    - `project_delete(api, name)` — data `{ deleted: name }`.
    - Keep `parents`/`parents/{name}` endpoints OUT of scope (server is the reparent authority; minimal per research plan-shape note).
    - Unit-test the modify-body construction: `SetOptions` with only `--title` serializes exactly `{"title":"T"}`.
  </action>
  <verify>
    `cargo test -p ignition-core` green incl. the modify-body unit test; actions contain no `println!`/`eprintln!`/`tracing` output of data.
  </verify>
  <done>
    The six verbs exist as pure functions over `dyn GatewayApi`, ready for CLI and (Phase 6) TUI dispatch.
  </done>
</task>

<task type="auto">
  <name>Task 3: `ign project` command tree + destructive delete dispatch + goldens</name>
  <files>crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/main.rs, crates/ignition-cli/src/render.rs, crates/ignition-cli/tests/contract_projects.rs, crates/ignition-core/tests/live_gateway.rs, README.md</files>
  <action>
    **cli.rs** — `Commands::Project(ProjectArgs)` (`#[command(arg_required_else_help = true)]`) with nested `ProjectCommand`:
    - `List {}` · `New { name: String, #[arg(long)] title/description/parent: Option<String>, #[arg(long)] inheritable: bool, #[arg(long)] disabled: bool }` · `Copy { src: String, dst: String }` · `Rename { old_name: String, new_name: String }` (avoid raw identifiers where unnecessary — golden-friendly) · `Set { name: String, #[arg(long)] title/description/parent: Option<String>, #[arg(long)] set_enabled/--disabled pair or Option<bool> shape clap supports cleanly, #[arg(long)] inheritable: Option<bool> }` · `Delete { name: String }`.
    - copy/rename/set carry NO `--yes` (planner decision per research: non-destructive mutations — they create or relabel, never destroy; delete is the only guarded verb in this family).

    **main.rs** — new `ActionOutput` variants (`ProjectsList`, `ProjectNew`, `ProjectCopy`, `ProjectRename`, `ProjectSet`, `ProjectDelete`) + dispatch:
    - **Delete arm — inherit the sessions-terminate shape VERBATIM:** `require_confirmation(cli.yes, "project delete")` BEFORE `resolve_gateway_api`; on refusal **`return (None, Err(err));`** from the dispatch match arm — `dispatch`'s tuple return carries the error to the single render/exit point in `main` (`render_error` + `ExitCode::from(err.exit_code())`, tuple consumed at main.rs:149, error path :155-158), yielding exit 2, `confirmation_required`, profile null, zero config/network work (LOCKED 02-03 pattern; copy source: the `Sessions::Terminate` arm at main.rs:254-262 — there is NO `render_failure_exit` helper, do not invent one).
    - All project arms use `resolve_gateway_api` (credential REQUIRED — inspection-command rule, exit 3 without).
    - **render.rs (human)** — list: table `name  title  enabled  parent  inheritable`; mutations: one confirmation line each (`created <name> (parent <p>)`, `copied a → b`, `renamed a → b`, `set <field(s)> on <name>`, `deleted <name>`).

    **contract_projects.rs (new goldens, snapbox + wiremock-backed profile config per the 02-03 pattern):** list (populated mock, all 3 modes); new success; set --title success; **delete WITHOUT --yes → exit 2 + confirmation_required envelope (golden)**; delete WITH --yes → success (mock DELETE proving `confirm=true` reached the wire); delete nonexistent → exit 6 not_found. Elide dynamic values with [..].

    **live_gateway.rs** — append a `#[ignore]` read-only `projects/list` check (env-gated skip, existing convention) — optional live truth for the list envelope the moment a token exists.

    **README.md** — `ign project` command rows; note delete joins the `--yes` destructive family; audit-logged-server-side note for mutations.
  </action>
  <verify>
    `cargo test --workspace` green; `SNAPSHOTS=overwrite` no-op; `ign project delete x` (no --yes) exits 2 with confirmation_required + null profile; clippy -D warnings + fmt clean.
  </verify>
  <done>
    PROJ-01 and PROJ-02 user-observable: full project CRUD from the terminal with inheritance info in the list and the destructive delete convention proven at both guard layers.
  </done>
</task>

</tasks>

<verification>
- Every CRUD wire shape is pinned by recorded-request proofs (paths with encoded names, exact JSON bodies, `confirm=true` query param) — not just response parsing.
- The delete refusal happens before ANY resolution (exit 2, profile null) and the wire request always carries `confirm=true`.
- All ~8 test doubles stub the 7 new methods; workspace compiles + clippy clean.
</verification>

<success_criteria>
A user replaces the gateway webpage's project list/create/copy/rename/inheritance/delete pages from the terminal; agents get a stable `{projects:[…]}` JSON shape; and the phase's first destructive command honors the frozen guard contract exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-operations/03-01-SUMMARY.md`
</output>
