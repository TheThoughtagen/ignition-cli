---
phase: 03-project-operations
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - crates/ignition-core/src/client/mod.rs
  - crates/ignition-core/src/client/resources.rs
  - crates/ignition-core/src/error.rs
  - crates/ignition-core/src/actions/mod.rs
  - crates/ignition-core/src/actions/resources.rs
  - crates/ignition-core/tests/resources_contract.rs
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/src/render.rs
  - crates/ignition-cli/tests/contract_resources.rs
  - crates/ignition-cli/tests/e2e_projects.rs
  - README.md
autonomous: true
user_setup:
  - service: ignition-live-rig
    why: "Opt-in live verification of the MEDIUM-confidence resource family + the e2e loop (#[ignore] tests, skip-by-default green — wiremock covers CI). NOT required for plan execution. Research notes the `ign-research` rig is RUNNING on :18088 but NO token exists yet; creating one (~1 min) closes the phase's open questions."
    env_vars:
      - name: IGNITION_LIVE_URL
        source: "Base URL of a commissioned Ignition 8.3+ gateway you control (e.g. http://localhost:18088)"
      - name: IGNITION_LIVE_TOKEN
        source: "Gateway UI → Platform → Security → API Keys → Create (Basic Token, UNCHECK 'Require secure connections' for http rigs, admin-level) — copy the FULL name:key string"
      - name: IGNITION_LIVE_MUTATIONS=1
        source: "Set alongside URL+TOKEN to opt the e2e loop's mutations in (02-04 precedent)"

must_haves:
  truths:
    - "User lists a project's resources: `ign resource list PROJECT [--prefix P]` — the `path` query filter rides the wire when given; the family's MEDIUM confidence is doc-flagged (single source — research Open Question 1)"
    - "User reads one resource: `ign resource get PROJECT PATH` returns JSON (pretty) or text content; a binary (data.bin-class) resource REFUSES with exit 6 `resource_binary` and an export/import hint — never corrupted through the JSON loop (Pitfall 7)"
    - "User writes one resource: `ign resource put PROJECT PATH --file F|-` upserts (create-or-replace) — JSON if parseable (application/json), else UTF-8 text (text/plain); binary-looking input refuses"
    - "`ign resource delete PROJECT PATH` refuses without --yes (exit 2, profile null, pre-resolution) — the surgical loop's destructive verb"
    - "Resource paths with spaces/mixed case ride the wire per-segment encoded, slashes preserved (`…/resources/com…/views/My%20Folder/View` recorded-request pin)"
    - "The e2e harness skeleton exists and is green by default; `-- --ignored` with the env contract runs the full create→export→edit→import(abort/overwrite)→rename→copy→delete loop via assert_cmd"
    - "An `#[ignore]` openapi-extract test fetches /openapi.json and writes a trimmed projects+resources extract into the phase dir — closes the resource-endpoint wire-truth question the moment a token exists"
  artifacts:
    - path: "crates/ignition-core/src/client/resources.rs"
      provides: "ResourceEntry (passthrough-heavy) + ResourceContent models + the 4 resource-family capabilities, all doc-flagged MEDIUM pending live capture"
      min_lines: 60
    - path: "crates/ignition-core/src/actions/resources.rs"
      provides: "list/get/put/delete actions incl. the JSON/text/binary sniffer (the classify discipline inverted)"
      min_lines: 60
    - path: "crates/ignition-cli/tests/e2e_projects.rs"
      provides: "#[ignore] assert_cmd e2e skeleton gated on IGNITION_LIVE_URL/TOKEN/MUTATIONS — skip-by-default green; extended by Phases 4–5"
      min_lines: 80
    - path: "crates/ignition-core/src/error.rs"
      provides: "ADDITIVE ResourceBinary variant (exit 6, slug resource_binary, export/import hint)"
      contains: "resource_binary"
  key_links:
    - from: "resource path builders"
      to: "per-segment percent encoding"
      via: "project name fully encoded (03-01's encode_segment), resource path encoded per segment KEEPING '/' (mcp's quote(path, safe='/') — Pitfall 6)"
      pattern: "encode"
    - from: "resource get/put actions"
      to: "content sniffer"
      via: "UTF-8 + JSON-parse → structured; UTF-8 → text; else NUL-in-first-8KiB binary heuristic → ResourceBinary refusal (Pitfall 7)"
      pattern: "resource_binary|sniff"
    - from: "e2e loop"
      to: "the built ign binary"
      via: "assert_cmd spawning `ign` (true e2e, not trait-level); timestamped project names leave forensic state on failure; cleanup best-effort"
      pattern: "assert_cmd|Command"
    - from: "resource delete dispatch arm"
      to: "require_confirmation guard"
      via: "guard BEFORE resolve_gateway_api — exit 2, profile null (LOCKED shape, third destructive caller in the family)"
      pattern: "require_confirmation\\("
---

<objective>
Ship the surgical resource-edit loop (PROJ-05) — list/get/put/delete individual project resources — plus the phase's e2e harness skeleton and the live-capture gate for the MEDIUM-confidence resource family.

Purpose: This is THE phase risk (research Pitfall 1): the resource endpoints exist only in ignition-mcp (single source; Phase 2 caught that client inventing paths), so the family is isolated here with wiremock-first development, passthrough-heavy models, binary-resource refusal, and an explicit openapi-extract + e2e capture path that settles wire truth the moment a rig token exists. The e2e skeleton is the roadmap's stated deliverable that Phases 4–5 extend.

Output: 4 GatewayApi methods, `ign resource list/get/put/delete`, the binary-refusal slug, `tests/e2e_projects.rs`, the openapi capture hook.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-project-operations/03-RESEARCH.md
@.planning/phases/03-project-operations/03-02-SUMMARY.md
@crates/ignition-core/src/client/mod.rs
@crates/ignition-core/src/client/connections.rs
@crates/ignition-cli/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: resource-family client capabilities (wiremock-first, MEDIUM-flagged)</name>
  <files>crates/ignition-core/src/client/resources.rs, crates/ignition-core/src/client/mod.rs, crates/ignition-core/tests/resources_contract.rs</files>
  <action>
    **client/resources.rs (new)** — the single-source family, treated exactly like 02-03's connections: passthrough-heavy, MEDIUM doc-flagged on every model ("ignition-mcp-derived; absent from the official 83-api collection; live-capture gate in tests/e2e_projects.rs").
    - `ResourceEntry { path: Option<String>, #[serde(flatten)] extra: BTreeMap<String, serde_json::Value> }` — list-item shape unverified; typed `path` when present, everything else passthrough (wire-truth corrections stay cheap).
    - `ResourceContent { bytes: Vec<u8>, content_type: Option<String> }` — the get result keeps RAW bytes; the JSON/text/binary decision belongs to the action layer (the classify/HTML-sniffer discipline inverted).
    - Path builders: `resources_list_path(project)` → `/data/api/v1/projects/{enc}/resources`; `resource_path(project, path)` → `/data/api/v1/projects/{enc}/resources/{path encoded per segment, '/' preserved}` (project through 03-01's `encode_segment`; the resource path through a new segment-splitting encoder — Pitfall 6).
    - **Trait growth (4 methods, single impl block):**
      - `project_resources(&self, project: &str, prefix: Option<&str>) -> Result<ListEnvelope<ResourceEntry>, CoreError>` — GET list; `path=<prefix>` QUERY param only when `Some` (mcp's `params={"path": …}`).
      - `project_resource_get(&self, project: &str, path: &str) -> Result<ResourceContent, CoreError>` — via the existing `get_bytes` pipeline (its bytes+content-type return fits; the `filename` field is simply unused here — reuse, don't generalize, churn is not warranted).
      - `project_resource_put(&self, project: &str, path: &str, body: Vec<u8>, content_type: &str) -> Result<(), CoreError>` — needs a `put_bytes(path, body, content_type)` pipeline helper (PUT + CONTENT_TYPE header + `.body(bytes)` → classify; the JSON flavor `put_json` already exists from 03-01). Upsert semantics per mcp docs ("if the resource doesn't exist it will be created").
      - `project_resource_delete(&self, project: &str, path: &str) -> Result<(), CoreError>` — `delete_with_query`, empty pairs.
    - **Doubles chore:** stub all 4 methods into every inline rig.
    - Do NOT hardcode resource-type knowledge (no `{module}/{type}` map — the gateway owns the taxonomy; `--prefix` is the user-facing filter — Don't-Hand-Roll table). Do NOT copy mcp's `com.inductiveautomation.ignition/…` doc examples — the observed core-module folder is `ignition/`.
  </action>
  <verify>
    tests/resources_contract.rs (wiremock): list parses a plausible `{items:[{path:"com.inductiveautomation.perspective/views/Dashboard",…}]}` with passthrough extras; **recorded-request proofs**: `path=view` query param present only when a prefix is given; a SPACED resource path (`…/resources/com.x/views/My%20Folder/V1`) hits the exact encoded path with slashes intact; put carries the exact body bytes + declared Content-Type; delete hits the exact path; 404 get → not_found; 401 HTML → Auth; `cargo test --workspace` green; clippy -D warnings + fmt clean.
  </verify>
  <done>
    The four surgical verbs exist as wiremock-pinned capabilities with encoding proofs and honest MEDIUM flags.
  </done>
</task>

<task type="auto">
  <name>Task 2: resource actions (binary sniffer) + `ign resource` dispatch + goldens</name>
  <files>crates/ignition-core/src/error.rs, crates/ignition-core/src/actions/resources.rs, crates/ignition-core/src/actions/mod.rs, crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/main.rs, crates/ignition-cli/src/render.rs, crates/ignition-cli/tests/contract_resources.rs, README.md</files>
  <action>
    **error.rs — ADDITIVE:** `ResourceBinary { path: String, endpoint: Option<String> }` — exit 6, slug `resource_binary`, hint: "resource content is binary — use `ign project export`/`import` for data.bin-class resources" (Pitfall 7). Two-places exit-table rule (error.rs + README).

    **actions/resources.rs (new; register in mod.rs) — serde-only:**
    - Shared sniffer `fn classify_content(bytes: &[u8]) -> ContentKind { Json(serde_json::Value) | Text(String) | Binary }` — UTF-8 + JSON-parse → Json; UTF-8 → Text; else (NUL-in-first-8KiB heuristic) → Binary. Unit tests for all three.
    - `resources_list(api, project, prefix) -> ResourcesResult` — data `{ resources: [ResourceEntry passthrough] }`.
    - `resource_get(api, project, path)` — Json → `ResourceGetResult::Json(Value)`; Text → `Text(String)`; Binary → `Err(ResourceBinary)` (refuse — a data.bin resource must never round-trip through the JSON loop).
    - `resource_put(api, project, path, input: Vec<u8>)` — sniff the INPUT: Json → `put(…, "application/json")`; Text → `put(…, "text/plain; charset=utf-8")`; Binary → `Err(ResourceBinary)`. Data `{ project, path, content_kind }`.
    - `resource_delete(api, project, path)` — data `{ deleted: path }`.

    **cli.rs** — `Commands::Resource(ResourceArgs)` (`arg_required_else_help`): `List { project: String, #[arg(long)] prefix: Option<String> }` · `Get { project: String, path: String }` · `Put { project: String, path: String, #[arg(long, value_name = "PATH")] file: String }` ("-" = stdin, the 03-02 convention) · `Delete { project: String, path: String }` — `path` is a positional that keeps its slashes.

    **main.rs** — new ActionOutput variants (`ResourcesList`, `ResourceGet`, `ResourcePut`, `ResourceDelete`) + dispatch:
    - **Delete arm: `require_confirmation(cli.yes, "resource delete")` BEFORE `resolve_gateway_api`** (exit 2, profile null — the LOCKED shape).
    - Put — an upsert (create-or-replace one resource), NOT in the --yes family per the research command surface (planner decision: the surgical edit loop must stay friction-free; agents pass explicit content). Read input in the dispatch layer (`std::fs::read` / tokio stdin), pass bytes to the action.
    - All resource arms: `resolve_gateway_api` (credential required, exit 3 without).
    - **render.rs (human)** — list: one resource path per line; get: pretty JSON or raw text; put: `put <path> (json|text)`; delete: `deleted <path>`.

    **contract_resources.rs goldens:** list (populated mock, 3 modes); get JSON (pretty in human mode); **get binary → exit 6 `resource_binary` golden**; put from file (JSON body proof); put from stdin (text/plain proof); **delete WITHOUT --yes → exit 2 confirmation golden; WITH --yes → success**; 404 get → not_found. [..] elisions.

    **README.md** — `ign resource` rows; the binary-refusal note; the surgical-edit example loop (get → edit → put); the MEDIUM-confidence caveat pointing at the openapi extract + e2e verification path.
  </action>
  <verify>
    `cargo test --workspace` green incl. sniffer unit tests; `SNAPSHOTS=overwrite` no-op; `ign resource delete p x` without --yes exits 2 profile-null; binary get/put refusals golden-pinned; clippy -D warnings + fmt clean.
  </verify>
  <done>
    PROJ-05 user-observable: change one view/script without re-importing everything — with binary resources fenced off and the destructive verb guarded.
  </done>
</task>

<task type="auto">
  <name>Task 3: e2e harness skeleton + openapi live-capture gate</name>
  <files>crates/ignition-cli/tests/e2e_projects.rs</files>
  <action>
    **tests/e2e_projects.rs (new)** — minimal, extensible, dogfoods the binary (research §e2e Harness Skeleton):
    - Every test `#[ignore]`-gated; helpers read `IGNITION_LIVE_URL` + `IGNITION_LIVE_TOKEN`; absent either → quiet skip (the 02-01 live-suite convention). Mutation tests additionally require `IGNITION_LIVE_MUTATIONS=1` (the 02-04 precedent) — absent → quiet skip.
    - Uses `assert_cmd` to spawn the built `ign` binary with `--profile`-free env-config pointing at the live gateway (isolate config per spawn — the snapbox lesson).
    - **The loop** (one `#[ignore]` test, timestamped project `ign-e2e-<unix_ts>` so failures leave forensic state; cleanup best-effort with `--yes`): `project new` → `project list` contains it → `project export -o tmp.zip` (assert file exists + `scope.includes`/`excludes` in --json data) → `resource put` a scratch script resource (`ignition/script-python/e2e/scratch` with a trivial `code.py`-style JSON/text body) → `resource get` verifies round-trip → `project import --file tmp.zip` into the SAME name with default abort → **expect exit 6 `project_exists`** → `--collision-policy overwrite` + `--yes` → success, then `resource get` proves the resource survived → `project rename` → `project copy` → `project delete` both names (cleanup).
    - **openapi-capture gate:** an `#[ignore]` test that GETs `{base}/openapi.json` (authed) and writes a trimmed extract of every `/data/api/v1/(projects|scan|resources)` path to `.planning/phases/03-project-operations/openapi-8.3.6-phase3-extract.json` (resolve via `env!("CARGO_MANIFEST_DIR")/../..`); soft-assert the projects family is present and PRINT whether `/projects/{project}/resources` appears — research Open Question 1 closes with an authoritative artifact the moment a token exists (02's extract is the precedent file).
    - Structure the file so Phase 4 (rig) and Phase 5 (webdev deploy) extend it: shared env/token helpers + one test per capability loop.
  </action>
  <verify>
    `cargo test --workspace` green with the file present (skip-by-default no-op proof — zero env set, all ignored); `cargo test -p ignition-cli --test e2e_projects -- --ignored` skips cleanly without env; with env set against the research rig it runs the full loop (optional — needs the ~1-min token from user_setup).
  </verify>
  <done>
    The phase's e2e skeleton exists (green by default, live-capable by opt-in) and the resource-family wire-truth question has a ready capture path — Phase 3's open questions close the moment a rig token exists.
  </done>
</task>

</tasks>

<verification>
- All four roadmap success criteria traceable: criterion 1 → 03-01 truths; criterion 2 (streamed ZIP, timeouts, collision) → 03-02 truths; criterion 3 (surgical loop) → 03-03 Task 2; criterion 4 (scope metadata) → 03-02 Task 2.
- Resource models stay passthrough-heavy and MEDIUM-flagged; no hardcoded resource-type taxonomy.
- The e2e suite adds ZERO default CI cost (skip-by-default green) while giving mutations a real-binary loop.
- `impl GatewayApi` doubles all stub the 4 new methods; clippy -D warnings + fmt clean.
</verification>

<success_criteria>
A user edits one view or script in a project from the terminal (get → edit → put) without touching anything else, deletes surgical resources under guard, and the project carries an executable e2e harness + capture gate that Phases 4–5 inherit — completing the webpage's project management replacement.
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-operations/03-03-SUMMARY.md`
</output>
