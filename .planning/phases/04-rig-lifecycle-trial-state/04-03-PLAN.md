---
phase: 04-rig-lifecycle-trial-state
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - crates/ignition-core/src/client/trial.rs
  - crates/ignition-core/src/client/idp.rs
  - crates/ignition-core/src/client/mod.rs
  - crates/ignition-core/src/actions/rig.rs
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/src/render.rs
  - README.md
  - crates/ignition-cli/tests/contract_rig.rs
  - crates/ignition-core/tests/trial_contract.rs
autonomous: true
user_setup:
  - service: live rigs (trial-reset e2e verification)
    why: "Success criterion demands the reset path verified against ≥2 gateway minor versions (8.3.3 git-module rig + 8.3.6 ign-research)"
    env_vars:
      - name: IGNITION_LIVE_URL
        source: "rig gateway URL (e.g. http://localhost:18088 for ign-research)"
      - name: IGNITION_LIVE_TOKEN
        source: "provisioned API token on the rig (tier-0 spike + authed calls)"
      - name: IGNITION_USER / IGNITION_PASSWORD
        source: "rig admin credentials (tier-1 OIDC login; the existing secret-chain basic tail)"
      - name: IGNITION_LIVE_MUTATIONS
        source: "set to 1 to arm mutation e2e (existing convention)"
    dashboard_config:
      - task: "Provision an API token on ign-research (trial expired — ideal reset subject)"
        location: "gateway web UI → Config → Security → API Tokens"
      - task: "git-module rig up (8.3.3) if not running — docker compose -f ~/Documents/whiskeyhouse/ignition-git-module/docker/docker-compose.yml up -d"
        location: "terminal"
must_haves:
  truths:
    - "rig trial status shows licenseMode, trialState, seconds left, expired — no credential required on a rig"
    - "rig trial reset (refuses without --yes) flips expired:true → false via the spike-chosen mechanism"
    - "The reset mechanism is verified end-to-end against 8.3.3 (git-module rig) AND 8.3.6 (ign-research)"
    - "Banners cross-check rides the same command output (severity/expireTime semantics honored)"
  artifacts:
    - path: "crates/ignition-core/src/client/trial.rs"
      provides: "GET /data/api/v1/trial + GET /data/api/v1/overview/banners wire-faithful models"
      exports: ["TrialState", "BannerSet"]
    - path: "crates/ignition-core/src/client/idp.rs"
      provides: "Native OIDC login+CSRF flow (tier 1) — token rotation, manual redirects, cookie capture"
    - path: "crates/ignition-core/tests/trial_contract.rs"
      provides: "Wiremock pins for trial/banners + the login-flow request-sequence proof"
  key_links:
    - from: "crates/ignition-core/src/actions/rig.rs"
      to: "crates/ignition-core/src/client/trial.rs"
      via: "trial status merges trial endpoint (primary) + banners (cross-check)"
      pattern: "trial_status|banners"
    - from: "crates/ignition-core/src/client/idp.rs"
      to: "POST /data/api/v1/trial"
      via: "session cookie + X-CSRF-Token after the challenge ladder (tier 1)"
      pattern: "csrf|x-csrf-token"
---

<objective>
Trial state: `ign rig trial status` and `ign rig trial reset`.

The roadmap-flagged spike is RESOLVED by research (04-RESEARCH.md): native Rust HTTP wins —
Playwright delegation needs Node+chromium, broke across 8.3.3's UI rewrite, and verifies via
DOM text. Implementation is a LADDER: tier 0 = token-auth POST /trial through the existing
client (one live call decides — research Open Question 1); tier 1 = the fully-mapped OIDC
challenge flow (client/idp.rs); tier 2 = documented Playwright fallback in README only,
never shipped.

Purpose: RIG-02 second half + RIG-03 (the phase's spike criterion).
Output: trial/banners wire models, idp.rs login machinery (spike-gated), trial commands.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rig-lifecycle-trial-state/04-RESEARCH.md
@.planning/phases/04-rig-lifecycle-trial-state/04-01-SUMMARY.md
@.planning/phases/04-rig-lifecycle-trial-state/04-02-SUMMARY.md
@crates/ignition-core/src/client/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trial wire models + trial-status action (wiremock-pinned)</name>
  <files>crates/ignition-core/src/client/trial.rs, crates/ignition-core/src/client/mod.rs, crates/ignition-core/src/actions/rig.rs, crates/ignition-core/tests/trial_contract.rs</files>
  <action>
**client/trial.rs** (per-capability file owns models + path consts; trait methods + one-line impl bodies in the single `impl GatewayApi` block in mod.rs — E0119 rule):
- `GET /data/api/v1/trial` → wire-faithful `TrialWire { licenseMode, trialState, trialSecondsLeft, expired, emergency, emergencySecondsLeft, development, developmentSecondsLeft }` (live-captured shape, research Code Examples) — two-layer naming: actions re-expose unit-explicit keys (`trial_remaining_s`, `expired`, `trial_state`, `license_mode`, `emergency`, `development`)
- `GET /data/api/v1/overview/banners` → `BannerSet { banners: Vec<Banner> }`, `Banner { order, r#type, data: BannerData }`, `BannerData { severity, expireTime (epoch MS or null — Pitfall 7), toolTips, actions }` — serde defaults; trialState domain `AllInDemo|SomeInDemo|NoneInDemo` documented at the model
- Two trait methods: `trial_status_wire()` + `banners()` — **degrade-to-header-less** (resolve_secret_opt pattern, version/wait precedent): these endpoints are unauth-verified live; a fresh rig has no token yet, so NO credential requirement (contrast: inspection commands exit 3 without a secret — trial is rig-family, header-less like StatusPing). Cred present → headers ride along harmlessly.
- Stub both into ALL existing test doubles (the established python-scripted chore).

**actions::rig::trial_status(gateway) -> TrialStatusResult**: primary = trial endpoint; cross-check = banners trial banner. Unit-explicit shape (all keys always):
`{license_mode, trial_state, trial_remaining_s, expired, emergency, emergency_remaining_s, development, banners: {severity, expire_time_ms (null-ok), active (bool: severity=="info" && expireTime>now_ms)}}` — expired-vs-active logic per Pitfall 7 (never derive active from banners alone).

**trial_contract.rs** (wiremock): live-captured bodies as fixtures — expired shape (AllInDemo, trialSecondsLeft 0) + banners warning/null; active shape from resetter capture (severity info, epoch-ms expireTime); unauth-works assertion (header-absence proof, StatusPing precedent); parse pins for expireTime epoch-ms vs null.
  </action>
  <verify>cargo test -p ignition-core trial (green); all test doubles compile (the chore ran); actions output shape asserted exactly via serde_json::json!</verify>
  <done>trial status is contract-complete, credential-free on a rig, banners cross-check correct for expired AND active states</done>
</task>

<task type="auto">
  <name>Task 2: Trial-reset ladder — tier-0 spike, tier-1 native OIDC flow</name>
  <files>crates/ignition-core/src/client/idp.rs, crates/ignition-core/src/client/mod.rs, crates/ignition-core/src/actions/rig.rs, crates/ignition-core/tests/trial_contract.rs</files>
  <action>
**SPIKE FIRST (executable decision rule — no user checkpoint needed):** before writing idp.rs machinery, run tier 0 live: `POST /data/api/v1/trial` with `X-Ignition-API-Token` on ign-research (env-gated #[ignore] test `trial_reset_tier0_probe`, IGNITION_LIVE_* + IGNITION_LIVE_MUTATIONS=1; the rig's trial IS expired — ideal). Record the outcome in the SUMMARY.
- **2xx → SHIP TIER 0 ONLY**: `rig trial reset` = one authed POST through the existing client pipeline (new trait method `trial_reset()`). Keep the tier-1 flow map as code-level documentation (module doc on idp.rs — NOT compiled machinery). Implementation collapses to ~30 lines + wiremock pin.
- **401/403/4xx → IMPLEMENT TIER 1** (the expected branch per research probability, but evidence decides):

**client/idp.rs — the 10-step flow (research Code Examples, live-probed 1–5):**
- Dedicated flow-local request path: build `reqwest::Client` with `redirect(Policy::none())` (matches locked client), NO cookie store — manually capture the ~4 known Set-Cookies into a Vec<(name,value)> and replay them on subsequent calls (fixed sequence; `cookies` feature stays OUT per research)
- Steps: (1) GET /data/app/login → capture Location + idp-relay cookie; (2) GET oidc/auth → Location + idp-sid cookie; (3) POST next-challenge {token:T0} → T1 (TOKEN ROTATES every call — thread forward, Pitfall 2); (4) POST submit-challenge/basic {token:T1, rememberMe:false, challenge:{username,password}} → success bool + T2; bad creds → `{"success":false}` → CoreError::Auth with hint "rig admin credentials rejected"; (5) POST next-challenge {token:T2} → complete + T3; (6) GET oidc/auth?…&token=T3 → Location callback; (7) GET /data/federate/callback/internal?code&state → session Set-Cookie → capture name; (8) GET /data/app/session (session cookie) → CSRF token JSON — **exact cookie name + CSRF field are the spike-with-creds deliverables** (research LOW-confidence flags): discover live in the same #[ignore] run, then wiremock-pin what was observed; (9) POST /data/api/v1/trial with session cookie + X-CSRF-Token header; (10) verify: GET trial → expired:false (the flip is the success proof — mutation actions read back, the 03-01 find precedent)
- Non-JSON 4xx from idp endpoints (consumed-token replay → Jetty HTML 400) → flow failure with HTML title extracted (classify-style sniff, Pitfall 2)
- The flow NEVER touches the locked client pipeline (flow-local redirect consumption — research anti-pattern)
- Credentials: from the existing secret chain's basic tail (IGNITION_USER/IGNITION_PASSWORD) or `--user` flag override; password via `Secret` newtype (redaction discipline)

**actions::rig::trial_reset(gateway or flow, creds) -> TrialResetResult**: run ladder tier 0 → tier 1; result `{rig_url, mechanism: "token"|"login", expired_before, expired_after, trial_remaining_s}` — read-back flip REQUIRED for success (no trusting the POST's 2xx alone).

**Wiremock pins (trial_contract.rs)**: full happy-path ladder as a scripted mock sequence asserting the REQUEST chain (token threading — assert each next-challenge body carries the PREVIOUS response's token; cookie replay; CSRF header on the final POST) — the Phase 2/3 request-not-response discipline. Live e2e (#[ignore], both rigs): tier-0 probe + full tier-1 reset on 8.3.3 (git-module rig) and 8.3.6 (ign-research) — the ≥2-minor-version success criterion; attempt live during execution if rigs reachable, quiet-skip otherwise, document how to run in 04-USER-SETUP notes within the SUMMARY.
  </action>
  <verify>cargo test -p ignition-core trial (green incl. ladder sequence pins); live probe attempted (or quiet-skipped with env documented); SUMMARY records which tier shipped and why</verify>
  <done>Trial reset works through the evidence-chosen tier; the ladder's decision is recorded; request-chain pins prove token rotation/cookie/CSRF handling</done>
</task>

<task type="auto">
  <name>Task 3: CLI wiring — trial arms (reset guarded), goldens, README</name>
  <files>crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/main.rs, crates/ignition-cli/src/render.rs, README.md, crates/ignition-cli/tests/contract_rig.rs</files>
  <action>
**cli.rs**: `RigCommand::Trial(TrialArgs { command: TrialCommand })` — `Status | Reset { user: Option<String> }` (password NEVER a flag — env/secret-chain only, redaction discipline).

**main.rs**:
- `Trial(Status)`: selection → resolve_plan → gateway_url_from(plan) → header-less-degrading client pointed at the RIG's URL (not the profile URL! — rig family addresses the rig; build a for_tests-style client against the derived URL; profile field: the resolved profile name if one exists, else null — document) → trial_status → render (human: countdown line reusing the status banner style)
- `Trial(Reset)`: `require_confirmation(yes, "rig trial reset")` BEFORE any resolution (destructive LOCKED) → selection/plan/creds (exit 3 with an explanatory hint if no token AND no user/password available — the hint names both paths) → trial_reset → render (human: mechanism + before/after flip)

**contract_rig.rs**: reset-without---yes → exit 2, null profile, zero discovery (cwd-with-no-rig proof, reset precedent); `trial --help` golden; a status-path golden is NOT binary-testable without a gateway — covered by trial_contract.rs unit/wiremock layer (document in test header).

**README.md**: trial section — status semantics (trialState domain, expired reading), reset guard + mechanism ladder (tier shipped named; tier-1 flow map summarized; tier-2 Playwright fallback documented as env contract only, never shipped — research Pattern 5), credential sourcing (token via env/keyring OR basic tail), the ≥2-version verification note.
  </action>
  <verify>cargo test --workspace green; clippy -D warnings clean; grep confirms no Password/--password flag exists</verify>
  <done>trial status/reset dispatched contract-complete; guard ordering binary-pinned; README documents the ladder and the shipped tier</done>
</task>

</tasks>

<verification>
- Spike decision recorded with live evidence (tier 0 outcome in SUMMARY)
- Reset success = read-back flip (expired false), never a bare 2xx
- Ladder request-chain pins: token threading, cookie replay, CSRF header — asserted on requests
- No password ever appears as a flag, in JSON, or in logs (Secret redaction)
- Header-less degradation: trial status needs NO credential (fresh-rig friendly)
- ≥2-minor-version e2e exists (#[ignore], env-gated, quiet-skip) — 8.3.3 + 8.3.6
</verification>

<success_criteria>
RIG-02 + RIG-03 complete: `rig trial status` is credential-free truth; `rig trial reset` (guarded) flips an expired rig back to active via the evidence-chosen native mechanism, with the two-version verification harness in place.
</success_criteria>

<output>
After completion, create `.planning/phases/04-rig-lifecycle-trial-state/04-03-SUMMARY.md`
</output>
