---
phase: 04-rig-lifecycle-trial-state
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - crates/ignition-core/src/client/backup.rs
  - crates/ignition-core/src/client/mod.rs
  - crates/ignition-core/src/actions/rig.rs
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/src/render.rs
  - README.md
  - crates/ignition-cli/tests/contract_rig.rs
  - crates/ignition-core/tests/backup_contract.rs
autonomous: true
must_haves:
  truths:
    - "rig snapshot writes a .gwbk (streamed) + per-project exports + manifest.json into a timestamped directory"
    - "rig restore --file <gwbk> (refuses without --yes) posts the backup, waits for gateway RUNNING, and warns that API tokens may have been reset"
    - "Snapshot round-trip: snapshot → mutate → restore → prior state back (e2e gate, two-sided like the 03-03 pin)"
    - "gwbk download streams to disk (never a Vec<u8> in memory)"
  artifacts:
    - path: "crates/ignition-core/src/client/backup.rs"
      provides: "backup_download (streaming, 300s) + backup_restore (raw octet-stream POST, 300s)"
    - path: "crates/ignition-core/src/actions/rig.rs"
      provides: "rig_snapshot (gwbk + project exports + manifest) + rig_restore (POST + restart-wait + warning)"
    - path: "crates/ignition-core/tests/backup_contract.rs"
      provides: "Wiremock pins: octet-stream download/restore, query params, content types"
  key_links:
    - from: "crates/ignition-core/src/client/backup.rs"
      to: "download_to_file"
      via: "gwbk streams through the 03-02 pipeline helper — the only body-consumption site"
      pattern: "download_to_file"
    - from: "crates/ignition-core/src/actions/rig.rs"
      to: "crates/ignition-core/src/poll.rs"
      via: "post-restore StatusPing→RUNNING wait (deadline ≥300s)"
      pattern: "wait_commissioned|poll::"
    - from: "crates/ignition-cli/src/main.rs"
      to: "require_confirmation"
      via: "rig restore guard fires before resolution"
      pattern: "require_confirmation.*restore"
---

<objective>
Snapshot & restore: `ign rig snapshot [-o DIR]` and `ign rig restore --file <path.gwbk>`.

Snapshot = native gwbk (`GET /data/api/v1/backup?type=roaming`, streamed) + per-project
exports (03-02 machinery reused) + a manifest.json recording composition. Restore = raw
octet-stream POST (NOT multipart — research), synchronous-then-restart semantics handled by
a ≥300s post-restore StatusPing wait, plus the token-clobber warning (Pitfall 5).

Purpose: RIG-04 — "repeatable state" for the rig; closes Phase 4.
Output: backup.rs wire methods, snapshot/restore actions + guarded commands, round-trip e2e gate.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rig-lifecycle-trial-state/04-RESEARCH.md
@.planning/phases/04-rig-lifecycle-trial-state/04-01-SUMMARY.md
@.planning/phases/04-rig-lifecycle-trial-state/04-02-SUMMARY.md
@.planning/phases/04-rig-lifecycle-trial-state/04-03-SUMMARY.md
@crates/ignition-core/src/actions/projects.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backup wire methods (streaming download + octet POST)</name>
  <files>crates/ignition-core/src/client/backup.rs, crates/ignition-core/src/client/mod.rs, crates/ignition-core/tests/backup_contract.rs</files>
  <action>
**client/backup.rs** (models + path consts; trait methods + impl bodies in the single impl block):
- `backup_download(out: &Path)` → `GET /data/api/v1/backup?type=roaming` — rides `download_to_file` VERBATIM (the 03-02 streaming pipeline helper — the only new body-consumption site is NONE; this is pure reuse), 300s per-request class (gwbk generation is not instant — research Pitfall 6), `Accept: application/octet-stream`
- `backup_restore(gwbk: &Path)` → `POST /data/api/v1/backup` with **raw octet-stream body** (Content-Type: application/octet-stream — NOT multipart; body read via tokio::fs buffered), query params: `restoreDisabled=false, disableTempProjectBackup=false, renameEnabled=false, restoreLocal=false` (all explicit — the server is the authority on defaults, agents see what was sent), 300s class (restore is synchronous AND blocks — a short timeout kills mid-restore into unknown state, Pitfall 6)
- `put_bytes` may fit the POST (03-03 helper) — if its shape (Content-Type parameterization) doesn't fit octet-stream, add a flow-local `post_octet_stream` beside it rather than bending put_bytes; whichever keeps ONE seam honest
- Stub both into ALL test doubles (the chore)

**backup_contract.rs** (wiremock): octet-stream download pinned with a binary-ish fixture body (assert bytes land on disk identical — read-back compare; do NOT golden binary content into snapbox); restore pinned asserting the REQUEST (Content-Type octet-stream, all four query params explicit, body bytes = file bytes — request-not-response discipline); 401 HTML unauth shape (live-verified) maps through classify.
  </action>
  <verify>cargo test -p ignition-core backup (green); grep confirms no Vec<u8> buffering of the download body</verify>
  <done>backup GET/POST contract-pinned: streaming down, octet up, params explicit, timeouts 300s</done>
</task>

<task type="auto">
  <name>Task 2: snapshot + restore actions (manifest, wait, warning)</name>
  <files>crates/ignition-core/src/actions/rig.rs</files>
  <action>
**`rig_snapshot(gateway, rig_name, out_dir: Option<&Path>) -> SnapshotResult`**:
1. Directory: `-o` override else `./ign-rig-snapshots/<rig>-<yyyyMMdd-HHmmss>/` (timestamp name — NO new dep, chrono-free: format from SystemTime manually or std — keep it dependency-free per research)
2. gwbk first: `backup_download(dir/<rig>.gwbk)`
3. Projects: `projects(list)` → for each, `project_export_to_file(name, dir/projects/<name>.zip)` (03-02 machinery reused verbatim — gwbk *should* include projects per postman semantics, MEDIUM confidence — research flags this; explicit exports are the honest redundancy that makes the manifest truthful either way)
4. `manifest.json`: `{rig, taken_at (epoch s), ignition: {version (from gateway_info)}, gwbk: "<file>", projects: [{name, file}], notes: ["trial clock state is NOT captured by gwbk (unknown behavior — reset separately via rig trial reset)" ]}` — the honest composition contract (roadmap criterion 4's scope-explicitity precedent from 03-02)
5. `SnapshotResult {dir, gwbk_bytes (u64), projects: [names], manifest_path}` — all keys always

**`rig_restore(gateway, gwbk: &Path, wait_timeout_s) -> RestoreResult`**:
1. File pre-checks: exists + non-empty + readable (invalid_input exit 2 precedent, 03-03's InvalidInput lesson — do NOT invent a new slug)
2. `backup_restore(gwbk)` (2xx = restore accepted)
3. Post-restore wait: `wait_commissioned`-family probe (StatusPing → RUNNING, deadline ≥300s — the gateway restarts after restore; the 04-01 shared wait fn reused; RESTART_FLOOR precedent respected by just using the shared probe)
4. WARNING in data (not stderr-only — agents must see it): `"API tokens may have been reset by restore — re-provision via gateway UI, then ign doctor"` (Pitfall 5, 83-api primary source)
5. `RestoreResult {restored_from, state, warnings: [...]}`

Unit tests: fake doubles scripting list/export/download calls (fake gateway records per-project export calls — ProjectsRig precedent); restore pre-check failures (missing file → exit 2 shape); manifest JSON asserted exactly (serde_json::json!); warnings content pinned.
  </action>
  <verify>cargo test -p ignition-core actions::rig snapshot+restore (green); manifest shape asserted exactly</verify>
  <done>snapshot composition truthful + manifest-pinned; restore waits for RUNNING and carries the token warning as data</done>
</task>

<task type="auto">
  <name>Task 3: CLI wiring (restore guarded), goldens, README, e2e round-trip gate</name>
  <files>crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/main.rs, crates/ignition-cli/src/render.rs, README.md, crates/ignition-cli/tests/contract_rig.rs, crates/ignition-cli/tests/e2e_projects.rs</files>
  <action>
**cli.rs**: `RigCommand::Snapshot { output: Option<PathBuf> }`, `RigCommand::Restore { file: PathBuf, timeout: u32 (default 300) }`.

**main.rs**:
- Snapshot: selection → resolve_plan → rig-URL client (the 04-03 trial precedent — rig family addresses the rig) → rig_snapshot → render (human: dir, gwbk size, project list)
- Restore: `require_confirmation(yes, "rig restore")` FIRST (destructive LOCKED — binary-pin the ordering) → selection/plan/client → rig_restore → render (human: restored + state + the token warning visibly)

**contract_rig.rs**: restore-without---yes → exit 2, null profile, no-rig-cwd zero-work proof (the reset precedent, third instance); `snapshot --help` + `restore --help` goldens.

**e2e round-trip gate** (extend e2e_projects.rs or sibling e2e_rig.rs — same conventions: #[ignore], IGNITION_LIVE_* env, quiet skip, IGNITION_LIVE_MUTATIONS=1): snapshot → create/put a marker project resource → restore → assert marker gone AND a pre-snapshot project/resource back (TWO-SIDED pin, the 03-03 replace-not-merge precedent translated to gwbk) → doctor runs clean post-restore (optional read-only tail). Attempt live if a rig is reachable; document env in SUMMARY.

**README.md**: snapshot/restore section — composition honesty (what gwbk covers, trial-clock caveat), restore guard + restart semantics, token-clobber warning + external-token advice (83-api), round-trip e2e run instructions.
  </action>
  <verify>cargo test --workspace green; clippy -D warnings clean; binary pin: restore refusal does zero work; e2e gate present and quiet-skips without env</verify>
  <done>snapshot/restore dispatched contract-complete; round-trip e2e gate exists; Phase 4 user surface complete</done>
</task>

</tasks>

<verification>
- Download streams (no Vec<u8>); restore POST is raw octet-stream with explicit params (request-pinned)
- Restore success = post-restore RUNNING witnessed, never a bare 2xx (Pitfall 6)
- Token-clobber warning rides data in BOTH render modes
- Manifest is the honest composition contract (trial-clock caveat explicit)
- Guard ordering binary-pinned (third destructive instance)
- No new dependencies (timestamp naming is std-only)
</verification>

<success_criteria>
RIG-04 complete: snapshot → mutate → restore returns a rig to its snapshotted state (two-sided e2e proof), warnings surface token clobbering, and Phase 4's four requirements (RIG-01..04) all stand — the project owns a self-managed gateway fixture.
</success_criteria>

<output>
After completion, create `.planning/phases/04-rig-lifecycle-trial-state/04-04-SUMMARY.md`
</output>
