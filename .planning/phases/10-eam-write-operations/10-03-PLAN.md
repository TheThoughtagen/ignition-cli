---
phase: 10-eam-write-operations
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - crates/ignition-core/src/actions/eam.rs
autonomous: true

must_haves:
  truths:
    - "Suspend/resume/cancel/modify/delete are executable as actions over GatewayApi, each carrying an authoritative re-check of verb applicability before the write fires"
    - "The blast-radius preview composes from read-only pre-flight reads (find + scheduled + history) into one struct naming the target task, its type/schedule/state/owner, target agents, and pending executions with can* flags"
    - "Modify is full-record read-modify-write: clone the found record, mutate only targeted keys, PUT with the ORIGINAL signature + collection — never compose-from-scratch (config.settings REQUIRED, the 422 trap)"
    - "Refusals surface through existing exit classes only (no new slugs unless a capture proved necessity — then Three-Place atomic)"
  artifacts:
    - path: "crates/ignition-core/src/actions/eam.rs"
      provides: "eam_task_suspend/resume/cancel/modify/delete actions + BlastRadiusPreview struct + build_blast_radius composer + per-verb guard verdicts (pure fns) + result models"
      contains: "BlastRadiusPreview"
      min_lines: 150
  key_links:
    - from: "actions::eam build_blast_radius"
      to: "GatewayApi eam_task_find + eam_tasks_scheduled"
      via: "read-only pre-flight composition (find record + pending executions)"
      pattern: "eam_task_find|eam_tasks_scheduled"
    - from: "actions::eam eam_task_delete"
      to: "captured delete-confirm policy"
      via: "gateway-side confirm query per 10-LIVE-CAPTURES.md decision"
      pattern: "confirm"
    - from: "pure guard fns"
      to: "10-04 main.rs dispatch + TUI Confirm gating"
      via: "shared pure verdicts usable pre-resolution by all three callers (the ladder's three-place rule)"
      pattern: "pub fn .*(verdict|guard)"
---

<objective>
Build the action layer for the EAM write lifecycle: suspend/resume/cancel/modify/delete actions with authoritative re-checks, the blast-radius preview composer (the read-only pre-flight that feeds every guard prompt), and the result models — all in actions/eam.rs beside the existing ladder.

Purpose: The CLI (10-04) needs one authoritative core layer it cannot bypass — the action re-checks what the pure pre-check decided, the established double-check pattern from task_create — plus the preview data both the CLI refusal message and the TUI Confirm modal will render.

Output: actions/eam.rs grows the write surface + BlastRadiusPreview. No CLI/TUI changes (10-04).
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-eam-write-operations/10-LIVE-CAPTURES.md
@.planning/phases/10-eam-write-operations/10-RESEARCH.md
@crates/ignition-core/src/actions/eam.rs
@crates/ignition-core/src/client/eam.rs
@crates/ignition-core/tests/eam_contract.rs
@.planning/debug/eam-create-422.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Runtime lifecycle actions — suspend/resume/cancel with authoritative re-checks</name>
  <files>crates/ignition-core/src/actions/eam.rs</files>
  <action>
Add three actions following the `eam_task_force` composition shape (find pre-read → runtime POST → result model):
1. `pub async fn eam_task_suspend(api: &dyn GatewayApi, name: &str) -> Result<EamLifecycleResult, CoreError>` (+ resume + cancel siblings). Each: `eam_task_find(name)` first (unknown name → let the find's not_found classify — the error names the task), re-read `scheduledTaskState` + `config.profile.isSuspended`, then the AUTHORITATIVE RE-CHECK per capture-locked truth: suspend refuses (existing error class per the capture decision — e.g. InvalidInput exit 2 naming the task) if the task is ALREADY suspended when the capture proves a distinct wire answer exists; cancel re-checks `canCancel` from the scheduled read when a pending execution exists — if NO pending execution exists, cancel returns an honest no-op-ish result (`cancelled: false`, `reason: "no pending execution"`) rather than firing a doomed POST (capture decides the exact semantics; if the capture shows the gateway's own answer for cancel-with-nothing-pending, mirror it). Do NOT invent state-machine rules the capture doesn't support — where the capture shows the gateway accepts the verb regardless, fire it and report the verbatim outcome (wire honesty over cleverness).
2. `EamLifecycleResult` model (all keys always, the agent-stable shape): `{task, action: "suspended"|"resumed"|"cancelled", previous_state: Option<String>, config_suspended: Option<bool> (the isSuspended mirror read-back, when find proves the sync), pending: Option<EamScheduledTask> (the post-write pending read when the action targets a pending execution)}`.
3. Pure verb-applicability helpers the CLI tier will reuse pre-resolution: `pub fn lifecycle_precheck(action: &str, task_name: &str) -> Result<(), CoreError>` — name-empty/whitespace refusals ONLY (exit 2 InvalidInput, zero network). Keep it pure: no client, no state lookups (those are the action's Tier-3 job). The guard-ladder three-place rule: pure fns shared by main.rs/TUI/action.
4. Unit tests: result models serialize all keys; lifecycle_precheck refusals; the re-check branches use the wiremock-free unit style where possible (model-level) and eam_contract-style integration tests already cover the wire — add action-level tests in actions/eam.rs's test module using the FakeApi double pattern ONLY if an existing double implements the new methods (otherwise keep action tests to model + pure-fn coverage; the wire path is already pinned by 10-02).
  </action>
  <verify>`cargo test -p ignition-core actions::eam` passes; `cargo build --workspace` clean; each of the three actions has a visible authoritative re-check branch (or a documented capture-backed reason none exists); no new error variants added to CoreError.</verify>
  <done>suspend/resume/cancel actions execute over GatewayApi with find-first honesty, re-check branches per capture, and stable result models; pure precheck exported for the CLI tier; existing error taxonomy only.</done>
</task>

<task type="auto">
  <name>Task 2: Config mutation actions — modify (full-record RMW) and delete (signature-keyed)</name>
  <files>crates/ignition-core/src/actions/eam.rs</files>
  <action>
1. `pub async fn eam_task_modify(api: &dyn GatewayApi, name: &str, change: TaskChange) -> Result<EamModifyResult, CoreError>` where `TaskChange` is a builder-ish struct of Optional targeted keys: `rename: Option<String>`, `enabled: Option<bool>`, `description: Option<String>`, `schedule_mode: Option<String>`, `settings_overlay: Option<Value>` (deep-merge into config.settings — reuse the existing `deep_merge`), `suspend_flag: Option<bool>` (profile.isSuspended — only if the capture proved PUT-driven isSuspended works; otherwise OMIT this field entirely per wire honesty). Flow: find → clone the FULL record (serde_json::Value round-trip preserving every key incl. config.settings, signature, collection, type, enabled, description, backupConfig — everything find returns) → apply ONLY the targeted mutations → PUT single-element array carrying the ORIGINAL signature + collection. **Never compose-from-scratch** (compose_task_definition composes CREATE bodies — do not reuse it here; the 422 trap is `config.settings: null`). If 10-LIVE-CAPTURES.md proved rename-via-PUT works, TaskChange.rename is a plain field; if capture showed rename is unsupported (creates-new or 4xx), RENAME THE API HONESTLY: omit rename from TaskChange and note the composite (create-new + delete-old) as a README-documented workflow for 10-04 instead of faking it.
2. `EamModifyResult`: `{task: String (post-write name), changed: Vec<String> (which keys were touched), definition: Value (the verbatim PUT body — the agent's read-back of what was sent), put_outcome: <captured ModifyOutcome reflection>, readback: Value (the post-PUT find, when the capture's echo semantics make a read-back meaningful)}`.
3. `pub async fn eam_task_delete(api: &dyn GatewayApi, name: &str) -> Result<EamDeleteResult, CoreError>`: find first (not_found honesty + the signature comes FROM the found record — never ask the caller for it), then `eam_task_delete(name, signature, confirm)` with the gateway-side confirm policy per 10-LIVE-CAPTURES.md's delete-without-confirm decision (if the capture showed an affected-resources body requires confirm, the ACTION passes confirm=true and the RESULT carries the affected-resource facts so the CLI tier could have previewed them — the blast radius). `EamDeleteResult`: `{task, deleted: bool, changes: <captured changes[]>, affected: Vec<String> (agent/resource names the delete touched, from the captured body shape)}`.
4. Unit tests: TaskChange application over a fixture full record (from the captured find body) — assert ONLY targeted keys mutated, settings preserved, signature/collection untouched; delete result model; the never-compose-from-scratch invariant (a modify of the fixture produces a PUT body whose config.settings is byte-equal to the fixture's).
  </action>
  <verify>`cargo test -p ignition-core actions::eam` passes; a test asserts modify's PUT body preserves the fixture's config.settings verbatim; delete derives signature from find (no signature parameter on the public fn); workspace builds clean.</verify>
  <done>modify performs full-record read-modify-write with targeted-key mutation and original-signature PUT; delete is signature-derived and capture-policy-confirm; rename handled per capture honesty (field or documented composite); all-keys result models.</done>
</task>

<task type="auto">
  <name>Task 3: Blast-radius preview — pure composer over read-only pre-flight reads</name>
  <files>crates/ignition-core/src/actions/eam.rs</files>
  <action>
1. `pub struct BlastRadiusPreview` (Serialize, all keys always): `{task: String, task_type: Option<String>, schedule_mode: Option<String>, state: Option<String> (scheduledTaskState.currentState), owner: Option<String>, config_suspended: Option<bool>, target_gateways: Vec<String> (config.settings.targetGateways — the AGENTS the write touches; empty when none), pending_executions: Vec<EamScheduledTask> (from the scheduled read — each carries canPause/canResume/canCancel/taskState), controller_impact: String (a one-line human sentence: e.g. "suspends task ign-p10-scratch (eam_backup) — future scheduled dispatches to 1 agent stop until resumed"), verb: String}`. The composer: `pub async fn build_blast_radius(api: &dyn GatewayApi, verb: &str, task_name: &str) -> Result<BlastRadiusPreview, CoreError>` — find (unknown → not_found propagates, the preview IS the pre-flight so a bad name refuses before any confirm prompt) + scheduled/true + scheduled/false (both literal segments; filter to the task name; tolerate empty) + history LIMIT — NO, keep it find+scheduled only (history is NOT needed for the radius and doubles the reads; the research lists history as optional — leave it out, document why in the doc-comment). `controller_impact` text per verb (suspend/resume/cancel/force/modify/delete) — factual, no dramatization, names the task and the agent count.
2. `pub fn render_preview_line(preview: &BlastRadiusPreview) -> String` — the single-line form the CLI's require_confirmation operation string will embed (the CLI tier composes: `"{verb} {task}: {controller_impact} targets: [a, b] pending: {n}"` — exact format here, ONE function so the CLI refusal and any future TUI body agree).
3. Force composition: `eam_task_force` currently finds owner itself — extend it to run through build_blast_radius (verb "force") and return the preview in `EamTaskForceResult` as `preview: BlastRadiusPreview` (additive field; the existing fields stay). EAMW-04 is "force composes with the preview surface" — this is that.
4. Unit tests: composer output over fixture find+scheduled bodies (task targeting 2 agents + 1 pending execution; empty-targets case; unknown-task propagation), render_preview_line contains task name + agent count + verb, per-verb controller_impact strings differ where the verbs differ.
  </action>
  <verify>`cargo test -p ignition-core actions::eam` green with the new preview tests; `cargo build --workspace` clean; eam_task_force's result carries `preview` (grep) and its existing callers still compile (ActionOutput mapping unchanged at this layer — the serde addition is additive).</verify>
  <done>BlastRadiusPreview composes from find+scheduled reads only, renders one stable line, force rides it, and every guarded verb can cite target agents + pending executions + controller impact from one composer.</done>
</task>

</tasks>

<verification>
- `cargo test -p ignition-core` fully green (unit + contract suites)
- `cargo build --workspace` + `cargo build --workspace --no-default-features` clean
- `grep -c "BlastRadiusPreview" crates/ignition-core/src/actions/eam.rs` ≥ 4 (struct + composer + force + render)
- No new CoreError variants: `grep -c "^    [A-Z]" crates/ignition-core/src/error.rs` unchanged vs pre-plan (or diff shows zero additions)
- modify's PUT-body-preserves-settings test present and passing
</verification>

<success_criteria>
All five write verbs execute through actions/eam.rs with authoritative re-checks; the blast-radius preview is one pure-ish composer feeding force and future CLI/TUI tiers; wire honesty held throughout (no invented state rules, no from-scratch modify bodies, no new slugs); existing taxonomy exclusively.
</success_criteria>

<output>
After completion, create `.planning/phases/10-eam-write-operations/10-03-SUMMARY.md`
</output>
