---
phase: 10-eam-write-operations
plan: 05
type: execute
wave: 5
depends_on: ["10-04"]
files_modified:
  - crates/ignition-core/tests/live_gateway.rs
  - .planning/phases/10-eam-write-operations/10-LIVE-GATE.md
autonomous: true
user_setup:
  - service: whk-controller-rig
    why: "SC-5 mandates one guarded write live-verified end-to-end against the REAL WHK controller rig — Claude cannot provision access to it"
    env_vars:
      - name: IGNITION_LIVE_URL (or the live_gateway.rs suite's existing env convention)
        source: "The WHK controller gateway's base URL — same env the #[ignore] live suite has used since Phase 4"
      - name: IGNITION_LIVE_TOKEN / credential env per the suite's existing convention
        source: "An API token on the WHK controller with EAM rights — user-provisioned"
    dashboard_config: []

must_haves:
  truths:
    - "An env-gated (#[ignore], skip-by-default) live test performs the full guarded lifecycle against the WHK controller rig: create scratch task → suspend → verify (find read-back + scheduled vocabulary) → resume → delete, cleaning up after itself"
    - "The gate REFUSES to operate on any task not matching its own scratch-task name (safety invariant enforced in test code, not convention)"
    - "The gate run is RECORDED in the phase dir (10-LIVE-GATE.md) with rig, timestamps, verbatim outcomes — SC-5's 'recorded during the phase'"
    - "Trial-expiry cannot false-fail the gate: lifecycle writes, not force EXECUTION, are the mandated path (pitfall 8)"
  artifacts:
    - path: "crates/ignition-core/tests/live_gateway.rs"
      provides: "live_eam_write_lifecycle #[ignore] test: scratch-task create/suspend/verify/resume/delete with name-assertion safety + cleanup on success AND failure paths"
      contains: "eam"
    - path: ".planning/phases/10-eam-write-operations/10-LIVE-GATE.md"
      provides: "Recorded gate run: env used (names only), per-step outcomes, any drift vs wiremock expectations, honest limitations"
      min_lines: 30
  key_links:
    - from: "live_gateway.rs scratch-task lifecycle"
      to: "actions::eam suspend/resume/delete"
      via: "the gate exercises the REAL action layer end-to-end (not raw curl) — CLI-equivalent proof"
      pattern: "eam_task_suspend|eam_task_delete"
    - from: "10-LIVE-GATE.md"
      to: "phase verification"
      via: "SC-5 evidence cited by verify-work / UAT"
      pattern: "suspend"
---

<objective>
Close SC-5: write and RUN the env-gated live gate proving one guarded EAM write end-to-end against the real WHK controller rig — scratch-task create → suspend (guarded) → verify read-back → resume → delete — recorded in the phase as 10-LIVE-GATE.md.

Purpose: The roadmap locks "env-gated live gate recorded during the phase, not bolted on after." Wiremock pins prove wire shape; only this plan proves the real controller accepts the guarded lifecycle through the real action layer.

Output: One #[ignore] live test in live_gateway.rs + the recorded 10-LIVE-GATE.md evidence file.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-eam-write-operations/10-LIVE-CAPTURES.md
@.planning/phases/10-eam-write-operations/10-04-SUMMARY.md
@crates/ignition-core/tests/live_gateway.rs
@crates/ignition-core/src/actions/eam.rs
@.planning/phases/09-agent-surface-api-diagnostics/09-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the env-gated scratch-task lifecycle live test with name-assertion safety</name>
  <files>crates/ignition-core/tests/live_gateway.rs</files>
  <action>
Add `#[tokio::test] #[ignore] async fn live_eam_write_lifecycle()` following the suite's existing env-gate convention (skip silently without the live env, like every check since Phase 4):

1. **Scratch-task isolation (the safety invariant)**: the test derives its task name from a fixed prefix (`ign-live-scratch`) + a timestamp/run suffix, CREATES the task itself (array-body create through the client's eam_task_create with a minimal eam_backup OnDemand definition — config.settings REQUIRED per the 422 trap), and ASSERTS the found record's name equals the scratch name before every write. Every subsequent operation (suspend/resume/delete) asserts `record.name == scratch_name` immediately before firing — a misconfigured IGNITION_LIVE_URL pointing at a production gateway can never touch a real task because the pre-write name assertion fails first. NO environment-supplied task name is ever used for a write.
2. **The guarded lifecycle** through the ACTION layer (not raw client calls — the gate proves the product): `actions::eam::eam_task_suspend` → find read-back asserting the captured suspend signature (isSuspended mirror and/or scheduledTaskState change per 10-LIVE-CAPTURES.md's Decision 1; if the capture proved NO config sync, assert on the scheduled read's state instead) → `eam_task_resume` → read-back → `eam_task_delete` → find asserting not_found. Where the controller-403 gate could legitimately fire (EAM not configured as controller on the target), the test detects EamNotController and FAILS WITH A CLEAR MESSAGE telling the operator the live URL is not an EAM controller (that is a rig-config error, not a code bug).
3. **Cleanup on every path**: wrap in a guard struct whose Drop best-effort deletes the scratch task (the wiremock mock-guard Drop pattern, but for the remote resource) — a mid-test panic must not leave scratch tasks behind; the recorded log notes any Drop-time cleanup.
4. **Trial-expiry immunity** (pitfall 8): the test contains NO force call — lifecycle writes only, so an expired trial cannot false-fail it. If 10-LIVE-CAPTURES.md proved suspend-while-never-run semantics needing a first run, note it; do NOT add force to make the state "interesting."
5. Follow the suite's assertions style: exit-code/envelope assertions via the action Results (typed), not string greps.
  </action>
  <verify>`cargo test -p ignition-core --test live_gateway` compiles and SKIPS cleanly without env (the default `cargo test` run stays green); `grep -c "scratch" crates/ignition-core/tests/live_gateway.rs` shows the name-assertion blocks; no force call in the test.</verify>
  <done>Env-gated lifecycle test exists with pre-write name assertions on every write, action-layer execution, drop-guard cleanup, controller-gate diagnostic, and trial-expiry immunity.</done>
</task>

<task type="auto">
  <name>Task 2: Run the gate against the WHK controller rig → record 10-LIVE-GATE.md</name>
  <files>.planning/phases/10-eam-write-operations/10-LIVE-GATE.md</files>
  <action>
1. Confirm the live env is present (the suite's env vars — names only, never values, echoed into the log). If ABSENT: STOP, record the absence in 10-LIVE-GATE.md as `status: blocked-on-env` with exactly which vars are needed (the user_setup contract), and surface it in the SUMMARY — do NOT fabricate a run, do NOT point the gate at a disposable rig and present it as WHK (the roadmap names the WHK controller rig specifically).
2. Run `cargo test -p ignition-core --test live_gateway live_eam_write_lifecycle -- --ignored --nocapture` and capture full output.
3. Write 10-LIVE-GATE.md: status (passed/blocked-on-env/failed), timestamp, rig identification (URL host redacted to name only), per-step verbatim outcomes (create/suspend/read-back/resume/read-back/delete + cleanup proof), any drift vs the wiremock expectations (e.g. the controller answered a shape wiremock mocked differently — capture wins, note the 10-02/10-03 follow-up), and the SC-5 verdict line.
4. If drift surfaced: record it as evidence. Wire-shape fixes belong in a follow-up gap plan — do NOT silently edit client code inside the live-gate plan (that would break the REQUEST pins' provenance chain). Exception: a pure test-side assertion fix (wrong expected constant) may be fixed and re-run within this plan, noted in the log.
5. If the run FAILED on a real defect (action bug, classification miss): record verbatim, keep the scratch cleanup honest, and mark the plan done-with-findings — the orchestrator decides gap-closure.
  </action>
  <verify>10-LIVE-GATE.md exists with a status line (one of passed / blocked-on-env / failed-with-findings); if passed: the log shows suspend→read-back→resume→delete with cleanup proof and zero non-scratch tasks touched; if blocked-on-env: the needed env var names are listed; `cargo test -p ignition-core` default suite still green (the ignored test didn't leak into normal runs).</verify>
  <done>SC-5 closed: the guarded write lifecycle ran end-to-end against the WHK controller through the real action layer and the evidence is recorded in the phase dir — or the precise blocker (env access) is recorded for the user to unblock.</done>
</task>

</tasks>

<verification>
- `cargo test -p ignition-core --test live_gateway` green (skips without env, passes with env when run --ignored)
- Scratch-name assertion precedes every write call in the test (read the test — one assertion block per write)
- 10-LIVE-GATE.md exists with status + per-step outcomes + cleanup proof (or blocked-on-env with the var list)
- Default test suite unaffected: `cargo test --workspace` green
</verification>

<success_criteria>
The phase's fifth success criterion is either PROVEN (recorded end-to-end WHK run of the guarded lifecycle) or PRECISELY BLOCKED (named env vars, recorded status) — never silently skipped, never substituted with a disposable rig.
</success_criteria>

<output>
After completion, create `.planning/phases/10-eam-write-operations/10-05-SUMMARY.md`
</output>
