---
phase: 10-eam-write-operations
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ignition-tui/src/ui/mod.rs
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "The TUI Confirm modal renders the full blast-radius body readably — the ~140-char preview line word-wraps inside the box; the agents/pending tail is visible; no mid-word right-edge clipping"
    - "The footer hint (y to confirm · Esc to cancel) stays inside the bordered box even when the body wraps to several rows"
    - "The clip is regression-pinned: a TestBackend test renders a realistic blast-radius body at a narrow frame and asserts every distinctive token of the body is present in the buffer"
  artifacts:
    - path: "crates/ignition-tui/src/ui/mod.rs"
      provides: "Confirm modal with .wrap(Wrap { trim: false }) + wrapped-row-aware content-driven height + non-clipping regression tests"
      contains: "Wrap"
  key_links:
    - from: "crates/ignition-tui/src/workers/mod.rs (eam_preview_body)"
      to: "crates/ignition-tui/src/ui/mod.rs (render_modal Confirm arm)"
      via: "multi-line Confirm body (preview line + agents line + pending line) whose long first line must wrap within the modal's inner width"
      pattern: "Modal::Confirm"
    - from: "render_modal height calc"
      to: "wrapped row count vs Ratio(1,2) inner width"
      via: "per-line wrapped-row estimation against (frame_width/2 - 2) inner width; +4 chrome rows preserved"
      pattern: "wrapped"
---

<objective>
Close UAT gap 1 (test 10): the TUI Confirm modal's blast-radius preview clips at the modal's right edge mid-word ("eam_bac…") — targets/pending tail unreadable. Root cause (10-UAT.md): the Confirm arm of `render_modal` (crates/ignition-tui/src/ui/mod.rs:137-147) renders the preview body as an UNWRAPPED single Line in a Ratio(1,2)-width box, and the height calc (line 108) counts raw lines, not display-wrapped rows — so a ~140-char preview line clips and any folded rows would overflow the computed height.

Purpose: the TUI Confirm body IS the blast radius (same one format the CLI refusal embeds) — a clipped preview hides exactly the targets/pending facts the gate exists to expose.

Output: wrapped Confirm rendering with wrapped-row-aware height, proven by buffer-level regression tests.

Scope guard: fix the Confirm arm ONLY. Input modals use deliberate line breaks (comment at ui/mod.rs:165-167) and the Result modal has its own scroll — do not touch them.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-eam-write-operations/10-UAT.md
@crates/ignition-tui/src/ui/mod.rs
@crates/ignition-tui/src/workers/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regression test — the clipped blast-radius preview, pinned in RED</name>
  <files>crates/ignition-tui/src/ui/mod.rs</files>
  <action>
In the existing `mod tests` of `crates/ignition-tui/src/ui/mod.rs` (TestBackend harness `rendered_rows_sized` already lives there — reuse it, no new deps), add a test that reproduces the UAT clip headlessly:

  - Build the realistic Confirm body in the exact shape `eam_preview_body` composes (crates/ignition-tui/src/workers/mod.rs:74-107): a ~140-char preview line modeled on the CLI refusal text (e.g. `suspends task ign-uat-scratch (eam_backup) — future scheduled dispatches to 1 agent stop until resumed targets: [_controller] pending: 0 is destructive; rerun with --yes to confirm`), then `agents: _controller`, then `pending executions: 0`.
  - `state.open_modal(Modal::Confirm { title: "suspend eam task".into(), body })`, render at 80x24 via `rendered_rows_sized` (box inner width = 80/2 − 2 = 38 — far narrower than the preview line, the exact clip condition).
  - Assert NON-CLIPPING: join the modal-interior rows and require every distinctive token of the body to be present — at minimum `dispatches`, `targets:`, `pending:`, `--yes`, `agents: _controller`, and the footer `y to confirm · Esc to cancel`. (Token presence across wrapped rows is the objective proof the tail is readable; a clipped render loses the tail tokens.)
  - Run it: it MUST fail today (RED — the current single-Line render clips and the tail tokens are absent). Commit the failing test first: `test(10-07): pin the Confirm-modal blast-radius clip (UAT test 10)`.

Do not change any rendering code in this task.
  </action>
  <verify>cargo test -p ignition-tui render — the new test FAILS with the tail tokens missing; `git log -1` shows the test-only commit.</verify>
  <done>The clip exists as a failing buffer-level assertion — the regression is pinned before the fix.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap the Confirm body + wrapped-row-aware height (GREEN)</name>
  <files>crates/ignition-tui/src/ui/mod.rs</files>
  <action>
Fix the Confirm arm of `render_modal` (ui/mod.rs:101-147):

  1. WRAP: add `.wrap(Wrap { trim: false })` to the Confirm Paragraph (import `ratatui::widgets::Wrap`). `trim: false` preserves the body's own leading whitespace across wrapped rows — do not use trim:true.
  2. HEIGHT: make the `Modal::Confirm { body, .. }` height arm (line 108) wrapped-row-aware. The modal area is `frame.area().centered(Ratio(1, 2), ...)` (line 133), so the inner (content) width for estimation is `(frame.area().width / 2).saturating_sub(2)` (borders). Write a small helper `fn wrapped_row_count(text: &str, max_width: u16) -> usize` that, per line of `text`, greedily packs whitespace-separated tokens up to max_width (char counts are acceptable — the preview content is ASCII-width; a slight over-estimate is SAFE, an under-estimate is the bug). A single token longer than max_width occupies ceil(len/max_width) rows (ratatui breaks overlong words). Sum per-line counts; minimum 1 row per line. Total stays `wrapped_rows + 4` (two borders + blank separator + footer — the existing +4 contract, line 108).
  3. Note in a comment why the estimate targets the Ratio(1,2) inner width and why over-estimating is safe (content-driven-height doctrine, lines 102-106), and that the buffer test is the arbiter if ratatui's word-wrapping disagrees with the estimate — iterate the estimator until the Task 1 test goes green at 80x24.
  4. Sanity-check the OTHER modal shapes still render (the shared height calc and clamp at line 132 are untouched for non-Confirm arms; run the full ui test module).

Run the Task 1 test to GREEN, then the full suite: `cargo test -p ignition-tui` and `cargo clippy -p ignition-tui`. Commit: `fix(10-07): wrap the Confirm blast-radius body and count wrapped rows in modal height`.
  </action>
  <verify>The Task 1 regression test passes at 80x24; `cargo test -p ignition-tui` fully green; `cargo clippy -p ignition-tui` clean.</verify>
  <done>The TUI Confirm modal renders the complete blast-radius body — preview line word-wrapped, agents/pending tail readable, footer inside the box — with the clip pinned by a permanent buffer-level regression test.</done>
</task>

</tasks>

<verification>
- The RED test from Task 1 passes after Task 2 (tail tokens + footer present in the 80x24 buffer).
- `cargo test -p ignition-tui` green; `cargo clippy -p ignition-tui` clean.
- No changes outside the Confirm arm (Input/Result/other modal shapes untouched).
- Visual re-confirmation rides the next verify-work/UAT pass (the UAT rig was torn down; the buffer-level test is the deterministic equivalent of the screenshot check).
</verification>

<success_criteria>
The UAT gap "TUI Confirm modal body IS the blast-radius preview (task + agents + pending), fully readable" is closed: the body wraps within the modal, the wrapped-row count drives the height, and a buffer-level regression test permanently pins the non-clipping behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/10-eam-write-operations/10-07-SUMMARY.md`
</output>
