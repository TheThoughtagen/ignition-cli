---
phase: 10-eam-write-operations
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - crates/ignition-core/src/client/eam.rs
  - crates/ignition-core/src/client/mod.rs
  - crates/ignition-core/tests/eam_contract.rs
autonomous: true

must_haves:
  truths:
    - "The client can POST suspend/resume/cancel for a task name and GET scheduled/{true|false} pending-execution reads over the real GatewayApi seam"
    - "The client can PUT a full task-definition record (single-element array, original signature + collection) and DELETE by {name}/{signature} with query params"
    - "Every new wire call classifies through the existing classifier: controller-403 → EamNotController, 404 → not_found, per the captured shapes — no second classification site"
    - "Lifecycle vocabulary (taskState, can* flags) is String-typed capture-locked consts provenance, NEVER an enum"
  artifacts:
    - path: "crates/ignition-core/src/client/eam.rs"
      provides: "Path fns (eam_task_suspend_path/resume/cancel, eam_tasks_scheduled_path, eam_tasks_modify_path, eam_task_delete_path) + wire models (EamScheduledTask with capture-locked String vocabulary)"
      contains: "eam-tasks/suspend"
    - path: "crates/ignition-core/src/client/mod.rs"
      provides: "GatewayApi trait methods eam_task_suspend/resume/cancel, eam_tasks_scheduled, eam_task_modify, eam_task_delete + impls beside the existing eam_task_* block"
      contains: "eam_task_suspend"
    - path: "crates/ignition-core/tests/eam_contract.rs"
      provides: "Per-verb wiremock REQUEST pinning: happy path AND captured controller-403 pair for every runtime verb; PUT echoes full settings; DELETE query params + success body"
      contains: "suspend"
  key_links:
    - from: "crates/ignition-core/src/client/eam.rs path fns"
      to: "10-LIVE-CAPTURES.md Decisions section"
      via: "vocabularies + response models match captured shapes verbatim"
      pattern: "capture"
    - from: "crates/ignition-core/src/client/mod.rs classify seam"
      to: "existing classify.rs arms"
      via: "new verbs ride the path-scoped controller-403 arm (is_eam_url) — zero new arms"
      pattern: "eam_not_controller|EamNotController"
---

<objective>
Add the EAM write-operation client surface — runtime lifecycle verbs (suspend/resume/cancel), the scheduled/running pending-execution read, the config-resource PUT modify, and the signature-keyed DELETE — with wiremock contract tests pinning every request shape, capture-locked per 10-LIVE-CAPTURES.md.

Purpose: 10-03's actions and 10-04's CLI can only be honest if the client speaks the captured wire verbatim; this plan is the single place wire shapes enter the codebase.

Output: Extended client/eam.rs + GatewayApi methods in client/mod.rs + eam_contract.rs REQUEST pins. No actions, no CLI (later plans).
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-eam-write-operations/10-LIVE-CAPTURES.md
@.planning/phases/10-eam-write-operations/10-RESEARCH.md
@crates/ignition-core/src/client/eam.rs
@crates/ignition-core/src/client/mod.rs
@crates/ignition-core/tests/eam_contract.rs
@.planning/debug/eam-create-422.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Runtime verb paths + trait methods + capture-locked scheduled-read model</name>
  <files>crates/ignition-core/src/client/eam.rs, crates/ignition-core/src/client/mod.rs</files>
  <action>
In client/eam.rs, beside the existing path fns:
1. Path builders following the `eam_force_path` shape: `eam_task_suspend_path(name)`, `eam_task_resume_path(name)`, `eam_task_cancel_path(name)` → `{EAM_BASE}/eam-tasks/{suspend|resume|cancel}/{name}` (name rides raw — gateway identifiers are URL-safe like the force path; no encode unless a capture shows otherwise). `eam_tasks_scheduled_path(running: bool)` → `{EAM_BASE}/eam-tasks/scheduled/{true|false}` — the LITERAL captured path segment (per-capture; if 10-LIVE-CAPTURES.md shows the segment form differs, capture wins).
2. Model `EamScheduledTask` for the scheduled read items: wire-faithful camelCase fields `{name, owner, type, execStart, message, repeats, canPause, canResume, canCancel, taskState}` with tolerant defaults matching the captured shape (Option where the capture shows absence; `type` renamed). **Vocabulary discipline (09-05 decision, locked): `taskState` (and any state-like String) stays a String field with the captured vocabulary recorded as doc-comment consts + per-element rig provenance — NEVER an enum; unobserved values must parse.** Module-level `pub(crate)` consts for the captured state set live here beside the model (the BUNDLE_*_STATES precedent in client/diagnostics.rs).
3. Response handling for suspend/resume/cancel: 204-on-success per the extract/captures — use `post_empty` (the force precedent) unless 10-LIVE-CAPTURES.md shows a 200 body (capture wins; if a body returns, model it leniently).
4. Config mutations: `eam_tasks_modify_path()` = same resource path as create (`/data/api/v1/resources/{EAM_TASKS_RESOURCE}`, array-body PUT via `put_json`); `eam_task_delete_path(name, signature)` byte-twin of tags.rs `tag_provider_delete_path` with query params per the captured delete (collection + confirm). If the capture's delete-confirm policy implies OUR client always sends `?confirm=true`, encode that here (the gateway-side confirm is a wire fact, distinct from OUR --yes guard which lives in the CLI tier).
5. Trait methods in client/mod.rs beside the eam_task_* block (single impl block — E0119 forbids splitting): `eam_task_suspend(&self, name)`, `eam_task_resume`, `eam_task_cancel`, `eam_tasks_scheduled(&self, running: bool) -> Result<Vec<EamScheduledTask>, CoreError>` (unwrap the captured list envelope per capture shape), `eam_task_modify(&self, definition: &serde_json::Value) -> Result<Option<ModifyOutcome>, CoreError>` where ModifyOutcome is the captured 200 `{success, changes[]}` shape (or `()` if captures show bare 200/204 — capture decides, keep the type minimal), `eam_task_delete(&self, name, signature, confirm: bool) -> Result<DeleteOutcome, CoreError>` per the captured body.
6. Classification: zero new classify.rs arms — the new verbs' URLs live under EAM_BASE so the path-scoped controller-403 arm classifies them; 404 handling rides the existing not_found arm. If 10-LIVE-CAPTURES.md recorded a signature-mismatch status/body that the existing taxonomy does NOT honestly cover (e.g. 400 with JSON body currently falling to Internal exit 1), note it in the module docs and surface it as a FINDING in the summary — do NOT add a slug unilaterally (Three-Place rule; new slugs are 10-03/10-04 decisions with atomic landings).
7. Unit tests in eam.rs's test module: path-fn shape assertions (the force_path test pattern) + model parse tests built from the VERBATIM captured bodies in 10-LIVE-CAPTURES.md (paste captured JSON as fixtures — the history_item test pattern).
  </action>
  <verify>`cargo test -p ignition-core eam` passes including the new path + parse tests; `cargo build -p ignition-core` clean; the new trait methods break NO existing test doubles silently — run `cargo build --workspace` and stub the new capabilities with `unimplemented!()` in every test double that implements GatewayApi (the established chore, version.rs pattern).</verify>
  <done>suspend/resume/cancel/scheduled/modify/delete reachable through GatewayApi; models parse the verbatim captured bodies; vocabulary is String consts with provenance, not enums; classification rides existing arms; all doubles stubbed; workspace builds.</done>
</task>

<task type="auto">
  <name>Task 2: Wiremock contract tests — per-verb REQUEST pinning + controller-403 pairs</name>
  <files>crates/ignition-core/tests/eam_contract.rs</files>
  <action>
Extend eam_contract.rs following the existing per-verb pattern (REQUEST pinning — assert the request, not just the response; `expect(1)` + `.mount()` guard discipline):
1. **Happy 204 pair for each runtime verb**: suspend/resume/cancel — mock POST to the exact path, assert 204 consumed and (via the recorded request) the method+path are exact. Zero body on the request (post_empty shape).
2. **Controller-403 pair for EACH new runtime verb** (pitfall 6): mock 403 with the captured controller message body (reuse the existing controller_403 mock's body) → assert the error is EamNotController (the path-scoped arm catches the new URLs — this test PROVES it, one test per verb). Plus one 403-WITHOUT-the-message test asserting it stays Auth (the existing non-message test pattern, extended to one new verb to prove the message-scoping still holds for the new paths).
3. **scheduled read**: mock GET `/data/eam/api/v1/eam-tasks/scheduled/true` and `/false` with the captured bodies (including the empty-list shape) → assert Vec<EamScheduledTask> parses with the captured taskState strings verbatim.
4. **PUT modify**: mock PUT to the resource path with 200 `{success, changes[]}` (captured shape) → assert the REQUEST BODY is the full single-element array INCLUDING config.settings (the 422-trap proof: the wiremock matcher pins that settings ride) and the original signature key is present; parse the response into ModifyOutcome.
5. **DELETE**: mock DELETE to `{resource}/{name}/{signature}` with the captured query params (collection, confirm per capture policy) → assert query params on the RECORDED request + parse the captured success body into DeleteOutcome. Also the no-confirm capture shape (whatever 10-LIVE-CAPTURES.md recorded: affected-resources 200 body or refusal) parses.
6. **404**: mock 404 on suspend/{name} with the captured 404 body (HTML or JSON per rig capture — mock what was captured) → assert not_found classification.
Fixture bodies come from 10-LIVE-CAPTURES.md verbatim. If a shape was honestly uncaptured (fallback rung b/c in 10-01), build the fixture from the openapi extract and mark the test doc-comment `// spec-shaped, NOT capture-proven (10-01 limitation: …)` — the provenance must be visible in the test file.
  </action>
  <verify>`cargo test -p ignition-core --test eam_contract` passes with the new tests; every new runtime verb has ≥2 tests (happy + controller-403); `grep -c "eam_not_controller\|EamNotController" crates/ignition-core/tests/eam_contract.rs` shows the pair coverage; full `cargo test -p ignition-core` stays green.</verify>
  <done>Every new wire call is pinned at the request level with a capture-backed fixture (or visibly marked spec-shaped); controller-403 classification proven for each new verb; suite green.</done>
</task>

</tasks>

<verification>
- `cargo build --workspace` clean (doubles stubbed, both default and `--no-default-features` lean build per CI)
- `cargo test -p ignition-core` green — old + new eam contract tests
- `grep -n "enum" crates/ignition-core/src/client/eam.rs` shows NO new lifecycle vocabulary enums (String consts only)
- Every fixture that came from a capture cites 10-LIVE-CAPTURES.md in its test name/doc-comment; uncaptured ones say spec-shaped explicitly
- `cargo fmt --check` + `cargo clippy --workspace -- -D warnings` clean
</verification>

<success_criteria>
The wire layer for all six new operations (suspend, resume, cancel, scheduled read, modify, delete) exists, is REQUEST-pinned by wiremock with capture-backed fixtures, rides existing classification with zero new arms, and no lifecycle vocabulary was enum-ified. Any capture-vs-spec gaps are recorded as visible findings for 10-03.
</success_criteria>

<output>
After completion, create `.planning/phases/10-eam-write-operations/10-02-SUMMARY.md`
</output>
