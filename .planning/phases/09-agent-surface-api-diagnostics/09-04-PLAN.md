---
phase: 09-agent-surface-api-diagnostics
plan: 04
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - crates/ignition-core/src/client/license.rs
  - crates/ignition-core/src/client/redundancy.rs
  - crates/ignition-core/src/client/gan.rs
  - crates/ignition-core/src/client/mod.rs
  - crates/ignition-core/src/actions/license.rs
  - crates/ignition-core/src/actions/redundancy.rs
  - crates/ignition-core/src/actions/gan.rs
  - crates/ignition-core/src/actions/mod.rs
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/src/render.rs
  - crates/ignition-tui/src/routes.rs
  - crates/ignition-cli/tests/contract_diagnostics.rs
  - README.md
autonomous: true
must_haves:
  truths:
    - "`ign license status` answers in ONE command: curated morning-check fields + trial companion, with the deep/variable remainder riding flatten passthrough (both rigs' captured shapes parse)"
    - "`ign redundancy status` answers in ONE command: the flat 11-field model with uptime/lastSyncTimestamp units documented FROM the captures"
    - "`ign gan status` answers in ONE command: the 5-field overview/gan model; the zero-connection capture parses"
    - "Every new wire model is version-tolerant: #[serde(default)] on optionals, rename + snake_case alias on every camelCase key, NO deny_unknown_fields, flatten passthrough remainder"
    - "routes.rs gains Dashboard rows for `license status`, `redundancy status`, `gan status` IN THE SAME TASK as their clap commands; clap walk green"
  artifacts:
    - path: crates/ignition-core/src/client/license.rs
      provides: "LicenseStatusWire (partial-curated + flatten) + capability method; trial companion reuses TrialWire"
      contains: "flatten"
    - path: crates/ignition-core/src/client/redundancy.rs
      provides: "RedundancyStatusWire — flat model, units documented from captures"
      contains: "RedundancyStatusWire"
    - path: crates/ignition-core/src/client/gan.rs
      provides: "GanStatusWire — 5 numeric fields + flatten"
      contains: "GanStatusWire"
    - path: crates/ignition-cli/tests/contract_diagnostics.rs
      provides: "binary contract tests for the three reads (envelope data matches capture-shaped fixtures; unknown extras ride through)"
      min_lines: 50
    - path: crates/ignition-tui/src/routes.rs
      provides: "three Screen(Dashboard) rows"
      contains: "license status"
  key_links:
    - from: 09-LIVE-CAPTURES.md
      to: crates/ignition-core/src/client/license.rs
      via: "captured bodies embedded verbatim as parse fixtures (both rigs)"
      pattern: "LIVE-CAPTURES"
    - from: crates/ignition-cli/src/main.rs
      to: crates/ignition-core/src/session.rs
      via: "all three dispatch arms resolve through Session::resolve"
      pattern: "Session::resolve"
---

<objective>
Ship the three curated morning-check reads — `license status`, `redundancy status`, `gan status` — as thin capability methods over version-tolerant wire models whose fixtures ARE the live captures from 09-02 (both rigs).

Purpose: success criterion 3 — "each return live gateway truth with one command". Curation = the morning-check fields typed + the point-release-variable remainder passthrough'd (the roadmap's version-tolerance directive).
Output: client/{license,redundancy,gan}.rs + actions + CLI leaves + dispatch + render + TUI rows + binary contract tests + README rows.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-agent-surface-api-diagnostics/09-RESEARCH.md
@.planning/phases/09-agent-surface-api-diagnostics/09-02-SUMMARY.md
@.planning/phases/09-agent-surface-api-diagnostics/09-LIVE-CAPTURES.md
@crates/ignition-core/src/client/trial.rs
@crates/ignition-core/src/client/version.rs
@crates/ignition-core/src/client/mod.rs
@crates/ignition-cli/src/cli.rs
@crates/ignition-cli/src/main.rs
@crates/ignition-cli/src/render.rs
@crates/ignition-tui/src/routes.rs
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: version-tolerant wire models + capability methods, capture-backed parse tests</name>
  <files>crates/ignition-core/src/client/license.rs, crates/ignition-core/src/client/redundancy.rs, crates/ignition-core/src/client/gan.rs, crates/ignition-core/src/client/mod.rs</files>
  <action>
The pattern is Pattern 3 (research) with TrialWire (client/trial.rs) as the canonical model — every camelCase wire key gets `#[serde(rename = "camelKey", alias = "snake_key")]` + `#[serde(default)]`; anything not live-captured stays `Option`; NO `deny_unknown_fields` anywhere; deep/variable remainders ride `#[serde(flatten)] pub extra: BTreeMap<String, serde_json::Value>`. The repo's own history is the cautionary tale: a missing rename silently DROPPED a live field (client/version.rs LicenseInfo comment). ALL field decisions come from 09-LIVE-CAPTURES.md — read it FIRST; if a capture contradicts this plan's field sketch, the capture wins (record in SUMMARY).

1. `client/license.rs`: `pub const LICENSES_PATH: &str = "/data/api/v1/licenses";`. `LicenseStatusWire` = partial-curated (per research: the fields the morning check needs — license mode/edition/count — pulled from the captures into typed fields) + `#[serde(flatten)] extra`. Nesting mirrors ONLY what captures show: `hardware: Vec<LicenseHardware>` (default) → `items: Vec<LicenseItem>` (default) → `details` stays `Option<serde_json::Value>` passthrough (deeply point-release-variable — never fully modeled). Doc comment cites the capture per field.
2. `client/redundancy.rs`: `pub const REDUNDANCY_PATH: &str = "/data/api/v1/redundancy";`. `RedundancyStatusWire` — role: String (the captures + GatewayInfo.redundancy_role show Independent/Primary/Backup — a String, not an enum: unknown roles must ride, not refuse); project_state/activity_level/local_id/peer_id: Option<String>; peer_connected/has_config_access/sync_pending/failover_pending: bool with default; uptime/last_sync_timestamp: Option<i64> — doc comments state the CAPTURED units verbatim (from 09-LIVE-CAPTURES.md Decisions section; if the capture was inconclusive, the doc comment says so and the field stays Option). Flatten extra.
3. `client/gan.rs`: `pub const GAN_OVERVIEW_PATH: &str = "/data/api/v1/overview/gan";`. `GanStatusWire` — total_connections/running_connections/remote_gateways: i64 default; outgoing_byte_rate/incoming_byte_rate: f64 default (byte rates may be fractional — capture decides int-vs-float; f64 parses both). Flatten extra. (Decision 4: overview/gan ONLY — no /gateway-network/gateways this phase; `api call` covers it.)
4. client/mod.rs: `GatewayApi` gains `async fn license_status(&self) -> Result<LicenseStatusWire, CoreError>`, `async fn redundancy_status(&self) -> Result<RedundancyStatusWire, CoreError>`, `async fn gan_status(&self) -> Result<GanStatusWire, CoreError>` — each `get_json(PATH, None, true)` (auth=true; these are /data routes).
5. Parse tests IN each module (TrialWire test style): the captured body from EACH rig embedded as a fixture — both must parse; unknown-key round-trip test (append a fake unknown key to a fixture → still parses AND rides `extra`).
  </action>
  <verify>cargo test -p ignition-core license && cargo test -p ignition-core redundancy && cargo test -p ignition-core gan && cargo clippy --all-targets -- -D warnings && cargo fmt --check</verify>
  <done>Both rigs' captured shapes parse per family; units documented from captures; passthrough remainders typed as flatten</done>
</task>

<task type="auto">
  <name>Task 2: actions + CLI leaves + dispatch + render + TUI Dashboard rows (clap + rows together)</name>
  <files>crates/ignition-core/src/actions/license.rs, crates/ignition-core/src/actions/redundancy.rs, crates/ignition-core/src/actions/gan.rs, crates/ignition-core/src/actions/mod.rs, crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/main.rs, crates/ignition-cli/src/render.rs, crates/ignition-tui/src/routes.rs</files>
  <action>
1. actions/license.rs: `pub async fn license_status(api: &dyn GatewayApi) -> Result<LicenseStatusResult, CoreError>` where `LicenseStatusResult { pub license: LicenseStatusWire, pub trial: TrialWire }` — the morning-check payload merges the /licenses read with the trial companion (reusing client/trial.rs::TrialWire + its capability method — ALREADY live-proven; no new trial code). Serialize derive.
2. actions/redundancy.rs / actions/gan.rs: thin wrappers returning the wire (Serialize) — `RedundancyStatusResult { status: RedundancyStatusWire }` / `GanStatusResult { status: GanStatusWire }` (the wrapper indirection keeps render arms symmetrical with the license merge).
3. cli.rs: THREE top-level families (decision 2), each `#[command(arg_required_else_help = true)]`: `License(LicenseArgs)` / `Redundancy(RedundancyArgs)` / `Gan(GanArgs)`, each `Args { #[command(subcommand)] command: LicenseCommand|RedundancyCommand|GanCommand }` with a single `Status` variant (`ign license status` etc. — uniform leaves, room to grow).
4. main.rs: three dispatch arms via `Session::resolve(cli.profile.as_deref())` → the actions. render.rs: `ActionOutput::LicenseStatus|RedundancyStatus|GanStatus` + arms. Human mode: small tables — license: trial mode/countdown line + per-hardware-key item rows (name/title/version); redundancy: role/peer/project-state/sync-pending rows + uptime + last-sync (units as documented); gan: total/running connections + in/out byte rates + remote gateways. JSON mode: envelope data = the action results verbatim.
5. routes.rs: `CliRoute { path: "license status" | "redundancy status" | "gan status", mapping: Mapping::Screen(Screen::Dashboard) }` (decision 3: curated reads → Dashboard — they are morning-check verbs; NO new Diagnostics screen this phase). Clap walk stays green (rows + commands in one task).
  </action>
  <verify>cargo build && cargo test -p ignition-cli --test tui_coverage && ./target/debug/ign license status --help >/dev/null && cargo clippy --all-targets -- -D warnings && cargo fmt --check</verify>
  <done>Three one-command reads dispatch through the seam; Dashboard rows land with the clap commands; walk green</done>
</task>

<task type="auto">
  <name>Task 3: binary contract tests + README command rows</name>
  <files>crates/ignition-cli/tests/contract_diagnostics.rs, README.md</files>
  <action>
1. New `crates/ignition-cli/tests/contract_diagnostics.rs` (isolated-config + wiremock; this file is ALSO 09-05's home — start it with a module doc saying so):
   - `license_status_envelope`: mount the 8.3.x captured license shape (+ trial shape) → `ign license status --json` → ok:true, data.license carries the typed fields, data.trial carries trial_seconds_left; a mounted UNKNOWN extra key rides into data (passthrough proof).
   - `redundancy_status_envelope` + `gan_status_envelope`: same shape — mounted captures parse; gan zero-connection fixture answers.
   - `reads_keep_existing_error_classes`: 404 → exit 6 not_found; 401 → exit 5 (these reads ride the CURATED pipeline — the 09-01 non-leak guarantee at the binary level).
2. README.md: command-reference rows for `ign license status` (mentions the trial companion merge), `ign redundancy status` (units as captured), `ign gan status` (zero-connection meaning on a non-GAN gateway).
  </action>
  <verify>cargo test -p ignition-cli --test contract_diagnostics && cargo test --workspace && cargo fmt --check</verify>
  <done>Three reads binary-contract-pinned with capture-shaped fixtures; README documents</done>
</task>

</tasks>

<verification>
- cargo test --workspace green
- rg -n 'deny_unknown_fields' crates/ignition-core/src/client/{license,redundancy,gan}.rs → ZERO hits
- Every typed field's doc comment cites its capture; flatten present in all three models
- clap walk + tui_coverage green; Session::resolve in all three dispatch arms
</verification>

<success_criteria>
- Success criterion 3 contract-ready: one command per read, capture-backed models, live proof lands in 09-06
- Zero guessed shapes: every model field traces to 09-LIVE-CAPTURES.md
</success_criteria>

<output>
After completion, create `.planning/phases/09-agent-surface-api-diagnostics/09-04-SUMMARY.md`
</output>
