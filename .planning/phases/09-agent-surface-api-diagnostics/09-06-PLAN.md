---
phase: 09-agent-surface-api-diagnostics
plan: 06
type: execute
wave: 5
depends_on: ["09-03", "09-04", "09-05"]
files_modified:
  - crates/ignition-cli/tests/e2e_api_diagnostics.rs
  - .planning/phases/09-agent-surface-api-diagnostics/09-RIG-NOTES.md
autonomous: true
must_haves:
  truths:
    - "e2e_api_diagnostics.rs exists with env-gated #[ignore] gates that are a quiet green no-op without IGNITION_LIVE_URL/IGNITION_LIVE_TOKEN"
    - "The api-call live gate exercises ONLY read-only paths (GETs + the capture-proven read-only 404/405 probes) — no POST/PUT/DELETE beyond those pinned probes (roadmap: passthrough can't nuke the rig)"
    - "The bundle generate→wait→download round-trip gate is gated behind IGNITION_LIVE_MUTATIONS=1 (the e2e_webdev refusal shape)"
    - "Every gate ran against BOTH rigs (8.3.3 port 19188 + 8.3.6 port 18188) and passed — success criteria 3 + 4 live-verified; per-rig results recorded in 09-RIG-NOTES.md"
  artifacts:
    - path: crates/ignition-cli/tests/e2e_api_diagnostics.rs
      provides: "env-gated live gates: license/redundancy/gan reads, api-call envelope + read-only 4xx partition, bundle round-trip (mutations-gated)"
      min_lines: 80
      contains: "IGNITION_LIVE_URL"
    - path: .planning/phases/09-agent-surface-api-diagnostics/09-RIG-NOTES.md
      provides: "per-rig PASS lines for every gate + verbatim timings + teardown record"
      contains: "PASS"
  key_links:
    - from: crates/ignition-cli/tests/e2e_api_diagnostics.rs
      to: .planning/phases/09-agent-surface-api-diagnostics/09-LIVE-CAPTURES.md
      via: "live assertions cite the capture-backed expectations (state vocabulary, units, 4xx probe answers)"
      pattern: "LIVE-CAPTURES"
    - from: .planning/phases/09-agent-surface-api-diagnostics/09-RIG-NOTES.md
      to: .planning/phases/04-rig-lifecycle-trial-state/04-VERIFICATION.md
      via: "rig provisioning follows the Phase-4 headless recipe (18188/19188, collection:\"core\" token)"
      pattern: "04-VERIFICATION"
---

<objective>
Close the phase with the live gates: env-gated, read-only-by-default e2e tests over the real binary against BOTH docker rigs, proving success criteria 3 + 4 (curated reads + bundle round-trip + the api-call contract) on live gateway truth — and running them.

Purpose: wiremock proves the contract; live rigs prove the wire. The both-rigs run is the phase's verification oracle; 8.3.x point-release variance is the documented failure mode this closes.
Output: e2e_api_diagnostics.rs + recorded both-rig runs + teardown. No product code changes.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-agent-surface-api-diagnostics/09-RESEARCH.md
@.planning/phases/09-agent-surface-api-diagnostics/09-03-SUMMARY.md
@.planning/phases/09-agent-surface-api-diagnostics/09-04-SUMMARY.md
@.planning/phases/09-agent-surface-api-diagnostics/09-05-SUMMARY.md
@.planning/phases/09-agent-surface-api-diagnostics/09-LIVE-CAPTURES.md
@.planning/phases/09-agent-surface-api-diagnostics/09-RIG-NOTES.md
@.planning/phases/04-rig-lifecycle-trial-state/04-VERIFICATION.md
@crates/ignition-cli/tests/e2e_webdev.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: write the env-gated live gates (quiet no-op without envs)</name>
  <files>crates/ignition-cli/tests/e2e_api_diagnostics.rs</files>
  <action>
Pattern: e2e_webdev.rs:14-73 verbatim (env helpers live_url()/live_token(), `#[ignore = "…"]` + early-return when envs absent, mutations refusal message for the gated test). File-header env table documents IGNITION_LIVE_URL / IGNITION_LIVE_TOKEN (full `name:key`) / IGNITION_LIVE_MUTATIONS (=1 unlocks ONLY the bundle round-trip). Real binary via `Command::cargo_bin("ign")` + isolated IGNITION_CLI_CONFIG pointing at the live URL (the version_gateway_contract.rs isolation shape).

Gates (each asserts against the 09-LIVE-CAPTURES.md expectations, not hardcoded guesses):
1. `license_status_live` (read-only): `ign license status --json` → ok:true; data.license parses; data.trial.trial_seconds_left present.
2. `redundancy_status_live` (read-only): ok:true; role ∈ the captured role set (Independent/Primary/Backup); units noted in an assert comment per captures.
3. `gan_status_live` (read-only): ok:true; running_connections ≤ total_connections (live-truth sanity, not a wire-format guess).
4. `api_call_envelope_live` (read-only): `ign api call --json --method GET --path /data/api/v1/gateway-info` → ok:true; data parses as JSON with the gateway's own keys (spot-check a capture-named key).
5. `api_call_4xx_partition_live` (READ-ONLY by construction): `--method GET --path /data/api/v1/definitely-not-a-real-endpoint` → exit 6, slug not_found; `--method DELETE --path /data/api/v1/gateway-info` → exit 2, slug gateway_client_error (the 09-02 capture proved this read-only route 405s; assert the captured answer — if the capture recorded something else, assert THAT). No other mutating verbs appear anywhere in this file.
6. `bundle_round_trip_live` (`IGNITION_LIVE_MUTATIONS=1` required — refusal early-return mirroring e2e_webdev:223-227): download to a tempdir → `ign diagnostics bundle generate --json` → `ign diagnostics bundle wait --interval 2 --timeout 300 --json` (state per captures) → `ign diagnostics bundle download -o <tmp>/b.zip --json` → file non-empty + starts `PK\x03\x04` → re-read `bundle status` → ok.
Quiet no-op proof: `cargo test -p ignition-cli --test e2e_api_diagnostics` (no envs) → all green, zero gate bodies executed.
  </action>
  <verify>cargo test -p ignition-cli --test e2e_api_diagnostics (green no-op) && rg -c 'IGNITION_LIVE_MUTATIONS' → only the bundle test gates on it && rg -n 'POST|--method PUT|--method DELETE' → only the pinned gateway-info DELETE probe</verify>
  <done>Gates written, read-only discipline provable from the file, no-env run is a quiet green no-op</done>
</task>

<task type="auto">
  <name>Task 2: run the full gate matrix on BOTH rigs, record, teardown</name>
  <files>.planning/phases/09-agent-surface-api-diagnostics/09-RIG-NOTES.md</files>
  <action>
1. Re-provision both rigs per Task-1 of 09-02 (the recipe is recorded in 09-RIG-NOTES.md; ports 18188=8.3.6, 19188=8.3.3). Sanity: authed gateway-info 200 per rig.
2. Per rig: `IGNITION_LIVE_URL=… IGNITION_LIVE_TOKEN=… cargo test -p ignition-cli --test e2e_api_diagnostics -- --ignored` (read-only gates) — all PASS; then `IGNITION_LIVE_MUTATIONS=1 … bundle_round_trip_live` (or the whole file with MUTATIONS set). Record each test's PASS + wall time per rig.
3. Any live discrepancy (a shape that moved vs 09-02 captures): STOP, capture the new truth, adjust the gate assertion to the captured reality, note the delta in 09-RIG-NOTES.md AND the SUMMARY (point-release variance is the expected failure mode — the gate must encode live truth, never the hypothesis).
4. Teardown both rigs (docker rm -f / compose down -v), confirm ports freed, record.
5. Final phase check: `cargo test --workspace && cargo clippy --all-targets -- -D warnings && cargo fmt --check`.
  </action>
  <verify>09-RIG-NOTES.md shows per-rig PASS lines for all six gates on 8.3.3 AND 8.3.6 with timings; docker ps → no ign-p9 containers; workspace test suite green</verify>
  <done>Success criteria 3 + 4 live-verified on both rigs with recorded evidence; rigs torn down; phase suite green</done>
</task>

</tasks>

<verification>
- No-env test run: quiet green no-op (the CI-default contract)
- Both-rig runs: recorded PASS evidence for every gate, both versions
- rg the gate file: zero mutating verbs beyond the pinned read-only probes; MUTATIONS gate on the bundle test only
- cargo test --workspace / clippy -D warnings / fmt --check green
</verification>

<success_criteria>
- The phase goal's "live-verified on both rigs" clause is EVIDENCED: recorded per-rig runs, not claimed
- The live-gate design honors the roadmap directive: read-only passthrough, mutations opt-in for generate only
</success_criteria>

<output>
After completion, create `.planning/phases/09-agent-surface-api-diagnostics/09-06-SUMMARY.md`
</output>
