---
phase: 09-agent-surface-api-diagnostics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-agent-surface-api-diagnostics/09-LIVE-CAPTURES.md
  - .planning/phases/09-agent-surface-api-diagnostics/09-RIG-NOTES.md
autonomous: true
must_haves:
  truths:
    - "Both rigs (8.3.3 + 8.3.6) are spun, commissioned, and headlessly token-provisioned per the Phase-4 recipe, and answer authed GET /data/api/v1/gateway-info with 200"
    - "Verbatim response bodies for /licenses, /trial, /redundancy, /overview/gan, and the diagnostics-bundle generate→status→download loop are captured from BOTH rigs and embedded (fenced, rig-labeled) in 09-LIVE-CAPTURES.md"
    - "The bundle `state` vocabulary is decided from CAPTURES ONLY (never guessed) — the generating-set membership is written down with its evidence"
    - "uptime / lastSyncTimestamp / fileSize units are determined from captures (magnitude cross-check against wall clock / rig age) and recorded"
    - "Read-only 4xx wire answers are captured: a bogus GET path (expected 404) and DELETE /data/api/v1/gateway-info (expected 405) on both rigs — the live 4xx partition evidence for 09-06's gates"
  artifacts:
    - path: .planning/phases/09-agent-surface-api-diagnostics/09-LIVE-CAPTURES.md
      provides: "Rig-labeled verbatim captures per endpoint + the Decisions-locked-by-captures section (state vocabulary, units, license nesting, gan zero-connection shape)"
      contains: "8.3.3"
    - path: .planning/phases/09-agent-surface-api-diagnostics/09-RIG-NOTES.md
      provides: "Ops log: container IDs/names, ports (18188=8.3.6, 19188=8.3.3), token NAMES (never keys), provisioning commands used, teardown record"
      contains: "18188"
  key_links:
    - from: .planning/phases/09-agent-surface-api-diagnostics/09-LIVE-CAPTURES.md
      to: .planning/phases/09-agent-surface-api-diagnostics/09-04-PLAN.md
      via: "captured bodies become the parse fixtures for the version-tolerant license/redundancy/gan models"
      pattern: "LIVE-CAPTURES"
    - from: .planning/phases/09-agent-surface-api-diagnostics/09-LIVE-CAPTURES.md
      to: .planning/phases/09-agent-surface-api-diagnostics/09-05-PLAN.md
      via: "captured state strings become BUNDLE_GENERATING_STATES + BundleStatusWire doc-comment vocabulary"
      pattern: "BUNDLE_GENERATING"
---

<objective>
Capture the live wire truth this phase's models must encode — BEFORE any model-finalization task (roadmap directive) — from BOTH supported rigs (8.3.3 + 8.3.6), using the Phase-4 headless docker recipe.

Purpose: 8.3.x point-release variance is the documented failure mode; the bundle `state` enum values, license nesting, and timestamp units exist NOWHERE in the docs (OpenAPI says only `string`). Guessing them is Pitfall 2; capturing them makes every downstream model fixture-backed.
Output: 09-LIVE-CAPTURES.md (verbatim bodies + locked decisions) + 09-RIG-NOTES.md (ops log). No code changes.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-agent-surface-api-diagnostics/09-RESEARCH.md
@.planning/phases/04-rig-lifecycle-trial-state/04-VERIFICATION.md
@crates/ignition-core/src/client/trial.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spin + commission + headless-token-provision both rigs</name>
  <files>.planning/phases/09-agent-surface-api-diagnostics/09-RIG-NOTES.md</files>
  <action>
Follow the proven Phase-4 recipe (04-VERIFICATION.md:168-286 — read it first; it is the authoritative recipe):

1. **Rig A** = `inductiveautomation/ignition:8.3.6` on host port **18188**, ephemeral container (Phase 4 used `docker run`, name it `ign-p9-836`). **Rig B** = `inductiveautomation/ignition:8.3.3` on host port **19188** (Phase 4 used its own compose with `IGNITION_RIG_ROOTS=/tmp/ign-p9-rigs` redirect; name `ign-p9-833`). Set `GATEWAY_ADMIN_PASSWORD=password`. Pre-flight: both ports must be free before starting (a held port means pick free alternates and record them — never stop another stack).
2. Wait for commissioning readiness (poll `/StatusPing` unauthenticated; follow the Phase-4 commissioning steps from 04-VERIFICATION.md — the env-password first-run flow, web-commission if the recipe's record shows it was needed).
3. **Headless API-token provisioning per rig** (04-VERIFICATION.md:246-286, version-agnostic, proven on both): session login → `POST /data/api/v1/api-token/generate` `{}` (session + `X-CSRF-Token`) → register via `POST /data/api/v1/resources/ignition/api-token` with the ARRAY body (`collection:"core"` — the 04-03 mystery value) → **the 403 fix**: GET then PUT the `security-properties` singleton (`?collection=core`, array body carrying the current `signature`) with both permission sets set to `AnyOf [{"name":"Authenticated","children":[]}]`.
4. Sanity: `curl -H "X-Ignition-API-Token: <name:key>" http://localhost:<port>/data/api/v1/gateway-info` → 200 per rig.
5. Record in 09-RIG-NOTES.md: image tags, container names/IDs, ports, every provisioning command shape that worked (with placeholders for secrets — NEVER record the token key; keys live only in the session shell env). Timestamps for commissioning.
  </action>
  <verify>curl -s -o /dev/null -w '%{http_code}' -H "X-Ignition-API-Token: $T_A" http://localhost:18188/data/api/v1/gateway-info → 200 (and :19188 with $T_B)</verify>
  <done>Both rigs answer authed gateway-info 200; ops log written without any secret material</done>
</task>

<task type="auto">
  <name>Task 2: Capture every wire shape per rig → 09-LIVE-CAPTURES.md, then teardown</name>
  <files>.planning/phases/09-agent-surface-api-diagnostics/09-LIVE-CAPTURES.md</files>
  <action>
For EACH rig, label every observation `8.3.3` or `8.3.6` and embed captured JSON VERBATIM in fenced blocks:

1. `GET /data/api/v1/licenses` — full pretty body. Record: top-level keys, array shapes, nesting depth, and any point-release differences between rigs. This decides the partial-curated vs passthrough split for 09-04 (morning-check fields get typed; the deep remainder rides flatten).
2. `GET /data/api/v1/trial` — full body (cross-check: `TrialWire` in client/trial.rs must still parse it; note any drift).
3. `GET /data/api/v1/redundancy` — full body. **Determine units**: compare `lastSyncTimestamp` magnitude against current wall-clock (epoch-seconds ≈ 1.7-1.8e9 vs epoch-ms ≈ 1.7-1.8e12); assess `uptime` against the rig's known age (a minutes-old rig rules out ms-since-epoch — likely seconds-since-start or epoch-seconds; record the reasoning + numbers).
4. `GET /data/api/v1/overview/gan` — full body. A fresh rig's zero-connection shape IS the capture (decision 4 evidence). Note number-vs-int shapes for the byte rates.
5. **Bundle state machine** (Pitfall 2 — capture, never guess): `POST /data/api/v1/diagnostics/bundle/generate` (no body) → capture the response; then poll `GET /data/api/v1/diagnostics/bundle/status` every ~2 s and record EVERY distinct `{state, fileSize}` observation with timestamps until the terminal/ready state; then `GET /data/api/v1/diagnostics/bundle/download` → record `Content-Disposition` (may be absent), `Content-Type`, total byte size, and the first 4 bytes (`PK\x03\x04` expected). This is the one benign-but-real mutation; it touches nothing else on the rig.
6. **Read-only 4xx probes** (the live partition evidence for 09-06): `GET /data/api/v1/definitely-not-a-real-endpoint` (expect 404 — capture status + verbatim body) and `DELETE /data/api/v1/gateway-info` (expect 405 — a read-only status route cannot mutate; capture status + verbatim body). If either answers differently, capture what actually happened — the capture is the truth, the expectation is just a hypothesis.
7. Close the file with **"Decisions locked by captures"**: the exact bundle state vocabulary per rig + which states are generating (this becomes `BUNDLE_GENERATING_STATES`), the timestamp/units findings, the license nesting summary, the gan zero-connection shape, and the 4xx probe outcomes.
8. **Teardown**: `docker rm -f ign-p9-836` / compose `down -v --remove-orphans` for Rig B; confirm ports freed; record teardown in 09-RIG-NOTES.md.
  </action>
  <verify>09-LIVE-CAPTURES.md contains fenced captures for all six probe groups labeled with both rig versions, and the Decisions section names state strings verbatim; docker ps shows no ign-p9 containers remaining</verify>
  <done>Every model field 09-04/09-05 will encode has a live-backed capture on both rigs; state vocabulary + units decided with recorded evidence; rigs torn down</done>
</task>

</tasks>

<verification>
- rg -c '8.3.3' .planning/phases/09-agent-surface-api-diagnostics/09-LIVE-CAPTURES.md and '8.3.6' → both present across all probe sections
- The Decisions section explicitly lists: generating-state set, terminal-state set, uptime unit, lastSyncTimestamp unit, fileSize unit, license top-level shape, gan zero-connection body
- No secrets in either .md (rg for 'key' values / token strings → only names/placeholders)
</verification>

<success_criteria>
- Model-finalization plans (09-04, 09-05) can cite a captured body for EVERY field they type — zero guessed shapes
- The bundle state vocabulary is captured evidence, not web memory
- Rigs are ephemeral: spun, used, torn down; nothing left running
</success_criteria>

<output>
After completion, create `.planning/phases/09-agent-surface-api-diagnostics/09-02-SUMMARY.md`
</output>
