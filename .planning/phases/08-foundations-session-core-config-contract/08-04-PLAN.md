---
phase: 08-foundations-session-core-config-contract
plan: 04
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - crates/ignition-tui/src/context.rs
  - crates/ignition-tui/src/lib.rs
autonomous: true
must_haves:
  truths:
    - "TUI profile→client resolution (resolve/rebuild) and rig clients flow through ignition-core::Session — no ReqwestGatewayApi::new in the TUI crate"
    - "The TUI's duplicated secret_chain and resolve_from overlay choreography are DELETED"
    - "A config typo confined to the NEW schema surface never kills TUI startup: load_for_tui degrades with a stderr warning; resolution failures still exit 3 before ratatui::init"
  artifacts:
    - path: crates/ignition-tui/src/context.rs
      provides: "build_client/rig_client_with via Session::resolve/for_url; resolve/rebuild loading via config::load_for_tui"
      contains: "Session::"
    - path: crates/ignition-tui/src/lib.rs
      provides: "updated pre-init doc-comment describing the degradation contract (resolution fatal / new-surface degrade)"
      contains: "load_for_tui"
  key_links:
    - from: crates/ignition-tui/src/context.rs
      to: crates/ignition-core/src/config/mod.rs
      via: "context::resolve calls config::load_for_tui (the degradation entry point from 08-01)"
      pattern: "load_for_tui"
    - from: crates/ignition-tui/src/context.rs
      to: crates/ignition-core/src/session.rs
      via: "build_client constructs via Session::resolve; rig_client_with via Session::for_url"
      pattern: "Session::(resolve|for_url)"
---

<objective>
Migrate the TUI's two client-construction clusters onto `ignition-core::Session` and wire the config-degradation contract into the TUI load path, so a config typo in the new schema surface warns and degrades instead of exiting before `ratatui::init`.

Purpose: CORE-09's TUI half + CORE-10's TUI half (success criterion 1). The TUI is the in-process caller the requirement names.
Output: context.rs on Session, duplicated chain deleted, degradation pinned by tests, lib.rs pre-init contract doc updated.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-foundations-session-core-config-contract/08-RESEARCH.md
@crates/ignition-tui/src/context.rs
@crates/ignition-tui/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: TUI client construction onto Session; delete duplicated choreography</name>
  <files>crates/ignition-tui/src/context.rs</files>
  <action>
1. `build_client` (context.rs:75-83): replace `ReqwestGatewayApi::new(&profile, Some(credential))` (line 81) with the Session path — `Session::resolve`/degraded as the credential policy requires (build_client is the Some(credential) required path today; match its exact behavior — do not silently change the required/degraded split). Return shape stays `(String, String, Arc<ReqwestGatewayApi>)` for now (the triple grows into a struct in plan 08-05 — do NOT grow it here; keep this plan's diff on construction only).
2. `rig_client_with` (context.rs:119-127): ad-hoc rig Profile assembly stays at the call site; construction becomes `Session::for_url(rig_url, credential, false)` — the TUI twin of main.rs's rig_gateway_client (already migrated in 08-03).
3. DELETE the TUI's byte-identical `secret_chain` (context.rs:28-34) and the duplicated overlay→selection choreography in `resolve_from` (context.rs:62-69) — both are core-owned by Session now (research gotcha 9: the v1.0 "choke files stay untouched" decision is superseded).
4. Keep `context::resolve(profile_flag)` (line 42) and `rebuild(profile_name)` (line 52) signatures and semantics unchanged — the profile switcher (`update.rs:545-587`) and `lib.rs` run() consume them.
  </action>
  <verify>rg -n 'ReqwestGatewayApi::new' crates/ignition-tui/ (expect zero hits) && rg -n 'fn secret_chain' crates/ignition-tui/ (expect zero hits) && cargo test -p ignition-tui && cargo test -p ignition-cli --test tui_coverage</verify>
  <done>Zero client construction in the TUI crate; duplicated chain deleted; resolve/rebuild behavior unchanged (profile switcher and run_loop untouched); all TUI suites green</done>
</task>

<task type="auto">
  <name>Task 2: Wire load_for_tui into the TUI path; pin the degradation boundary</name>
  <files>crates/ignition-tui/src/context.rs, crates/ignition-tui/src/lib.rs</files>
  <action>
1. In context.rs, switch the config-load step inside `resolve`/`rebuild` to `ignition_core::config::load_for_tui` (from plan 08-01). Everything downstream (selection, secret via Session) is unchanged.
2. Update the pre-init contract doc-comment at lib.rs:48-52 ("Resolution failures return BEFORE the terminal is touched") to state the NEW contract precisely: resolution failures (raw TOML parse, profile deserialize, selection, auth) still return before `ratatui::init()` with exit 3; NEW-surface schema failures (`[ui]` contents, `poll_interval_secs` type/clamp) degrade to defaults with a stderr tracing warning and the TUI starts. The NoActiveProfile refusal for the authed cockpit (context.rs:38-41) is LOCKED and untouched.
3. Tests (context.rs `#[cfg(test)]`, ENV_LOCK at ignition-tui/src/lib.rs:39 + tempdir config):
   a. config with `poll_interval_secs = "banana"` → resolve returns Ok (degraded field), tracing warned — pin via the returned value shape today (interval consumer lands in 08-05; assert the load-level outcome through resolve succeeding where it previously would have… it previously would have been a type-error ConfigInvalid — the point is resolve now succeeds).
   b. config with `poll_interval_secs = 0` → resolve returns Ok (clamp degraded per the load_for_tui contract) — TUI starts with default cadence.
   c. config with a broken profile url → resolve returns Err (resolution failure still fatal — the boundary).
   d. config with garbage TOML (raw parse failure) → Err — pre-init exit preserved.
  </action>
  <verify>cargo test -p ignition-tui && cargo test --workspace && cargo clippy --all-targets -- -D warnings && cargo fmt --check && cargo build -p ignition-cli --no-default-features</verify>
  <done>TUI degrades new-surface config failures with warnings and starts; resolution failures still hard-exit pre-init; all four boundary tests green; workspace gates clean</done>
</task>

</tasks>

<verification>
- rg 'ReqwestGatewayApi::new' crates/ignition-tui/ → empty
- rg 'secret_chain' crates/ignition-tui/ → empty
- cargo test --workspace (tui_coverage walk green — registry untouched)
- cargo clippy --all-targets -- -D warnings && cargo fmt --check && cargo build -p ignition-cli --no-default-features
</verification>

<success_criteria>
- Success criterion 1 (TUI half): invalid new-surface config degrades to defaults and the TUI still starts; resolution failures still exit 3
- Success criterion 4 (TUI half): in-process TUI callers resolve through ignition-core::Session
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundations-session-core-config-contract/08-04-SUMMARY.md`
</output>
