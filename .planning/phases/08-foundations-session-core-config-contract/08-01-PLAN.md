---
phase: 08-foundations-session-core-config-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ignition-core/src/config/profile.rs
  - crates/ignition-core/src/config/mod.rs
  - crates/ignition-core/src/error.rs
  - README.md
  - crates/ignition-core/src/actions/profile.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-tui/src/context.rs
  - crates/ignition-tui/src/update.rs
  - crates/ignition-tui/src/rig/mod.rs
autonomous: true
must_haves:
  truths:
    - "A config carrying top-level [ui].theme and per-profile poll_interval_secs loads cleanly with zero unknown-key warnings"
    - "A structurally invalid value in the NEW schema surface (wrong-typed [ui] contents or wrong-typed poll_interval_secs) degrades to defaults with a stderr tracing warning instead of failing the load"
    - "poll_interval_secs = 0 is refused by validation with the clear poll_interval_too_small slug on the existing ConfigInvalid exit-3 class (no new exit code)"
    - "Legacy configs round-trip byte-identically (new fields are Option + skip_serializing_if) and contract_profile goldens stay untouched"
  artifacts:
    - path: crates/ignition-core/src/config/profile.rs
      provides: "UiConfig { theme: Option<String> } as Config.ui + poll_interval_secs: Option<u64> on Profile, lenient deserialize_with on both"
      contains: "poll_interval_secs"
    - path: crates/ignition-core/src/config/mod.rs
      provides: "KNOWN_* warn-list growth, clamp validation, load_for_tui degradation entry point"
      contains: "load_for_tui"
    - path: crates/ignition-core/src/error.rs
      provides: "additive slug poll_interval_too_small mapped to exit 3 + enumerated test literals extended"
      contains: "poll_interval_too_small"
    - path: README.md
      provides: "exit-code table exit-3 row gains poll_interval_too_small"
      contains: "poll_interval_too_small"
  key_links:
    - from: crates/ignition-core/src/config/mod.rs
      to: crates/ignition-core/src/error.rs
      via: "clamp validation returns the poll_interval_too_small error"
      pattern: "poll_interval_too_small"
    - from: crates/ignition-core/src/config/mod.rs
      to: crates/ignition-tui/src/context.rs
      via: "load_for_tui is the TUI's load entry point (wired in plan 08-04)"
      pattern: "load_for_tui"
---

<objective>
Land the ONE config migration: top-level `[ui].theme` and per-profile `poll_interval_secs` in a single schema change — with lenient deserialization on the new surface, the hard sub-second clamp, the additive `poll_interval_too_small` slug, and the `load_for_tui` degradation entry point the TUI will adopt in plan 08-04.

Purpose: CORE-10 + the validation half of TUIX-05. Everything downstream (cadence parameterization, theme rendering in P12) reads these fields; the clamp and degradation contract must exist before any consumer.
Output: Extended Config/Profile schema, validation + slug, `load_for_tui`, warn-silent new keys, all struct-literal compile breaks fixed, tests green.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-foundations-session-core-config-contract/08-RESEARCH.md
@crates/ignition-core/src/config/profile.rs
@crates/ignition-core/src/config/mod.rs
@crates/ignition-core/src/error.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add [ui] and poll_interval_secs with lenient deserialization + warn-list growth</name>
  <files>crates/ignition-core/src/config/profile.rs, crates/ignition-core/src/config/mod.rs, crates/ignition-cli/src/main.rs, crates/ignition-tui/src/context.rs, crates/ignition-tui/src/update.rs, crates/ignition-tui/src/rig/mod.rs, crates/ignition-core/src/actions/profile.rs</files>
  <action>
In `crates/ignition-core/src/config/profile.rs`:

1. Add `pub struct UiConfig { pub theme: Option<String> }` deriving `Debug, Clone, Default, PartialEq, Serialize, Deserialize` (match Config's derive set). PLUMBING ONLY — theme is carried, never rendered (rendering is Phase 12, TUIX-03/04; do NOT touch any ui/ render module).
2. Add `ui: UiConfig` to `Config` (line 16-32) with `#[serde(default)]` + `skip_serializing_if = "UiConfig::is_default"` so existing configs keep their exact on-disk shape (follow the `rig_config_is_empty` precedent at profile.rs:36-38).
3. Add `poll_interval_secs: Option<u64>` to `Profile` (line 64-88) with `#[serde(default, skip_serializing_if = "Option::is_none", deserialize_with = "lenient_u64")]`.
4. Write `fn lenient_u64<'de, D: Deserializer<'de>>(d: D) -> Result<Option<u64>, D::Error>`: deserialize into `toml::Value`; if it is an integer → `Ok(Some(v))` (0 included — the clamp in Task 2 rejects that, serde stays permissive); ANYTHING else (string, float, negative, table) → `tracing::warn!` with the key name and `Ok(None)`. This is the degradation mechanism: a typo in the NEW key never fails the load.
5. Same lenient treatment for the `ui` field: `deserialize_with = "lenient_ui"` — deserialize into `toml::Value`, `try_into::<UiConfig>()`; on failure `tracing::warn!` + `Ok(UiConfig::default())`.

In `crates/ignition-core/src/config/mod.rs`:

6. Grow the warn-lists at lines 71-73: `KNOWN_TOP_LEVEL` += `"ui"`, `KNOWN_PROFILE_KEYS` += `"poll_interval_secs"`. Without this the new keys load fine but warn as unknown on every load.

Fix the struct-literal compile breaks (Rust literals are exhaustive — research §2 inventory). Add `poll_interval_secs: None` at the 7 `Profile {` sites: main.rs:2259, context.rs:120, context.rs:196 (test), update.rs:4182 (test), config/mod.rs:216 + config/mod.rs:228 (tests), actions/profile.rs:79. Add `ui: UiConfig::default()` at the 3 breaking `Config {` sites: update.rs:4175, rig/mod.rs:621, rig/mod.rs:645 (context.rs:189 survives via `..Config::default()`). These edits in main.rs/context.rs/update.rs are LITERAL-ONLY one-liners — behavior changes in those files belong to plans 08-03/08-04/08-05, do not restructure anything.

CRITICAL: do NOT add `poll_interval_secs` (or `ui`) to `ProfileListResult` or any action render model in `actions/profile.rs` — the render model is separate from the serde struct and keeping it untouched is what keeps the `contract_profile.rs` goldens byte-identical (the "one goldens migration" constraint).

Tests (inline `#[cfg(test)]`, following the round-trip tests at config/mod.rs:243-262 and profile.rs:148-210):
- round-trip: config with `[ui]\ntheme = "dark"` and a profile with `poll_interval_secs = 10` → load → save → load, `PartialEq` lossless.
- legacy-shape: config WITHOUT the new keys serializes without `ui` / `poll_interval_secs` keys on disk.
- warn-silent pin: loading a config WITH both new keys emits no unknown-key warning (assert via the warn-lists membership — the lists contain the new keys).
- lenient degrade: `poll_interval_secs = "banana"` → `load` returns Ok, field is None; `[ui]` containing a non-UiConfig shape (e.g. `ui = 42`) → Ok with default UiConfig.
  </action>
  <verify>cargo test -p ignition-core config && cargo test -p ignition-cli --test contract_profile && cargo test -p ignition-tui</verify>
  <done>New keys load warn-silent; typo'd new keys degrade to defaults with a warning; legacy configs and contract_profile goldens byte-identical; clippy/fmt clean</done>
</task>

<task type="auto">
  <name>Task 2: Sub-second clamp + additive poll_interval_too_small slug (two-place table update)</name>
  <files>crates/ignition-core/src/config/mod.rs, crates/ignition-core/src/error.rs, README.md</files>
  <action>
1. In `config/mod.rs` `load()` (lines 50-69): after deserialization, add a `validate()` step — for every profile, if `poll_interval_secs == Some(0)`, return the refusal. Follow the Phase 7 additive-slug precedent (`eam_not_controller`, `script_exec_not_configured`): the new slug `poll_interval_too_small` rides the EXISTING `ConfigInvalid` class, exit 3. Message: "poll_interval_secs must be >= 1 (sub-second polling refused)"; hint pointing at the profile key. NEVER a new exit code (frozen taxonomy, REQUIREMENTS.md:75).
2. In `error.rs`: add the slug to the code() mapping (or the payload mechanism Phase 7 used for additive slugs), keep `exit_code() -> 3`, add a `hint()` line. Extend the `exit_code_mapping_enumerated` test at error.rs:846 with the new triple in the SAME commit. If the doc-comment taxonomy table at error.rs:8-16 lists per-slug content, add the slug there too (that table is prose-exempt from machine tests but must not drift).
3. In `README.md` exit-code table (lines 39-48): add `poll_interval_too_small` to the exit-3 row. This is the locked two-place rule (README.md:50-52) — enum test + README table move together.

Tests:
- unit (config/mod.rs): config with `poll_interval_secs = 0` → `load` returns Err whose code() is `poll_interval_too_small` and exit_code() is 3; `Some(1)` passes (1 is the floor).
- binary contract test (crates/ignition-cli/tests/ — follow contract_profile.rs spawn conventions: `IGNITION_CLI_CONFIG` → tempdir): `ign profile list` against a config with `poll_interval_secs = 0` → exit 3, stderr envelope carries `poll_interval_too_small`. (Snapbox inline golden or assert-based — your call; this slug surface is new, assert-based avoids a golden pin that later slugs would bump.)
  </action>
  <verify>cargo test -p ignition-core error && cargo test -p ignition-core config && cargo test -p ignition-cli --test contract_profile && cargo clippy --all-targets -- -D warnings && cargo fmt --check</verify>
  <done>poll_interval_secs = 0 refused on CLI with exit 3 + poll_interval_too_small; exit-1-to-7 taxonomy otherwise untouched; enumerated test + README table + error.rs updated in one commit</done>
</task>

<task type="auto">
  <name>Task 3: load_for_tui degradation entry point</name>
  <files>crates/ignition-core/src/config/mod.rs</files>
  <action>
Add `pub fn load_for_tui(...)` alongside `load` (same signature/arguments): identical parse and lenient extraction, but a clamp violation confined to the NEW schema surface does NOT error — it emits `tracing::warn!` carrying the same `poll_interval_too_small` slug/message, substitutes `None` (default cadence), and returns Ok. Everything else propagates unchanged: raw TOML parse failures, profile deserialize failures, selection failures — these are RESOLUTION failures and must keep hard-exiting (frozen `config_invalid` exit-3 taxonomy + the locked NoActiveProfile refusal; a config that cannot name a reachable profile cannot start the authed cockpit).

Implementation shape: internal `load_inner(strict_clamp: bool)` shared by both entry points; `load` = strict, `load_for_tui` = degrading-on-clamp. Document the contract on the fn doc-comment: "schema-surface failures degrade with a warning; resolution failures are fatal — the TUI is an authed cockpit."

Unit tests (config/mod.rs, using ENV_LOCK + tempdir conventions):
- `load_for_tui` on `poll_interval_secs = 0` → Ok, field None (warn emitted).
- `load_for_tui` on a broken profile url → Err (resolution failure still fatal).
- `load_for_tui` on garbage TOML (raw parse failure) → Err ConfigInvalid.
  </action>
  <verify>cargo test -p ignition-core config && cargo build -p ignition-cli --no-default-features && cargo clippy --all-targets -- -D warnings && cargo fmt --check</verify>
  <done>load_for_tui exists and degrades ONLY new-surface validation failures; resolution failures still error; all CI gates green</done>
</task>

</tasks>

<verification>
- cargo test --workspace (contract_profile goldens byte-identical — zero golden edits this plan)
- cargo clippy --all-targets -- -D warnings && cargo fmt --check
- cargo build -p ignition-cli --no-default-features
- rg -n '"ui"' crates/ignition-core/src/config/mod.rs and rg -n 'poll_interval_secs' crates/ignition-core/src/config/mod.rs → warn-lists contain both
- rg -n 'poll_interval_too_small' crates/ignition-core/src/error.rs README.md → present in both places
</verification>

<success_criteria>
- Success criterion 1 (first half): new keys load cleanly; invalid new-surface values degrade to defaults
- Success criterion 2 (first half): sub-second values refused by config validation with clear error
- No scope creep: no rendering, no action-model changes, no new exit codes
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundations-session-core-config-contract/08-01-SUMMARY.md`
</output>
