---
phase: 08-foundations-session-core-config-contract
plan: 05
type: execute
wave: 3
depends_on: ["08-04"]
files_modified:
  - crates/ignition-tui/src/context.rs
  - crates/ignition-tui/src/state.rs
  - crates/ignition-tui/src/workers/refresh.rs
  - crates/ignition-tui/src/update.rs
  - crates/ignition-tui/src/lib.rs
autonomous: false
must_haves:
  truths:
    - "Setting per-profile poll_interval_secs visibly changes the dashboard refresh cadence (poll_interval_secs = 1 refreshes ~1s; absent key = 5s default)"
    - "The profile switcher adopts the NEW profile's interval immediately — no restart, no silent old cadence (the update.rs:545-587 trap)"
    - "TICK stays 250ms (LOCKED redraw floor — poll_interval_secs parameterizes data polling, never the redraw tick)"
  artifacts:
    - path: crates/ignition-tui/src/context.rs
      provides: "resolve/rebuild return a small context struct carrying the configured poll interval"
      contains: "poll_interval"
    - path: crates/ignition-tui/src/state.rs
      provides: "AppState.poll_interval: Duration field"
      contains: "poll_interval"
    - path: crates/ignition-tui/src/workers/refresh.rs
      provides: "spawn_refresh reads state.poll_interval instead of the hardcoded REFRESH_PERIOD at refresh.rs:131"
      contains: "state.poll_interval"
  key_links:
    - from: crates/ignition-core/src/config/profile.rs
      to: crates/ignition-tui/src/context.rs
      via: "Profile.poll_interval_secs (from 08-01) becomes ResolvedContext.poll_interval"
      pattern: "poll_interval_secs"
    - from: crates/ignition-tui/src/context.rs
      to: crates/ignition-tui/src/workers/refresh.rs
      via: "rebuild's struct → AppState.poll_interval → spawn_refresh — the switch_profile chain must carry the NEW profile's interval (update.rs:584)"
      pattern: "spawn_refresh"
---

<objective>
Deliver TUIX-05's visible half: per-profile polling cadence. The configured `poll_interval_secs` flows profile → context → AppState → `spawn_refresh`, including across in-TUI profile switches; absent keys default to the 5s constant; the LOCKED 250ms TICK is untouched. Closes with the phase's single human-verification checkpoint.

Purpose: TUIX-05 (locked: fully delivered in Phase 8 — plumbing was 08-01, this is the parameterization + visible behavior).
Output: Cadence plumbing + tests + one blocking human-verify checkpoint covering cadence and degraded start.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-foundations-session-core-config-contract/08-RESEARCH.md
@crates/ignition-tui/src/context.rs
@crates/ignition-tui/src/state.rs
@crates/ignition-tui/src/workers/refresh.rs
@crates/ignition-tui/src/update.rs
@crates/ignition-tui/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Interval plumbing — context struct → AppState → spawn_refresh</name>
  <files>crates/ignition-tui/src/context.rs, crates/ignition-tui/src/state.rs, crates/ignition-tui/src/workers/refresh.rs, crates/ignition-tui/src/update.rs, crates/ignition-tui/src/lib.rs</files>
  <action>
1. context.rs: replace the `(String, String, Arc<ReqwestGatewayApi>)` triple from `resolve`/`rebuild` with a small struct (e.g. `pub struct ResolvedContext { pub profile_name: String, pub profile_url: String, pub poll_interval: Duration, pub api: Arc<ReqwestGatewayApi> }`). `poll_interval` = `Duration::from_secs(profile.poll_interval_secs.unwrap_or(REFRESH default))` — import the 5s default from workers::refresh (REFRESH_PERIOD, refresh.rs:21) so there is ONE source of the default. Update the two consumers: `lib.rs` run_loop (lib.rs:66-84 sets state.client/profile/profile_url — add state.poll_interval) and the profile-switcher path.
2. state.rs: add `pub poll_interval: Duration` to AppState (defined state.rs:1231-1273), defaulting to REFRESH_PERIOD at construction. Do NOT touch ClientHandle or worker signatures — workers stay concrete `Arc<ReqwestGatewayApi>` (planner decision, dyn rides P14).
3. refresh.rs: `spawn_refresh` (lines 120-133) reads `state.poll_interval` at line 131 instead of the REFRESH_PERIOD constant (`handle.spawn(refresh_worker(client, tx, shutdown_rx, era, state.poll_interval))`). `refresh_worker` ALREADY takes `period: Duration` (lines 86-92, `MissedTickBehavior::Skip` set) — the only change is the argument source. Keep the `Handle::try_current` no-op guard (state-machine tests stay unaffected). Do NOT parameterize WATCH/ALARMS/TAIL periods or TICK — minimal-compliant scope is the dashboard refresh worker (research §4); name the others as follow-ups in the SUMMARY.
4. update.rs — THE SWITCH TRAP (gotcha 6): `switch_profile` (update.rs:545-587) calls `context::rebuild(name)` then `spawn_refresh(state)` at line 584. rebuild now returns ResolvedContext — set `state.poll_interval = ctx.poll_interval` alongside the existing field assignments BEFORE spawn_refresh, so the newly-selected profile's cadence takes effect immediately (miss this and the dashboard silently keeps the old cadence after a switch).
  </action>
  <verify>cargo test -p ignition-tui && cargo clippy --all-targets -- -D warnings && cargo fmt --check</verify>
  <done>Interval flows config → ResolvedContext → AppState → spawn_refresh; profile switch adopts the new interval; TICK/other workers untouched; TUI suites green</done>
</task>

<task type="auto">
  <name>Task 2: Cadence plumbing tests</name>
  <files>crates/ignition-tui/src/context.rs, crates/ignition-tui/src/update.rs, crates/ignition-tui/src/workers/refresh.rs</files>
  <action>
Plumbing assertions (NOT timing tests — avoid flaky wall-clock polling tests; the visible cadence is verified by the checkpoint in Task 3):
1. context.rs unit (ENV_LOCK + tempdir config): profile with `poll_interval_secs = 2` → `resolve` returns `poll_interval == Duration::from_secs(2)`; absent key → `Duration::from_secs(5)` (REFRESH_PERIOD); clamp-degraded (0 or typo) → default 5s.
2. rebuild parity: `rebuild(name)` on a second profile with a different `poll_interval_secs` returns THAT profile's interval (the switch trap's unit proof).
3. update.rs state-machine test (era/ENV_LOCK conventions from the existing switch tests): after `switch_profile` to a profile carrying `poll_interval_secs = 1`, `state.poll_interval == Duration::from_secs(1)`.
4. refresh.rs: assert `spawn_refresh` outside a runtime still no-ops safely (existing guard test, if present, stays green); if the module has a state-based test asserting the spawned period, extend it — otherwise the Task-1 code change plus 1-3 above is the contract.
  </action>
  <verify>cargo test -p ignition-tui && cargo test --workspace && cargo clippy --all-targets -- -D warnings</verify>
  <done>All cadence plumbing pinned by unit tests: configured value, default, degraded-default, and the switch-path adoption</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — cadence, degraded start, clamp refusal</name>
  <files>none (verification only)</files>
  <action>
Pause for the user to verify TUIX-05 end to end on a real terminal (steps below). No code changes in this task; fix-and-retest if the user reports issues.
  </action>
  <verify>User confirms: (1) dashboard cadence follows poll_interval_secs, (2) live profile switch adopts the new interval, (3) typo'd config degrades with warning and TUI starts, (4) poll_interval_secs = 0 exits 3 with poll_interval_too_small</verify>
  <done>User types "approved" (or all reported issues are fixed and re-verified)</done>
  <what-built>
TUIX-05 end to end, plus the phase's degradation contract: per-profile dashboard cadence (with live profile-switch adoption), degraded start on new-surface config typos, and the CLI clamp refusal.
  </what-built>
  <how-to-verify>
Run from the repo root (build first: `cargo build`).

1. VISIBLE CADENCE — write a temp config with a REAL profile URL and `poll_interval_secs = 1`:
   `IGNITION_CLI_CONFIG=/tmp/ign-p8.toml cargo run -p ignition-cli -- tui`
   Watch the dashboard panels: they should visibly refresh about once per second (compare: remove the key → the familiar 5s cadence). Press `q` to quit.
2. LIVE SWITCH ADOPTION — in the same TUI, press `p`, switch to a profile whose `poll_interval_secs` differs (e.g. 5); the cadence changes without restarting the TUI.
3. DEGRADED START — set `poll_interval_secs = "banana"` in the temp config, launch the TUI again: a warning about the invalid value appears (stderr/before-init) and the TUI STILL STARTS with the default 5s cadence.
4. CLAMP REFUSAL (CLI side) — set `poll_interval_secs = 0` and run:
   `IGNITION_CLI_CONFIG=/tmp/ign-p8.toml cargo run -p ignition-cli -- profile list`
   Expected: exit code 3 and the stderr envelope carries `poll_interval_too_small` with a clear message.
5. (optional) THEME PLUMBING — add `[ui]\ntheme = "dark"` to the config: the TUI starts identically (theme rendering is Phase 12 — nothing should visibly change).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- cargo test --workspace (all suites green)
- cargo clippy --all-targets -- -D warnings && cargo fmt --check && cargo build -p ignition-cli --no-default-features
- rg -n 'REFRESH_PERIOD' crates/ignition-tui/src/workers/refresh.rs → constant still defined (the default source) but line 131's spawn reads state.poll_interval
- rg -n 'TICK' crates/ignition-tui/src/lib.rs → untouched at 250ms
- Human checkpoint approved
</verification>

<success_criteria>
- Success criterion 2: per-profile poll_interval_secs visibly changes TUI polling cadence (checkpoint-verified), including across live profile switches; sub-second refusal already enforced by 08-01's validation
- Success criterion 1 (visible confirmation): typo'd config degrades and the TUI starts
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundations-session-core-config-contract/08-05-SUMMARY.md`
</output>
