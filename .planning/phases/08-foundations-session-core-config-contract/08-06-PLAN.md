---
phase: 08-foundations-session-core-config-contract
plan: 06
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - crates/ignition-core/src/error.rs
  - README.md
  - crates/ignition-cli/tests/tui_coverage.rs
  - crates/ignition-cli/tests/contract_stdout_purity.rs
  - crates/ignition-tui/src/routes.rs
autonomous: true
must_haves:
  truths:
    - "The Three-Place slug rule is machine-enforced: a slug present in CoreError's code() but missing from the README table (or present under the wrong exit code, or a stale table row) fails the test suite"
    - "The OutOfBand pinned test documents mcp/lsp/edit as RESERVED slugs with written justification; the clap-walk stays green with completions as the only OutOfBand row"
    - "A single stray stdout byte under maximum diagnostics (unknown-key warnings + IGNITION_LOG=trace) fails the purity harness"
  artifacts:
    - path: crates/ignition-core/src/error.rs
      provides: "readme_exit_table_agreement test parsing the README table via include_str!"
      contains: "README"
    - path: crates/ignition-cli/tests/contract_stdout_purity.rs
      provides: "byte-exact stdout purity harness over the real spawned binary (assert-based, not snapbox-golden)"
      min_lines: 40
    - path: crates/ignition-cli/tests/tui_coverage.rs
      provides: "pinned OutOfBand test updated with reserved-slug justification (set stays exactly [completions])"
      contains: "mcp"
    - path: crates/ignition-tui/src/routes.rs
      provides: "taxonomy docs pre-declaring mcp/lsp/edit as reserved OutOfBand slugs"
      contains: "reserved"
  key_links:
    - from: crates/ignition-core/src/error.rs
      to: README.md
      via: "agreement test reads README.md via include_str! and cross-checks every slug ↔ exit-code row"
      pattern: "include_str!"
    - from: crates/ignition-cli/tests/contract_stdout_purity.rs
      to: the ign binary
      via: "assert_cmd::Command::cargo_bin(\"ign\") spawn with IGNITION_CLI_CONFIG tempdir + IGNITION_LOG=trace"
      pattern: "cargo_bin"
---

<objective>
Codify contract discipline as executable rituals (CORE-11): the Three-Place slug rule enforced by a README-parsing agreement test, the OutOfBand taxonomy pre-declared for `mcp`/`lsp`/`edit` with justification (rows land with their clap commands in P13/14 — orphan rows would fail the clap walk), and a byte-exact stdout-purity harness over the real spawned binary.

Purpose: "so nothing later mutates the frozen contract by accident" — the phase goal's enforcement half.
Output: Agreement test, updated pinned test + taxonomy docs, new purity harness. No new dependencies.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-foundations-session-core-config-contract/08-RESEARCH.md
@crates/ignition-core/src/error.rs
@README.md
@crates/ignition-cli/tests/tui_coverage.rs
@crates/ignition-cli/tests/cli_chassis.rs
@crates/ignition-tui/src/routes.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three-Place agreement test — README table ↔ code() machine-enforced</name>
  <files>crates/ignition-core/src/error.rs, README.md</files>
  <action>
1. New test `readme_exit_table_agreement` next to `exit_code_mapping_enumerated` (error.rs:846): `include_str!("../../../README.md")` (from crates/ignition-core/src/ that reaches the repo root — verify the relative depth when writing; run the test, a wrong path fails immediately). Parse the exit-code table (README.md:39-48, rows like `| 3 | ... slugs ... |`): extract the exit number and the backticked slug tokens per row, std-only string ops (split on '|', trim, filter tokens starting with a letter containing no spaces).
2. Assert BOTH directions using the same (variant, exit_code, code) literal triples the enumerated test already lists (duplicate the triple list — the enumerated test is the literal source of truth): (a) every slug from the triples appears in the README row matching its exit_code(); (b) every slug token appearing in the README table exists in the triples (catches stale/deleted rows — exit-6 carries 22 slugs today, so a full cross-check is the value).
3. Update the prose at README.md:50-52: the sync sentence now names BOTH tests — the enumerated mapping unit test (enum ↔ literals) and the README table agreement test (README ↔ enum) — so the "Three-Place" rule (enum test + README table + prose) is executable.
4. The error.rs doc-comment taxonomy table (lines 8-16) is EXEMPT from machine parsing (fragile to parse); eyeball-verify it lists poll_interval_too_small (added by 08-01) and touch it up in this commit if it drifted.
Negative-proof while developing: temporarily remove a slug from the README table, run the test, watch it fail, restore. Record this in the SUMMARY.
  </action>
  <verify>cargo test -p ignition-core readme_exit_table_agreement && cargo test -p ignition-core error</verify>
  <done>README table and CoreError slugs are machine-synced in both directions; prose names both tests; the negative proof is recorded</done>
</task>

<task type="auto">
  <name>Task 2: OutOfBand taxonomy extension — reserved slugs with justification, no rows</name>
  <files>crates/ignition-cli/tests/tui_coverage.rs, crates/ignition-tui/src/routes.rs</files>
  <action>
STRUCTURAL CONSTRAINT (research §3): the sibling clap-walk test `every_row_requiring_cli_node_is_mapped_and_no_orphans` (tui_coverage.rs:79-113) asserts bidirectional set equality with the live clap tree — registry rows for `mcp`/`lsp`/`edit` CANNOT exist until those clap commands land (P13/P14); they would fail as orphan rows. The Phase 8 extension is taxonomy + justification, NOT rows.

1. `out_of_band_rows_are_exactly_the_completions_leaf` (tui_coverage.rs:119-131): KEEP the expected set exactly `["completions"]`. Extend the test's doc-comment with the justification text: `mcp`, `lsp`, and `edit` are RESERVED OutOfBand slugs — MCP stdio and LSP speak their own protocols on stdout; `edit` is an editor round-trip, not a cockpit verb. Their registry rows land TOGETHER with their clap commands in Phases 13/14 (adding rows earlier fails the orphan check by design — this pinned test is the pre-declaration).
2. routes.rs taxonomy documentation (module docs at lines 14-24 and the sanctioned-exceptions prose block around lines 322-329): add the reserved-slug pre-declaration with the same one-line justifications, so the registry's own docs state the intent where future implementers will look.
3. Verify both walk tests stay green — proving no orphan rows were introduced and completions remains the single OutOfBand row.
  </action>
  <verify>cargo test -p ignition-cli --test tui_coverage && rg -n 'reserved' crates/ignition-tui/src/routes.rs crates/ignition-cli/tests/tui_coverage.rs</verify>
  <done>OutOfBand set still exactly [completions]; mcp/lsp/edit pre-declared with justification in the pinned test and routes.rs docs; clap walk green</done>
</task>

<task type="auto">
  <name>Task 3: stdout-purity byte-scan harness over the real spawned binary</name>
  <files>crates/ignition-cli/tests/contract_stdout_purity.rs</files>
  <action>
New test file `crates/ignition-cli/tests/contract_stdout_purity.rs` (binary-level tests live here — the cli_chassis.rs/contract_*.rs home). Dev-deps already present (assert_cmd, predicates, tempfile) — NO new dependencies. Pattern to copy: cli_chassis.rs:9-10 (spawn setup) and cli_chassis.rs:78-90 (`verbose_keeps_stdout_version_line_only`). The invariant being pinned: tracing is stderr-only by construction (`init_tracing` ends `.with_writer(std::io::stderr)`, main.rs:2433-2450).

Harness design — ASSERT-based, deliberately NOT snapbox goldens so `SNAPSHOTS=overwrite` cannot silently sanitize a failure (research gotcha in §3):

1. `stdout_is_byte_pure_under_max_diagnostics`: tempdir config (`IGNITION_CLI_CONFIG`, macOS/XDG gotcha) whose config FILE carries an unknown top-level key (e.g. `bogus_key = 1` — fires `warn_unknown_keys` tracing on every load) + env `IGNITION_LOG=trace` (max diagnostics). Spawn `ign profile list --json` (offline, deterministic). Assert on RAW BYTES: `output.stdout` exactly equals the expected envelope bytes for the empty-list JSON (single trailing newline included) — derive the expected string from the current pinned contract_profile golden ONCE and hard-code it; additionally assert the stdout is valid UTF-8 and parses as exactly ONE serde_json value with nothing after it (belt and braces: byte-equality already implies it, but the parse assertion documents intent and localizes failures).
2. `human_stdout_is_line_pure_under_max_diagnostics`: same noise env, spawn `ign profile list` (human mode), assert stdout bytes exactly equal the expected human-mode lines (header + empty-list output — copy the current golden's exact bytes once).
3. Negative-proof while developing: temporarily print one stray `eprintln!`→ no — stdout probe: inject a stray `println!` in a debug build path or (simpler, no prod-code touch) verify the harness FAILS by asserting against a deliberately off-by-one expected string in a scratch run; revert. Record in SUMMARY.

Note: these tests run the REAL binary — if a regression ever leaks a tracing line, panic message, or partial write to stdout under noise, the byte compare fails on that single stray byte.
  </action>
  <verify>cargo test -p ignition-cli --test contract_stdout_purity && cargo test --workspace && cargo clippy --all-targets -- -D warnings && cargo fmt --check</verify>
  <done>Byte-exact stdout purity enforced over the real binary under unknown-key warnings + trace logging; harness is assert-based (overwrite-proof); negative proof recorded</done>
</task>

</tasks>

<verification>
- cargo test --workspace (agreement test + walk tests + purity harness all green together)
- rg -n 'readme_exit_table_agreement' crates/ignition-core/src/error.rs → present
- rg -n 'completions' crates/ignition-cli/tests/tui_coverage.rs → pinned set unchanged; rg -n 'mcp' crates/ignition-tui/src/routes.rs → reserved-slug docs present, NO registry rows added
- cargo clippy --all-targets -- -D warnings && cargo fmt --check
</verification>

<success_criteria>
- Success criterion 3: Three-Place slug rule test-enforced; OutOfBand registry extended (taxonomy + pinned test + justification); stdout-purity byte-scan harness fails on a single stray byte
- No scope creep: no new clap commands, no registry rows, no new exit codes, no new dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundations-session-core-config-contract/08-06-SUMMARY.md`
</output>
