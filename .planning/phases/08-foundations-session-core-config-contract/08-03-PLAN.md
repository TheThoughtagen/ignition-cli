---
phase: 08-foundations-session-core-config-contract
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - crates/ignition-cli/src/main.rs
autonomous: true
must_haves:
  truths:
    - "Every CLI auth/gateway client resolution flows through ignition-core::Session — no ReqwestGatewayApi::new remains in main.rs"
    - "The duplicated secret_chain and overlay→selection choreography are DELETED from main.rs (core-owned now)"
    - "All command behavior is byte-identical: goldens untouched, exit codes untouched, resolution modes preserved (required / degraded / rig headerless)"
  artifacts:
    - path: crates/ignition-cli/src/main.rs
      provides: "8 former construction sites routed through Session; resolve_gateway_api / named_profile_client / resolve_headerless_api / rig_gateway_client reimplemented as thin Session delegates"
      min_lines: 1
  key_links:
    - from: crates/ignition-cli/src/main.rs
      to: crates/ignition-core/src/session.rs
      via: "all 8 sites call Session::{resolve, resolve_degraded, for_url}"
      pattern: "Session::(resolve|resolve_degraded|for_url)"
    - from: crates/ignition-cli/src/main.rs
      to: completion grep
      via: "rg 'ReqwestGatewayApi::new' crates/ returns only ignition-core session module + for_tests sites"
      pattern: "ReqwestGatewayApi::new"
---

<objective>
Migrate all 8 CLI client-construction sites onto `ignition-core::Session` and delete the duplicated choreography from main.rs. This is the criterion-4 sweep's CLI half — the tree must grep clean of second client construction.

Purpose: CORE-09. main.rs's duplicated `secret_chain` + overlay/selection choreography (a deliberate v1.0 decision) is dissolved; the resolution logic lives in core.
Output: main.rs thin; behavior byte-identical; sweep grep clean.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-foundations-session-core-config-contract/08-RESEARCH.md
@crates/ignition-cli/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route all 8 construction sites through Session, delete duplicated choreography</name>
  <files>crates/ignition-cli/src/main.rs</files>
  <action>
Migrate each site to its Session constructor (behavior-preserving — criterion 4 changes the construction path, never the behavior):

| Site | Current | Becomes |
|------|---------|---------|
| main.rs:431 `Commands::Version` arm | degraded credential (`resolve_secret_opt`) | `Session::resolve_degraded(flag)` |
| main.rs:760 wait-family headerless site | degraded via `resolve_headerless_api` | `Session::resolve_degraded` |
| main.rs:1248 dispatch arm | required credential inline | `Session::resolve(flag)` |
| main.rs:2000 `ScriptCommand::Run` | required credential inline | `Session::resolve(flag)` |
| main.rs:2158-2172 `resolve_gateway_api` (shared helper, ~10 callers) | required | body → `Session::resolve(profile_flag)` |
| main.rs:2178-2186 `named_profile_client` (per-side, project diff/sync) | required | body → `Session::resolve(Some(name))` |
| main.rs:2223-2237 `resolve_headerless_api` (wait gateway/restart) | degraded to None | body → `Session::resolve_degraded(flag)` |
| main.rs:2254-2268 `rig_gateway_client` (ad-hoc rig Profile) | headerless-by-construction | keep the ad-hoc Profile/URL assembly at the call site, construct via `Session::for_url(rig_url, credential, false)` |

STRATEGY — keep the four helper fn NAMES (`resolve_gateway_api`, `named_profile_client`, `resolve_headerless_api`, `rig_gateway_client`) but reimplement their bodies as one-line Session delegates (returning `Session`, or `session.api_handle()` where callers need the `Arc<ReqwestGatewayApi>` shape). This keeps ~10+ call sites untouched while eliminating every construction site. Adjust only the immediate dispatch arms (431, 760, 1248, 2000) to call Session directly.

DELETE the duplicated choreography now that core owns it:
- `secret_chain` (main.rs:2287-2293) — gone; the chain lives in Session.
- The overlay→selection sequence in `resolve_profile_context` (main.rs:2111-2118) — absorbed by `Session::resolve`; delete if nothing else uses its pieces, otherwise reduce to the Session call.

Preserve exactly: the required/degraded/rig mode split, exit codes and slugs on resolution failures (profile_not_found / config_invalid / auth surfaces — all frozen), and the `[profile: NAME]` envelope threading. Do NOT touch the TUI files (08-04 owns context.rs) and do NOT touch any action fn (they stay free functions over `&GatewayApi`; Session's Deref feeds them unchanged).
  </action>
  <verify>cargo test -p ignition-cli && rg -n 'ReqwestGatewayApi::new' crates/ignition-cli/ (expect zero hits) && rg -n 'secret_chain' crates/ignition-cli/ (expect zero hits)</verify>
  <done>All 8 sites route through Session; helpers are thin delegates; duplicated secret_chain and overlay choreography deleted from main.rs; ignition-cli tests green with zero golden edits</done>
</task>

<task type="auto">
  <name>Task 2: Criterion-4 sweep verification — full workspace gates + grep-clean proof</name>
  <files>crates/ignition-cli/src/main.rs</files>
  <action>
Run and record the completion evidence (goes in the SUMMARY):
1. `rg -n 'ReqwestGatewayApi::new' crates/` → the ONLY production hit is `crates/ignition-core/src/session.rs` (client/mod.rs's fn definition body + the ~50 `for_tests` sites in `#[cfg(test)]`/tests/ are sanctioned exceptions).
2. `rg -n 'fn secret_chain' crates/` → zero hits (core's chain lives inside session.rs, named differently if at all — the point is one implementation).
3. `cargo test --workspace` → green, INCLUDING contract_profile.rs / contract_version.rs goldens byte-identical and tui_coverage walk green (no behavior change).
4. CI parity: `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check`, `cargo build -p ignition-cli --no-default-features`.
If any grep finds a straggler construction site, migrate it in this task — the sweep is absolute.
  </action>
  <verify>rg -n 'ReqwestGatewayApi::new' crates/ shows only session.rs + test-only for_tests; cargo test --workspace green; clippy/fmt/lean-build green</verify>
  <done>Grep-clean CLI half of criterion 4 with recorded evidence; all suites and CI gates green</done>
</task>

</tasks>

<verification>
- rg 'ReqwestGatewayApi::new' crates/ignition-cli/ → empty
- rg 'ReqwestGatewayApi::new' crates/ → session.rs only (+ for_tests in tests)
- cargo test --workspace (goldens byte-identical)
- cargo clippy --all-targets -- -D warnings && cargo fmt --check && cargo build -p ignition-cli --no-default-features
</verification>

<success_criteria>
- Success criterion 4 (CLI half): all CLI dispatch resolves clients through ignition-core::Session; no second client construction in main.rs
- Frozen contract intact: exit codes, slugs, goldens untouched
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundations-session-core-config-contract/08-03-SUMMARY.md`
</output>
