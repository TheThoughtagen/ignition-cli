---
phase: 08-foundations-session-core-config-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ignition-core/src/session.rs
  - crates/ignition-core/src/lib.rs
autonomous: true
must_haves:
  truths:
    - "Session::resolve(flag) reproduces the exact resolution choreography verbatim: env overlay scoped to the would-be selection FIRST, then selection, then the LOCKED secret chain (env token → keyring → basic pair)"
    - "All three credential modes are constructible through Session: required (default resolve), degraded-to-None (version/waits), and headerless-by-construction rigs (for_url)"
    - "Within ignition-core, the only production ReqwestGatewayApi::new caller is session.rs (client/mod.rs definition + for_tests excepted); the tree-wide grep-clean sweep (rg 'ReqwestGatewayApi::new' crates/ → only session.rs + for_tests) is proven by plan 08-03 Task 2"
    - "Secret::expose stays confined to apply_auth — Session introduces no new exposure path"
  artifacts:
    - path: crates/ignition-core/src/session.rs
      provides: "Session type (profile name + Arc<ReqwestGatewayApi>) with resolve / resolve_degraded / for_url + Deref + api() accessors"
      exports: ["Session"]
      min_lines: 80
    - path: crates/ignition-core/src/lib.rs
      provides: "pub mod session export"
      contains: "pub mod session"
  key_links:
    - from: crates/ignition-core/src/session.rs
      to: crates/ignition-core/src/client/mod.rs
      via: "the ONE ReqwestGatewayApi::new construction site (client/mod.rs:398 pattern) now lives only here"
      pattern: "ReqwestGatewayApi::new"
    - from: crates/ignition-core/src/session.rs
      to: crates/ignition-core/src/config
      via: "reuses config::{load/apply_env_overlay/resolve_selection/resolve_secret} public fns — choreography COPIED from main.rs:2111-2118 + main.rs:2287-2293, never redesigned"
      pattern: "resolve_selection"
---

<objective>
Create `ignition-core::Session` — the ONE execution seam every auth/gateway client resolution flows through. This plan builds the type and proves its behavior parity with today's duplicated choreography; plans 08-03/08-04 migrate the CLI and TUI onto it.

Purpose: CORE-09 first half. Phases 9-12 all build on this seam; it must exist and be behavior-identical before any call site moves.
Output: New session module with the three credential modes + for_url rig constructor, precedence/secret-chain tests, no behavior change anywhere.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-foundations-session-core-config-contract/08-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@crates/ignition-core/src/client/mod.rs
@crates/ignition-core/src/config/mod.rs
@crates/ignition-cli/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Session type with resolve / resolve_degraded / for_url</name>
  <files>crates/ignition-core/src/session.rs, crates/ignition-core/src/lib.rs</files>
  <action>
Create `crates/ignition-core/src/session.rs` (top-level module; disambiguate in the module doc-comment: "execution session" vs the unrelated `client::sessions` gateway-session family). Export via `pub mod session;` in lib.rs. The module is feature-free (must compile under `cargo build -p ignition-cli --no-default-features`).

Type (concrete-with-deref — PLANNER DECISION, research open question 4): `pub struct Session { profile: String, api: Arc<ReqwestGatewayApi> }`. ARCHITECTURE.md Pattern 2 prescribes `Arc<dyn GatewayApi>`, but the TUI's workers/ClientHandle are concretely typed and Phase 8's goal is construction-site unification, not handle-type churn — dyn-widening rides Phase 14 where MCP actually needs dyn. Expose `pub fn api(&self) -> &ReqwestGatewayApi`, `pub fn api_handle(&self) -> Arc<ReqwestGatewayApi>`, `pub fn profile_name(&self) -> &str`, and `impl Deref for Session { type Target = ReqwestGatewayApi }` (feeds every existing free-fn action over `&GatewayApi` unchanged — the `version()` dyn precedent at actions/version.rs:46-47 and the `rig_stream.rs:188` cast precedent both keep working).

Constructors — the three credential modes, choreography COPIED VERBATIM from the duplicated sites (the research-locked precedence: env overlay scoped to the would-be selection FIRST, then selection, then secret):
1. `pub fn resolve(profile_flag: Option<&str>) -> Result<Session, CoreError>` — REQUIRED credential. Port `resolve_profile_context` (main.rs:2111-2118) + `secret_chain` (main.rs:2287-293) into core using the existing public config fns (`apply_env_overlay`, `resolve_selection`, `resolve_secret`). This is the default path that replaces `resolve_gateway_api` (main.rs:2158-2172) and `named_profile_client` (main.rs:2178-2186 — per-side resolution is just `resolve(Some(name))`).
2. `pub fn resolve_degraded(profile_flag: Option<&str>) -> Result<Session, CoreError>` — credential degrades to None when unavailable (the `resolve_secret_opt` behavior behind version/waits). Replaces the main.rs:431/760 sites and `resolve_headerless_api` (main.rs:2223-2237).
3. `pub fn for_url(url: url::Url, credential: Option<Credential>, ssl_verify: bool) -> Result<Session, CoreError>` — headerless-by-construction clients for the rig family. Replaces `rig_gateway_client` (main.rs:2254-2268, ssl_verify=false, default auth) and `context::rig_client_with` (context.rs:119-127). The ad-hoc Profile assembly (derived rig URL) STAYS at the call sites — only the `ReqwestGatewayApi::new` call moves behind this constructor (full sweep per research open question 3: criterion 4's "no second client construction anywhere" is absolute).

The ONE `ReqwestGatewayApi::new` call site in production code after this task is inside session.rs (client/mod.rs:398's body is unchanged; `for_tests` at line 408 stays test-only and untouched). Do NOT widen `Secret::expose` — the chain composes existing config fns; CORE-02's grep-auditable redaction boundary must hold. Preserve the main.rs:431 comment's contract in the resolve doc-comment: "the client is built from the POST-OVERLAY profile."
  </action>
  <verify>cargo build -p ignition-core && cargo build -p ignition-cli --no-default-features && cargo clippy --all-targets -- -D warnings && cargo fmt --check</verify>
  <done>session.rs compiles clean feature-free; three constructors exist; within ignition-core the only production ReqwestGatewayApi::new caller is session.rs (client/mod.rs definition + for_tests excepted); zero behavior change yet — CLI/TUI call sites deliberately untouched, tree-wide grep-clean proof deferred to 08-03 Task 2</done>
</task>

<task type="auto">
  <name>Task 2: Session behavior-parity tests (precedence + secret chain + modes)</name>
  <files>crates/ignition-core/src/session.rs, crates/ignition-core/tests/</files>
  <action>
Tests proving Session reproduces today's choreography exactly — this is the safety net that lets 08-03/08-04 migrate call sites with confidence. Conventions: `ENV_LOCK` for in-process env mutation (config/mod.rs:31), `IGNITION_CLI_CONFIG` → `tempfile::TempDir` for config isolation, wiremock or dead-port URLs for network targets (tests/common/mod.rs patterns).

1. Precedence: env `IGNITION_URL` overrides the SELECTED profile only (overlay-before-selection contract, main.rs:431's comment) — select profile B via flag while env carries A's URL; the session's api targets the overlay URL for the selected profile, and a second session without the overlay env targets B's own URL.
2. Selection: flag > `IGNITION_PROFILE` env > active; unknown name → `ProfileNotFound` with known-profiles context; none resolvable → the existing no-selection error (whatever `resolve_selection`'s Ok(None) consumer does today — mirror main.rs's behavior, do not invent a new one).
3. Secret chain order (LOCKED): env token for the profile → token_env ref → generic `IGNITION_TOKEN` → keyring (unavailable = warn+skip, never fatal) → basic pair. Prove at least: env-token wins over basic pair; no secret anywhere → required mode errors, degraded mode returns Ok with header-less client (assert via wiremock expecting zero auth headers — the P02 headerless StatusPing pattern).
4. for_url: rig client carries the given URL + credential, ssl_verify=false propagates (assert the reqwest builder path via a wiremock https-skip case OR assert on the constructed Profile-derived config observable — the danger_accept_invalid_certs setting), default auth (no secret) tolerated.
5. api()/Deref reach an action: call an existing free-fn action (e.g. `actions::version::version(&*session, ...)`) against a wiremock and assert it works unchanged through the Session handle.
  </action>
  <verify>cargo test -p ignition-core session && cargo test -p ignition-core && cargo clippy --all-targets -- -D warnings</verify>
  <done>All Session tests green; precedence/secret-chain/mode behavior byte-for-byte equivalent to the duplicated choreography being replaced</done>
</task>

</tasks>

<verification>
- cargo test --workspace (existing suites untouched and green — this plan changes no call sites)
- cargo build -p ignition-cli --no-default-features
- rg -n 'ReqwestGatewayApi::new' crates/ignition-core/src/ → only session.rs + client/mod.rs definition + for_tests
- cargo clippy --all-targets -- -D warnings && cargo fmt --check
</verification>

<success_criteria>
- Success criterion 4 (foundation): the seam exists with all three credential modes and for_url; production construction is consolidated to one module, ready for 08-03/08-04 migration
- No behavior change: all existing suites green without modification
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundations-session-core-config-contract/08-02-SUMMARY.md`
</output>
