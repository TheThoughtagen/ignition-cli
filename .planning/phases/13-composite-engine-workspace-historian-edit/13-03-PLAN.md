---
phase: 13-composite-engine-workspace-historian-edit
plan: 03
type: execute
wave: 2
depends_on: ["13-02"]
files_modified:
  - crates/ignition-core/src/actions/workspace.rs
  - crates/ignition-core/src/actions/mod.rs
  - crates/ignition-core/tests/workspace_checkout.rs
autonomous: true

must_haves:
  truths:
    - "workspace_checkout(api, project, target_dir, decode_scripts) exports the project zip via the existing client method, maps members through build_mapping, writes the tree, and records `.ign-workspace.json` with gateway↔local path pairs + checkout-time (descriptor-normalized) hashes — the mapping is RECORDED, downstream never re-derives"
    - "--decode-scripts checkout produces nvim-editable `.py` sidecars + scripts-manifest.json (decode_export_tree verbatim), and an UNEDITED tree encodes back to a BYTE-EXACT zip — the codec's sacred invariant proven at tree scale (the roadmap's 'workspace hardens decode/encode before edit rides' step)"
    - "The checkout source is strictly the project export zip — a contract test pins the tree contains ONLY zip-derived members + the codec's manifest/sidecars (+ the workspace manifest + .gitignore): no tag value can appear because no tag surface is ever ingested (SC-2's exclusion clause)"
    - "Checkout refuses a non-empty target directory that is not a valid existing workspace (never clobbers an unrelated directory)"
  artifacts:
    - path: "crates/ignition-core/src/actions/workspace.rs"
      provides: "WorkspaceManifest model (serde, skip-serialize-at-defaults), manifest read/write, checkout action"
      contains: ".ign-workspace.json"
    - path: "crates/ignition-core/src/actions/mod.rs"
      provides: "workspace module declaration"
    - path: "crates/ignition-core/tests/workspace_checkout.rs"
      provides: "Round-trip tests: unedited byte-exact re-encode; decode-scripts tree shape (sidecars + manifest); tag-exclusion pin; non-empty-target refusal"
      min_lines: 60
  key_links:
    - from: "actions/workspace.rs WorkspaceManifest"
      to: "13-06 status/push"
      via: "status three-way compare and push's member reads consume the RECORDED manifest (hash + path pairs), never recompute the mapping"
      pattern: "ign-workspace"
    - from: "actions/workspace.rs checkout"
      to: "client/scripts_codec.rs decode_export_tree"
      via: "--decode-scripts rides the existing codec unchanged — no new script extraction"
      pattern: "decode_export_tree"
---

<objective>
Land workspace checkout: the manifest design (`.ign-workspace.json` — the three-way-compare state) and the checkout action that turns a gateway project export into a mapped, hash-recorded local directory tree, with `--decode-scripts` producing nvim-editable script files that encode back cleanly. Proves the codec leg at tree scale (byte-exact unedited round-trip) — the roadmap's precondition for `ign edit` riding the same codec.

Purpose: SC-1 begins here (checkout), SC-2's decode-scripts round-trip and tag-exclusion clauses close here. Everything downstream (status drift, guarded push) consumes the manifest this plan defines.
Output: actions/workspace.rs (manifest + checkout) + the round-trip/exclusion/refusal test suite. No CLI yet (13-07) — core actions only, per the vertical-slice-then-surface pattern.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-composite-engine-workspace-historian-edit/13-RESEARCH.md
@.planning/phases/13-composite-engine-workspace-historian-edit/13-02-SUMMARY.md
@crates/ignition-core/src/client/resources.rs
@crates/ignition-core/src/client/scripts_codec.rs
@crates/ignition-core/src/actions/projects.rs
@crates/ignition-core/src/actions/resources.rs
</context>

<planner_locks>
Planner decisions locked at plan level (research Pattern 2 + OQ3, resolved here):

- **Manifest name: `.ign-workspace.json`** at the tree root — dot-prefixed so it can never collide with a member-derived path; DISTINCT from the codec's `scripts-manifest.json` (encode_export_tree refuses/strips only its own — verified scripts_codec.rs:685/775).
- **Manifest content:** `{schema_version: 1, project, profile, checked_out_at (RFC3339), members: {gateway_path → {local_path, hash}}}` — hash is the FNV-1a descriptor-normalized value from member_hashes at checkout time (identical semantics to gateway-side hashes). Additive-only envelope discipline applies to any envelope exposure later (13-07): skip-serialize at defaults.
- **Manifest is COMMITTED (git) — it is the workspace's identity.** Checkout writes a `.gitignore` at the tree root listing `scripts-manifest.json` and the sidecar pattern (`*.py` sidecars are codec artifacts, not source) per the codec tree convention. Document in 13-07's README section.
- **Unknown-local-files policy:** checkout owns a clean dir; status/push (13-06) decide unknown-file handling at the action layer.
- **Zero new WebDev routes, zero route-bundle bump** — checkout rides `project_export_to_file` (actions/resources.rs:150 shape). If implementation discovers a route need, STOP and escalate (11-02 atomic-bump pattern is a separate decision, not this plan's).
- **No new exit slugs** — refusals ride InvalidInput (exit 2).
</planner_locks>

<tasks>

<task type="auto">
  <name>Task 1: WorkspaceManifest model + read/write with strict ownership rules</name>
  <files>crates/ignition-core/src/actions/workspace.rs, crates/ignition-core/src/actions/mod.rs</files>
  <action>
1. Declare the new `workspace` module in actions/mod.rs (alphabetical with siblings — check the file's convention).
2. NEW actions/workspace.rs:
   - `pub const WORKSPACE_MANIFEST_NAME: &str = ".ign-workspace.json";`
   - `#[derive(Serialize, Deserialize)] pub struct WorkspaceManifest { schema_version: u8, project: String, profile: String, checked_out_at: String, members: BTreeMap<String, ManifestMember> }` where `ManifestMember { local_path: String, hash: u64 }` — BTreeMap for deterministic serialization; `#[serde(skip_serializing_if = "Option::is_none")]` posture on any future optional field (envelope discipline).
   - `pub fn read_manifest(root: &Path) -> Result<WorkspaceManifest, CoreError>`: missing file → InvalidInput "not an ign workspace — run `ign workspace checkout` first" naming the expected path; schema_version mismatch → InvalidInput naming found vs expected; unparseable JSON → InvalidInput (never a panic, never a silent default).
   - `pub fn write_manifest(root: &Path, manifest: &WorkspaceManifest) -> Result<(), CoreError>`: serde_json pretty (stable field order via struct order), trailing newline, parents created; 0640 perms.
   - Unit tests: round-trip write/read equality; missing/mismatched/corrupt manifest refusals with exact message prefixes (these prefixes become 13-07's golden anchors).
  </action>
  <verify>`cargo test -p ignition-core workspace::manifest` green; clippy -D warnings green</verify>
  <done>The manifest model exists with strict read ownership (missing/foreign/corrupt all refuse with stable messages) and deterministic serialization</done>
</task>

<task type="auto">
  <name>Task 2: checkout action — export → map → write tree → record manifest (+ decode-scripts, .gitignore, refusals)</name>
  <files>crates/ignition-core/src/actions/workspace.rs, crates/ignition-core/tests/workspace_checkout.rs</files>
  <action>
1. `pub struct CheckoutOutcome { project: String, target: PathBuf, member_count: usize, scripts_decoded: bool }` (Serialize for 13-07's envelope).
2. `pub async fn workspace_checkout(api: &dyn GatewayApi, project: &str, target_dir: &Path, decode_scripts: bool) -> Result<CheckoutOutcome, CoreError>`:
   a. Export: the existing project-export-to-file client capability (actions/resources.rs:150 site) → zip bytes in memory.
   b. Refusal FIRST (before any write): if target_dir exists and is non-empty AND does not contain a valid WORKSPACE_MANIFEST_NAME → InvalidInput "target directory is not empty and is not an ign workspace" naming the dir. (Re-checkout OVER an existing workspace of the same project is allowed: manifest must match project name, else refuse — enables 13-06 refresh semantics without clobbering unrelated dirs.)
   c. Map: `resources::` member enumeration on the zip → `build_mapping(&members)` (13-02 — refusals propagate verbatim).
   d. Decode: if decode_scripts → `decode_export_tree(&zip_bytes, &target_dir)` verbatim (sidecars + codec manifest + shadow/collision refusals ride free); else write members raw. Write each member's bytes at its MAPPED local path (never path.join on the raw member name — the mapping IS the fs truth).
   e. Record: `member_hashes` over the freshly written SOURCE bytes (zip-side, descriptor-normalized — the Tree-side hash must equal it for identical bytes per 13-02 equivalence), workspace manifest write, `.gitignore` write (skip if already listing both entries — idempotent).
3. Integration tests (tests/workspace_checkout.rs) over a synthetic project export zip fixture (build with the existing rewrite/replace helpers — same fixture style as resources.rs tests; include: nested folders, a `resource.json` descriptor with `attributes.lastModification` volatility noise, and a member carrying embedded Python so the codec leg is exercised):
   - unedited checkout → `encode_export_tree`-equivalent reconstruction → byte-exact vs source zip (the pinned invariant at tree scale; if the codec's encode path needs the manifest+sidecars, run the decode-scripts variant for this assertion);
   - decode_scripts=true → tree contains sidecars + scripts-manifest.json; edited-script → re-encode keeps OTHER members byte-identical and re-splices only the edited one (structural assertion);
   - tag-exclusion pin: assert the resulting tree contains EXACTLY zip-derived member files + `.gitignore` + workspace manifest (+ codec artifacts when decode_scripts) — nothing else, proving no tag surface was ingested;
   - non-empty unrelated target refuses; foreign-manifest (different project) refuses; a path mapping refusal (case-collision fixture from 13-02) propagates with both names.
  </action>
  <verify>`cargo test -p ignition-core --test workspace_checkout` green — including the byte-exact round-trip; clippy + fmt clean</verify>
  <done>Checkout produces the mapped tree + recorded manifest; decode-scripts round-trips byte-exact unedited; tag exclusion and clobber refusals are contract-pinned</done>
</task>

</tasks>

<verification>
- `cargo test -p ignition-core` workspace-green (13-02's property suite + 13-03's checkout suite + all pre-existing pins)
- grep: actions/workspace.rs contains no format-specific readers beyond the zip/tree codecs already in client/ (byte-faithfulness invariant — no XML/CSV/whatever Readers in action code)
- No WebDev route files touched; no ROUTE_BUNDLE_VERSION change; no new exit slugs
- The unedited round-trip test is byte-exact (not structural) — cite its assert in the SUMMARY
</verification>

<success_criteria>
`ign workspace checkout`'s core exists: manifest-recorded mapping, byte-exact --decode-scripts round-trip at tree scale, tag-value exclusion structurally pinned, clobber-safe refusals — SC-1's checkout half and SC-2's decode-scripts + exclusion clauses are code-complete pending CLI (13-07).
</success_criteria>

<output>
After completion, create `.planning/phases/13-composite-engine-workspace-historian-edit/13-03-SUMMARY.md`
</output>
