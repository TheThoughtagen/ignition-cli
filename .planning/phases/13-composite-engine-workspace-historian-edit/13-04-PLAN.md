---
phase: 13-composite-engine-workspace-historian-edit
plan: 04
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - crates/ignition-core/src/actions/tags.rs
  - crates/ignition-cli/src/render.rs
  - README.md
  - crates/ignition-cli/tests/e2e_webdev.rs
  - .planning/phases/13-composite-engine-workspace-historian-edit/13-LIVE-GATE.md
autonomous: true

must_haves:
  truths:
    - "TAGS-13: `tags config get` human output names the history properties (historyEnabled / historyProvider / sampleMode / the diff-captured historical-group key) WHEN PRESENT in the config — a wiremock contract test pins presence AND absence renders byte-identically to today (additive-only render)"
    - "TAGS-14 resolves per the 13-01 spike record — BOTH branches are legitimate and the plan implements whichever the record names: (a) closure: the working binding field-set is documented + contract-pinned and the live gate proves write→query DATA on a licensed trial rig, or (b) documented-limitation: the README limitation section cites the committed Designer-diff evidence and the live gate proves the honest outcome (config accepted, query structurally sound, null values documented)"
    - "History properties are NEVER modeled as typed Rust structs — Value passthrough + reparse only (Pitfall 12); every name cited in docs/code comes from 13-01's committed diffs"
    - "The historian live gate is env-gated (#\[ignore\]), serialized under LIVE_GATE, uses unique provider names per run, runs on BOTH trial rigs inside 2h windows, and its evidence is recorded in 13-LIVE-GATE.md during the phase (Phase-10 live-gate contract)"
  artifacts:
    - path: "crates/ignition-core/src/actions/tags.rs"
      provides: "history_summary extraction (pure fn over the config Value — Option<String> summary, None when no history keys)"
      contains: "history"
    - path: "crates/ignition-cli/src/render.rs"
      provides: "render_tags_config_get_human extension: the history block appended only when history keys exist"
      contains: "history"
    - path: "crates/ignition-cli/tests/e2e_webdev.rs"
      provides: "live_tags_history_bindings #[ignore] test: historian + bound tag → TAGS-13 summary live assert + SC-4 branch per the spike record, both rigs"
      contains: "p13hist"
    - path: ".planning/phases/13-composite-engine-workspace-historian-edit/13-LIVE-GATE.md"
      provides: "Recorded gate run: status line, both-rig outcomes, trial-window timestamps, SC-3 + SC-4 verdicts"
      min_lines: 30
  key_links:
    - from: "13-LIVE-CAPTURES.md verdict + field-set table"
      to: "actions/tags.rs + README historian section"
      via: "implementation cites the spike record — closure field-set or limitation evidence — never re-derived, never guessed"
      pattern: "SPIKE VERDICT"
    - from: "e2e_webdev.rs live gate"
      to: "SC-3/SC-4 phase verification"
      via: "licensed-gateway evidence recorded in-phase (the Phase-10/11 gate discipline)"
      pattern: "LIVE-GATE"
---

<objective>
Close the historian slice per the 13-01 spike record: TAGS-13 (history bindings visible in `tags config` output — on wiremock pins and live on licensed trial rigs), TAGS-14's done-state (binding recipe pinned and live-proven, OR documented-limitation-with-evidence — both legitimate per roadmap), and the both-rig live gate recorded in 13-LIVE-GATE.md.

Purpose: SC-3 and SC-4 close here. The spike record (13-LIVE-CAPTURES.md verdict + field-set table) is this plan's implementation contract — the plan is deliberately structured to satisfy either spike outcome without re-planning.
Output: tags.rs summary helper + render block + README historian section + the live gate test + 13-LIVE-GATE.md evidence.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-composite-engine-workspace-historian-edit/13-RESEARCH.md
@.planning/phases/13-composite-engine-workspace-historian-edit/13-01-SUMMARY.md
@.planning/phases/13-composite-engine-workspace-historian-edit/13-LIVE-CAPTURES.md
@crates/ignition-core/src/actions/tags.rs
@crates/ignition-cli/src/render.rs
@crates/ignition-cli/tests/e2e_webdev.rs
@README.md
</context>

<planner_locks>
- **2h trial-window budgeting (USER-locked):** the gate run fits ONE window per rig; unique provider names (`p13hist` + run suffix); pre-clean helpers (clean_tag_configs + delete_internal_historian) run first for idempotent re-runs; NEVER count on a reset mid-run.
- **Passthrough posture:** history properties ride `config` as Value; the summary is an extraction/view, never a model. New envelope exposure (if the summary rides data) is additive-only, skip-serialize-when-absent.
- **No new WebDev routes, no new exit slugs.** The gate's data assertion is the closure proof; a null-data outcome on the closure branch after the spike PROVED closure would be a drift finding recorded in 13-LIVE-GATE.md (capture wins; follow-up gap plan decides).
- This plan's renders/tests must not disturb pre-existing tags config renders: absence of history keys renders byte-identically (additive-only render contract, pinned by test).
</planner_locks>

<tasks>

<task type="auto">
  <name>Task 1: TAGS-13 — history summary extraction + additive render + wiremock contract pins</name>
  <files>crates/ignition-core/src/actions/tags.rs, crates/ignition-cli/src/render.rs, README.md</files>
  <action>
1. In actions/tags.rs: `pub fn history_summary(config: &serde_json::Value) -> Option<HistorySummary>` — a pure extraction over the tag's config object. `HistorySummary` carries Option fields read BY NAME from the config: `enabled` (from `historyEnabled`), `provider` (from `historyProvider`), `sample_mode` (from `sampleMode`), and the historical-group key UNDER THE NAME THE 13-01 DIFF CAPTURED (read the diff artifact — if the diff shows the group key, read it; if the Designer set it via a nested object, extract per the captured shape; if the diff showed NO group key, leave the field None and note it). Returns None when NO history keys are present (absent = byte-identical behavior). No typed modeling beyond this view — values stay strings/bools as captured; unknown additional history keys in a config are IGNORED by the summary (passthrough posture: the full config still reaches the user via the existing output).
2. In render.rs (`render_tags_config_get_human`, line ~1516): when history_summary returns Some, append a `history:` block — one line per present field (`enabled: true`, `provider: p13hist836`, `sample mode: Tag Group`, `historical group: <captured-name>`); when None, output is BYTE-IDENTICAL to today (pinned).
3. README (Tag config section): document the history properties the CLI surfaces — names, types, and meaning per the 13-01 field-set table; cite 13-LIVE-CAPTURES.md as provenance.
4. Wiremock contract tests (existing tags config test file/style): (a) a config carrying history keys → summary block present with captured field names; (b) a config WITHOUT history keys → render byte-identical to the pre-change golden (additive-only proof); (c) a config with historyEnabled but no provider → block shows enabled only (partial-shape honesty).
  </action>
  <verify>`cargo test -p ignition-cli` tags-config render pins green including the byte-identical-absence test; clippy + fmt clean</verify>
  <done>History bindings are visible in tags config output when present, invisible when absent — additive-only, passthrough-posture, capture-named (SC-3's code half)</done>
</task>

<task type="auto">
  <name>Task 2: TAGS-14 done-state — implement the spike-record branch (closure recipe OR documented limitation)</name>
  <files>README.md, crates/ignition-cli/tests/e2e_webdev.rs</files>
  <action>
READ 13-LIVE-CAPTURES.md's verdict FIRST; implement exactly that branch (both branches include e2e scaffold work — the branch decides the assertions, not the existence, of the gate):

**Branch A — spike verdict = closure (field set pinned):**
- README: a working recipe — "creating/updating a tag↔historian binding via the CLI": the exact `ign tags config create/edit` payload shape using ONLY the diff-captured field names + values, the write/query follow-through, and the provenance citation (13-LIVE-CAPTURES.md + the designer-diff artifacts).
- The write path needs NO new code if the spike replay already succeeded through the existing tagConfig route (passthrough) — verify that claim by re-reading 13-01's SUMMARY replay section; if the replay required any client-side accommodation beyond passthrough, implement the MINIMAL accommodation here and pin it with a wiremock REQUEST test (recorded request: method+path+body — 10-02 discipline).

**Branch B — spike verdict = documented-limitation:**
- README "Tag history — known limitation" section: states honestly that tag↔historian binding create/update is NOT achievable via the tag write path, WITH the evidence citations (designer-diff artifacts, the provider-probe finding, the replay attempts) and what WOULD be needed (the diff-captured field set that the CLI cannot yet deliver, or the provider-level state the Designer writes outside the tag config).
- No accommodation code. The limitation IS the deliverable (roadmap-legitimate done state).

**Both branches — gate test scaffold** in e2e_webdev.rs: `#[tokio::test] #[ignore = "opt-in e2e: set IGNITION_LIVE_URL + IGNITION_LIVE_TOKEN + IGNITION_LIVE_MUTATIONS=1"] async fn live_tags_history_bindings()` following the 05-06 fixture skeleton (LIVE_GATE lock, isolated_live_config, pre-clean, provision_internal_historian with a UNIQUE p13hist-run name, webdev deploy first, self-cleaning tag + historian deletion in a finally): (1) create the bound tag per the branch's payload shape; (2) TAGS-13 live assert — `ign tags config get` human output contains the history block with the live provider name; (3) branch A: write values → history query → ASSERT DATA rows (closure evidence); branch B: history query → assert the STRUCTURAL outcome (exit 0, t_stamp + tag columns) and record null-values as the documented limitation (the 05-06 structural-assert pattern).
  </action>
  <verify>`cargo test -p ignition-cli --test e2e_webdev` compiles and the new test SKIPS cleanly without env (default suite stays green); README contains the branch-appropriate section citing 13-LIVE-CAPTURES.md; grep proves no field name appears in README/code that is absent from the committed designer-diff artifacts</verify>
  <done>TAGS-14's done-state is implemented exactly per the spike record — closure recipe pinned and gate-ready, or documented limitation with cited evidence — and the live gate scaffold exists</done>
</task>

<task type="auto">
  <name>Task 3: Run the historian live gate on BOTH trial rigs → record 13-LIVE-GATE.md</name>
  <files>.planning/phases/13-composite-engine-workspace-historian-edit/13-LIVE-GATE.md</files>
  <action>
1. Spin + commission ONE trial rig (11-RIG-NOTES recipe, ~4 min; port 18188), record the 2h window start. Deploy the CURRENT bundle (this branch's), provision historian + bound tag via the gate test itself: `cargo test -p ignition-cli --test e2e_webdev live_tags_history_bindings -- --ignored --nocapture`.
2. If the trial window is live and healthy, run rig B (8.3.3, port 19188) the same way within the window budget — both-rig evidence per the historian both-rig rule. If the window is tight, record rig B as a second window run (trials reset — stage it cleanly rather than rushing; NEVER count on a reset mid-run).
3. Write 13-LIVE-GATE.md: status line (`passed` / `passed-with-limitation` (branch B) / `failed-with-findings` / `blocked-on-env`), rig identification, window timestamps, per-step verbatim outcomes (provision → bind → TAGS-13 live block → write/query → cleanup), any drift vs the spike record (capture wins — a closure-branch null-data on live is a FINDING, not something to code around in this plan), and the SC-3/SC-4 verdict lines.
4. Teardown clean (tag + historian deleted; rig torn down or explicitly kept for the phase's later work — state which).
  </action>
  <verify>13-LIVE-GATE.md exists with a status line + both-rig (or documented second-window) evidence + SC-3/SC-4 verdict lines; the default test suite is untouched-green; no secret material in the repo</verify>
  <done>SC-3 proven on licensed trial rigs (bindings visible live); SC-4's done-state evidenced live per the spike branch — recorded in-phase per the Phase-10 live-gate contract</done>
</task>

</tasks>

<verification>
- `cargo test --workspace` default suites green (the #[ignore] gate skips without env)
- README historian section matches the 13-01 spike record branch — every history field name greps clean against the committed designer-diff artifacts
- 13-LIVE-GATE.md: status + verdicts + window timestamps (the in-phase evidence contract)
- Absence-render byte-identity pin present (additive-only render contract)
- Zero route changes, zero new slugs, zero typed history models (grep: no `struct.*History` beyond the summary view)
</verification>

<success_criteria>
The historian slice closes honestly: bindings are VISIBLE through tags config output on a licensed gateway (SC-3), and TAGS-14 lands in the spike-recorded done-state — working recipe live-proven OR documented limitation with Designer-diff evidence — with both-rig gate evidence recorded during the phase (SC-4).
</success_criteria>

<output>
After completion, create `.planning/phases/13-composite-engine-workspace-historian-edit/13-04-SUMMARY.md`
</output>
