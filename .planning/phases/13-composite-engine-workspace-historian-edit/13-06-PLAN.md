---
phase: 13-composite-engine-workspace-historian-edit
plan: 06
type: execute
wave: 3
depends_on: ["13-03"]
files_modified:
  - crates/ignition-core/src/actions/workspace.rs
  - crates/ignition-core/tests/workspace_status_push.rs
autonomous: true

must_haves:
  truths:
    - "workspace_status(root, api, project) performs the manifest three-way compare — per recorded path: clean (local==manifest.hash && gateway==manifest.hash) / local_edit (local differs only) / gateway_drift (gateway differs only) / conflict (both differ) — with additions/deletions by set-difference against the manifest"
    - "The compare rides MemberSource + member_hashes (descriptor-normalized) — NO second comparison implementation, NO byte-compare that flags lastModification noise as changed"
    - "workspace_push reads changed members via the RECORDED manifest (never re-derives the mapping), splices them into the FRESH gateway zip via the existing replace_member/remove_member (descriptor landing rules ride free), and performs ONE import"
    - "Conflicts REFUSE push outright — NOT --yes-able (clobbering a concurrent Designer edit is beyond any flag, Pitfall W2); empty effective selection performs ZERO requests that mutate (zero-write honesty, projects.rs:577 precedent); deletions require opt-in --delete (project_sync semantics)"
    - "The guard is ONE gate function in the preview_then_confirm shape (build blast radius → render → require_confirmation): the refusal message IS the preview; wiremock traffic-pinning proves a refused push issues exactly the read GETs and ZERO import calls"
  artifacts:
    - path: "crates/ignition-core/src/actions/workspace.rs"
      provides: "WorkspaceStatus { rows: Vec<StatusRow> } with direction semantics documented+pinned; workspace_push with conflict refusal, --delete opt-in, and the single preview_then_confirm-style gate"
      contains: "conflict"
    - path: "crates/ignition-core/tests/workspace_status_push.rs"
      provides: "Three-way matrix pins (all four row kinds + add/delete), conflict refusal, zero-write honesty, traffic-pinned guard proof, stale-zip splice safety"
      min_lines: 80
  key_links:
    - from: "actions/workspace.rs workspace_status"
      to: "13-02 MemberSource"
      via: "status sources the gateway side via MemberSource::Zip and the local side via MemberSource::Tree over the manifest mapping — one hash/normalization implementation"
      pattern: "MemberSource"
    - from: "actions/workspace.rs workspace_push"
      to: "client/resources.rs replace_member/remove_member"
      via: "push splices into the FRESH gateway zip (never pushes a stale full zip) — the 'import answers ok while nothing lands' bug stays closed by the proven landing rules"
      pattern: "replace_member"
    - from: "actions/workspace.rs status rows"
      to: "13-07 CLI render + goldens"
      via: "StatusRow is the envelope shape (additive, skip-serialize-at-defaults) and the golden anchor for the status render"
      pattern: "StatusRow"
---

<objective>
Land workspace status and guarded push: the manifest three-way compare that distinguishes local edits from gateway drift from conflicts, and the push path that splices only locally-changed members into a FRESH gateway export behind the single composed confirmation gate — conflicts refuse outright, deletions opt-in, empty selections write nothing.

Purpose: SC-1's drift + guard halves. Pitfall W2 is the design driver: the Designer is a real concurrent writer; without the three-way compare a push cannot tell "user edited locally" from "gateway moved on" and would clobber. The 10-04 one-gate lesson governs the refusal shape.
Output: status + push actions + the matrix/guard test suite. No CLI yet (13-07).
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-composite-engine-workspace-historian-edit/13-RESEARCH.md
@.planning/phases/13-composite-engine-workspace-historian-edit/13-02-SUMMARY.md
@.planning/phases/13-composite-engine-workspace-historian-edit/13-03-SUMMARY.md
@crates/ignition-core/src/actions/workspace.rs
@crates/ignition-core/src/actions/projects.rs
@crates/ignition-core/src/client/resources.rs
@crates/ignition-core/src/client/workspace.rs
</context>

<planner_locks>
Planner decisions locked at plan level (research Pattern 2/4 + the label-reconciliation trap, resolved here):

- **Status direction, pinned:** rows are PUSH-RELATIVE — each row describes what `workspace push` would do to the gateway (local_edit = would write this member; gateway_drift = push would NOT touch it, a pull/refresh would; conflict = refused). The projects.rs:535-555 lesson (diff speaks B-relative, promotion speaks source→target) applied explicitly and pinned in tests — the label direction is load-bearing for agents reading the output.
- **Conflict = hard refusal, not --yes-able** (planner lock carries from research): both diverged sides are named; resolution = explicit human action (pull fresh / force-checkout is NOT built in this phase — documented as manual).
- **Push splice base = FRESH export:** the zip spliced INTO is always re-exported at push time (staleness-safe at the tree level); conflict detection is what protects the concurrent writer, the fresh base protects against a stale full-zip import silently resurrecting deleted members.
- **Zero new slugs:** conflict/unknown-file refusals ride InvalidInput; the confirmation gate rides the existing confirmation_required class (exit 2) verbatim.
- **Unknown local files (in the tree, not in the manifest):** status reports them as `untracked` rows (additive enum kind — envelope additive-only); push IGNORES untracked files (never imports them) — bringing a file into the gateway is checkout/replace territory, not push's job; documented in 13-07's README.
</planner_locks>

<tasks>

<task type="auto">
  <name>Task 1: status — the manifest three-way compare over MemberSource</name>
  <files>crates/ignition-core/src/actions/workspace.rs, crates/ignition-core/tests/workspace_status_push.rs</files>
  <action>
1. `#[derive(Serialize)] pub struct WorkspaceStatus { project: String, clean: bool, rows: Vec<StatusRow> }`; `pub struct StatusRow { pub path: String, pub kind: StatusKind }`; `pub enum StatusKind { Clean, LocalEdit, GatewayDrift, Conflict, Added { local: bool }, Deleted { local: bool }, Untracked }` — serialize with `skip_serializing_if` on payloads (envelope discipline; ALL-rows-always semantics like the sessions family: filtered kinds are absent, never null).
   Direction doc comment on StatusKind: PUSH-RELATIVE (planner lock) — pin the semantics in a doc example test.
2. `pub async fn workspace_status(root: &Path, api: &dyn GatewayApi, project: &str) -> Result<WorkspaceStatus, CoreError>`:
   a. `read_manifest(root)` (13-03's strict reader); manifest.project must match else InvalidInput.
   b. Gateway side: export zip → `MemberSource::Zip(...).member_hashes()`. Local side: walk the MANIFEST's members reading local bytes via `MemberSource::Tree`-equivalent per-member hash (fs read at the RECORDED local_path — the manifest is the mapping truth; a missing local file = local deletion, not an error).
   c. Per manifest path: `clean | local_edit | gateway_drift | conflict` by comparing (manifest.hash, local_hash, gateway_hash) — the exact three-way matrix, as a pure fn `classify(manifest, local, gateway)` unit-tested exhaustively (all 8 combinations → 4 kinds; None-handling for absent sides).
   d. Set differences: manifest path missing from the fresh gateway export = `Deleted { local: false }` (gateway-side deletion — pull territory); member present in gateway but NOT in manifest (exported since checkout) = `Added { local: false }` with a doc note that checkout --refresh semantics are manual in v1; local file in the tree but not in manifest = `Untracked`.
   e. `clean: bool` = no rows of any kind except Clean.
3. Tests: the exhaustive classify matrix; an integration status over a synthetic tree + wiremock-served export covering one row of EVERY kind (fixtures mutate one member locally, one on the gateway, one both, delete one gateway-side, add one untracked file); direction-semantics pin (a local_edit row's prose/doc example says "push would write").
  </action>
  <verify>`cargo test -p ignition-core --test workspace_status_push` green including the exhaustive matrix; clippy + fmt clean</verify>
  <done>Drift is correctly classified four ways plus add/delete/untracked, all through the ONE hash/normalize implementation, with push-relative semantics pinned</done>
</task>

<task type="auto">
  <name>Task 2: push — manifest-recorded splice into a fresh export, conflicts refuse, one guard gate</name>
  <files>crates/ignition-core/src/actions/workspace.rs, crates/ignition-core/tests/workspace_status_push.rs</files>
  <action>
1. `pub struct PushPreview { rows: Vec<StatusRow>, would_write: Vec<String>, would_delete: Vec<String> }` (Serialize — the blast radius).
2. `pub async fn workspace_push(root, api, project, yes: bool, delete: bool) -> Result<PushOutcome, CoreError>`:
   a. Status first (Task 1) → any `Conflict` row ⇒ InvalidInput refusing with BOTH diverged members named and the resolution hint ("pull a fresh checkout or reconcile manually — conflicts are never force-pushed"). NOT gated by --yes (planner lock).
   b. Selection: would_write = LocalEdit rows; would_delete = manifest paths absent locally AND locally-deleted — only when `delete: true` (project_sync's opt-in semantics; without --delete, locally-deleted members are REPORTED as skipped rows, never removed gateway-side).
   c. Zero-write honesty: empty selection ⇒ PushOutcome { wrote: [], deleted: [], skipped: [...] } with ZERO mutation requests (traffic-pinned).
   d. Guard (ONE gate, preview_then_confirm composition): build PushPreview → render a deterministic preview line-set (the message text) → `require_confirmation` unless --yes. The refusal's message IS the preview (10-04 pattern). This gate fn is THE single confirmation site for workspace push — no per-branch guards.
   e. Execute: fresh export → for each would_write member: `replace_member(fresh_zip, member, local_bytes)` (descriptor merge/synthesize rides free); for each confirmed delete: `remove_member`; ONE `project_import` of the spliced zip (overwrite semantics — the existing import call). Outcome carries wrote/deleted/skipped lists.
3. Tests (wiremock REQUEST-pinning, 10-02 discipline): refused-by-gate traffic pin (expect exactly the read GETs, zero import calls, and the preview text in the error envelope); --yes happy path pin (one import whose body is the spliced zip — assert the spliced member bytes present via re-parse of the recorded body); conflict refusal (both names in message) fires even WITH --yes; --delete-less local deletion = skipped row + no removal in the spliced zip; empty-selection zero-mutation pin; splice-into-fresh proof (gateway fixture diverges on an UNRELATED member between checkout and push — the pushed zip carries the gateway's version of that member, not checkout's).
  </action>
  <verify>`cargo test -p ignition-core --test workspace_status_push` green — all traffic pins assert expect(1)-style counts; clippy + fmt clean</verify>
  <done>Push writes only manifest-recorded local edits into a fresh export behind ONE composed gate; conflicts refuse unconditionally; deletions and empty selections are honest — SC-1's guard semantics complete pending CLI</done>
</task>

</tasks>

<verification>
- `cargo test -p ignition-core` workspace-green (13-02 + 13-03 + status/push suites + pre-existing pins)
- The one-gate property holds structurally: grep actions/workspace.rs — require_confirmation appears in exactly ONE push code path
- Zero new slugs, zero route changes; no re-serialization of member bytes anywhere in the push path (splice = raw bytes)
- Push-relative direction semantics documented on StatusKind and pinned by a test
</verification>

<success_criteria>
Workspace status and push exist at the action layer with the three-way compare correctly separating local edits from gateway drift from conflicts, pushes splicing manifest-recorded changes into fresh exports behind the single guard, and traffic-pinned proof that a refusal never mutates — SC-1 is code-complete pending its CLI surface.
</success_criteria>

<output>
After completion, create `.planning/phases/13-composite-engine-workspace-historian-edit/13-06-SUMMARY.md`
</output>
