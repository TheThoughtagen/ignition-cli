---
phase: 13-composite-engine-workspace-historian-edit
plan: 08
type: execute
wave: 5
depends_on: ["13-05", "13-07"]
files_modified:
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-tui/src/routes.rs
  - crates/ignition-cli/tests/tui_coverage.rs
  - crates/ignition-cli/tests/contract_edit.rs
  - README.md
autonomous: true

must_haves:
  truths:
    - "A user runs `ign edit <project> <resource-path> [--yes]`: fetch → decode → $EDITOR opens the resource → on save, encode + staleness gate + guard → push; an unchanged save exits clean as a no-op (no push, no prompt)"
    - "The `edit` OutOfBand registry row and its clap command land in the SAME task — the pinned test extends to exactly [api call, completions, edit] with written justification, and the bidirectional clap walk proves row+command atomicity (the Phase-8 pre-declared reservation fulfilled)"
    - "`ign edit` owns its stdout completely: with a scripted no-op editor under max diagnostics, stdout carries ZERO bytes (prose on stderr) — the byte-scan harness pins edit's OutOfBand stdout contract over the real spawned binary"
    - "The guard composes ONE gate (preview_then_confirm): without --yes the staged diff summary IS the refusal; a stale gateway refuses regardless of --yes; a JSON-breaking save refuses fail-closed with the kept temp path"
    - "README documents the edit contract: $EDITOR resolution (VISUAL→EDITOR, IDE --wait expectation), content-decided no-op, staleness semantics (re-run to fetch fresh), fail-closed encoding, and the kept-temp recovery path"
  artifacts:
    - path: "crates/ignition-cli/src/cli.rs"
      provides: "Commands::Edit(EditArgs) — project + resource-path positional, --yes via the global flag"
      contains: "Edit"
    - path: "crates/ignition-tui/src/routes.rs"
      provides: "CliRoute { path: \"edit\", mapping: OutOfBand } with the justification comment (hands the terminal to a child editor — completions/api-call genre)"
      contains: "edit"
    - path: "crates/ignition-cli/tests/tui_coverage.rs"
      provides: "out_of_band_rows_are_pinned extended to [\"api call\", \"completions\", \"edit\"] + justification; clap walk green"
      contains: "\"edit\""
    - path: "crates/ignition-cli/tests/contract_edit.rs"
      provides: "Binary-level contract tests with scripted-$EDITOR shims + wiremock: no-op exit, guarded refusal golden, stale refusal, fail-closed exit-2 with kept path, happy-path push"
      min_lines: 80
  key_links:
    - from: "crates/ignition-cli/src/main.rs edit dispatch"
      to: "13-05 StagedEdit"
      via: "dispatch runs edit_pipeline, renders the staged summary, composes the ONE guard, then project_import — the gate site is the dispatch layer"
      pattern: "edit_pipeline|StagedEdit"
    - from: "tui_coverage pinned OutOfBand set"
      to: "routes.rs edit row"
      via: "set equality [api call, completions, edit] — the 08-06 pre-declared `edit` reservation becomes a row in the same commit as its clap command"
      pattern: "out_of_band"
    - from: "contract_edit.rs stdout pin"
      to: "SC-5 verification"
      via: "zero-stdout proof over the real binary closes the edit slice's purity obligation (Phase-8 harness contract for new stdout-writing surfaces)"
      pattern: "stdout"
---

<objective>
Surface `ign edit`: the clap command + Session-seam dispatch wrapping 13-05's pipeline, the OutOfBand registry row landing ATOMICALLY with its command (the Phase-8 `edit` reservation fulfilled — pinned set extends to [api call, completions, edit]), the guard composition at dispatch (diff summary IS the refusal), edit-specific stdout purity proof, binary contract tests with scripted editors, and the README edit section.

Purpose: SC-5's user-facing half and the phase's final contract ritual. This plan lands LAST in the wave order deliberately: 13-05 proves the pipeline, 13-07 owns the CLI-surface files first — this plan then edits those same files sequentially (file-ownership rule) so the two CLI surfaces never collide.
Output: edit command + OutOfBand row + purity pin + contract_edit.rs + README section.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-composite-engine-workspace-historian-edit/13-RESEARCH.md
@.planning/phases/13-composite-engine-workspace-historian-edit/13-05-SUMMARY.md
@.planning/phases/13-composite-engine-workspace-historian-edit/13-07-SUMMARY.md
@crates/ignition-cli/src/cli.rs
@crates/ignition-cli/src/main.rs
@crates/ignition-tui/src/routes.rs
@crates/ignition-cli/tests/tui_coverage.rs
@crates/ignition-core/src/actions/edit.rs
@README.md
</context>

<planner_locks>
- **OutOfBand stdout contract (planner decision, pinned):** `ign edit` writes NOTHING to stdout in any mode — all prose (fetched/edited/no-op/pushed summaries, refusals) renders to stderr; the editor inherits the terminal directly. This is the OutOfBand declaration's concrete meaning for edit and is byte-scan pinned. (completions writes raw stdout by documented exception; edit goes further — zero stdout — because the child editor owns the terminal.)
- **Atomicity (USER-locked):** the `edit` OutOfBand row, the pinned-test extension, and the clap command land in ONE task/commit — an orphan row fails the clap walk by design.
- **No new exit slugs:** no-op is exit 0; guarded refusal = confirmation_required (exit 2); stale/fail-closed/invalid = InvalidInput (exit 2). The staleness refusal stays NOT --yes-able (13-05 planner lock carries through dispatch — dispatch must not add an override).
- **Guard composition:** the dispatch renders StagedEdit's changed-members summary and folds require_confirmation — ONE gate site for edit (13-08's dispatch is the only place; no per-flag forks).
</planner_locks>

<tasks>

<task type="auto">
  <name>Task 1: `ign edit` surface — clap command + dispatch + guard + OutOfBand row + pinned set (ONE atomic task)</name>
  <files>crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/main.rs, crates/ignition-tui/src/routes.rs, crates/ignition-cli/tests/tui_coverage.rs</files>
  <action>
ONE task because the executable contract forbids a split: a clap node without its routes row leaves the bidirectional tui_coverage walk RED (09-03 precedent — "clap and rows land together").

1. cli.rs: `Edit(EditArgs)` on Commands — doc: "fetch a gateway resource, edit it in $EDITOR, push back (guarded)". `EditArgs { project: String, resource_path: String (positional), }` — --yes is the GLOBAL flag (globals-once). NOTE the arm shape: `ign edit <project> <resource-path>` (research's whole-tree codec + scoped editor — the resource-path scopes what the EDITOR opens; push rides project_import).
2. main.rs dispatch:
   a. Resolve editor env FIRST (VISUAL/EDITOR present?) — absent ⇒ InvalidInput refusal pre-resolve (null profile, zero requests — usage-class convention).
   b. `Session::resolve` → construct TokioEditor → `actions::edit::edit_pipeline(api, &editor, &project, Some(&resource_path))`.
   c. NoOp ⇒ stderr prose `edit: no changes — nothing pushed` + exit 0. Never constructs an import.
   d. Ready ⇒ render the changed-members summary to stderr (the blast radius — same text the refusal would carry) → `require_confirmation` unless global --yes → the existing project-import client call with the StagedEdit zip → stderr `edit: pushed N member(s) to <project>`.
   e. Stale/fail-closed refusals propagate from the pipeline verbatim (their stable message prefixes + kept-path recovery text render through the standard error envelope on stderr).
   f. Human render helper lives in render.rs ONLY if shared machinery demands it — edit's prose is deliberately stderr printlns in the dispatch arm (OutOfBand: no ActionOutput/envelope involvement). Do NOT add an ActionOutput::Edit variant (that would route success through stdout).
3. routes.rs: `CliRoute { path: "edit", mapping: Mapping::OutOfBand }` with the justification comment: hands the terminal to a child $EDITOR process and writes all prose to stderr — not a cockpit verb; the edit loop IS the product (completions/api-call genre). Update the module doc's OutOfBand prose to three rows.
4. tui_coverage.rs: extend `out_of_band_rows_are_pinned`'s expected set to `vec!["api call", "completions", "edit"]` (current literal at ~line 153) + extend its doc-comment justification with the edit rationale + the Phase-8 pre-declaration citation (the reservation existed since 08-06 with zero rows — orphan rows fail the clap walk by design, so row+command MUST land together — they do, in this task). The bidirectional walk must stay green with the new `edit` clap node IN THE SAME commit.
  </action>
  <verify>`cargo build` green; `ign edit proj res` against a wiremock export with EDITOR=true (no-op script): exit 0, stdout EMPTY, stderr carries the no-op line; EDITOR unset: exit 2 refusal pre-resolve; `cargo test -p ignition-cli --test tui_coverage` green with the extended pinned set AND the walk bidirectional-green in the same state</verify>
  <done>The `edit` reservation is fulfilled atomically — clap command + dispatch + guard + OutOfBand row + pinned-set extension in one landing — and the loop runs through dispatch with the guard composed at ONE site, all prose on stderr</done>
</task>

<task type="auto">
  <name>Task 2: stdout purity byte-scan + first contract tests (editor-env, resource-path, guard)</name>
  <files>crates/ignition-cli/tests/contract_edit.rs</files>
  <action>
1. Stdout purity (contract_edit.rs): byte-scan pins over the REAL spawned binary (Phase-8 harness style — assert-based, SNAPSHOTS cannot sanitize): run `ign edit` with a scripted no-op editor + wiremock export, `--verbose` (max diagnostics), assert stdout is EMPTY (0 bytes) and stderr carries the prose; repeat the no-op and happy-path cases. Add `edit` to the purity harness's sweep if it enumerates commands explicitly.
2. First contract tests: editor-env refusal (pre-resolve, null profile, zero requests); resource-path-not-found refusal naming valid candidates; guard refusal golden (staged summary text = refusal message; wiremock traffic pin: export GETs only, zero imports without --yes).
  </action>
  <verify>`cargo test -p ignition-cli --test contract_edit` green so far; the purity pins pass (grep the test for the zero-byte assert)</verify>
  <done>Edit's zero-stdout OutOfBand contract is machine-pinned over the real binary, and the usage-class refusals have goldens</done>
</task>

<task type="auto">
  <name>Task 3: full-loop binary contract tests + README edit section</name>
  <files>crates/ignition-cli/tests/contract_edit.rs, README.md</files>
  <action>
1. contract_edit.rs completes the matrix (scripted-$EDITOR shims as real spawned scripts + wiremock serving export/import — the binary-test pattern; reuse 13-05's shim-script authoring approach):
   - Happy path: editor modifies a member → --yes → assert the import request recorded by wiremock carries the spliced bytes (re-parse the recorded body), stderr summary lines, exit 0.
   - Guarded: same fixture WITHOUT --yes → exit 2 confirmation_required, stderr = the diff summary (golden), traffic pin: export GETs only, zero imports.
   - Stale: wiremock serves divergent exports between fetch and the pipeline's re-check → exit 2 with the stable staleness message; assert it fires WITH --yes too.
   - Fail-closed: JSON-breaking editor → exit 2, stderr carries the kept-temp path; assert the temp dir EXISTS after the run (recovery path) and is removed on the success/no-op paths.
   - No-op: byte-compare editor → exit 0, zero import traffic (pin), stdout empty.
2. README edit section (the agent/human contract):
   - The loop (fetch → decode → $EDITOR → encode → push) and `ign edit <project> <resource-path>` usage.
   - $EDITOR contract: VISUAL→EDITOR; IDE users set the wait flag INSIDE EDITOR (e.g. `EDITOR="code --wait"`); no $EDITOR = refusal; exit codes are advisory — content decides.
   - Behavior table: unchanged save → clean no-op (nothing pushed); invalid JSON after save → refused, temp tree kept (path printed) — fix and re-run; gateway changed since fetch → refused (re-run to fetch fresh; never force-pushed).
   - Stdout contract: edit is OutOfBand — zero stdout, prose on stderr.
   - Cross-link the workspace section (whole-tree authoring vs single-resource edit).
3. Final phase sweep: `cargo test --workspace`, clippy -D warnings, fmt --check — all green; README exit-table agreement + all contract greps pass.
  </action>
  <verify>contract_edit.rs fully green (all five scenarios + traffic pins); full workspace suite + clippy + fmt green; `grep -c "ignition-cli/src/render.rs" <(git diff --name-only)` shows render.rs UNTOUCHED by this plan (no ActionOutput::Edit exists — grep proves it)</verify>
  <done>The edit slice is contract-complete end to end — the kubectl-edit loop live at the binary level with every failure mode pinned — SC-5 fully closed</done>
</task>

</tasks>

<verification>
- `cargo test --workspace` green; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo fmt --check`
- Pinned OutOfBand set is EXACTLY ["api call", "completions", "edit"] — grep tui_coverage.rs; the row and clap command landed in this plan's Task 2/Task 1 (same plan, walk-proven atomicity)
- Stdout purity: edit pins zero-stdout under max diagnostics over the real binary
- The staleness refusal fires identically with and without --yes (test asserts both)
- README's edit + workspace sections agree with the implemented behavior (no aspirational docs)
- Phase-level: all 5 SCs now have binary-level evidence — spot-run the phase verification checklist
</verification>

<success_criteria>
`ign edit` delivers the full kubectl-edit loop with content-decided no-op, fail-closed validation, unconditional staleness blocking, and a single composed guard — with the `edit` OutOfBand row landed atomically per the Phase-8 contract and the stdout purity obligation proven — SC-5 closed and the phase's contract rituals complete.
</success_criteria>

<output>
After completion, create `.planning/phases/13-composite-engine-workspace-historian-edit/13-08-SUMMARY.md`
</output>
