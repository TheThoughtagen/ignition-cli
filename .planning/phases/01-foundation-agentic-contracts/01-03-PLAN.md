---
phase: 01-foundation-agentic-contracts
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - crates/ignition-core/src/config/mod.rs
  - crates/ignition-core/src/config/profile.rs
  - crates/ignition-core/src/config/secret.rs
  - crates/ignition-core/src/lib.rs
  - crates/ignition-core/src/actions/profile.rs
  - crates/ignition-core/src/actions/mod.rs
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/src/render.rs
  - crates/ignition-core/tests/keyring_smoke.rs
  - crates/ignition-cli/tests/contract_profile.rs
  - .github/workflows/ci.yml
autonomous: true

must_haves:
  truths:
    - "User can add, list, and switch profiles (profile add / profile list / profile use) and the active profile name appears in every command's JSON envelope AND in human-mode output as an active-profile header/prefix (research Pattern 4)"
    - "--profile NAME and IGNITION_PROFILE=NAME both select the profile; flag wins over env"
    - "Secrets resolve env-first (IGNITION_TOKEN_<PROFILE> → token_env → IGNITION_TOKEN → keyring → IGNITION_USER/PASSWORD) and a canary token NEVER appears in stdout or stderr of any command, including --verbose (CORE-02 acceptance test)"
    - "Config file is written with 0600 perms; unknown TOML keys warn but do not fail"
    - "Unknown profile exits 3 with profile_not_found envelope + hint; no-config + no --profile for profile commands degrades gracefully (version still exits 0 — fresh-install truth)"
    - "Keyring smoke test passes on headless Linux CI via the gnome-keyring recipe (STATE.md blocker CLOSED)"
  artifacts:
    - path: "crates/ignition-core/src/config/mod.rs"
      provides: "Config load/save, discovery (IGNITION_CLI_CONFIG → ProjectDirs), env overlay, 0600 perms"
      contains: "IGNITION_CLI_CONFIG"
    - path: "crates/ignition-core/src/config/profile.rs"
      provides: "Config/Profile/AuthRef serde structs (BTreeMap for deterministic list)"
      contains: "BTreeMap"
    - path: "crates/ignition-core/src/config/secret.rs"
      provides: "Secret newtype (no Serialize, Debug/Display → ***), SecretStore trait, EnvStore, KeyringStore, resolve_secret order"
      contains: "pub struct Secret"
    - path: "crates/ignition-core/src/actions/profile.rs"
      provides: "add/list/use_profile actions returning serde models (no printing — layering invariant)"
    - path: "crates/ignition-core/tests/keyring_smoke.rs"
      provides: "#[ignore]-gated real-keyring round-trip (set/get/delete via KeyringStore)"
    - path: "crates/ignition-cli/tests/contract_profile.rs"
      provides: "profile list goldens (3 render modes), profile_not_found exit-3 golden, redaction canary test"
  key_links:
    - from: "crates/ignition-core/src/config/mod.rs"
      to: "directories::ProjectDirs"
      via: "config_path(): env override first, platform path second"
      pattern: "IGNITION_CLI_CONFIG"
    - from: "crates/ignition-core/src/config/secret.rs"
      to: "keyring::Entry"
      via: "KeyringStore maps Entry::new failure → warn + skip (store unavailable), never fatal"
      pattern: "Entry::new"
    - from: "crates/ignition-cli/src/main.rs"
      to: "crates/ignition-core/src/actions/profile.rs"
      via: "dispatch resolves profile context then calls action fns; envelope threads resolved profile name"
      pattern: "actions::profile"
    - from: "crates/ignition-core/src/config/secret.rs"
      to: "serde"
      via: "Secret deliberately has NO Serialize impl (type-level redaction)"
      pattern: "impl Serialize.*Secret.* MUST NOT EXIST"
    - from: "crates/ignition-cli/src/render.rs"
      to: "resolved profile name"
      via: "every human-mode render leads with active-profile header line [profile: NAME] per research Pattern 4 (CORE-01 human path)"
      pattern: "\\[profile:"
---

<objective>
Profiles, config, and secrets: TOML config with discovery + env overlay, the SecretStore seam (env-first, keyring fallback — the STATE.md blocker resolution), the redaction guarantee proven by a canary test, and the `profile add/list/use` subcommands born contract-complete through the 01-02 envelope.

Purpose: CORE-01 (multiple profiles, visible in every output), CORE-02 (token/basic auth from env/config/keyring, secrets never echoed), and the config-error class (exit 3) of CORE-04/05 become real and golden-tested.
Output: config/ module, Secret/SecretStore, profile actions + subcommands, config-error goldens, redaction canary test, keyring smoke test + CI job.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-agentic-contracts/01-RESEARCH.md
@.planning/phases/01-foundation-agentic-contracts/01-02-SUMMARY.md

@crates/ignition-core/src/error.rs
@crates/ignition-core/src/output.rs
@crates/ignition-cli/src/main.rs
@crates/ignition-cli/src/cli.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config module — discovery, TOML load/save (0600), structs, env overlay</name>
  <files>crates/ignition-core/src/config/mod.rs, crates/ignition-core/src/config/profile.rs, crates/ignition-core/src/lib.rs</files>
  <action>
Implement research Pattern 2 (~line 180) exactly:

`profile.rs`: `Config { active: Option<String>, profiles: BTreeMap<String, Profile> }` (BTreeMap = deterministic list order for goldens), `Profile { url: url::Url, label: Option<String>, ssl_verify: bool (default true), auth: AuthRef }` — `label` is CORE-01's "label" wording made real: an optional display label with `#[serde(default, skip_serializing_if = "Option::is_none")]` so existing configs and goldens don't churn when unset, `AuthRef` — serde untagged/tagged enum covering the three reference forms: `token_env` (env var NAME), `keyring` (user string like "profile:prod"), `user_env`+`password_env` (basic pair). Secrets are REFERENCES, never values — no field of Profile ever holds a secret string. NO `deny_unknown_fields` anywhere (Pitfall 7 — forward compat).

`mod.rs`:
- `config_path()`: `IGNITION_CLI_CONFIG` env (OsString → PathBuf) FIRST, else `ProjectDirs::from("","","ignition-cli").config_dir().join("config.toml")` — verbatim per research. (macOS gotcha: XDG ignored; that's why every test uses the env override.)
- `load(path) -> Result<Config, CoreError>`: missing file → Ok(Config::default()) (fresh install is not an error); unreadable/invalid TOML → `CoreError::ConfigInvalid` (exit 3) with hint naming the path; unknown keys → `tracing::warn!`, parse succeeds (test this).
- `save(path, &Config)`: create parent dirs; write via `OpenOptions` with `.mode(0o600)` under `#[cfg(unix)]` (Pitfall 3.6 prevention — verified by test below).
- Env overlay, applied per research precedence (CLI flag > IGNITION_* env > profile value > default) — the env half lives here as `apply_env_overlay(&mut config, selected_profile)`: `IGNITION_URL` overrides the selected profile's URL. (`IGNITION_PROFILE` selection and `IGNITION_TOKEN` auth resolution are handled in main/secret.rs respectively — keep each concern in one place.)
- `resolve_selection(&Config, flag: Option<&str>) -> Result<Option<(String, Profile)>, CoreError>`: flag > `IGNITION_PROFILE` (already applied to cli.profile by apply_env_defaults in 01-01) > `config.active`. Unknown name → `CoreError::ProfileNotFound { name, known }` (exit 3). None found → Ok(None) — callers decide whether None is an error (gateway commands: yes via NoActiveProfile; version/profile-list: tolerated).

Unit tests (in-file `#[cfg(test)]`, all via `IGNITION_CLI_CONFIG` → tempfile): round-trip save/load; 0600 perms asserted (`#[cfg(unix)]` metadata mode check); unknown-key warn-not-fail; missing active + no flag → Ok(None); unknown profile → ProfileNotFound with known list; overlay override of URL.
  </action>
  <verify>cargo test -p ignition-core && cargo clippy -p ignition-core -- -D warnings</verify>
  <done>All config unit tests pass; 0600 perms proven; deterministic profile ordering; unknown keys tolerated</done>
</task>

<task type="auto">
  <name>Task 2: Secrets — Secret newtype, SecretStore seam, env-first resolution, keyring smoke + CI job</name>
  <files>crates/ignition-core/src/config/secret.rs, crates/ignition-core/src/lib.rs, crates/ignition-core/tests/keyring_smoke.rs, .github/workflows/ci.yml</files>
  <action>
Implement research Pattern 3 (~line 240) exactly — this closes the STATE.md keyring blocker:

`secret.rs`:
- `pub struct Secret(String)` with `Debug`/`Display` → `Secret(***)` / `***`, NO Serialize impl (type-level redaction), single accessor `expose(&self) -> &str` (grep-auditable: the only read site will be the reqwest header construction in 01-04)
- `pub trait SecretStore: Send + Sync { fn resolve(&self, profile: &str, auth: &AuthRef) -> Result<Option<Secret>, CoreError> }` — `Ok(None)` = not found here, `Err` = found-but-unreadable
- `EnvStore`: `IGNITION_TOKEN_<PROFILE_UP>` (profile uppercased, non-alnum → `_`) → profile's `token_env` var name → generic `IGNITION_TOKEN` → `IGNITION_USER`+`IGNITION_PASSWORD` basic pair
- `KeyringStore`: `keyring::Entry::new("ignition-cli", &format!("profile:{profile}"))`; **map ANY `Entry::new` failure → `tracing::warn!` + `Ok(None)` (store unavailable — headless Linux, no D-Bus — skip, never fatal, never hang)**; a real get/set error on a live entry surfaces as Err
- `resolve_secret(profile, auth, stores: &[Box<dyn SecretStore>]) -> Result<Credential, CoreError>`: first Some wins; exhausted → `CoreError::SecretUnavailable` with the research hint text (env vars are the supported headless path). Return type models token-OR-basic: make the resolved value an `enum Credential { Token(Secret), Basic(Secret, Secret) }` so 01-04 can build the right header (never both — that rule gets enforced at the header site).

`tests/keyring_smoke.rs`: `#[ignore]`-gated real-keychain round-trip (set_password/get_password/delete_credential via KeyringStore) — default `cargo test` never touches a real OS keychain (Pitfall 8); it only runs in the dedicated CI job.

`.github/workflows/ci.yml`: append the `keyring-smoke` job verbatim from research Pitfall 2 (~line 506): ubuntu-latest, checkout + rust setup, `apt install gnome-keyring`, `gnome-keyring-daemon --components=secrets --daemonize --unlock <<< 'foobar'`, then `cargo test -p ignition-core --test keyring_smoke -- --ignored`. This is the STATE.md blocker's closure, using keyring-rs's own CI recipe.
  </action>
  <verify>cargo test -p ignition-core (env-store paths, no daemon needed) && cargo test -p ignition-core --test keyring_smoke -- --ignored (on macOS dev machine — Keychain present; may prompt once, that's expected locally) && yaml lint ci.yml</verify>
  <done>Resolution order implemented and unit-tested with EnvStore + a Vec<Box<dyn SecretStore>> test double; KeyringStore fail-soft proven; smoke test exists and passes locally; CI has the keyring-smoke job</done>
</task>

<task type="auto">
  <name>Task 3: profile add/list/use actions + subcommands + envelope threading + goldens + redaction canary</name>
  <files>crates/ignition-core/src/actions/profile.rs, crates/ignition-core/src/actions/mod.rs, crates/ignition-core/src/lib.rs, crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/main.rs, crates/ignition-cli/src/render.rs, crates/ignition-cli/tests/contract_profile.rs</files>
  <action>
`actions/profile.rs` (single file, three pub fns — actions are the shared verb layer; NO printing, return serde models only, per ARCHITECTURE.md invariant "CLI handlers and TUI both call actions"):
- `add(config_path, name, url, label: Option<String>, auth_ref, set_active: bool) -> ProfileAddResult` — validates URL parses, writes config (0600), model includes name + active flag; `label` (CORE-01 display label, None when `--label` absent) is stored on the Profile
- `list(config) -> ProfileListResult { active: Option<String>, profiles: Vec<ProfileSummary> }` — ProfileSummary { name, label: Option<String> (skip_serializing_if-none, mirroring Profile), url, auth_kind } where auth_kind is a safe string ("token_env"/"keyring"/"basic") — NEVER a secret or env value; `label` surfaces CORE-01's display label
- `use_profile(config_path, name) -> ProfileUseResult` — sets active; unknown name → CoreError::ProfileNotFound

`cli.rs`: `Commands::Profile(ProfileCmd)` with `Add { name, url, --label (Option<String> — CORE-01 label), --token-env, --keyring, --user-env/--password-env, --active }`, `List`, `Use { name }` — flags-only, zero interactive prompts (anti-pattern: "Prompting ever"). `arg_required_else_help` on Profile.

`main.rs`: dispatch resolves the profile context ONCE via `resolve_selection` and threads the resolved name into every envelope (`profile: Some("dev")` replaces the 01-02 placeholder `null`). Rules: `version` tolerates None (fresh install, exit 0, profile stays null); profile add/list tolerate None; `profile use` on a missing config yields `CoreError::ProfileNotFound` (load() returns the default empty config on file-absent, so the requested name simply isn't in the known list — exit 3, same class as unknown profile; no golden pins this path beyond the existing unknown-profile golden). Update the 01-02 version goldens via `SNAPSHOTS=overwrite` when run WITH a config (profile field now real) — keep one no-config version golden asserting `"profile": null`.

`render.rs` (human mode — CORE-01 says the active profile is "visible in every command's output"; research Pattern 4 locks the human-path mechanism as a prefix/header): grow the 01-02 human renderer so EVERY human-mode render — success AND error — begins with an active-profile header line `[profile: dev]` (exact literal, golden-anchored) when a profile resolved; omit the header when none did (fresh install keeps the bare version line). `profile list` rows then render one per line after the header as `name  label-or-name  url  auth_kind` (label falls back to name when unset). JSON/compact modes are untouched — the envelope's top-level `profile` field is their mechanism.

`tests/contract_profile.rs` (harness pattern from 01-02):
1. `profile_lifecycle_golden` — add two profiles (dev with `--label "Dev rig"`, prod without) → `profile list --json` golden: `{"ok":true,"profile":"dev","data":{"active":"dev","profiles":[...]}}` where dev's row includes `"label":"Dev rig"` and prod's row OMITS the field (skip_serializing_if proven); also human + compact modes (Pitfall 6: all three modes golden-tested), with the human golden asserting the first stdout line is exactly `[profile: dev]` and the dev row shows the label (CORE-01 human-path truth)
2. `profile_use_switches_active` — `profile use prod` → subsequent list envelope shows `"profile":"prod"` (CORE-01 switch truth)
3. `env_profile_selection` — `IGNITION_PROFILE=prod ign profile list --json` → profile=prod; `--profile dev` beats env
4. `unknown_profile_exit_3_golden` — `--profile nope profile list --json` → exit 3, stderr envelope `error.code == "profile_not_found"`, `error.hint` is a string mentioning known profiles (CORE-05)
5. `secret_redaction_canary` — CORE-02 acceptance: write config with `token_env = "IGNITION_TOKEN"`, export `IGNITION_TOKEN=CANARY-t0k3n-zzz`, run `profile list --json --verbose` (and again with `-vv`), assert canary substring absent from stdout AND stderr; also assert it IS in the environment (sanity that the test could have failed)
6. `no_config_version_exit_0` — no config file, `ign version --json` → exit 0, `"profile": null`
  </action>
  <verify>cargo test --workspace && SNAPSHOTS=overwrite cargo test -p ignition-cli; git diff --stat (review golden churn only)</verify>
  <done>profile add/list/use work end-to-end through the envelope; active profile visible in every output; --profile/IGNITION_PROFILE selection proven; unknown profile exits 3 with slug+hint; canary never leaks even at -vv</done>
</task>

</tasks>

<verification>
- `cargo test --workspace` green (env-store paths only — no daemon dependency)
- `ign profile add dev http://localhost:9088 --token-env IGNITION_TOKEN --active && ign profile list --json` → envelope with profile:"dev" and both-field data shape
- `IGNITION_PROFILE=nope ign version --json; echo $?` → 3 with profile_not_found slug on stderr
- Canary grep: `cargo test -p ignition-cli secret_redaction_canary` passes at -vv verbosity
- `grep -rn "impl serde::Serialize for Secret\|impl Serialize for Secret" crates/ | wc -l` → 0 (type-level redaction intact)
- CI: check job + keyring-smoke job both present; keyring smoke recipe verbatim from keyring-rs CI
</verification>

<success_criteria>
CORE-01 complete (add/list/switch, active profile in every envelope, flag+env selection). CORE-02 complete (env-first token/basic resolution through the SecretStore seam, keyring fail-soft, canary-proven redaction, no Serialize on Secret). Config-error class of CORE-04/05 golden-enforced (exit 3, slug, hint). STATE.md keyring blocker closed by the smoke job.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-agentic-contracts/01-03-SUMMARY.md`
</output>
