---
phase: 01-foundation-agentic-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - .gitignore
  - crates/ignition-cli/Cargo.toml
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/tests/cli_chassis.rs
  - crates/ignition-core/Cargo.toml
  - crates/ignition-core/src/lib.rs
  - crates/ignition-tui/Cargo.toml
  - crates/ignition-tui/src/lib.rs
  - webdev/README.md
  - .github/workflows/ci.yml
autonomous: true

must_haves:
  truths:
    - "cargo build --workspace succeeds and cargo build -p ignition-cli --no-default-features also succeeds (lean agent build)"
    - "ign --help shows all five global flags (--profile, --json, --compact, --yes, --verbose)"
    - "ign version prints the CLI version on a fresh install with no config file (exit 0)"
    - "IGNITION_JSON=1 / IGNITION_YES=1 / IGNITION_PROFILE env apply without flags (env parsed in one place)"
    - "Diagnostics go to stderr only — stdout is never polluted by logs"
    - "CI runs green on ubuntu-latest and macos-latest from the first commit (no Windows — LOCKED decision)"
  artifacts:
    - path: "Cargo.toml"
      provides: "Virtual workspace manifest, rust-version 1.88, workspace.dependencies single-sourcing"
      contains: "rust-version"
    - path: "crates/ignition-cli/src/cli.rs"
      provides: "Cli derive struct with 5 global args + Version/Tui stub subcommands"
      contains: "global = true"
    - path: "crates/ignition-cli/src/main.rs"
      provides: "Single-exit-point main: try_parse → apply_env_defaults → init_tracing → dispatch → ExitCode"
      contains: "fn main() -> ExitCode"
    - path: "crates/ignition-tui/src/lib.rs"
      provides: "TUI stub lib (no ratatui dep until Phase 6)"
    - path: ".github/workflows/ci.yml"
      provides: "CI check job: fmt/clippy/build/test/no-default-features on ubuntu+macos"
  key_links:
    - from: "crates/ignition-cli/src/main.rs"
      to: "clap error handling"
      via: "Cli::try_parse() → Err(e) => e.exit() (clap renders usage errors, exit 2)"
      pattern: "try_parse"
    - from: "crates/ignition-cli/src/main.rs"
      to: "env defaults"
      via: "apply_env_defaults(&mut cli) reading IGNITION_PROFILE / IGNITION_JSON / IGNITION_YES"
      pattern: "apply_env_defaults"
    - from: "crates/ignition-cli/src/main.rs"
      to: "stderr diagnostics"
      via: "init_tracing writes to stderr, never stdout"
      pattern: "with_writer"
---

<objective>
Create the Cargo workspace skeleton and CLI chassis that every later plan builds on: three crates (ignition-cli bin / ignition-core lib / ignition-tui stub lib), the five global clap args defined once at the root, a single-exit-point `main()`, env-default application, stderr-only tracing, the `tui` feature gate, and a day-one CI workflow.

Purpose: This is the foundation phase's foundation — the workspace shape, MSRV, dependency versions, and main() dispatch shape are final from commit one, so later plans only add modules, never restructure.
Output: Compiling workspace, runnable `ign` binary with `--help`/`--version`/`version` stub, green CI on ubuntu+macos.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-agentic-contracts/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace manifests + three crates + tui feature gate + webdev/ scaffold</name>
  <files>Cargo.toml, .gitignore, crates/ignition-cli/Cargo.toml, crates/ignition-core/Cargo.toml, crates/ignition-tui/Cargo.toml, crates/ignition-core/src/lib.rs, crates/ignition-tui/src/lib.rs, webdev/README.md</files>
  <action>
Create the virtual workspace manifest EXACTLY per research "Installation" block (01-RESEARCH.md ~line 58): `resolver = "3"`, members = the three crates under `crates/`, `[workspace.package]` with `edition = "2024"`, `rust-version = "1.88"` (CORRECTED from STACK.md's 1.85 — keyring 4.1.x floor; do NOT use 1.85), `version = "0.1.0"`. Copy the full `[workspace.dependencies]` table verbatim from research (clap 4.6 derive, clap_complete 4.6, serde, serde_json, tokio 1.53, reqwest 0.13 json, toml 1.1, directories 6.0, keyring 4.1, thiserror 2.0, semver 1.0, tracing 0.1, tracing-subscriber 0.3 env-filter, url 2.5, plus dev: wiremock 0.6, snapbox 1.2 with cmd/json/regex features, assert_cmd 2.2, predicates 3.1, tempfile 3.27). Single-sourcing now means later plans never touch the root manifest for versions.

Crate skeletons:
- `crates/ignition-core`: lib crate. `[dependencies]` pulls workspace serde/serde_json/toml/directories/keyring/thiserror/semver/tracing/url/reqwest/tokio (core will need these by Phase 1's end); `[dev-dependencies]` wiremock + tempfile. `src/lib.rs` = doc comment + `pub mod` declarations added by later plans (empty for now beyond crate docs).
- `crates/ignition-tui`: lib crate, ZERO dependencies (no ratatui until Phase 6 — this is the structural proof the workspace shape is final). `src/lib.rs`: `pub fn run() -> ! { unimplemented!("TUI cockpit arrives in Phase 6") }` (or a Result stub — anything dependency-free).
- `crates/ignition-cli`: bin crate named `ign` (`[[bin]] name = "ign"`). `[features] default = ["tui"]`, `tui = ["dep:ignition-tui"]`. Depends on ignition-core (path via workspace) + clap + tracing + tracing-subscriber + tokio. `[dev-dependencies]`: assert_cmd, predicates, tempfile, snapbox.
- `webdev/README.md`: one short paragraph — "Gateway-side WebDev routes ship here in Phase 5 (com.inductiveautomation.webdev/resources/IgnitionCLI/*). Empty scaffold for now."
- `.gitignore`: `/target`, `Cargo.lock` is COMMITTED (binary crate — reproducible agent installs), `.DS_Store`, `/tmp`.

Windows CI is SKIPPED (LOCKED planner decision — primary targets are macOS/Linux; `cfg(unix)`-clean perms code only).
  </action>
  <verify>cargo build --workspace && cargo build -p ignition-cli --no-default-features && cargo tree -p ignition-tui (must show zero external deps)</verify>
  <done>Workspace builds in both feature configurations; `cargo metadata` shows rust-version 1.88; ignition-tui has an empty dependency tree</done>
</task>

<task type="auto">
  <name>Task 2: Clap chassis — 5 global args, single-exit-point main, env defaults, stderr tracing, version stub</name>
  <files>crates/ignition-cli/src/main.rs, crates/ignition-cli/src/cli.rs</files>
  <action>
`cli.rs` — copy research Pattern 1 (~line 144) near-verbatim: `Cli` derive struct with the five globals defined ONLY here with `global = true`: `--profile` (Option<String>), `--json` (SetTrue), `--compact` (SetTrue), `--yes`/`-y` (SetTrue), `--verbose`/`-v` (Count → u8). `#[command(name = "ign", version, propagate_version = true)]`. `Commands` enum with exactly two variants for now: `Version` (CLI version only — gateway check arrives in plan 01-04) and `Tui` (STUB — under `#[cfg(feature = "tui")]`, body errors "TUI arrives in a later phase"; proves the feature gate compiles lean both ways). Never mark a global arg `required` (research gotcha).

`main.rs` — the dispatch chassis per research Pattern 4 skeleton (~line 360), minus error mapping (CoreError doesn't exist until plan 01-02):
- `fn main() -> ExitCode` (NOT `fn main() -> Result<...>` — the single-exit-point contract starts here)
- `Cli::try_parse()` → `Err(e) => e.exit()` — clap renders usage errors itself, exit 2, by design; do NOT build a clap error hook (research Pattern 1 gotcha)
- `apply_env_defaults(&mut cli)`: one function, the ONLY place env→flag happens — `IGNITION_PROFILE` → profile (if flag absent), `IGNITION_JSON=1` → json, `IGNITION_YES=1` → yes. Subcommand code will only ever read the struct (research: "single precedence point, trivially testable")
- `init_tracing(cli.verbose)`: tracing-subscriber with env-filter, **writer to stderr only** — stdout is reserved for data output; filter levels: 0=warn (default), 1=info, 2=debug, 3+=trace; `IGNITION_LOG` env var, when set, overrides the verbosity-derived filter (pass-through to EnvFilter)
- tokio multi-thread runtime + `block_on(dispatch(cli))` — dispatch returns `Result<ActionOutput, CoreError>` shape can be simplified to plain output for now; the full Result plumbing lands in 01-02
- `Commands::Version` → print CLI version + crate name to stdout (plain text; envelope rendering arrives in 01-02)

Binary-level tests (in `crates/ignition-cli/tests/cli_chassis.rs`, assert_cmd): (1) `ign --help` exit 0 and stdout mentions all five flags; (2) `ign --version` exit 0; (3) `ign version` exit 0, prints version, works with NO config file present (fresh-install truth); (4) env application: run with `IGNITION_JSON=1` — accepted, no error (assert exit 0; envelope behavior tested in 01-02); (5) unknown flag `--bogus` exits 2 (clap renders it — proves the try_parse→e.exit() wiring); (6) stdout/stderr separation: `ign version -v` → any tracing output on stderr, stdout still parses as the version line only. Use `IGNITION_CLI_CONFIG` pointed at a tempfile in every binary test that could touch config (research Pitfall 3 — `directories` ignores XDG on macOS).
  </action>
  <verify>cargo test --workspace && cargo run -q -- version && cargo run -q -- --bogus; echo "exit=$?" (expect exit=2)</verify>
  <done>All chassis tests pass; usage errors exit 2 from clap; env defaults apply in exactly one function; tracing never appears on stdout</done>
</task>

<task type="auto">
  <name>Task 3: CI workflow — check job on ubuntu+macos (day-one MSRV guard)</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create `.github/workflows/ci.yml` per research "CI workflow" example (~line 602), check job ONLY (the keyring-smoke job is added in plan 01-03 when the smoke test exists — adding it now would fail on a missing test target):
- Triggers: push to main + all pull_requests
- Matrix: `[ubuntu-latest, macos-latest]` — NO Windows (LOCKED decision)
- Steps: actions/checkout@v7 → actions-rust-lang/setup-rust-toolchain@v1 (toolchain stable, components rustfmt+clippy) → Swatinem/rust-cache@v2 → `cargo fmt --all --check` → `cargo clippy --workspace --all-targets -- -D warnings` → `cargo build --workspace` → `cargo test --workspace` → `cargo build -p ignition-cli --no-default-features`

This workflow is the Pitfall-1 guard (MSRV drift breaks CI on day one) and proves the default test path needs NO secret service (env-store only until 01-03). Verify locally that `cargo fmt --all --check` and `cargo clippy --workspace --all-targets -- -D warnings` pass BEFORE committing — CI red on first push wastes a cycle. If the repo has no GitHub remote configured, still commit the workflow file (it activates whenever pushed).
  </action>
  <verify>cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && yaml is valid (e.g. `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))" || npx --yes yaml-lint .github/workflows/ci.yml)</verify>
  <done>CI workflow exists with ubuntu+macos matrix, all six steps, fmt/clippy clean locally; no keyring-smoke job yet</done>
</task>

</tasks>

<verification>
- `cargo build --workspace && cargo build -p ignition-cli --no-default-features` — both feature configs compile
- `cargo test --workspace` green, including chassis tests (help/version/env/exit-2/stdout-clean)
- `grep -r "global = true" crates/ignition-cli/src/cli.rs | wc -l` → 5 (globals defined exactly once)
- `grep -rn "process::exit" crates/ | wc -l` → 0 (single-exit-point discipline; only `e.exit()` from clap inside main is allowed)
- CI manifest committed; fmt + clippy -D warnings clean
</verification>

<success_criteria>
Workspace shape is FINAL (three crates, feature gate, webdev/ scaffold, MSRV 1.88). The `ign` binary parses all five global flags, applies env defaults in one place, renders clap usage errors with exit 2, keeps stdout data-only, and works on a fresh install. CI is green-shaped from day one on ubuntu+macos.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-agentic-contracts/01-01-SUMMARY.md`
</output>
