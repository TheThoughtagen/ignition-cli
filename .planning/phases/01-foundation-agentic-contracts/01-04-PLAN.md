---
phase: 01-foundation-agentic-contracts
plan: 04
type: execute
wave: 4
depends_on: ["01-02", "01-03"]
files_modified:
  - Cargo.toml
  - crates/ignition-core/Cargo.toml
  - crates/ignition-cli/Cargo.toml
  - crates/ignition-core/src/client/mod.rs
  - crates/ignition-core/src/client/version.rs
  - crates/ignition-core/src/actions/version.rs
  - crates/ignition-core/src/lib.rs
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/completions.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-core/tests/gateway_info_contract.rs
  - crates/ignition-cli/tests/version_gateway_contract.rs
  - README.md
autonomous: true

must_haves:
  truths:
    - "ign version with a resolvable profile checks the gateway and reports both versions; gateway < 8.3.1 or unparseable → exit 6 gateway_too_old envelope with upgrade hint (CORE-08)"
    - "ign version on an unreachable gateway → exit 0 + warning inside data (LOCKED decision — version is a local-info command; hard-failing scripts on a sleeping rig is hostile)"
    - "ign version on a fresh install (no config) still exits 0 (regression-guarded)"
    - "GatewayApi sends X-Ignition-API-Token when a token resolved, else Authorization: Basic — never both (wiremock-proven)"
    - "ign completions bash|zsh|fish emits the script on stdout (the one sanctioned stdout exception regardless of --json)"
    - "Destructive-op guard helper refuses without --yes/IGNITION_YES with exit 2 + confirmation_required hint (pattern proven by unit test for later phases)"
    - "Network (4) / auth (5) / target-state (6) exit classes are binary-level tested against wiremock"
  artifacts:
    - path: "crates/ignition-core/src/client/mod.rs"
      provides: "GatewayApi trait (async_trait — LOCKED), ReqwestGatewayApi with auth headers + ssl_verify + timeouts, for_tests constructor"
      contains: "async_trait"
    - path: "crates/ignition-core/src/client/version.rs"
      provides: "GatewayInfo model + MIN_GATEWAY 8.3.1 + below_minimum (suffix/short-version tolerant, unparseable → refuse)"
      contains: "8.3.1"
    - path: "crates/ignition-core/src/actions/version.rs"
      provides: "version action: CLI version + optional gateway check implementing the locked behavior matrix"
    - path: "crates/ignition-cli/src/completions.rs"
      provides: "clap_complete::aot::generate for bash/zsh/fish"
      contains: "clap_complete"
    - path: "crates/ignition-core/tests/gateway_info_contract.rs"
      provides: "wiremock HTTP contract tests: token header sent, 200 parse, 401→Auth, unreachable→Network"
    - path: "crates/ignition-cli/tests/version_gateway_contract.rs"
      provides: "Binary-level exit-code tests for classes 4/5/6 + unreachable exit-0 warning, via wiremock-backed profiles"
  key_links:
    - from: "crates/ignition-core/src/client/mod.rs"
      to: "gateway REST"
      via: "GET /data/api/v1/gateway-info with credential attached when present, auth not required on 200"
      pattern: "gateway-info"
    - from: "crates/ignition-core/src/client/mod.rs"
      to: "Secret"
      via: "Credential::Token → X-Ignition-API-Token header; Basic → Authorization header; secret.expose() called ONLY here"
      pattern: "X-Ignition-API-Token"
    - from: "crates/ignition-core/src/actions/version.rs"
      to: "GatewayApi + below_minimum"
      via: "gateway_info() → GatewayTooOld (exit 6) | unreachable → warning in data (exit 0)"
      pattern: "GatewayTooOld|below_minimum"
    - from: "crates/ignition-cli/src/main.rs"
      to: "config::apply_env_overlay"
      via: "IGNITION_URL overlays the resolved profile's URL before ReqwestGatewayApi construction (research-locked precedence)"
      pattern: "apply_env_overlay"
---

<objective>
The gateway seam and the Phase-1 finish line: `GatewayApi` trait (async_trait — LOCKED) with one method (`gateway_info`) proven against wiremock, the `ign version` gateway minimum-version check with the locked behavior matrix, shell completions, and the `--yes` confirmation guard that later phases inherit.

Purpose: CORE-08 (refuse cleanly on <8.3.1), CORE-07 (completions), CORE-06 (guard pattern), and the network/auth/target-state classes of CORE-04/05 — completing every Phase-1 requirement and proving the auth-header construction end-to-end before Phase 2 builds the full client on this seam.
Output: client/ module + version action + completions + guard, wiremock contract tests at core and binary level.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-agentic-contracts/01-RESEARCH.md
@.planning/phases/01-foundation-agentic-contracts/01-02-SUMMARY.md
@.planning/phases/01-foundation-agentic-contracts/01-03-SUMMARY.md

@crates/ignition-core/src/error.rs
@crates/ignition-core/src/config/secret.rs
@crates/ignition-cli/src/main.rs
@crates/ignition-cli/src/cli.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: GatewayApi trait + ReqwestGatewayApi + wiremock contract tests</name>
  <files>Cargo.toml, crates/ignition-core/Cargo.toml, crates/ignition-core/src/client/mod.rs, crates/ignition-core/src/client/version.rs, crates/ignition-core/src/lib.rs, crates/ignition-core/tests/gateway_info_contract.rs</files>
  <action>
Add `async-trait = "0.1"` to workspace deps + ignition-core deps (LOCKED DECISION: async_trait over RPITIT — dyn-compatible today, ubiquitous; research Open Question 2 resolved per recommendation).

`client/mod.rs` per research Pattern 5 (~line 380):
- `#[async_trait::async_trait] pub trait GatewayApi: Send + Sync { async fn gateway_info(&self) -> Result<GatewayInfo, CoreError>; }` — ONE method in Phase 1; keep the trait COARSE (per capability, not per endpoint) so Phase 2 grows it without churn
- `ReqwestGatewayApi { base: url::Url, credential: Credential, client: reqwest::Client }`; `new(profile: &Profile, credential: Credential)`: connect_timeout 10s / timeout 30s; `profile.ssl_verify == false` → `danger_accept_invalid_certs(true)` (dev-rig only, per-profile, never global)
- Header rule (verified from ignition-mcp): `Credential::Token` → `X-Ignition-API-Token: <secret.expose()>`; `Credential::Basic` → `Authorization: Basic <b64>`; NEVER both — enforce with a match, not if/if-else chains. `secret.expose()` is called at exactly this one site (grep-auditable redaction boundary)
- Error mapping: connect/timeout/TLS → `CoreError::Network { url, source }` (exit 4); HTTP 401/403 → `CoreError::Auth { status }` (exit 5); both populate `endpoint`
- `for_tests(base_url: &str, credential: Credential)` constructor (research example) — no profile needed

`client/version.rs`: `GatewayInfo { version, edition, state, uptime? }` serde struct (field names match gateway response); `pub const MIN_GATEWAY: &str = "8.3.1"`; `below_minimum(raw) -> bool` per research (~line 439): strip `-`/space suffixes, tolerate short "8.3" by appending ".0", `semver::Version` compare against 8.3.1.0; **unparseable → true** (refuse safely rather than guess). Unit tests: "8.3.1" ok, "8.3.2" ok, "8.3.0" too old, "8.1.10" too old, "8.3" too old (short form), "8.3.1-SNAPSHOT.20260801" ok (suffix), "garbage" too old.

`tests/gateway_info_contract.rs` per research example (~line 410): (1) 200 + token header matcher → parses version, `guard.received_requests()` proves the header was sent; (2) Basic credential → Authorization matcher instead (never both — assert X-Ignition-API-Token ABSENT via a rejecting mock); (3) 401 → `CoreError::Auth`, `exit_code()==5`, endpoint populated; (4) connection refused (server dropped / closed port) → `CoreError::Network`, exit 4.
  </action>
  <verify>cargo test -p ignition-core --test gateway_info_contract && grep -c "expose()" crates/ignition-core/src/client/mod.rs (expect exactly 1-2 call sites, all inside header construction)</verify>
  <done>All four wiremock contract tests pass; auth-header rule (token xor basic) proven; error classes 4/5 mapped with endpoint field</done>
</task>

<task type="auto">
  <name>Task 2: version action — gateway check with LOCKED behavior matrix + binary-level exit tests</name>
  <files>crates/ignition-core/src/actions/version.rs, crates/ignition-core/src/actions/mod.rs, crates/ignition-core/src/lib.rs, crates/ignition-cli/src/main.rs, crates/ignition-cli/tests/version_gateway_contract.rs</files>
  <action>
`actions/version.rs` — implement the LOCKED behavior matrix (research Pattern 6, ~line 449). LOCKED DECISION (planner, per orchestrator — no user available to ask): unreachable gateway → **exit 0 + warning**, because version is a local-info command and hard-failing scripts on a sleeping rig is hostile; the refusal contract applies only when the gateway ANSWERED:

| situation | output | exit |
|-----------|--------|------|
| no config / no profile resolvable | cli_version only, `profile: null` | 0 |
| profile resolves, gateway reachable, ≥8.3.1 | cli_version + `gateway: {version, edition, state}` in data | 0 |
| profile resolves, gateway answered, <8.3.1 or unparseable | `CoreError::GatewayTooOld { found, minimum }` → envelope (exit 6), hint "upgrade gateway to ≥8.3.1; see `ign doctor` (Phase 2)" | 6 |
| profile resolves, gateway unreachable / no secret for a *check* | cli_version + `gateway: null` + `warnings: ["gateway unreachable: …"]` inside `data` (top-level stays exactly {ok,profile,data} — LOCKED envelope never grows fields) | 0 |

Action signature: `version(api: Option<&dyn GatewayApi>, cli_version: &'static str) -> Result<VersionResult, CoreError>` — the CLI resolves the profile + credential first; only `SecretUnavailable` degrades version to the header-less/no-check branch (version must not demand a secret) via the adapter specified in the `main.rs` paragraph below — every OTHER credential error propagates. `VersionResult` serde model: `cli_version`, optional `gateway`, optional `warnings` array.

`main.rs`: version dispatch resolves the profile, then applies `apply_env_overlay` (from the 01-03 config module) so `IGNITION_URL` overrides the resolved profile's URL BEFORE constructing `ReqwestGatewayApi` — the research-locked precedence (flag > `IGNITION_*` env > profile value) must hold at the client-construction site, not just in 01-03's config unit tests. The client is built whenever a profile resolved (post-overlay). Credential attach: 01-03's `resolve_secret` returns `Result<Credential, CoreError>` — exhaustion is `Err(SecretUnavailable)`, never `Ok(None)` — so adapt at the dispatch site: `resolve_secret_opt(...) = resolve_secret(...).map(Some).or_else(|e| match e { CoreError::SecretUnavailable { .. } => Ok(None), other => Err(other) })`. `Some(credential)` → build with headers; `Ok(None)` → proceed header-less (gateway-info is marked `auth: none` in the 83-api collection; verify empirically in Phase 2); other errors propagate.

`tests/version_gateway_contract.rs` (binary-level, wiremock-backed profiles via `IGNITION_CLI_CONFIG` → tempfile whose profile URL points at the mock server): (1) 200 with `{"version":"8.3.2",...}` → exit 0, envelope data.gateway.version == "8.3.2"; (2) 200 with `"8.1.14"` → exit 6, stderr envelope slug `gateway_too_old`, hint mentions 8.3.1; (3) 401 → exit 5, slug `auth_rejected`; (4) unreachable (profile URL → `http://127.0.0.1:1`) → **exit 0**, data.warnings non-empty, data.gateway null; (5) fresh-install regression: no config → exit 0, profile null; (6) `ignition_url_overlay` — config profile URL → `http://127.0.0.1:1` (dead port), run with `IGNITION_URL` pointing at a wiremock 200 `8.3.2` server → exit 0, data.gateway.version == "8.3.2" (proves the env overlay is applied BEFORE client construction). These complete the exit-class golden coverage for 4/5/6 (CORE-04/05 all classes now binary-tested).
  </action>
  <verify>cargo test -p ignition-cli --test version_gateway_contract && cargo run -q -- version (no config: exit 0)</verify>
  <done>All five matrix rows binary-tested; IGNITION_URL overlay wired before client construction and binary-proven; exit classes 4/5/6 proven through the whole stack; unreachable→exit-0+warning locked and tested</done>
</task>

<task type="auto">
  <name>Task 3: completions subcommand + --yes confirmation guard</name>
  <files>crates/ignition-cli/Cargo.toml, crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/completions.rs, crates/ignition-cli/src/main.rs</files>
  <action>
`completions.rs` per research Pattern 7 (~line 456): add `clap_complete` to ignition-cli deps (workspace version). `Commands::Completions { shell: clap_complete::aot::Shell }` with `value_parser!(Shell)` (Shell implements the value-parser trait — drops straight in) and `#[command(arg_required_else_help = true)]`. Handler: `Cli::command()` → `clap_complete::aot::generate(shell, &mut cmd, "ign", &mut std::io::stdout())`. Import from `aot` — the old `shells`/`generator` paths are deprecated; do NOT use the `unstable-dynamic` engine (`COMPLETE=$SHELL` is flag-gated). Completions print to stdout **regardless of --json** — the one sanctioned exception (scripts pipe them); document the exception in a code comment and in README's contract section.

Confirmation guard (CORE-06 pattern for later phases): `fn require_confirmation(yes: bool, action_desc: &str) -> Result<(), CoreError>` in main.rs (or a small confirm.rs): `yes == false` → `CoreError::ConfirmationRequired` (exit 2 — usage class, it names a flag the user must add) with hint `"this operation is destructive; re-run with --yes or set IGNITION_YES=1"`. `--yes` flag already merges `IGNITION_YES` via apply_env_defaults (01-01). No destructive command exists in Phase 1, so prove the guard by unit test: without yes → Err with exit_code 2 + slug `confirmation_required` + hint; with yes=true → Ok. Phase 3+ (`project delete`, `rig reset`) inherits this helper verbatim.

Tests (extend cli chassis tests): `completions bash|zsh|fish` → exit 0, non-empty stdout containing the shell-appropriate marker (e.g. bash output contains `_ign"`, fish contains `complete -c ign`, zsh contains `#compdef ign`); `ign completions` with no shell → exit 2 (arg_required_else_help).
  </action>
  <verify>cargo test --workspace && cargo run -q -- completions bash | head -5 && cargo run -q -- completions fish | grep -c "complete -c ign"</verify>
  <done>All three shells generate; bare `completions` exits 2; guard unit-proven with exit 2 + slug + hint; README notes the stdout exception</done>
</task>

</tasks>

<verification>
Phase-complete checks (all CORE requirements):
- CORE-01: profile add/list/use + envelope echo (01-03 tests green)
- CORE-02: canary redaction + no Serialize on Secret (01-03 tests green)
- CORE-03: --json/--compact goldens across 3 modes (01-02/01-03 goldens green)
- CORE-04: exit classes 0/2/3/4/5/6 all binary-tested across 01-02/03/04 suites
- CORE-05: envelope code/message/endpoint/hint asserted in every error golden
- CORE-06: guard unit-proven, zero interactive prompts anywhere (grep for read_line/confirm → 0)
- CORE-07: completions for 3 shells
- CORE-08: <8.3.1 → exit 6 refusal; fresh install and unreachable both exit 0
- `cargo test --workspace` green; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo build -p ignition-cli --no-default-features` still green
- `grep -rn "process::exit" crates/ | wc -l` → 0; `grep -c "expose()" crates/ignition-core/src -r` → call sites only in client header construction
</verification>

<success_criteria>
Every Phase-1 requirement (CORE-01…CORE-08) implemented and test-enforced. The GatewayApi seam exists with wiremock-proven auth headers and error mapping — Phase 2 builds the full client by adding trait methods, zero restructuring. Phase goal met: `ign` is installable/configurable with multiple profiles, secure auth, and the frozen machine-readable contract.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-agentic-contracts/01-04-SUMMARY.md`
</output>
