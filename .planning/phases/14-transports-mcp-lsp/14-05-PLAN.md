---
phase: 14-transports-mcp-lsp
plan: 05
type: execute
wave: 4
depends_on: ["14-04"]
files_modified:
  - /Users/pmannion/whiskeyhouse/ignition-nvim/lua/ignition/lsp.lua
autonomous: true

must_haves:
  truths:
    - "ignition-nvim registers a SECOND LSP client ('ignition_live', cmd {'ign','lsp'}) beside the existing Python statics client, guarded by vim.fn.executable('ign')"
    - "The Python ignition-lsp registration and its statics ownership (definition/codeAction/workspace symbols) are UNTOUCHED — the patch only adds, never edits the statics path"
    - "Headless end-to-end verification in the sibling repo: opening an ignition filetype buffer attaches BOTH clients, with ignition_live advertising completion+hover"
    - "The patch is committed in the ignition-nvim repository (canonical checkout /Users/pmannion/whiskeyhouse/ignition-nvim) with its own conventions"
  artifacts:
    - path: "/Users/pmannion/whiskeyhouse/ignition-nvim/lua/ignition/lsp.lua"
      provides: "second vim.lsp.config('ignition_live') + dual start in the FileType autocmd + executable guard"
      contains: "ignition_live"
    - path: "/Users/pmannion/whiskeyhouse/ignition-nvim/scripts or test scratch (executor's choice)"
      provides: "headless nvim verification script/evidence (may be scratch, not committed, if the repo has no test harness)"
  key_links:
    - from: "lua/ignition/lsp.lua FileType autocmd"
      to: "vim.lsp.start for both configs"
      via: "ignition_lsp (statics, unchanged) + ignition_live (ign lsp) started in the same callback; nvim merges completions across clients and aggregates diagnostics natively"
      pattern: "ignition_live"
    - from: "ignition_live config"
      to: "ign lsp"
      via: "cmd = {'ign','lsp'} — the sibling binary the IDE-03 requirement names"
      pattern: "ign.*lsp"
---

<objective>
Wire ignition-nvim to `ign lsp` (IDE-03): register the second client in detection order, keep the Python statics composition intact, and verify end-to-end in the sibling repo — headless.

Purpose: SC-3 (consumption) and SC-4. nvim's native multi-client merge is the composition mechanism — neither server learns about the other.
Output: the lsp.lua patch, committed in the ignition-nvim repo, with headless attach evidence.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-transports-mcp-lsp/14-RESEARCH.md
@/Users/pmannion/whiskeyhouse/ignition-nvim/lua/ignition/lsp.lua

Research: "The nvim-side patch" section (verified anatomy: find_lsp_server() :93-130 returns ONE cmd; M.setup :16-91 registers ONE vim.lsp.config + ONE FileType autocmd; the ~5-8-line honest patch shape — the roadmap's "one-line" phrasing is optimistic, plan for the real anatomy), Pitfall 5 (executable guard mirrors the existing venv/exepath checks at :97-114; capability overlap is avoided by 14-03's narrow caps).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch lsp.lua — second client registration + dual start with executable guard</name>
  <files>/Users/pmannion/whiskeyhouse/ignition-nvim/lua/ignition/lsp.lua</files>
  <action>
    1. Read the live file first (the research line anchors are from 2026-09-15 — re-verify they still hold; if the repo moved, re-derive the anatomy from the actual code and note it).
    2. Add a second `vim.lsp.config('ignition_live', ...)` in M.setup's style: `cmd = { 'ign', 'lsp' }`, `filetypes` IDENTICAL to the existing ignition_lsp config (whatever the live list is — mirroring keeps attachment behavior consistent), `root_markers` mirroring the existing config's root detection (research sketch: 'project.json' — match the live config, not the sketch, if they differ).
    3. Guard with `if vim.fn.executable('ign') == 1 then ... end` (Pitfall 5: ign absent → config not registered → no failed spawn loop), mirroring the existing venv/exepath guard style at lsp.lua:97-114.
    4. In the FileType autocmd callback, start BOTH: the existing ignition_lsp start path stays byte-identical; add the ignition_live start beside it (only when the config was registered). Do NOT modify find_lsp_server() or the statics chain — the patch is purely additive.
    5. Style: match the repo's Lua conventions (read neighboring code; lspconfig-era vs native vim.lsp.config style — follow whatever the file already uses).
  </action>
  <verify>`luac -p` or `nvim --headless +"lua require('ignition.lsp')" +qa` loads the module without error; `git -C /Users/pmannion/whiskeyhouse/ignition-nvim diff` shows ONLY additive changes to lsp.lua (statics chain untouched); `rg -n "definition|code_action|workspace_symbol" /Users/pmannion/whiskeyhouse/ignition-nvim/lua/ignition/lsp.lua` shows the ignition_live config claiming none of them.</verify>
  <done>ignition-nvim carries the second client registration, guarded, additive-only, in the repo's own style; the statics path is provably untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Headless end-to-end verification in the sibling repo + commit</name>
  <files>/Users/pmannion/whiskeyhouse/ignition-nvim/lua/ignition/lsp.lua</files>
  <action>
    1. Build/install `ign` first: `cargo build -p ignition-cli` in ignition-cli and ensure the `ign` on PATH (or used by the test) is the freshly built binary — the headless test must exercise THIS phase's server, not a stale install. Use `PATH="<target/debug>:$PATH"` scoping for the nvim run.
    2. Write a throwaway headless verification script (scratch, e.g. /tmp/ign_lsp_e2e.lua + `nvim --headless -u <minimal init loading the ignition plugin>`): open a buffer with an ignition filetype + a root with the configured root marker → wait (bounded, e.g. `vim.wait(15000, ...)`) until `#vim.lsp.get_clients{bufnr=0}` reaches the expected count for a machine with both servers available (if the Python venv is absent on this machine, the ignition_live client alone attaching is still a PASS for the IDE-03 claim — record which case ran) → assert the client named `ignition_live` is attached and its `server_capabilities` include completionProvider + hoverProvider → print evidence lines and exit 0/1 accordingly.
    3. Run it; capture output as the end-to-end evidence (goes into the SUMMARY verbatim). If the attach fails, debug with `:LspInfo`-equivalent dumps (`vim.print(vim.lsp.get_clients())`, client config, stderr of the ign process via `vim.lsp.start`'s handlers or by running `ign lsp` under the same config manually) — systematic-debugging discipline, no blind retry loops.
    4. Commit the patch IN the ignition-nvim repo following ITS conventions (read its recent `git log` for message style; suggested: `feat(lsp): register ign lsp as a second (live-truth) client beside ignition-lsp`). Record the commit sha in the task notes. Do NOT commit scratch scripts.
  </action>
  <verify>`git -C /Users/pmannion/whiskeyhouse/ignition-nvim log --oneline -1` shows the patch commit; the headless script's evidence output shows `ignition_live` attached with completion+hover capabilities; re-run the headless script once more after the commit to verify the committed state (not just the working tree).</verify>
  <done>IDE-03's mechanical half is DONE in the sibling repo: `ign lsp` is in ignition-nvim's detection order, verified end-to-end headless with both clients (or ignition_live alone where the statics venv is absent — recorded honestly), committed with a sha to cite.</done>
</task>

</tasks>

<verification>
- Sibling-repo diff is additive-only; statics registration byte-identical
- Headless attach evidence: ignition_live client with completion/hover caps on an ignition buffer
- Patch committed in ignition-nvim with its conventions; sha recorded
- The verified binary is this phase's fresh `ign` build (PATH-scoped)
</verification>

<success_criteria>
- SC-4 (mechanical half): `ign lsp` registered in ignition-nvim's detection order and verified end-to-end in that repo
- SC-3 (composition): both clients coexist — statics untouched, live client additive; nvim's native merge does the composing
</success_criteria>

<output>
After completion, create `.planning/phases/14-transports-mcp-lsp/14-05-SUMMARY.md` (include the ignition-nvim commit sha + headless evidence output)
</output>
