---
phase: 14-transports-mcp-lsp
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - crates/ignition-cli/tests/contract_mcp.rs
  - crates/ignition-cli/tests/mcp_oracle.py
autonomous: true

must_haves:
  truths:
    - "A scripted client over the REAL spawned `ign mcp serve` binary completes initialize → notifications/initialized → ping → tools/list → tools/call, with every stdout line parsing as exactly one JSON-RPC message under IGNITION_LOG=trace + noisy config (the byte-scan)"
    - "tools/call returns the envelope the direct CLI would print byte-for-byte (fixture-stable verb), isError true exactly on ok:false"
    - "Omitting confirm on a GUARDED_OPS verb returns the frozen confirmation_required failure envelope AS the tool result (isError:true, content = the envelope JSON)"
    - "Ping replies BEFORE an in-flight dead-gateway tools/call resolves — starvation is structurally impossible, pinned permanently"
    - "Unknown tool → -32601; invalid params/parse errors → -32602; the server survives malformed stdin and keeps serving"
    - "A real MCP client (Python mcp SDK via uv, test-time only) drives the full flow as a conformance oracle — green-skip when tooling is absent"
  artifacts:
    - path: "crates/ignition-cli/tests/contract_mcp.rs"
      provides: "ScriptedClient harness (byte-scan readline, isolated config, ambient-IGNITION_* strip) + full protocol contract suite"
      min_lines: 200
    - path: "crates/ignition-cli/tests/mcp_oracle.py"
      provides: "Python mcp SDK client oracle (initialize/tools-list/tools-call) run under `uv run --with mcp`, green-skips without uv"
  key_links:
    - from: "tests/contract_mcp.rs"
      to: "Command::cargo_bin(\"ign\")"
      via: "spawn the real binary with IGNITION_CLI_CONFIG tempdir + IGNITION_LOG=trace (contract_stdout_purity.rs:10-26 recipe)"
      pattern: "cargo_bin"
    - from: "tests/contract_mcp.rs reader thread"
      to: "serde_json::from_str"
      via: "every stdout line must parse as one JSON-RPC message — the stray-byte trap"
      pattern: "from_str"
    - from: "tests/contract_mcp.rs wiremock"
      to: "temp profile config"
      via: "tools/call against a wiremock-backed profile; envelope equality vs a direct binary run"
      pattern: "wiremock"
---

<objective>
Prove the MCP server's contract at binary level: the scripted-client byte-scan harness over the real spawned binary, the full 2025-06-18 lifecycle, the confirm-refusal envelope, the ping-starvation guarantee, and a real-client conformance oracle.

Purpose: SC-1/SC-2/SC-5(MCP half) verification. These tests are the permanent regression wall — they fail on a stray stdout byte, a starved ping, or any catalog/confirm drift.
Output: `tests/contract_mcp.rs` (always-run) + `tests/mcp_oracle.py` (uv-gated conformance oracle).
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-transports-mcp-lsp/14-RESEARCH.md
@crates/ignition-cli/src/mcp.rs
@crates/ignition-cli/tests/contract_stdout_purity.rs
@crates/ignition-cli/tests/tui_coverage.rs

Research: Pattern 5 (byte-scan harness), Code Examples (ScriptedClient shape), Pitfalls 2/3 (starvation + stray byte), Supporting stack (Python mcp SDK as test-time oracle — never a code dependency).
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScriptedClient harness + protocol lifecycle contract tests</name>
  <files>crates/ignition-cli/tests/contract_mcp.rs</files>
  <action>
    1. **Harness** (research Code Examples shape; duplicate the ~20-line isolated-config helper as the other contract test files do — each integration test is its own crate): `ScriptedClient::spawn()` runs `Command::cargo_bin("ign")` with `["mcp","serve"]`, stdin/stdout piped, `IGNITION_CLI_CONFIG` → tempdir config (ambient `IGNITION_*` knobs stripped: IGNITION_PROFILE/IGNITION_JSON/IGNITION_YES — purity-harness recipe, contract_stdout_purity.rs:10-26), `IGNITION_LOG=trace` for max noise. A reader thread reads stdout line-by-line and MUST `serde_json::from_str::<Value>` every line before forwarding — THE BYTE-SCAN: one stray byte fails the test. `recv_timeout` (e.g. 10s) on an mpsc so protocol death hangs fail loudly, never silently. Provide `send(&str)` and `expect_response(id, timeout)` helpers matching ids (string AND numeric ids both exercised — spec allows both).
    2. **Lifecycle tests**:
       - `initialize` with protocolVersion "2025-06-18" → response echoes it exactly; serverInfo.name == "ign"; capabilities.tools present.
       - `initialize` with an OLDER version ("2024-11-05") → server responds "2025-06-18" (spec negotiation rule).
       - `notifications/initialized` → NO response bytes arrive (assert quiet window via short recv_timeout miss).
       - `ping` → `result: {}` promptly (< 2s).
       - `tools/list` → non-empty `tools[]`; every entry has name/description/inputSchema; a sampled leaf set matches the clap walk (spot-pins: `project_delete` present with confirm in required + properties; `status` present without confirm; `mcp`/`lsp`/`tui`/`completions`/`edit` absent).
       - Round-trip smoke: for a deterministic sample of catalog tools (at minimum every GUARDED_OPS verb plus 5 read verbs), assert `Cli::try_parse_from(["ign"] + path + schema-required-arg tokens)` parses — catalog names cannot drift from parseable argv.
    3. Keep every assertion assert-based over parsed values (NEVER snapbox goldens — 08 purity-harness doctrine: overwrite must not sanitize).
  </action>
  <verify>`cargo test -p ignition-cli --test contract_mcp` green; introduce a fake `println!("noise")` in mcp.rs serve → byte-scan test red → remove (anti-sabotage recorded in task notes).</verify>
  <done>Scripted client drives the full 2025-06-18 lifecycle over the real binary; stdout purity is enforced by construction (every line must parse); negotiation, ping, tools/list catalog pins all green.</done>
</task>

<task type="auto">
  <name>Task 2: tools/call contract (envelope verbatim, confirm refusal, error mapping, starvation) + Python mcp SDK oracle</name>
  <files>crates/ignition-cli/tests/contract_mcp.rs, crates/ignition-cli/tests/mcp_oracle.py</files>
  <action>
    1. **Envelope-verbatim pin**: spin a wiremock gateway (dev-dep exists; binary-level contract-test convention) serving a fixture-stable read endpoint; write a temp config profile pointing at it; `tools/call` a read verb → assert the tool-result `content[0].text`, parsed, EQUALS the envelope from running the same invocation directly: `Command::cargo_bin("ign").args(["--json","--compact", <verb tokens...>]).env(IGNITION_CLI_CONFIG=...)` output — byte-for-byte equality of the two envelope strings (frozen-contract verbatim proof).
    2. **Confirm gate** (SC-2): `tools/call project_delete {name:"x"}` (confirm OMITTED) → result.isError == true and content text parses to `{ok:false, error.code:"confirmation_required", ...}` — the frozen refusal envelope as the tool result. Then `tools/call project_delete {name:"x", confirm:true}` against the wiremock → ok:true and the mock records the DELETE. Repeat the omit-case with `IGNITION_YES=1` in the spawned env → STILL refused (apply_env_defaults bypass is behavioral, pinned).
    3. **Error mapping**: unknown tool name → JSON-RPC error -32601; `tools/call` with wrong-typed args → -32602; send a malformed (non-JSON) line → server responds -32602 (or stays quiet if no id recoverable) and a SUBSEQUENT ping succeeds — protocol survives malformed input.
    4. **Ping starvation** (SC-5, research Pitfall 2): profile points at a dead port; send `tools/call status` (will hang on connect timeout), then immediately `ping` → the ping RESPONSE arrives BEFORE the tool result (expect_response ordering assertion). Keep this test permanent.
    5. **Python oracle** (`tests/mcp_oracle.py`): stdlib-only script that uses `mcp` (FastMCP client) via `uv run --with mcp python tests/mcp_oracle.py -- <path-to-ign>`: spawns the binary, completes initialize → tools/list (assert >0 tools) → tools/call a read verb against the wiremock config → asserts a text content result. Wire it as a documented optional gate: a small `#[test] #[ignore]` runner in contract_mcp.rs (or a doc-commented script step) that green-skips when `uv` is absent — same genre as the live-gate green-skip pattern (10-USER-SETUP precedent). NEVER a Cargo dependency on Python.
  </action>
  <verify>`cargo test -p ignition-cli --test contract_mcp` green (envelope equality, confirm refusal ± IGNITION_YES, -32601/-32602, starvation ordering); `uv run --with mcp python crates/ignition-cli/tests/mcp_oracle.py -- target/debug/ign` completes the full flow when uv is present (record output in task notes; absence green-skips); full `cargo test -p ignition-cli` green.</verify>
  <done>Every SC-1/SC-2/SC-5(MCP) claim has a binary-level contract test: catalog parity over the wire, envelope verbatim both outcomes, confirm-only gate (env-proof), protocol error mapping, malformed-input survival, and the permanent ping-starvation pin. A real MCP client validates the server end-to-end.</done>
</task>

</tasks>

<verification>
- `cargo test -p ignition-cli --test contract_mcp` fully green; byte-scan sabotage-proven
- Envelope byte-equality between MCP tool results and direct CLI runs on a fixture-stable verb
- IGNITION_YES bypass attempt refused behaviorally (test with the env var set)
- Oracle runs under uv when available; suite remains green without Python/uv
</verification>

<success_criteria>
- SC-1 verified: a scripted client completes initialize → tools/list → tools/call; catalog on the wire matches the clap tree
- SC-2 verified: omitted confirm → refusal envelope AS tool result; confirm:true → operation executes; no env bypass
- SC-5 (MCP) verified: byte-scan over the real spawned binary under max tracing noise; ping-before-tool-result ordering pinned
</success_criteria>

<output>
After completion, create `.planning/phases/14-transports-mcp-lsp/14-02-SUMMARY.md`
</output>
