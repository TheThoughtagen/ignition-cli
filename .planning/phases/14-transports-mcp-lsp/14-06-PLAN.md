---
phase: 14-transports-mcp-lsp
plan: 06
type: execute
wave: 5
depends_on: ["14-02", "14-05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "A real MCP client (Claude Code or Claude Desktop) completes initialize → tools/list → tools/call against `ign mcp serve` live — SC-1's 'an AI agent' claim verified by an actual agent client"
    - "protocolVersion '2025-06-18' survives live negotiation with the production client (roadmap-flagged implementation-time live smoke)"
    - "The nvim composition is visually confirmed: both clients attached, completions/hover working, statics unharmed"
  artifacts: []
  key_links: []
---

<objective>
Phase-final human verification: the live MCP smoke against Claude Code/Claude Desktop (the roadmap explicitly defers this to implementation time) and the visual nvim composition check — closing all five Phase-14 success criteria with a human in the loop.

Purpose: SC-4's end-to-end human gate + SC-1's live-agent clause + SC-3's felt experience. Everything automatable already ran in 14-02/14-04/14-05; this checkpoint verifies what only a human with their real client can.
Output: recorded verification evidence in the phase; APPROVED resume signal.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-transports-mcp-lsp/14-RESEARCH.md
@.planning/ROADMAP.md

Research: Open Question 5 (protocolVersion live smoke — implementation-time, roadmap-flagged), State of the Art (2025-06-18 is the current served revision), Pitfall 3 (a stray stdout byte kills the client — if the live client drops the connection, that's the first suspect).
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Phase-final human verification — MCP live smoke + nvim composition</name>
  <files>none (verification only)</files>
  <action>
Pause for the user to run the live verifications below. No code changes in this task; fix-and-retest if the user reports issues.
  </action>
  <verify>User confirms: (1) a real MCP client connects and completes initialize/tools-list/tools-call, (2) the confirm refusal arrives as a tool result, (3) nvim shows both LSP clients with completions/hover working and statics intact</verify>
  <done>User types "approved" (or all reported issues are fixed and re-verified)</done>
  <what-built>
    The complete Phase-14 transport surface, all binary-level contract tests green:
    - `ign mcp serve`: hand-rolled JSON-RPC 2.0 stdio server; clap-derived catalog; confirm-gated writes; frozen-envelope tool results; ping-never-starves; Python mcp SDK oracle passed (14-02)
    - `ign lsp`: lsp-server stdio LSP; three-family completions, TTL-stamped hover, cached diagnostics; dead-gateway no-blocking proof (14-03/14-04)
    - ignition-nvim: ignition_live client registered + headless-verified + committed (14-05)
  </what-built>
  <how-to-verify>
    A. MCP live smoke (Claude Code or Claude Desktop — pick whichever you use):
       1. Add the server to your client's MCP config: command `ign`, args `["mcp", "serve"]` (Claude Code: `claude mcp add ign -- ign mcp serve`; Claude Desktop: edit claude_desktop_config.json mcpServers entry).
       2. Restart the client. Start a new conversation and check the MCP/extension status shows the `ign` server CONNECTED (a dropped/failed connection = first suspect is a stray stdout byte — report exactly that if seen).
       3. Ask the agent to list available ign tools — confirm the catalog loads (spot-check it contains `status`, `tags_browse`, `project_delete`).
       4. Ask it to call a read-only tool (e.g. gateway status via `ign`) — confirm it returns a sensible result.
       5. Ask it to attempt a destructive op (e.g. `project delete` on a scratch name) WITHOUT confirming — confirm the refusal comes back as the tool result (confirmation_required envelope).
       6. Note the protocolVersion the client negotiated (visible in client logs) — expected 2025-06-18.
    B. nvim composition (SC-3/SC-4 visual gate):
       7. Open an ignition-related buffer in nvim with the updated plugin. `:LspInfo` (or `:checkhealth vim.lsp`) shows BOTH clients attached: ignition_lsp (statics) + ignition_live.
       8. Trigger completion (omni/Ctrl-X Ctrl-O or your usual) — entries include live tag/provider/named-query paths alongside statics completions.
       9. Hover a known tag path — text carries the "gateway snapshot Ns old" stamp. Go-to-definition still works from the statics client (composition, not replacement).
    Resume signal: type "approved" — or describe exactly what failed (client logs / :LspInfo output / unexpected tool results).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Live MCP client completes the full flow; protocolVersion 2025-06-18 negotiated live (record the observed value)
- Refusal-as-tool-result observed in the real client
- nvim shows both clients; completions/hover/statics all behave (SC-3 felt, SC-4 closed)
- Any failure here routes back as gap-closure plans (--gaps) with the recorded evidence
</verification>

<success_criteria>
- SC-1 closed with a real AI agent client (the roadmap's literal wording)
- SC-2 observed live (refusal envelope as tool result)
- SC-3/SC-4 closed visually in ignition-nvim (both clients, statics composition intact)
- SC-5 already binary-proven (14-02/14-04); live clients did not abort on stray bytes — confirming in production conditions
</success_criteria>

<output>
After completion, create `.planning/phases/14-transports-mcp-lsp/14-06-SUMMARY.md` (record the live-smoke evidence + nvim observations verbatim)
</output>
