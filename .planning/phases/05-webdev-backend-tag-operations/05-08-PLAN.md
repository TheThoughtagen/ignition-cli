---
phase: 05-webdev-backend-tag-operations
plan: 08
type: execute
wave: 2
depends_on: ["05-07"]
gap_closure: true
files_modified:
  - crates/ignition-cli/src/render.rs
  - crates/ignition-core/src/actions/tags.rs
  - crates/ignition-core/src/client/webdev.rs
  - crates/ignition-cli/tests/contract_tags.rs
autonomous: true

must_haves:
  truths:
    - "the eventId `tags alarms active` prints in human mode can be copied VERBATIM into `tags alarms ack` and acks (full UUID in the table)"
    - "short (prefix) eventIds still work in ack — expanded via the active-alarm list; ambiguous or unknown prefixes refuse invalid_input exit 2 naming the candidates / the miss"
    - "when a WebDev route denial carries a traceback, the CLI's webdev_route_error output shows it (no more black-box IllegalArgumentException)"
  artifacts:
    - path: "crates/ignition-cli/src/render.rs"
      provides: "alarms active table printing the FULL event_id"
      contains: "event_id"
    - path: "crates/ignition-core/src/actions/tags.rs"
      provides: "tags_alarms_ack id normalization: full-UUID passthrough + prefix expansion against active alarms"
      contains: "tags_alarms_ack"
    - path: "crates/ignition-core/src/client/webdev.rs"
      provides: "denial_to_error carries route traceback into the error message"
      contains: "denial_to_error"
  key_links:
    - from: "render_tags_alarms_active_human (render.rs)"
      to: "tags_alarms_ack input"
      via: "table prints exactly what ack accepts (full UUID, or a prefix ack expands)"
      pattern: "event_id"
    - from: "tags_alarms_ack (actions/tags.rs)"
      to: "tags_alarms_active lookup"
      via: "prefix-match expansion for short ids before the acknowledge route call"
      pattern: "tags_alarms_active"
    - from: "denial_to_error (client/webdev.rs)"
      to: "CoreError::WebdevRouteError"
      via: "traceback appended to the message when the route body carries it"
      pattern: "traceback"
---

<objective>
Close UAT Gap 2 (minor): the human view→ack alarm loop is broken at the seam — `tags alarms active` prints SHORT eventIds (first 8 chars), but `tags alarms ack` passes ids verbatim to a route that requires FULL UUIDs, so copying the printed ID dies as a generic `webdev_route_error` (the real cause — "Invalid UUID string" — hides in the route traceback the CLI never shows).

Purpose: what the table shows, ack must accept; and route denials must not be a black box.
Output: full-UUID table + short-id prefix expansion in ack (both directions of the loop work), traceback surfaced in webdev_route_error.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-webdev-backend-tag-operations/05-UAT.md

@crates/ignition-cli/src/render.rs
@crates/ignition-core/src/actions/tags.rs
@crates/ignition-core/src/client/webdev.rs
@crates/ignition-cli/tests/contract_tags.rs

Sequenced after 05-07 (wave 2) for live-rig exclusivity — its Task 3 gate mutates projects and may `rig reset`; no file overlap otherwise. Rig state (healthy + token) is recorded in 05-07-SUMMARY.md.
</context>

<tasks>

<task type="auto">
  <name>Task 1: View→ack loop — print the FULL event_id in the active table, expand short-id prefixes in ack</name>
  <files>crates/ignition-cli/src/render.rs, crates/ignition-core/src/actions/tags.rs, crates/ignition-cli/tests/contract_tags.rs</files>
  <action>
Design decision (planner-locked, per the gap's "pick one" — we take BOTH ends so the loop works from either copy-paste habit): the table prints the FULL UUID, AND ack still accepts short prefixes via lookup. No new taxonomy slug; no error.rs change.

1. **render.rs `render_tags_alarms_active_human` (~line 1062)** — drop the `short_id` truncation; print `alarm.event_id` verbatim, widen the column (`{:<38}` for a 36-char UUID + gutter), keep source/state/priority/name columns and row order. Update the doc comment (it currently documents the short form). The `--json` shape already carries the full id — untouched.
2. **actions/tags.rs `tags_alarms_ack` (~line 547)** — normalize ids BEFORE the acknowledge route call:
   - Id classification: a 36-char hyphenated string passes through verbatim (shape check only — `len == 36 && chars at indices 8/13/18/23 are '-'`; do NOT add a uuid dep); anything shorter is a prefix.
   - Prefix expansion: call the existing `tags_alarms_active` action (same project — it runs its own precondition; the extra probe round trip is the family's locked correctness-over-latency trade) and prefix-match `event_id.starts_with(prefix)`:
     - exactly one match → substitute the full UUID;
     - multiple matches → `CoreError::InvalidInput` (exit 2) listing the matching FULL ids (the user must disambiguate);
     - zero matches → `CoreError::InvalidInput` (exit 2): no active alarm's eventId starts with `{id}` — it may already be acknowledged/cleared; full ids ride `tags alarms active --json`.
   - Mixed short/full ids expand independently. The acknowledge wire call then carries only full UUIDs. Unit-test with the existing tags test rig (script `webdev_route_call`: the active action's response then the acknowledge response; assert the acknowledge body's `eventIds` carried the EXPANDED uuid — request-level proof, the family pattern).
3. **contract_tags.rs** — update the human-mode `tags_alarms_active_golden` (~line 1232; the table shape moves: full ids) and the ack golden (~line 1377) if its fixture uses short ids. Add one wiremock test: ack with a SHORT id against a scripted active list (two alarms, one matching prefix) → exit 0, and the recorded acknowledge request body contains the FULL uuid (assert on the wiremock recorded request, the surgery/wiremock family convention). Add refusal goldens: ambiguous prefix (two matches) and unknown prefix → exit 2 invalid_input.
4. **Live spot-check** (rig per 05-07-SUMMARY): write an alarm tag past setpoint → `tags alarms active` (human) → copy the PRINTED id verbatim → `tags alarms ack <printed> --username uat` exits 0 acknowledged:1; also `ack` with the 8-char prefix → same result. This is the exact UAT-failure loop, now green.
  </action>
  <verify>cargo test -p ignition-core -p ignition-cli green (moved + new goldens); the live copy-paste loop works both full and prefix; cargo clippy -D warnings clean.</verify>
  <done>the eventId the human table prints acks verbatim; short prefixes expand or refuse honestly (exit 2 with candidates/miss); the UAT view→ack loop is live-proven.</done>
</task>

<task type="auto">
  <name>Task 2: Surface the route traceback in webdev_route_error (the black-box fix)</name>
  <files>crates/ignition-core/src/client/webdev.rs, crates/ignition-cli/tests/contract_tags.rs</files>
  <action>
The route envelope carries `error.traceback?` on `route_error` denials (05-01 locked contract); the CLI drops it today.

1. **client/webdev.rs** — thread the optional traceback from the parsed denial body through `denial_to_error` (~line 168): keep the signature change minimal (add a `traceback: Option<&str>` param or pass the whole error object — match the local style), and when present append to the mapped message: `"\nroute traceback: {tb}"`. The `WebdevRouteError` VARIANT is untouched (message is a String — no error.rs edit, no taxonomy change, slug stable); absent traceback → message byte-identical to today, so only denials-with-traceback goldens move.
2. Update every `denial_to_error` call site (probe sweep in actions/webdev.rs:244 area + the route-call path) to pass the traceback through.
3. **contract_tags.rs** — one golden: the journal-missing-style denial shape EXTENDED with a `traceback` field on a generic `route_error` code → stderr envelope's message contains the traceback text; and confirm the existing no-traceback denials' goldens are byte-unchanged.
  </action>
  <verify>cargo test green; a denial with traceback shows the traceback in the error message; a denial without one renders byte-identical to before.</verify>
  <done>route-side exceptions (the "Invalid UUID string" class) are visible in CLI output instead of requiring raw curl to diagnose.</done>
</task>

</tasks>

<verification>
- `cargo test -p ignition-core -p ignition-cli` green; `cargo clippy --workspace -- -D warnings` clean.
- Frozen contracts intact: envelope, slug set, exit taxonomy, and the shared ConfirmationRequired hint all untouched (no error.rs edit in this plan).
- Live: human-mode copy-paste ack loop works (full printed id AND short prefix) on the rig 05-07 left healthy.
</verification>

<success_criteria>
UAT Gap 2 closed: `alarms active` prints ackable ids, ack accepts both full and short forms (honest refusals otherwise), and route tracebacks are no longer invisible.
</success_criteria>

<output>
After completion, create `.planning/phases/05-webdev-backend-tag-operations/05-08-SUMMARY.md` — include the live ack-loop transcript (full id + prefix) so UAT re-test can pin it.
</output>
