---
phase: 05-webdev-backend-tag-operations
plan: 05
type: execute
wave: 4
depends_on: ["05-04"]
files_modified:
  - crates/ignition-core/src/client/tags.rs
  - crates/ignition-core/src/actions/tags.rs
  - crates/ignition-core/tests/tags_contract.rs
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/src/render.rs
  - crates/ignition-cli/tests/contract_tags.rs
  - crates/ignition-cli/tests/e2e_webdev.rs
  - README.md
autonomous: true

must_haves:
  truths:
    - "User can get a tag config as JSON and create/edit/delete tag configs through the configure/deleteTags route actions — JSON in, JSON out"
    - "Stringified value/defaultValue sub-dicts inside configs are re-parsed so agents see real JSON, not JSON-in-a-string"
    - "User can list UDT types and get a full UDT definition (parameters + nested children)"
    - "User can bulk export a tag subtree to a JSON file and import it into a target provider with collision policy defaulting to abort (exit 6 pre-check) and overwrite behind --yes"
  artifacts:
    - path: "crates/ignition-core/src/actions/tags.rs"
      provides: "config get/create/edit/delete, udt types/def, export/import actions"
    - path: "crates/ignition-core/tests/tags_contract.rs"
      provides: "wiremock pins for the seven tagConfig route actions + stringified re-parse + collision guards"
  key_links:
    - from: "crates/ignition-core/src/actions/tags.rs"
      to: "webdev_route_call (tagConfig route)"
      via: "getConfig/configure/deleteTags/listUDTTypes/getUDTDefinition/exportTags actions"
      pattern: "tagConfig"
    - from: "crates/ignition-core/src/actions/tags.rs"
      to: "collision conventions (Phase 3)"
      via: "import abort = provider pre-check browse exit 6; overwrite = --yes, no pre-check"
      pattern: "CollisionPolicy|collision"
---

<objective>
Ship tag config CRUD (TAGS-05), UDT types/definitions (TAGS-06), and bulk provider export/import (TAGS-09) — all riding the tagConfig route action set from 05-01, with the stringified-JSON re-parse and the locked collision conventions.

Purpose: Roadmap criterion 3 + 5. This is the surgical tag-edit loop (config in/out) plus the bulk transfer that makes providers portable between gateways — the last piece of the ignition-mcp tag surface.
Output: `ign tags config|udt|export|import` arms, stringified re-parse, export/import round-trip with abort-default collisions, live round-trip gate.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-webdev-backend-tag-operations/05-RESEARCH.md
@.planning/phases/05-webdev-backend-tag-operations/05-04-SUMMARY.md

# Collision conventions being reused verbatim:
@.planning/phases/03-project-operations/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: config get/create/edit/delete actions</name>
  <files>crates/ignition-core/src/client/tags.rs, crates/ignition-core/src/actions/tags.rs, crates/ignition-core/tests/tags_contract.rs</files>
  <action>
actions/tags.rs extends over the tagConfig route (require_routes precondition inherited):
- `tags_config_get(api, project, path)`: route action `getConfig {tagPath}` (route already enforces the STRING arg). Post-process the envelope's `config`: any `value`/`defaultValue` field that is a STRING containing parseable JSON object/array is re-parsed into real JSON (research Pitfall 4-adjacent: configs carry stringified JSON) — expose `{path, tag_type, config}` with the re-parsed config (unit-explicit keys over the wire dict). Unparseable strings pass through verbatim.
- `tags_config_create(api, project, path, definition)`: derive basePath + name from path (split last path segment as the tag name; the rest — bracket-qualified — is basePath; `[default]` when path is bare), then route action `configure {basePath, tags: [{name, ...definition}], collisionPolicy: 'a'}`. The caller's definition dict may include tagType/value/alarms/history — the CLI does NOT re-shape it (the route owns the four configure traps; README documents the required shape: tagType discriminator, nested children, alarms-as-LIST).
- `tags_config_edit(api, project, path, definition)`: same configure call with collisionPolicy 'o' scoped to the single name (edit = overwrite that node) — NOT --yes-guarded (single-node edit, not a project-wide destructive).
- `tags_config_delete(api, project, paths)`: guarded destructive (CLI layer) → route action `deleteTags {paths}` → `{deleted: n}`.
Result shapes all-keys-always. Wiremock: getConfig passthrough + stringified re-parse unit (fixture with `"value": "{\"dataType\": ...}"` string → parsed object), create/edit body pins (basePath split + collisionPolicy char), deleteTags pin, precondition refusal inherited (one regression pin).
  </action>
  <verify>`cargo test -p ignition-core tags_contract` green — the stringified re-parse fixture proves agents see real JSON.</verify>
  <done>Config CRUD rides the route with path-splitting basePath derivation and the re-parse; create=abort-collision, edit=overwrite-node semantics documented.</done>
</task>

<task type="auto">
  <name>Task 2: UDT types/def + bulk export/import with collision policy</name>
  <files>crates/ignition-core/src/actions/tags.rs, crates/ignition-core/tests/tags_contract.rs</files>
  <action>
- `tags_udt_types(api, project, provider)`: route action `listUDTTypes {provider}` → `{provider, types: [{name, tag_type}]}`.
- `tags_udt_def(api, project, provider, name)`: route action `getUDTDefinition` → `{provider, name, definition}` (route walks recursively; same stringified re-parse applied).
- `tags_export(api, project, paths, out: Option<Path>)`: route action `exportTags {paths}` (kwargs enforced route-side) → the JSON-string payload is PARSED and validated as JSON (not stored opaque), written pretty to out file or stdout per the export-streaming convention (file default `<last-segment>.json`); result `{paths, file | stdout, tag_count?}` (payload is a list of subtrees — count top-level entries).
- `tags_import(api, project, provider, file|stdin, collision)`: read the payload file (InvalidInput class errors pre-resolution, Phase 3 conventions), then the LOCKED collision matrix mapped onto configure's 'a'/'o': abort (default) = pre-check via a browse of the target basePath for existing collisions → refuse exit 6 `tag_collision` (additive slug, hint names --collision-policy overwrite) BEFORE any route write, then configure 'a'; overwrite = --yes-guarded pre-resolution, NO pre-check (server authority), configure 'o'. merge stays not-a-clap-value (Designer-only, README). Body pin: `configure {basePath: '[provider]', tags: <parsed payload>, collisionPolicy}`.
Formats: JSON ONLY (planner lock — research resolved the roadmap's json/xml/csv sketch to the native JSON interchange; xml/csv deferred to backlog as documented format-discretion; README notes the rationale).
  </action>
  <verify>`cargo test -p ignition-core tags_contract` green — collision pre-check refusal with ZERO route writes (mock expect(0) proof, the 03-02 pattern); overwrite --yes path pinned; round-trip unit: export payload → import into different provider → shapes match.</verify>
  <done>UDT list/def + export/import complete with the abort-default collision matrix proven zero-write; tag_collision slug added (additive, two places).</done>
</task>

<task type="auto">
  <name>Task 3: CLI arms, goldens, live round-trip gate</name>
  <files>crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/main.rs, crates/ignition-cli/src/render.rs, crates/ignition-cli/tests/contract_tags.rs, crates/ignition-cli/tests/e2e_webdev.rs, README.md</files>
  <action>
Arms: `tags config get PATH`, `tags config create PATH --file|-` (definition JSON from file/stdin), `tags config edit PATH --file|-`, `tags config delete PATH...` (guarded), `tags udt types [--provider default]`, `tags udt def NAME [--provider default]`, `tags export PATH... [-o FILE]`, `tags import --file -|--provider P [--collision-policy abort|overwrite]` (overwrite implies the guard). Dispatch follows the family pattern; stdin readers in dispatch per resource-put precedent.
render.rs: config get = pretty JSON (agents and humans both want the object); udt types = name list; export = artifact line; import = counts + provider line.
Goldens: config get pretty-JSON golden (with re-parsed values visible), create/edit body-driven success lines, delete guard zero-work pin, udt types/def, export-to-file + stdout, import abort refusal (exit 6 tag_collision) + overwrite guarded success.
e2e_webdev.rs EXTENDS: the live round-trip the research proved — config create `[p5e2e]T1` (Int4 memory tag, value 123) → read back Good/123 (05-04's read) → export `[p5e2e]` → import into fresh provider `p5import` (abort path first: expect refusal if exists, then overwrite --yes) → read `[p5import]T1` == 123/Good → config delete cleanup → provider deletes. This gate IS TAGS-09's live proof.
README: config/udt/export/import rows, the configure-shape traps table (tagType/nesting/alarms-list/basePath — copied from research for users authoring definitions), JSON-only rationale, collision semantics, exit-table sync.
  </action>
  <verify>`cargo test -p ignition-cli` green; `cargo clippy --workspace --all-targets -- -D warnings` clean; help surfaces all new verbs.</verify>
  <done>`ign tags config|udt|export|import` wired; the live round-trip gate closes TAGS-05/06/09 with the research-proven loop.</done>
</task>

</tasks>

<verification>
- TAGS-05: get/create/edit/delete config JSON-in/out with stringified re-parse.
- TAGS-06: UDT types + recursive definitions.
- TAGS-09: bulk export/import with abort-default (zero-write refusal proof) and --yes overwrite; JSON round-trip live-proven.
- Parity: get/create/edit/delete_tag_config + list_udt_types + get_udt_definition map onto the MCP surface.
</verification>

<success_criteria>
- The surgical tag-edit loop (get → edit file → write back) works end-to-end.
- Providers are portable: export → import round-trip proven live with values intact.
- Collision conventions identical to Phase 3's (abort pre-check / overwrite guarded, no pre-check).
</success_criteria>

<output>
After completion, create `.planning/phases/05-webdev-backend-tag-operations/05-05-SUMMARY.md`
</output>
