---
phase: 05-webdev-backend-tag-operations
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - crates/ignition-core/src/client/tags.rs
  - crates/ignition-core/src/client/mod.rs
  - crates/ignition-core/src/actions/tags.rs
  - crates/ignition-core/src/actions/mod.rs
  - crates/ignition-core/src/actions/{projects,sessions,version,doctor,connections,inspect,logs,rig}.rs
  - crates/ignition-core/tests/tags_contract.rs
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/src/render.rs
  - crates/ignition-cli/tests/contract_tags.rs
  - crates/ignition-cli/tests/e2e_webdev.rs
  - README.md
autonomous: true

must_haves:
  truths:
    - "User can list tag providers (with tagCount metrics), create a standard provider, and delete one (signature-chained, --yes-guarded) — through native config-resource REST"
    - "User can browse tags as a tree (Property children filtered by default, substring filter) and sees providers at the root"
    - "User can read a single tag or a batch and write a value, with quality and timestamp shown"
    - "Every webdev-dependent tags command refuses exit 6 naming `ign webdev deploy` when the routes are absent or version-mismatched (the precondition probe)"
  artifacts:
    - path: "crates/ignition-core/src/client/tags.rs"
      provides: "native provider CRUD wire methods + tags-route browse/read/write wire models"
      exports: ["tag_provider_list", "tag_provider_create", "tag_provider_delete", "BrowseEntry"]
    - path: "crates/ignition-core/src/actions/tags.rs"
      provides: "provider/browse/read/write actions with the version precondition + tree/filter logic"
    - path: "crates/ignition-core/tests/tags_contract.rs"
      provides: "wiremock pins: provider CRUD native paths + route-call shapes + precondition refusals"
  key_links:
    - from: "crates/ignition-core/src/client/tags.rs"
      to: "/data/api/v1/resources/**/ignition/tag-provider"
      via: "native config-resource REST (list/find/create/delete-by-signature)"
      pattern: "ignition/tag-provider"
    - from: "crates/ignition-core/src/actions/tags.rs"
      to: "webdev_route_call"
      via: "browse/read/write POST action dispatch on the tags route"
      pattern: "webdev_route_call"
    - from: "crates/ignition-core/src/actions/tags.rs"
      to: "webdev_route_probe"
      via: "precondition version probe before every webdev-dependent action"
      pattern: "webdev_route_probe"
---

<objective>
Ship the first user-facing tag operations: provider CRUD (TAGS-01, native REST) plus browse-as-filtered-tree, single/batch read, and write (TAGS-02/03/04, deployed tags route) — with the version precondition every webdev-dependent command inherits.

Purpose: Roadmap criterion 2. Providers are the native seam (research: healthier data via config-resource REST — tagCount metrics, no route needed); browse/read/write are the first route consumers and the template for 05-05/06.
Output: client/tags.rs, actions/tags.rs, `ign tags provider|browse|read|write` commands, contract tests, live gate extension.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-webdev-backend-tag-operations/05-RESEARCH.md
@.planning/phases/05-webdev-backend-tag-operations/05-03-SUMMARY.md

# The seam this plan rides + the choke-file conventions:
@crates/ignition-core/src/client/webdev.rs
@crates/ignition-core/src/client/mod.rs
@crates/ignition-cli/src/cli.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: native provider CRUD client + actions</name>
  <files>crates/ignition-core/src/client/tags.rs, crates/ignition-core/src/client/mod.rs, crates/ignition-core/src/actions/tags.rs, crates/ignition-core/src/actions/mod.rs, crates/ignition-core/src/actions/{projects,sessions,version,doctor,connections,inspect,logs,rig}.rs, crates/ignition-core/tests/tags_contract.rs</files>
  <action>
Create client/tags.rs — NATIVE seam (research table verbatim):
- `tag_provider_list` → GET `/data/api/v1/resources/list/ignition/tag-provider` → ListEnvelope-shaped items including `config`, `metrics.tagCount`, `healthchecks.status` (wire-faithful camelCase passthrough model, two-layer naming: action re-exposes unit-explicit keys).
- `tag_provider_find(name)` → GET `.../resources/find/ignition/tag-provider/{name}` → record incl. `signature` (delete needs it).
- `tag_provider_create(body)` → POST `.../resources/ignition/tag-provider` with the ARRAY body `[{name, type: "ignition/tag-provider", collection: "core", enabled: true, config: {profile: {type: "STANDARD"}, settings: {}}}]` — live-proven shape.
- `tag_provider_delete(name, signature)` → DELETE `.../resources/ignition/tag-provider/{name}/{signature}`.
Trait methods + impls in the ONE block + unreachable stubs in all doubles.
actions/tags.rs (new, +mod.rs registration): `tag_provider_list` (unit-explicit result: name/enabled/tag_count/health per provider, System provider flagged managed), `tag_provider_create(name)` (STANDARD profile only at MVP — the create body is fixed-shape; document DB-backed providers as out of scope), `tag_provider_delete(name)` = find→signature→delete chain with `provider_not_found` additive slug (exit 6) when find misses, reusing the destructive-verb guard convention at the CLI layer (Task 3).
tests: wiremock pins of all four native paths incl. the array-body POST (recorded-request) and the signature-chained DELETE path encoding.
  </action>
  <verify>`cargo test -p ignition-core tags_contract` green — array body + signature chain pinned on requests.</verify>
  <done>Provider CRUD rides native REST with wire-pinned bodies; provider_not_found slug added (additive, two places).</done>
</task>

<task type="auto">
  <name>Task 2: browse/read/write actions with the version precondition</name>
  <files>crates/ignition-core/src/actions/tags.rs, crates/ignition-core/tests/tags_contract.rs</files>
  <action>
Extend actions/tags.rs — WEBDEV seam over `webdev_route_call` (no new trait methods — the 05-03 generic carries typed payloads):
- ONE shared precondition helper `require_routes(api, project) -> Result<(), CoreError>`: probe the tags route; Absent → routes_not_deployed, Unlicensed → webdev_unlicensed, version mismatch → route_version_mismatch (slugs from 05-03; hints already name `ign webdev deploy`). EVERY webdev-dependent action in this and later plans calls it first — one extra round trip, correctness over latency (documented; no caching this phase).
- `tags_browse(api, project, path, filter, include_properties)`: route action `browse` → entries; filter: case-insensitive substring on name+fullPath when provided; Property children dropped UNLESS include_properties (research display default). Result: flat ordered list `{path, name, tag_type, has_children, data_type}` (unit-explicit keys) — tree RENDERING is render.rs's job (Task 3) but the data carries fullPath so nesting is derivable.
- `tags_read(api, project, paths: Vec<String>)`: route action `read` (single path = one-element vec; the route is always batch) → `{path, value, quality, timestamp}[]` verbatim from the envelope (quality strings carry embedded detail — never parse them further).
- `tags_write(api, project, path, value: serde_json::Value)`: route action `write` — value is a JSON scalar the CLI passes through untyped (number/bool/string; document the write-scalar-is-JSON rule in README) → `{path, quality}`.
Wiremock: precondition refusal matrix (405 → exit 6 routes_not_deployed with deploy hint; version mismatch string), browse filter unit tests (Property filtering + substring), read passthrough shape, write body pin (path+value exactly).
  </action>
  <verify>`cargo test -p ignition-core` green — the refusal matrix pins exit 6 + slug + hint for absent and mismatched routes.</verify>
  <done>Every webdev-dependent action refuses honestly pre-deploy; browse filters Property children by default; read/write shapes verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: CLI family, tree renderer, goldens, live gate</name>
  <files>crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/main.rs, crates/ignition-cli/src/render.rs, crates/ignition-cli/tests/contract_tags.rs, crates/ignition-cli/tests/e2e_webdev.rs, README.md</files>
  <action>
`ign tags` family (grouped-subfamily pattern): `tags provider list`, `tags provider create NAME`, `tags provider delete NAME` (guarded pre-resolution — 6th destructive verb set, binary-pinned), `tags browse [PATH] [--filter F] [--include-properties]`, `tags read PATH...` (one or more), `tags write PATH --value V` (clap value parsed as JSON scalar; a bare string stays a string — parse errors → invalid_input exit 2 pre-resolution). `--project` global-ish flag on webdev-dependent arms (default ign-cli, same default as deploy).
render.rs: browse human mode renders an INDENTED TREE derived from fullPath nesting (planner discretion exercised: tree > table for a tag hierarchy) with tagType badges; JSON mode is the flat list (agents get the stable flat shape). Provider list human mode = table (name, enabled, tags, health). Read = aligned rows path/value/quality/timestamp.
contract_tags.rs goldens: provider list/create/delete (incl. delete's exit-2 guard zero-work pin), browse tree render with filtered Properties, read single+batch, write JSON-scalar hint, precondition refusal exit 6 (mocked 405 via wiremock-backed golden harness — same pattern as prior families).
e2e_webdev.rs EXTENDS (same #[ignore] gate): post-deploy live loop — provider create → browse sees it at root → write a value to a fresh tag via config-less path? NO: read/write need an existing tag — the loop creates a tag via provider + tags write to `[provider]T1`? Tags must exist; the live gate WRITES `[default]`'s… simpler honest loop: provider create/delete (native, no route dependency), browse `[default]` root (route), read `[default]` on a tag the gate first creates via the tagConfig configure action (05-05 ships it — instead read the provider-level browse and, if 05-05 has run, its gate covers reads on created tags; here: create provider p5e2e, browse root contains it, read a nonexistent path asserts Bad_NotFound quality passthrough (honest — quality IS data), write to it likewise returns Bad quality (verifiable without mutation side effects), delete provider). Keep the loop self-contained.
README: tags command rows, JSON-scalar write rule, Property-filter default, precondition paragraph, provider STANDARD-only note, exit-table sync.
  </action>
  <verify>`cargo test -p ignition-cli` green; help output lists all six verbs; `cargo clippy --workspace --all-targets -- -D warnings` clean.</verify>
  <done>`ign tags provider|browse|read|write` fully wired with goldens + live loop; destructive provider delete binary-pinned guarded.</done>
</task>

</tasks>

<verification>
- TAGS-01: providers list/create/delete via native REST — signature chain wire-pinned.
- TAGS-02/03/04: browse (filtered tree), read (single/batch), write — all through the deployed route with the precondition refusal proven at three layers (unit, wiremock, golden).
- Parity: browse_tags / read_tags / write_tag / list-get-create-delete_tag_provider map 1:1 onto the 21-tool MCP surface (intent, not bugs).
</verification>

<success_criteria>
- A user with deployed routes can run the complete providers → browse → read → write loop from the terminal.
- Pre-deploy, every route-dependent command exits 6 with the deploy hint.
- No new GatewayApi surface beyond the 4 native provider methods (the generic seam carries the rest).
</success_criteria>

<output>
After completion, create `.planning/phases/05-webdev-backend-tag-operations/05-04-SUMMARY.md`
</output>
