---
phase: 05-webdev-backend-tag-operations
plan: 06
type: execute
wave: 5
depends_on: ["05-05"]
files_modified:
  - crates/ignition-core/src/client/tags.rs
  - crates/ignition-core/src/actions/tags.rs
  - crates/ignition-core/src/error.rs
  - crates/ignition-core/tests/tags_contract.rs
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/src/render.rs
  - crates/ignition-cli/tests/contract_tags.rs
  - crates/ignition-cli/tests/e2e_webdev.rs
  - README.md
autonomous: true

must_haves:
  truths:
    - "User can view active alarms (eventId/source/state/priority/name) with optional filters"
    - "User can query alarm history on a journal-provisioned gateway; on a default rig the command refuses with an actionable error naming the missing journal chain"
    - "User can acknowledge alarms (3-arg form) and see the unacknowledged remainder"
    - "User can query tag history (t_stamp + per-tag columns) — structurally working on any rig; data requires a provisioned historian, which the live gate provisions via InternalHistorian"
  artifacts:
    - path: "crates/ignition-core/src/actions/tags.rs"
      provides: "alarms active/history/ack + history query actions with journal-missing mapping"
    - path: "crates/ignition-cli/tests/e2e_webdev.rs"
      provides: "live gate: alarm lifecycle (configure alarm → active → ack) + InternalHistorian provisioning + history query + the binding spike"
  key_links:
    - from: "crates/ignition-core/src/actions/tags.rs"
      to: "webdev_route_call (alarms + tagHistory routes)"
      via: "active/history/acknowledge/query actions"
      pattern: "alarms|tagHistory"
    - from: "crates/ignition-core/src/actions/tags.rs"
      to: "alarm_journal_missing slug"
      via: "route error code no_alarm_journal → actionable exit 6"
      pattern: "no_alarm_journal|alarm_journal_missing"
---

<objective>
Ship alarm operations (TAGS-07: active/history/acknowledge) and tag history query (TAGS-08) over the alarms + tagHistory routes — with the honest alarm-journal-missing refusal, the InternalHistorian live fixture, and the bounded tag↔historian binding spike.

Purpose: Roadmap criterion 4 — the final tag-lifecycle capability and the phase closer. Completes the 21-tool MCP parity surface (get_active_alarms, get_alarm_history, acknowledge_alarms, get_tag_history).
Output: `ign tags alarms active|history|ack` + `ign tags history query` arms, journal/historian e2e fixtures, binding spike outcome documented.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-webdev-backend-tag-operations/05-RESEARCH.md
@.planning/phases/05-webdev-backend-tag-operations/05-05-SUMMARY.md

# Headless token + historian provisioning recipe (InternalHistorian via native REST needs no database):
@.planning/phases/04-rig-lifecycle-trial-state/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: alarms client actions + journal-missing mapping</name>
  <files>crates/ignition-core/src/client/tags.rs, crates/ignition-core/src/actions/tags.rs, crates/ignition-core/src/error.rs, crates/ignition-core/tests/tags_contract.rs</files>
  <action>
actions/tags.rs extends over the alarms route (precondition inherited):
- `tags_alarms_active(api, project, source: Option, priority: Option, state: Option)`: route action `active` with present filters as kwargs passthrough → `{alarms: [{event_id, source, state, priority, name}], count}` (eventId/source/state passthrough verbatim — states read 'Active, Unacknowledged').
- `tags_alarms_history(api, project, start_ms, end_ms, ...)`: route action `history`. The route's structured `no_alarm_journal` code maps to ADDITIVE `alarm_journal_missing` (exit 6, hint: "alarm history needs a journal profile — provision a database connection + alarm-journal profile on the gateway, then retry; see README 'Alarm history'"). Other errors ride webdev_route_error. Success → journal rows verbatim (unit-explicit keys passthrough; the wire shape is journal-dataset-dependent — model as serde_json::Value rows under `{columns, rows}`, the tagHistory convention).
- `tags_alarms_ack(api, project, ids: Vec<String>, note, username)`: route action `acknowledge` (route enforces the 3-arg String[] form) → `{acknowledged: n, unacknowledged: [...]}` — the 8.3 return is the UNacknowledged remainder; acknowledged = requested − remainder, computed client-side honestly.
Error slug `alarm_journal_missing` added in both exit-table places. Wiremock: active filter passthrough body pin, history success passthrough, no_alarm_journal → exit 6 + hint pin, ack body pin (string ids + note + username), ack-remainder shape unit.
  </action>
  <verify>`cargo test -p ignition-core tags_contract` green — the no_alarm_journal fixture proves the actionable refusal.</verify>
  <done>Alarm active/history/ack actions complete with the journal-missing honest refusal and remainder-honest ack result.</done>
</task>

<task type="auto">
  <name>Task 2: tag history query + live fixtures (InternalHistorian, binding spike, journal chain)</name>
  <files>crates/ignition-core/src/actions/tags.rs, crates/ignition-core/tests/tags_contract.rs, crates/ignition-cli/tests/e2e_webdev.rs</files>
  <action>
- `tags_history_query(api, project, paths, start_ms, end_ms, return_size, aggregation)`: route action `query` → `{columns, rows}` verbatim (t_stamp column preserved EXACTLY — never renamed; tag columns are provider-relative). CLI accepts --start/--end as RFC3339 OR epoch-ms (parse to ms; the route wraps Date(long) route-side — client sends ms).
- e2e_webdev.rs EXTENDS with two #[ignore] fixtures (env-gated as always; both self-cleaning):
  (a) HISTORIAN FIXTURE + BINDING SPIKE (bounded ≤30 min per research Open Question 1): provision `InternalHistorian` via native REST (`POST /data/api/v1/resources/com.inductiveautomation.historian/historian-provider`, profile type InternalHistorian — live-proven, no DB) using the reqwest dev-dep + Phase 4 token recipe; configure one Int4 tag with `historyEnabled: true, historicalProvider: 'p5hist'` via the deployed tagConfig route; write values; query history. THEN THE SPIKE: if rows are still null (the research's unresolved binding), try the documented candidates in order — (1) execution scan-class keys in the configure shape, (2) wider time windows + aggregationMode variations, (3) browseHistoricalTags cross-check. RESOLUTION RULE: if a candidate produces data, pin the winning shape as a comment in the e2e + README. If none do within the budget, ASSERT the structural outcome only (columns present, no error, null values) and record the spike outcome in the SUMMARY + README as a documented limitation with the Designer-diff follow-up noted — do NOT block the phase on it (query capability is the criterion; the research pre-cleared this fallback).
  (b) ALARM LIFECYCLE FIXTURE: configure a tag with a LIST-form alarm (AboveValue 100, High) via tagConfig → write a value > setpoint via tags write → poll `alarms active` until the event appears (bounded retries) → ack with note+username → assert state flip to 'Active, Acknowledged' and the remainder shape. History-leg: IF the rig has a journal, query it; if not (default rig), assert the alarm_journal_missing refusal live (the honest default-rig path IS the pinned behavior). Journal provisioning (sidecar postgres + database-connection + alarm-journal chain) is a documented STRETCH: attempt only if (a) completes quickly and a postgres container is startable; otherwise skip-with-reason printed in the gate output — the wiremock pins carry the capability proof.
  </action>
  <verify>`cargo test -p ignition-core tags_contract` green (query passthrough + t_stamp preservation unit); e2e extensions quiet-skip without env; the spike has a written outcome either way (assert-and-document, never hang).</verify>
  <done>History query ships with fixtures; binding spike executed with a pinned outcome; alarm lifecycle proven live end-to-end (configure → trigger → ack).</done>
</task>

<task type="auto">
  <name>Task 3: CLI arms, goldens, README, phase-close check</name>
  <files>crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/main.rs, crates/ignition-cli/src/render.rs, crates/ignition-cli/tests/contract_tags.rs, README.md</files>
  <action>
Arms: `tags alarms active [--source S] [--priority P] [--state S]`, `tags alarms history --start T --end T`, `tags alarms ack ID... --note N --username U` (explicit username — the 3-arg form needs it; no default-guessing), `tags history query PATH... --start --end [--return-size N] [--aggregation last_value]`. ack is --yes-guarded? Ack is a state-changing but non-destructive operation (it un-acknowledges nothing) — NOT guarded, consistent with `sessions` family's read-adjacent verbs; document.
render.rs: alarms active = table (eventId short, source, state, priority) — planner discretion exercised per CONTEXT; history = columns/rows aligned; ack = counts + remainder; history query = t_stamp column + value columns.
Goldens: active table, journal-missing refusal, ack success + remainder, history query passthrough with t_stamp visible.
README: alarms + history rows, alarm-history provisioning chain paragraph (database-connection → alarm-journal profile → general-alarm-settings), InternalHistorian note, binding-spike outcome, exit-table sync. Final phase-close sweep: command table lists the full `ign tags` + `ign webdev` surface (11 requirements WEB-01/02 + TAGS-01..09 all mapped to shipped verbs).
  </action>
  <verify>`cargo test --workspace` green; `cargo clippy --workspace --all-targets -- -D warnings` clean; README requirement-mapping table complete.</verify>
  <done>All alarm/history verbs wired; README maps every phase requirement to its command; workspace green.</done>
</task>

</tasks>

<verification>
- TAGS-07: active (filters), history (journal-gated with the actionable refusal), acknowledge (3-arg, remainder-honest) — live lifecycle proven.
- TAGS-08: history query works structurally anywhere; the InternalHistorian fixture + spike outcome documented.
- Parity complete: all 21 MCP tag-domain tools now have CLI equivalents (intent-corrected, not bug-replicated).
</verification>

<success_criteria>
- Alarm lifecycle: configure → trigger → observe → acknowledge, all from the CLI on a live rig.
- Default-rig alarm history refuses honestly; provisioned history queries.
- Tag history query ships with t_stamp fidelity and a written spike outcome (data-flow either pinned or documented with the Designer-diff path).
</success_criteria>

<output>
After completion, create `.planning/phases/05-webdev-backend-tag-operations/05-06-SUMMARY.md`
</output>
