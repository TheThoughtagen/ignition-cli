---
phase: 05-webdev-backend-tag-operations
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - crates/ignition-core/src/client/mod.rs
  - crates/ignition-core/src/client/projects.rs
  - crates/ignition-core/src/client/resources.rs
  - crates/ignition-core/src/actions/resources.rs
  - crates/ignition-core/src/error.rs
  - README.md
  - crates/ignition-cli/tests/contract_resources.rs
  - crates/ignition-cli/tests/contract_projects.rs
  - crates/ignition-cli/tests/e2e_projects.rs
autonomous: true

must_haves:
  truths:
    - "resource put of a NEW resource LANDS it: a follow-up `resource get` returns the written content (fresh AND used project, live-proven)"
    - "a gateway import denial ({success:false} riding HTTP 200) surfaces as exit 6, slug import_denied, with the gateway's problem text in the message — never ok:true"
    - "resource put, resource delete, project import, and webdev deploy ALL inherit the denial honesty (one seam: ReqwestGatewayApi::project_import)"
    - "the live e2e_projects loop runs green with IGNITION_LIVE_MUTATIONS=1 (it covers put-new on a fresh project and was never live-run — the escape hatch this gap rode)"
  artifacts:
    - path: "crates/ignition-core/src/client/mod.rs"
      provides: "project_import checks the 200 body's success flag"
      contains: "ImportDenied"
    - path: "crates/ignition-core/src/error.rs"
      provides: "ImportDenied variant, slug import_denied, exit 6"
      contains: "ImportDenied"
    - path: "crates/ignition-core/src/client/resources.rs"
      provides: "put-new append shape that the gateway importer accepts (or unchanged if fallback)"
    - path: "crates/ignition-cli/tests/e2e_projects.rs"
      provides: "live put-new witness incl. a NEW member on a USED project"
  key_links:
    - from: "crates/ignition-core/src/client/mod.rs (project_import)"
      to: "CoreError::ImportDenied"
      via: "success:false detection on the parsed 200 body"
      pattern: "ImportDenied"
    - from: "crates/ignition-core/src/actions/resources.rs (resource_put)"
      to: "gateway project import"
      via: "surgery zip that lands new members (dir-entry ancestors) OR read-back verification"
      pattern: "replace_member|read_member"
    - from: "crates/ignition-cli/tests/e2e_projects.rs"
      to: "live gateway"
      via: "#[ignore] loop with IGNITION_LIVE_MUTATIONS=1"
      pattern: "IGNITION_LIVE_MUTATIONS"
---

<objective>
Close UAT Gap 1 (major): `resource put` of a NEW resource claims ok:true while nothing lands, and the CLI never surfaces the gateway's HTTP-200 `{success:false}` import denial.

Two layers, both live-proven on the commissioned 8.3.3 rig:
1. **Seam**: `ReqwestGatewayApi::project_import` (client/mod.rs:1043) parses the 200 body opaquely and never checks `success` — the 200-denial class handled for WebDev routes but missed here.
2. **Transport**: overwrite-import of an export zip with an APPENDED member is rejected by the gateway ("resource already exists: ResourceId{...}" — even for brand-new folder chains); REPLACE and DELETE import cleanly.

Purpose: put-new must never report ok when get would say not_found; every import caller must be denial-honest.
Output: checked seam (+ additive `import_denied` slug), a put-new surgery shape the importer accepts (dir-entry ancestors — spike-verified live, honest read-back fallback if no shape lands), green live e2e gate.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-webdev-backend-tag-operations/05-UAT.md

@crates/ignition-core/src/client/mod.rs
@crates/ignition-core/src/client/projects.rs
@crates/ignition-core/src/client/resources.rs
@crates/ignition-core/src/actions/resources.rs
@crates/ignition-core/src/error.rs
@crates/ignition-cli/tests/e2e_projects.rs
@crates/ignition-cli/tests/contract_resources.rs
@crates/ignition-cli/tests/contract_projects.rs

Live rig: docker `ignition-devops`, port 9088, profile `uat`. Token: env `IGNITION_TOKEN` (format `NAME:key`) or `/var/folders/jy/nmh43099607fl9kmv2s8gbdh0000gn/T/opencode/token.txt`. Trial may be expired — if so: `ign rig reset --yes`, then re-provision the headless token per the copy-paste recipe in `.planning/phases/04-rig-lifecycle-trial-state/04-VERIFICATION.md` (addendum), then `ign webdev deploy` to leave routes healthy.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import-denial seam — check success:false on the 200 body, additive import_denied slug, wiremock + contract proofs</name>
  <files>crates/ignition-core/src/client/mod.rs, crates/ignition-core/src/client/projects.rs, crates/ignition-core/src/error.rs, README.md, crates/ignition-cli/tests/contract_resources.rs, crates/ignition-cli/tests/contract_projects.rs</files>
  <action>
1. **error.rs** — new additive variant (near InvalidImportFile; follow the family's doc-comment style):
   ```rust
   #[error("gateway rejected the project import for {project:?}: {problem}")]
   ImportDenied { project: String, problem: String, endpoint: Option<String> },
   ```
   slug `import_denied` in `code()`, exit 6 in `exit_code()`. Two-places rule: add the row to README.md's exit-code table AND update the enumerated `exit_code_mapping_enumerated` test (error.rs:616) — the enumeration failing IS the enforcement working.
2. **client/projects.rs** — small PURE helper next to `ImportOutcome` (keeps the logic unit-testable and out of the impl block):
   `pub(crate) fn import_denied(body: &serde_json::Value) -> Option<String>` — returns the problem string when `body.success == false` (problem from `body["problem"]` as_str, else a standing message like "gateway reported success:false without a problem message"). Bodies without an explicit `success:false` (the opaque family: missing key, `true`, the `{"status":"success"}` fallback) return None — we only refuse on an EXPLICIT denial. Unit-pin: success:false+problem → Some, success:true → None, no-success-key → None, success:"false"-as-string → None (only bool false counts).
3. **client/mod.rs `project_import` (line ~1043)** — after the existing opaque parse, before returning `Ok(ImportOutcome{...})`:
   ```rust
   if let Some(problem) = projects::import_denied(&parsed) {
       return Err(CoreError::ImportDenied {
           project: name.to_string(),
           problem,
           endpoint: Some(url.to_string()),
       });
   }
   ```
   This ONE seam fixes ALL four callers at once: resource put/delete (actions/resources.rs:234/253), the `project import` verb (actions/projects.rs:506), webdev deploy (actions/webdev.rs:171) — do NOT add per-caller checks; the seam is the contract (the WebDev 200-denial precedent applied to the import family). Inline test doubles implement the trait, not ReqwestGatewayApi, so no double needs changing.
4. **contract_resources.rs** — `mount_import` (line ~123) hardcodes `{"success": true}`. Parameterize or add `mount_import_denied` answering `{"success":false,"problem":"resource already exists: ResourceId{resourcePath=com.example, collectionName=views}"}`. New test: `resource put ... --yes` against the denial fixture exits 6, stderr envelope slug `import_denied`, message contains the problem text, endpoint populated (snapbox golden, the family's stderr-envelope pattern). Same denial shape on `resource delete --yes` → same refusal (one test each is enough).
5. **contract_projects.rs** — one import-overwrite denial test: wiremock import answering success:false → `project import --collision-policy overwrite --yes` exits 6 `import_denied` (this pins the 03-02 verb sharing the seam).
  </action>
  <verify>cargo test -p ignition-core -p ignition-cli passes (incl. updated enumeration test); the two new contract tests fail against a pre-seam-check build is impossible to show after the fact — instead assert the goldens pin slug import_denied; cargo clippy -D warnings clean.</verify>
  <done>success:false import bodies surface as exit 6 import_denied with problem text at every caller; README table + enumerated test updated; wiremock + contract goldens pin the behavior.</done>
</task>

<task type="auto">
  <name>Task 2: put-new landing — live spike (dir-entry ancestors first), implement the winning shape in the surgery helpers, honest fallback only if nothing lands</name>
  <files>crates/ignition-core/src/client/resources.rs, crates/ignition-core/src/actions/resources.rs</files>
  <action>
Live spike FIRST (rig per context; ~30min box), reusing the manual wire repro (export zip + python-appended member + `POST /data/api/v1/projects/import/<p>?overwrite=true`), in this order:
1. **Directory entries** (most likely fix — real gateway-produced zips carry folder entries; ad-hoc appenders skip them): rebuild the appended zip with explicit zip DIRECTORY entries (`zf.writestr(ZipInfo('com.example/'), '')` style) for EVERY ancestor folder of the new member (e.g. for `ignition/resources/script-python/uat/hello2.py`: `ignition/resources/script-python/uat/` chain). Test on a fresh project AND on a project that already has members.
2. **Two-pass identical import**: re-POST the identical denied zip immediately (if the denial left partial folder state, pass 2 behaves as replace). Live evidence says nothing lands, so expect no — cheap to disprove.
3. **Format inspection**: dump the member list (names + dir flags) of a REAL gateway export containing multi-level folders — if the format carries per-folder descriptor FILES (not just dir entries), replicate that shape for new ancestors.

Then implement in the PURE surgery helpers (no GatewayApi surface — locked contract):
- **If a shape lands** (expected: candidate 1): extend `replace_member` (client/resources.rs:209) so append-when-absent ALSO writes directory entries for every ancestor of the member path that the archive doesn't already carry (`add_directory`-equivalent on the zip writer, same `rewrite_options()`). `resource_members` already skips dir entries (`is_dir` continue) and `read_member` keys by file name — list/get are unaffected; pin both with unit tests appended to the existing `replace_member_appends_when_absent` family: new-member append produces a zip whose ancestor dir entries exist, member reads back, list output unchanged.
- **If NOTHING lands after the spike box**: implement read-back verification in `resource_put` (actions layer, orchestration lives there per the locked split): after import, re-export and `read_member` the path; if absent or bytes differ from the input, refuse with a NEW additive slug `resource_write_failed` (exit 6, two-places rule, message naming the gateway-importer limitation and pointing at `project export/import` for creating new resources). Document the limitation in README's resource section. Do NOT ship this fallback without first exhausting the spike candidates — record which candidates were tried and their wire answers in the summary.
- `resource_delete` and replace-path put need no surgery changes (both import clean, live-proven).
  </action>
  <verify>cargo test -p ignition-core passes with the new unit pins; manual live check per the spike (put NEW member on fresh + used project → `resource get` returns content, exit 0) — full live sweep is Task 3.</verify>
  <done>put-new lands via a gateway-accepted surgery shape (dir-entry ancestors or spike winner) with unit-pinned zip structure — OR, only if every candidate is disproven live, put-new refuses honestly (resource_write_failed) and never reports ok when the member did not land.</done>
</task>

<task type="auto">
  <name>Task 3: Live e2e gate — run the never-run loop, extend it to put-new on a USED project, prove the gap truth closed</name>
  <files>crates/ignition-cli/tests/e2e_projects.rs</files>
  <action>
1. **Rig bootstrap**: verify the rig answers (profile `uat`, port 9088 — token per context header). If the trial expired: `ign rig reset --yes`, re-provision the headless token per `.planning/phases/04-rig-lifecycle-trial-state/04-VERIFICATION.md` addendum recipe, re-create the `uat` profile, `ign webdev deploy` (leave routes healthy for 05-08's live check).
2. **Extend e2e_projects.rs** (the #[ignore] loop, IGNITION_LIVE_MUTATIONS=1 convention): after the existing second-put/export section, add ONE step — put a NEW member in a NEW folder chain on the now-USED project (e.g. `ignition/script-python/uat2/second.py` — different top-folder under resources than the first scratch), then `resource get` asserts the content reads back and `resource list` contains it. This pins the UAT's exact failure shape (put-new on a used project), which the fresh-project half alone missed... the fresh half WAS in the loop and never live-run; the used-project half is the new witness.
3. **Run the full gate**: `IGNITION_LIVE_URL=http://localhost:9088 IGNITION_LIVE_TOKEN=<name:key> IGNITION_LIVE_MUTATIONS=1 cargo test -p ignition-cli --test e2e_projects -- --ignored --nocapture` — every step green, including the pre-existing put/get/list/two-sided-honesty/export/import sections.
4. **Denial honesty live spot-check**: not directly triggerable on a healthy import path (the wiremock goldens own it) — instead confirm the unmodified self-import still answers success and the loop's overwrite import stays green (regression guard that Task 1's check didn't over-fire on a success body).
  </action>
  <verify>The gate command above exits 0 with all loop steps green; capture the run transcript (step names) for the summary.</verify>
  <done>e2e_projects live loop green end-to-end including put-new on fresh AND used projects; UAT Gap-1 truth ("resource put of a NEW resource lands the resource in the project") demonstrably closed on the live rig.</done>
</task>

</tasks>

<verification>
- `cargo test -p ignition-core -p ignition-cli` green (all existing goldens intact or intentionally moved; enumerated exit-code test updated).
- `cargo clippy --workspace -- -D warnings` clean.
- Wiremock: import-denial fixture pinned for resource put/delete AND project import (slug `import_denied`, exit 6, problem text in message).
- Live: e2e_projects gate green with IGNITION_LIVE_MUTATIONS=1 (put-new lands on fresh + used projects).
- README exit table carries the new slug; no other verb's golden moved (frozen envelope + shared ConfirmationRequired hint untouched).
</verification>

<success_criteria>
UAT Gap 1 closed: put-new lands (or honestly refuses — only if every spike candidate is live-disproven), import denials never masquerade as success at any of the four callers, and the live gate that would have caught this is green and permanently in the suite.
</success_criteria>

<output>
After completion, create `.planning/phases/05-webdev-backend-tag-operations/05-07-SUMMARY.md` — include: which spike candidate won (with the wire evidence), the live gate transcript highlights, and any rig/token re-provisioning steps taken (05-08's executor needs a working rig).
</output>
