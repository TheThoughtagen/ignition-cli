---
phase: 05-webdev-backend-tag-operations
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - crates/ignition-core/src/client/webdev.rs
  - crates/ignition-core/src/client/mod.rs
  - crates/ignition-core/src/error.rs
  - crates/ignition-core/src/actions/webdev.rs
  - crates/ignition-core/src/actions/mod.rs
  - crates/ignition-core/src/actions/{projects,sessions,version,doctor,connections,inspect,logs,rig,resources}.rs
  - crates/ignition-core/tests/webdev_contract.rs
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/src/render.rs
  - crates/ignition-cli/tests/contract_webdev.rs
  - crates/ignition-cli/tests/e2e_webdev.rs
  - README.md
autonomous: true
user_setup:
  - service: live-rig (optional — live gates are #[ignore] and quiet-skip)
    why: "Live deploy/status/status-matrix verification on a real gateway"
    env_vars:
      - name: IGNITION_LIVE_URL
        source: "A commissioned 8.3.x rig (research recipe: git-module docker machinery + base.gwbk restore; ign-p5-research on :28088 during research)"
      - name: IGNITION_LIVE_TOKEN
        source: "Phase 4 headless provisioning recipe (04-VERIFICATION.md addendum) — fully automatable, no dashboard"
      - name: IGNITION_LIVE_MUTATIONS
        source: "=1 to allow the deploy gate (webdev deploy mutates the gateway)"

must_haves:
  truths:
    - "User runs `ign webdev deploy` and the CLI's versioned routes install into the dedicated ign-cli project; a second deploy clean-replaces (overwrite=true)"
    - "`ign webdev status` probes every route's version action and reports present/absent/unlicensed/version-mismatch per route"
    - "A WebDev-dependent command run before deploy refuses exit 6 with an error naming `ign webdev deploy`"
    - "scriptExec deploys ONLY with --with-script-exec; its deploy generates a fresh secret (template substitution), stores it in the profile config at 0600, and the route rejects calls without the matching X-Ignition-CLI-Secret header"
    - "doctor's WebDev check reports 405=absent (not 404) and 402=unlicensed"
  artifacts:
    - path: "crates/ignition-core/src/client/webdev.rs"
      provides: "route_call POST dispatch + body-envelope parse + route_probe status discrimination + deploy zip builder"
      exports: ["webdev_route_call", "webdev_route_probe", "build_deploy_zip"]
    - path: "crates/ignition-core/src/actions/webdev.rs"
      provides: "webdev_deploy + webdev_status actions (version table, secret lifecycle)"
    - path: "crates/ignition-core/tests/webdev_contract.rs"
      provides: "wiremock matrix: 405/402/401/200-denial/version shapes + deploy import request"
    - path: "crates/ignition-cli/tests/e2e_webdev.rs"
      provides: "live deploy→status→redeploy→scriptexec gate"
  key_links:
    - from: "crates/ignition-core/src/client/webdev.rs"
      to: "system/webdev/{project}/cli/{route}"
      via: "POST action dispatch, never branching on HTTP status for body errors"
      pattern: "system/webdev"
    - from: "crates/ignition-core/src/actions/webdev.rs"
      to: "crates/ignition-core/src/webdev (embedded bundle)"
      via: "build_deploy_zip over ROUTE_FILES / SCRIPT_EXEC_TEMPLATE"
      pattern: "ROUTE_FILES|SCRIPT_EXEC_TEMPLATE"
    - from: "crates/ignition-core/src/actions/webdev.rs"
      to: "project_import"
      via: "deploy = project zip import with overwrite=true"
      pattern: "project_import"
    - from: "crates/ignition-core/src/actions/webdev.rs"
      to: "profile config store"
      via: "webdev secret persisted 0600 per profile"
      pattern: "webdev_secret"
---

<objective>
Ship the WebDev client seam (route action calls with body-envelope parsing, presence/version probing, deploy zip building from the embedded bundle) and the `ign webdev deploy` / `ign webdev status` commands with the full version-negotiation refusal matrix — plus the scriptExec secret lifecycle and the doctor 405 re-pin.

Purpose: WEB-01 + WEB-02. This is the hinge plan: every tag command in 05-04..06 rides the seam this plan builds, and the refusal matrix is the roadmap's "actionable error on mismatch" criterion.
Output: client/webdev.rs, actions/webdev.rs, 4 additive error slugs, webdev CLI family, wiremock matrix, live e2e gate.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-webdev-backend-tag-operations/05-RESEARCH.md
@.planning/phases/05-webdev-backend-tag-operations/05-01-SUMMARY.md

# Import machinery reused by deploy (application/zip body + overwrite query, 300s timeout):
@.planning/phases/03-project-operations/03-02-SUMMARY.md

# The embedded bundle this plan deploys:
@crates/ignition-core/src/webdev/mod.rs
# The trait/impl choke file + doctor's webdev check to re-pin:
@crates/ignition-core/src/client/mod.rs
@crates/ignition-core/src/actions/doctor.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: client seam — route_call, probe, deploy zip builder, error slugs</name>
  <files>crates/ignition-core/src/client/webdev.rs, crates/ignition-core/src/client/mod.rs, crates/ignition-core/src/error.rs, crates/ignition-core/src/actions/{projects,sessions,version,doctor,connections,inspect,logs,rig,resources}.rs, crates/ignition-core/tests/webdev_contract.rs</files>
  <action>
Create `client/webdev.rs`:
- Path builder: `pub(crate) fn route_url(project: &str, route: &str) -> String` → `/system/webdev/{project}/cli/{route}` (NOT /data/webdev — research wire protocol). The `cli/` folder segment is part of the route folder path.
- `webdev_route_call(&self, project: &str, route: &str, body: &serde_json::Value, extra_headers: &[( &str, &str )]) -> Result<serde_json::Value, CoreError>` trait method: POST the action JSON. classify() runs normally for transport/status errors BUT the 200 body is parsed as the route envelope `{ok, data|error}`: `ok:false` → map `error.code` to slugs (see below) with the route's message; `ok:true` → return `data`. NEVER treat HTTP 200 as success by itself (denials ride 200). Unknown error codes → `webdev_route_error` carrying code+message.
- `webdev_route_probe(&self, project: &str, route: &str, headers) -> Result<RouteProbe, CoreError>` trait method returning an enum: `Present { route_version: String }` (200 + ok body from the version action), `Absent` (405 — NOT 404, the live-proven 8.3 marker), `Unlicensed` (402 — trial-expired module), `AuthGated` (401 — present but gated), `Denied { code, message }` (200 envelope denial, e.g. secret mismatch). This bypasses classify-style error mapping the way doctor's webdev_route_status always has — the status code IS the answer.
- `build_deploy_zip(project_title: &str, with_script_exec: bool, secret: Option<&str>) -> Vec<u8>` — free function in webdev.rs: pack `webdev::ROUTE_FILES` verbatim (substituting the project title into project.json's `title` ONLY when the --project override differs from `ign-cli`), and when with_script_exec: substitute `secret` into `SCRIPT_EXEC_TEMPLATE`'s `__IGN_CLI_SECRET__` placeholder and append scriptExec's three members. Assert: secret required iff with_script_exec (fail closed at the type level — `Option` handled by match, None+true = Internal bug guard). Use the zip crate (added in 05-02) with fixed SimpleFileOptions.
Error slugs (ADDITIVE, exit 6 target-state class, two-places exit-table rule): `routes_not_deployed` (probe Absent), `webdev_unlicensed` (402), `route_version_mismatch` (deployed != ROUTE_BUNDLE_VERSION — message names deployed vs expected + "run `ign webdev deploy`"; deployed > expected → "update ign" variant message, same slug), `webdev_route_error` (unmapped envelope denial). Hints are actionable per the roadmap criterion.
Trait stubs (`unreachable!`) for the 2 new methods across ALL inline doubles (now 10 modules after 05-02 removed none — projects/sessions/version/doctor/connections/inspect/logs/rig + resources' rig if present; discover via `grep "impl GatewayApi for"`).
tests/webdev_contract.rs wiremock matrix: version-action 200-ok (Present with version parse), 405→Absent, 402→Unlicensed, 401→AuthGated, 200-body-denial→Denied, route_call ok:false with unknown code→webdev_route_error, version mismatch string passthrough. Plus a deploy pin: webdev_deploy POSTs to `/data/api/v1/projects/import/ign-cli?overwrite=true` with application/zip and a body the test unpacks (zip crate) asserting members == ROUTE_FILES manifest (+scriptExec when flagged) and that scriptExec's doPost.py contains the substituted secret NOT the placeholder.
  </action>
  <verify>`cargo test -p ignition-core webdev_contract` green; `grep -c "unreachable" crates/ignition-core/src/actions/` shows every double gained the 2 stubs.</verify>
  <done>Seam compiles with the full probe discrimination matrix wiremock-pinned; deploy zip builder round-trip verified member-by-member; 4 slugs in both exit-table places.</done>
</task>

<task type="auto">
  <name>Task 2: deploy + status actions, secret lifecycle, doctor re-pin</name>
  <files>crates/ignition-core/src/actions/webdev.rs, crates/ignition-core/src/actions/mod.rs, crates/ignition-core/src/actions/doctor.rs</files>
  <action>
Create actions/webdev.rs (+ `pub mod webdev;` in actions/mod.rs):

`webdev_deploy(api, project, with_script_exec, rotate_secret) -> WebdevDeployResult`:
- Secret lifecycle: load existing `webdev_secret` from the ACTIVE profile's config (the profile.rs 0600 store — add a field alongside existing profile fields; absent = None). Generate when (a) with_script_exec and no secret exists, or (b) rotate_secret: read 32 bytes from /dev/urandom (zero-dep — the workspace has no rand; unix-only is fine, no Windows CI is locked), hex-encode, persist 0600 via the profile config save path. Secret NEVER appears in action results, logs, or JSON envelopes (redaction discipline — the Phase 1 canary pattern).
- Build zip (scriptExec only when with_script_exec), then `project_import(project, bytes, overwrite=true)` — always overwrite (the CLI owns the ign-cli project wholesale; replace-not-merge is correct). NO pre-flight project create (the first-import quirk — Pitfall 10).
- Result shape (all keys always): `{project, routes: [names deployed], script_exec: bool, secret_rotated: bool, import: <ImportOutcome passthrough>}`.

`webdev_status(api, project, secret) -> WebdevStatusResult`:
- Probe the 4 always-on routes + scriptExec's version action WHEN a scriptExec secret is configured (send `X-Ignition-CLI-Secret`; Open Question 4 resolution: AuthGated → "present (auth-gated)"; Denied with secret codes → "present (secret mismatch — redeploy or --rotate-secret)").
- Compare each Present route_version against `webdev::ROUTE_BUNDLE_VERSION` → per-route status enum {present, absent, unlicensed, auth_gated, secret_mismatch, version_mismatch}.
- Result: `{project, routes: [{route, status, deployed_version, expected_version}], ok: bool}` — ok=true only when all always-on routes are present with matching versions.

Doctor re-pin (actions/doctor.rs): the webdev_route_status-based check now uses `webdev_route_probe` against the ign-cli tags route: 405→absent (replacing the documented-but-wrong 404 assumption — research Pitfall 1), 402→"module unlicensed", 200→present. Keep doctor's existing output vocabulary; update its unit fixtures from 404-shape to 405-shape.
  </action>
  <verify>`cargo test -p ignition-core actions::webdev` green — including: deploy without secret never ships scriptExec; rotate regenerates; status matrix maps all probe states; secret absent from every serialized result (redaction test).</verify>
  <done>Deploy/status actions complete with secret lifecycle + redaction proof; doctor re-pinned to 405 semantics with updated fixtures.</done>
</task>

<task type="auto">
  <name>Task 3: CLI wiring, goldens, live deploy gate</name>
  <files>crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/main.rs, crates/ignition-cli/src/render.rs, crates/ignition-cli/tests/contract_webdev.rs, crates/ignition-cli/tests/e2e_webdev.rs, README.md</files>
  <action>
CLI family (`ign webdev`): `deploy [--project ign-cli] [--with-script-exec] [--rotate-secret]` and `status [--project ign-cli]`. Deploy is NOT --yes-guarded (the dedicated project is CLI-owned — overwrite is the contract; document). Dispatch per the family pattern (profile-resolved client, stderr progress, ActionOutput variants). Render: deploy → routes + import outcome lines; status → per-route table lines + ok summary.

contract_webdev.rs goldens: deploy success (4 routes), deploy --with-script-exec (5 routes + secret rotated/generated line WITHOUT the secret value), status all-present, status missing-route (exit 0? NO — status with absent routes exits 6 routes_not_deployed only when the FAMILY is undeployed; per-route degradation is data. LOCK: webdev status exits 0 whenever the probe sweep completes; route states are data — status is a read, mirroring doctor), precondition refusal golden: a webdev-dependent command (defer to 05-04's tags commands — here, pin the seam-level refusal via status against a 405-mocked gateway… simpler: pin the ACTION-level refusal in core tests; CLI golden covers status-absent data shape).

e2e_webdev.rs (#[ignore], IGNITION_LIVE_URL + IGNITION_LIVE_TOKEN + IGNITION_LIVE_MUTATIONS): full live loop — (1) status on undeployed gateway → routes absent (data, exit 0); (2) deploy → status all-present versions match; (3) redeploy → clean replace, still all-present; (4) deploy --with-script-exec → status 5/5 + probe scriptExec WITHOUT secret header via raw route_call expects Denied(secret_required), WITH secret expects version ok (the security posture witnessed live); (5) wrong-secret → Denied(secret_mismatch). Uses the e2e_projects.rs helper conventions (isolated config, expect_ok, data_envelope).
README: webdev command rows, the refusal matrix table, scriptExec security section (secret mechanism, deploy-only-on-request, threat-model honesty note), exit-table rows, doctor's 405 correction note.
  </action>
  <verify>`cargo test -p ignition-cli` green; `cargo test -p ignition-cli --test e2e_webdev` quiet-skips without env; `cargo clippy --workspace --all-targets -- -D warnings` clean.</verify>
  <done>webdev family wired with goldens; live gate covers deploy/status/redeploy/scriptExec-secret positive+negative; README documents the contract.</done>
</task>

</tasks>

<verification>
- Wiremock proves: 405-vs-402-vs-401-vs-200-denial discrimination; body-envelope errors never masquerade as success; deploy import carries overwrite=true + application/zip; scriptExec never deploys unsubstituted (test unpacks the zip).
- Version negotiation: deployed==expected passes; any other combination refuses exit 6 with a message naming `ign webdev deploy` or `update ign`.
- The secret appears in exactly one place (the baked zip member) — never in output, envelopes, or logs.
</verification>

<success_criteria>
- WEB-01: deploy installs; status verifies; redeploy clean-replaces.
- WEB-02: the refusal matrix is complete and actionable; no auto-upgrade.
- scriptExec LOCKED posture shipped: own auth (secret gate), explicit-request deploy, fail-closed.
- doctor's 405 re-pin closes the research-flagged wrong assumption.
</success_criteria>

<output>
After completion, create `.planning/phases/05-webdev-backend-tag-operations/05-03-SUMMARY.md`
</output>
