---
phase: 05-webdev-backend-tag-operations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/ignition-core/Cargo.toml
  - crates/ignition-core/src/client/resources.rs
  - crates/ignition-core/src/client/mod.rs
  - crates/ignition-core/src/actions/resources.rs
  - crates/ignition-core/src/actions/{projects,sessions,version,doctor,connections,inspect,logs,rig}.rs
  - crates/ignition-core/tests/resources_contract.rs
  - crates/ignition-cli/tests/contract_resources.rs
  - crates/ignition-cli/tests/e2e_projects.rs
  - README.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User's `ign resource list/get/put/delete` UX is unchanged but every op now works against a real 8.3 gateway (export zip → member surgery → import)"
    - "resource get returns text members verbatim and still refuses binary members (exit 6 resource_binary) — now sniffed from zip member bytes"
    - "resource put/delete are --yes-guarded before resolution (they implicitly overwrite-import the project)"
    - "The e2e witness loop runs green live: resource ops are verifiable against a real gateway for the first time since Phase 3"
  artifacts:
    - path: "crates/ignition-core/src/client/resources.rs"
      provides: "pure zip-member surgery helpers (list/read/replace-inject/remove)"
      exports: ["resource_members", "read_member", "replace_member"]
    - path: "crates/ignition-core/src/actions/resources.rs"
      provides: "export→surgery→import(overwrite) orchestration for list/get/put/delete"
    - path: "crates/ignition-core/tests/resources_contract.rs"
      provides: "wiremock pins of the export+import request sequences"
  key_links:
    - from: "crates/ignition-core/src/actions/resources.rs"
      to: "project_export_to_file"
      via: "temp-file export before every resource op"
      pattern: "project_export_to_file"
    - from: "crates/ignition-core/src/actions/resources.rs"
      to: "project_import"
      via: "overwrite=true import after put/delete surgery"
      pattern: "project_import"
    - from: "crates/ignition-core/src/actions/resources.rs"
      to: "crates/ignition-core/src/client/resources.rs"
      via: "zip member surgery between export and import"
      pattern: "read_member|replace_member|remove_member"
---

<objective>
Re-point the Phase 3 `resource` family from the nonexistent `/projects/{p}/resources/**` REST routes onto project-export zip surgery — resolving the cross-phase blocker routed to this phase. `ign resource` keeps its UX; the implementation becomes export-to-temp-zip → member surgery → import-overwrite. Add the `zip` crate (the ONLY new dependency, research-approved). Re-point the e2e witnesses so the loop is live-runnable.

Purpose: The STATE.md blocker — resource ops 404 against every real 8.3 gateway (openapi-evidenced, 575 paths, zero matches). Research triple-verified no native per-resource endpoints exist; export/import round-trip is native and the machinery already ships (03-02). This plan closes the defect AND unblocks the e2e_rig resource-witness step.
Output: Working surgical resource loop, zip surgery helpers, rewritten contract tests, live-verified e2e witnesses.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-webdev-backend-tag-operations/05-RESEARCH.md

# Export/import machinery being reused (streaming export, buffered import, locked collision conventions):
@.planning/phases/03-project-operations/03-02-SUMMARY.md

# The family being replaced (current shapes, sniffer, e2e harness):
@.planning/phases/03-project-operations/03-03-SUMMARY.md
@crates/ignition-core/src/client/resources.rs
@crates/ignition-core/src/actions/resources.rs
@crates/ignition-cli/tests/e2e_projects.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: zip dep + pure member-surgery helpers</name>
  <files>Cargo.toml, crates/ignition-core/Cargo.toml, crates/ignition-core/src/client/resources.rs</files>
  <action>
Add `zip = { version = "8.6", default-features = false, features = ["deflate"] }` to [workspace.dependencies] and ignition-core (research said 6.x; crates.io's latest stable is 8.6.0 — verified at planning, use 8.6). Gate imports run deflate; no other features needed (fixed timestamps via `SimpleFileOptions` defaults).

REWRITE `client/resources.rs` from REST path builders into PURE zip-surgery helpers (no GatewayApi trait surface, no I/O beyond the zip crate — highly unit-testable):
- `pub fn resource_members(zip_bytes: &[u8]) -> Result<Vec<String>, CoreError>` — member names under the resource roots. Zip layout: `project.json`, `<collection>/resources/<rest>` (collections look like `com.inductiveautomation.perspective`). Map to the Phase-3 user-facing path form `<collection>/<rest>` (strip the `resources/` segment) — the UX-unchanged contract. Skip `project.json`.
- `pub fn read_member(zip_bytes: &[u8], member: &str) -> Result<Vec<u8>, CoreError>` — user path → zip member path (`<collection>/resources/<rest>`), missing member → the existing not-found error shape.
- `pub fn replace_member(zip_bytes: &[u8], member: &str, content: &[u8]) -> Result<Vec<u8>, CoreError>` — full-zip rewrite: copy every member (decompressed→recompressed, deflate), REPLACING the target (or appending if absent — put can create new resources). Preserve original member order, deterministic options.
- `pub fn remove_member(zip_bytes: &[u8], member: &str) -> Result<Vec<u8>, CoreError>` — same rewrite minus the member; missing member → not-found error.
DELETE the old REST path builders (`resources_list_path`, `resource_path`, `encode_resource_path`) — `encode_segment` in client/projects.rs is untouched (04-04 export filenames depend on it). Keep or adapt existing unit tests; add round-trip units: build a small zip fixture in-test (zip crate writer), then list/read/replace/remove/verify-member-content assertions.
  </action>
  <verify>`cargo test -p ignition-core resources` green including new round-trip units; `cargo clippy -p ignition-core -- -D warnings` clean.</verify>
  <done>zip 8.6 is the only new dependency; the four pure helpers exist with unit tests covering happy paths + missing-member errors + replace-appends-when-absent.</done>
</task>

<task type="auto">
  <name>Task 2: actions re-point + trait cleanup</name>
  <files>crates/ignition-core/src/client/mod.rs, crates/ignition-core/src/actions/resources.rs, crates/ignition-core/src/actions/{projects,sessions,version,doctor,connections,inspect,logs,rig}.rs, crates/ignition-core/tests/resources_contract.rs</files>
  <action>
Trait cleanup in client/mod.rs: DELETE `project_resources`, `project_resource_get`, `project_resource_put`, `project_resource_delete`, and the now-orphaned `put_bytes` pipeline helper (verified at planning: put_bytes has no other caller). Delete their impl bodies. Remove the corresponding `unreachable!` stubs from ALL inline test doubles (actions/{projects,sessions,version,doctor,connections,inspect,logs,rig}.rs — mechanical deletion, the doubles SHRINK).

REWRITE actions/resources.rs to orchestrate over the existing 03-02 machinery (keep the exported action signatures/result shapes the CLI already renders — the agent JSON stays stable):
- list: `project_export_to_file(name, temp)` → read bytes → `resource_members` → the existing list result shape. Not-found project surfaces through export's existing error path.
- get: export → `read_member` → run the EXISTING classify_content sniffer on member bytes (binary → ResourceBinary exit 6, unchanged slug) → the flat stable shape {project, path, content_kind, content}.
- put: read --file/stdin (unchanged InvalidInput path) → guard `require_confirmation` BEFORE resolution (NEW: put now implicitly overwrite-imports the whole project — replace-not-merge wipes concurrent edits, so put joins the guarded-verb set; 03-03's unguarded put is superseded, document in README) → export → `replace_member` → `project_import(name, surg_bytes, overwrite=true)`.
- delete: existing pre-resolution guard (unchanged) → export → `remove_member` → import overwrite.
Temp files: `tempfile` crate if already available as dev/transitive dep, else `std::env::temp_dir()` + unique name (uuid-free: pid+timestamp) — avoid a new dep.
REWRITE tests/resources_contract.rs as wiremock request-SEQUENCE pins: list/get trigger exactly one export GET and ZERO import POSTs; put/delete trigger export GET then import POST with `overwrite=true` query, `application/zip` content-type, and a body the test round-trips through the surgery helpers to assert the member changed (byte-exact zip equality is not required — assert member-level). Not-found project → existing project error path pins.
  </action>
  <verify>`cargo test --workspace` green (all doubles compile after stub removal); wiremock pins show export-then-import sequences with zero old-route requests.</verify>
  <done>No code path references /projects/{p}/resources/** anymore (grep clean); the four actions orchestrate export→surgery→import; put/delete guarded; contract tests pin the request sequences.</done>
</task>

<task type="auto">
  <name>Task 3: goldens, e2e witness re-point, README</name>
  <files>crates/ignition-cli/tests/contract_resources.rs, crates/ignition-cli/tests/e2e_projects.rs, README.md</files>
  <action>
Update contract_resources.rs goldens: UX is unchanged for list/get (same envelopes); put/delete goldens gain the confirmation-refusal exit-2 shape (profile null, zero network — the sessions-terminate pattern); hint text on put/delete mentions the project overwrite-import consequence ("re-imports the project; concurrent Designer edits are replaced"). ResourceBinary golden unchanged.
Re-point e2e_projects.rs witnesses: the full_project_resource_loop now runs resource list/get/put/delete through the surgery implementation and additionally asserts two-sided honesty on put: export before put contains member M with old content; export after put contains M with new content. Live gate stays #[ignore]/IGNITION_LIVE_MUTATIONS-gated (env contract unchanged). Add a comment noting the STATE.md blocker this closes (execution updates STATE.md, not this file).
README: rewrite the resource section — zip-surgery semantics, the whole-project-export perf honesty note, the concurrent-edit race documentation (research's accepted-tradeoff language), put's new --yes guard, and the exit-table rows (slugs unchanged; put/delete now guarded).
  </action>
  <verify>`cargo test -p ignition-cli` green (goldens updated); `cargo test -p ignition-cli --test e2e_projects` quiet-skips without env (default CI cost zero).</verify>
  <done>Goldens reflect guarded put/delete; e2e witnesses ride the surgery verbs; README documents the mechanism + tradeoffs; workspace green + clippy clean.</done>
</task>

</tasks>

<verification>
- `grep -r "projects/{}/resources" crates/` returns only historical doc references, no live client path.
- `ign resource` family UX (commands, flags, JSON shapes) unchanged from Phase 3 — only the transport changed.
- zip 8.6 is the sole new dependency (research-approved).
- The Phase 3 cross-phase blocker (STATE.md) is closable: resource ops work against any real 8.3 gateway.
</verification>

<success_criteria>
- All four resource ops round-trip through export/import zip surgery with wiremock-pinned request sequences.
- Binary fencing and the flat get shape survive the re-point (agent contract stable).
- e2e witness loop is live-runnable for the first time since Phase 3.
</success_criteria>

<output>
After completion, create `.planning/phases/05-webdev-backend-tag-operations/05-02-SUMMARY.md` — note the STATE.md blocker (Phase 3 resource-family defect) as CLOSED by this plan so execute-phase updates state.
</output>
