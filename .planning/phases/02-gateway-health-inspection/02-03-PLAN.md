---
phase: 02-gateway-health-inspection
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - crates/ignition-core/src/client/mod.rs
  - crates/ignition-core/src/client/sessions.rs
  - crates/ignition-core/src/client/connections.rs
  - crates/ignition-core/src/actions/mod.rs
  - crates/ignition-core/src/actions/sessions.rs
  - crates/ignition-core/src/actions/connections.rs
  - crates/ignition-core/tests/sessions_contract.rs
  - crates/ignition-core/tests/live_gateway.rs
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/src/render.rs
  - crates/ignition-cli/tests/contract_sessions.rs
  - README.md
autonomous: true

must_haves:
  truths:
    - "User runs `ign sessions` and sees designers, Perspective sessions, and Vision clients in one merged output (filterable by --type designer|perspective|vision)"
    - "User can terminate a session: `ign sessions terminate --type <t> --id <ID> [--message MSG]` — refused with exit 2 unless --yes/IGNITION_YES=1; with --yes the gateway-side DELETE succeeds"
    - "Terminating a nonexistent id exits 6 with the not_found slug (wiremock-proven 404)"
    - "User runs `ign connections` and sees database and OPC connections with name/enabled and healthchecks status as the gateway reports it"
    - "The Perspective sessions request hits the exact trailing-slash path /data/perspective/api/v1/sessions/ (wiremock path-pinned — Pitfall 8)"
  artifacts:
    - path: "crates/ignition-core/src/client/sessions.rs"
      provides: "Designer/PerspectiveSession/VisionClient models + list + terminate/prune capabilities"
      min_lines: 70
    - path: "crates/ignition-core/src/client/connections.rs"
      provides: "resources/list database-connection + opc-connection capabilities with healthchecks passthrough"
      min_lines: 40
    - path: "crates/ignition-core/src/actions/sessions.rs"
      provides: "sessions list/terminate actions incl. the require_confirmation wiring"
      min_lines: 50
    - path: "crates/ignition-cli/src/main.rs"
      provides: "require_confirmation dead_code gate REMOVED (first real caller)"
      contains: "require_confirmation"
  key_links:
    - from: "sessions terminate dispatch arm"
      to: "require_confirmation guard"
      via: "FIRST destructive caller — the cfg_attr(not(test), expect(dead_code)) gate must be deleted in the same commit (clippy -D warnings fails on an unfulfilled expect)"
      pattern: "require_confirmation\\("
    - from: "client/sessions.rs perspective list"
      to: "/data/perspective/api/v1/sessions/"
      via: "GET the trailing-slash path exactly; DELETE uses /data/perspective/api/v1/sessions?sessionId=X&message=Y (NO trailing slash on DELETE — spec)"
      pattern: "sessions"
    - from: "client/connections.rs"
      to: "/data/api/v1/resources/list/ignition/{database,opc}-connection"
      via: "network-captured resource-list mechanism (NOT the invented /connections/database path — 404)"
      pattern: "resources/list"
---

<objective>
Ship session visibility + termination and DB/OPC connection status (HLTH-08, HLTH-05, HLTH-06), including the CLI's first destructive operation under the --yes guard.

Purpose: Sessions (designers/Perspective/Vision) and connections are the webpage's monitoring pages; termination is the first mutation and must prove the Phase-1 confirmation-guard pattern end-to-end. Connection healthchecks have a LOW-confidence populated shape (research Open Question 1) — modeled as passthrough with a live verification step.

Output: 7 GatewayApi methods, 2 command groups (`ign sessions`, `ign connections`), guard gate removal, goldens.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gateway-health-inspection/02-RESEARCH.md
@.planning/phases/02-gateway-health-inspection/02-02-SUMMARY.md
@crates/ignition-core/src/client/mod.rs
@crates/ignition-cli/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: session list + terminate capabilities (wiremock-proven)</name>
  <files>crates/ignition-core/src/client/sessions.rs, crates/ignition-core/src/client/mod.rs, crates/ignition-core/tests/sessions_contract.rs</files>
  <action>
    **client/sessions.rs (new)** — models from live captures (research §Sessions), passthrough extras on each:
    - `DesignerInfo { id, address, user, project, memory, uptime, lastcomm, timeout, timezone }`
    - `PerspectiveSession { id, username, authorized, project, client_address, last_comm, active_pages, user_agent }` (serde-rename from camelCase captures)
    - `VisionClient` — same shape as DesignerInfo + `tag_count`.
    - Trait growth (impls call the 02-01 pipeline):
      - `designers(&self, query: &ListQuery) -> ListEnvelope<DesignerInfo>` — GET `/data/api/v1/designers`.
      - `perspective_sessions(&self, query: &ListQuery) -> ListEnvelope<PerspectiveSession>` — GET `/data/perspective/api/v1/sessions/` — **exact trailing slash** (Pitfall 8: the module-scoped prefix differs from core; base-URL joining must not collapse it — pin with a wiremock exact-path matcher).
      - `vision_clients(&self, query: &ListQuery) -> ListEnvelope<VisionClient>` — GET `/data/vision/api/v1/clients`.
      - `terminate_perspective_session(&self, id: &str, message: Option<&str>)` — **DELETE** `/data/perspective/api/v1/sessions` with query `sessionId=<id>` (+ optional `message`) — NO trailing slash on DELETE (spec); needs a `delete_with_query` pipeline variant (token-auth DELETE needs no CSRF — verified).
      - `terminate_vision_client(&self, id: &str)` — DELETE `/data/vision/api/v1/client/{id}`.
      - `prune_designer(&self, id: &str)` — DELETE `/data/api/v1/designer/{id}`.
    - Mutations are audit-logged server-side (official docs) — note in doc comments; nothing to do client-side.
  </action>
  <verify>
    tests/sessions_contract.rs (wiremock + IgnitionMock): each list parses its live capture; perspective GET matches the exact trailing-slash path; DELETE carries `sessionId` as a QUERY param (recorded-request proof) and no body; terminating a nonexistent id (mock 404 JSON) → `NotFound`/exit 6; HTML 403 on DELETE → Auth.
  </verify>
  <done>
    All three session families listable and terminable through the coarse trait; the trailing-slash and query-param subtleties are contract-pinned.
  </done>
</task>

<task type="auto">
  <name>Task 2: DB/OPC connections via resources/list + healthchecks verification</name>
  <files>crates/ignition-core/src/client/connections.rs, crates/ignition-core/src/client/mod.rs, crates/ignition-core/tests/sessions_contract.rs, crates/ignition-core/tests/live_gateway.rs</files>
  <action>
    **client/connections.rs (new)** — the real mechanism is the web UI's resource-list poll (network-captured; the ignition-mcp `/connections/*` paths are inventions):
    - `GatewayConnection { name: String, enabled: bool, healthchecks: serde_json::Value }` — healthchecks kept as RAW passthrough Value with a doc-comment flagging the LOW-confidence populated shape (research Open Question 1: the test gateway had zero connections; envelope + mechanism verified, populated detail not).
    - Trait: `database_connections(&self) -> ListEnvelope<GatewayConnection>` — GET `/data/api/v1/resources/list/ignition/database-connection?limit=-1`; `opc_connections(&self)` — same for `ignition/opc-connection`.
    - Do NOT use `/data/api/v1/overview/connections` for the list (web-UI presentation objects; research recommends the resource list).
    - **Verification step (research-mandated):** if `IGNITION_LIVE_URL`+`IGNITION_LIVE_TOKEN` point at a gateway where a connection exists, run the appended live check and capture the populated healthchecks shape into a fixture; if none is available, add the live check as list-only (empty ok) and record the open question in the SUMMARY for UAT against a gateway with configured connections. (Stretch, optional: docker-run a throwaway postgres and create a connection via REST resource PUT — do not block on it.)
  </action>
  <verify>
    Wiremock: both resource-list paths parse a plausible item `{name, enabled, healthchecks:{…}}` via passthrough; limit=-1 sent; 401 HTML → Auth. Live: `cargo test -p ignition-core -- --ignored live_connections` runs or skips cleanly.
  </verify>
  <done>
    HLTH-05/06 server side complete with the honest passthrough and a documented path to verifying the populated shape.
  </done>
</task>

<task type="auto">
  <name>Task 3: `ign sessions` (+ terminate, first --yes caller) + `ign connections` + goldens</name>
  <files>crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/main.rs, crates/ignition-cli/src/render.rs, crates/ignition-core/src/actions/sessions.rs, crates/ignition-core/src/actions/connections.rs, crates/ignition-core/src/actions/mod.rs, crates/ignition-cli/tests/contract_sessions.rs, README.md</files>
  <action>
    **actions/sessions.rs + actions/connections.rs (new; register in actions/mod.rs):**
    - `sessions(api, type_filter: Option<SessionType>) -> SessionsResult` — data `{ designers: […], perspective: […], vision: […] }` with unfiltered keys present as empty arrays when the filter excludes them (stable shape for agents). Calls only the requested families when filtered.
    - `terminate_session(api, kind, id, message) -> TerminateResult` — maps kind→endpoint (designer→prune, perspective→terminate with message, vision→terminate).
    - `connections(api, filter) -> ConnectionsResult { database: […], opc: […] }`.

    **cli.rs** — `Commands::Sessions(SessionsArgs)` with nested enum: `List { #[arg(long, value_enum)] r#type: Option<SessionType> }` and `Terminate { #[arg(long, value_enum)] r#type: SessionType, id: String, #[arg(long)] message: Option<String> }`; `Commands::Connections { #[arg(long, value_enum)] r#type: Option<ConnectionType> }`.

    **main.rs — THE FIRST DESTRUCTIVE CALLER:**
    - The Terminate dispatch arm calls `require_confirmation(yes, "sessions terminate")` BEFORE any API construction; on ConfirmationRequired the guard's exit-2 envelope names --yes + IGNITION_YES (pinned Phase-1 behavior, now exercised for real).
    - **Delete the `#[cfg_attr(not(test), expect(dead_code, …))]` attribute** on `require_confirmation` in the same commit — an unfulfilled expect fails `clippy -D warnings` once the function gains a caller (01-04 key-decision; the attribute's own reason string says so).
    - SUMMARY note (executor): record that the dead-code gate came off here in Phase 2 / 02-03 — EARLIER than STATE.md's logged "until Phase 3's first destructive caller" — a sanctioned roadmap planner refinement; the attribute's own reason string mandates removal at the first real caller, which is sessions terminate.
    - Credential: required (authed reads/mutations) — resolve_secret, not the degrading variant.

    **render.rs (human)** — sessions: one section per family (`designers (1)`, `perspective (2)`, `vision (0)`) with a row per item (`id  user/username  project  address  lastcomm`); terminate: `terminated <kind> session <id>`; connections: `database (1)` / `opc (0)` rows (`name  enabled  healthchecks-as-reported` — render healthchecks as compact JSON on the row).

    **contract_sessions.rs (new)** — goldens: sessions list (populated mock, all three modes), --type perspective filter, terminate WITHOUT --yes → exit 2 + confirmation_required envelope (golden), terminate WITH --yes → success (mock DELETE), terminate nonexistent id → exit 6 not_found, connections list. Elide ids/addresses/timestamps with [..].

    **README.md** — command rows + a "destructive operations" note (sessions terminate joins the --yes family).
  </action>
  <verify>
    `cargo test --workspace` green; `SNAPSHOTS=overwrite` no-op; `ign sessions terminate --type perspective --id x` (no --yes) exits 2 with confirmation_required; clippy -D warnings clean WITH the gate removed (this is the proof the removal happened); fmt clean.
  </verify>
  <done>
    HLTH-08, HLTH-05, HLTH-06 user-observable; the Phase-1 confirmation guard has its first live caller and the dead_code scaffolding is gone.
  </done>
</task>

</tasks>

<verification>
- Termination flows through the guard (exit 2 → --yes → success → 404 paths all golden-pinned).
- Perspective path subtleties pinned: GET trailing slash, DELETE with query param (recorded-request assertions, not just response parsing).
- Connections modeled as passthrough with the LOW-confidence flag documented in code + SUMMARY.
</verification>

<success_criteria>
A user replaces the webpage's Sessions and Connections pages from the terminal, can terminate any session non-interactively (agents pass --yes), and the destructive-command convention is proven in production code rather than a dead helper.
</success_criteria>

<output>
After completion, create `.planning/phases/02-gateway-health-inspection/02-03-SUMMARY.md`
</output>
