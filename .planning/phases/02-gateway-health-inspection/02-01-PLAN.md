---
phase: 02-gateway-health-inspection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ignition-core/src/error.rs
  - crates/ignition-core/src/client/mod.rs
  - crates/ignition-core/src/client/version.rs
  - crates/ignition-core/src/client/classify.rs
  - crates/ignition-core/src/client/query.rs
  - crates/ignition-core/src/lib.rs
  - crates/ignition-core/tests/common/mod.rs
  - crates/ignition-core/tests/gateway_info_contract.rs
  - crates/ignition-core/tests/live_gateway.rs
  - crates/ignition-cli/src/render.rs
  - crates/ignition-cli/tests/version_gateway_contract.rs
  - crates/ignition-cli/tests/contract_version.rs
  - README.md
autonomous: true
user_setup:
  - service: ignition-live-rig
    why: "Opt-in live-gateway verification (#[ignore] tests). NOT required for CI or plan execution — wiremock covers everything. Only needed to run the live suite / close the auth gap empirically on your own gateway."
    env_vars:
      - name: IGNITION_LIVE_URL
        source: "Base URL of any commissioned Ignition 8.3+ gateway you control"
      - name: IGNITION_LIVE_TOKEN
        source: "Gateway UI → Platform → Security → API Keys → Create (Basic Token, UNCHECK 'Require secure connections' for http rigs, admin-level) — copy the FULL name:key string"

must_haves:
  truths:
    - "A live-captured 8.3.6 gateway-info body (ignitionVersion field) deserializes into GatewayInfo — proven by wiremock golden with the exact captured JSON"
    - "A 401 Jetty HTML error page from any /data/api/v1 route classifies as auth_rejected (exit 5) with a hint naming the name:key token format — never an internal decode error"
    - "A 403 classifies as auth_rejected with the three-part setup hint (level, permissions, secure-channel)"
    - "A 302 redirect to /welcome classifies as gateway_not_commissioned (exit 6) with a commissioning-wizard hint"
    - "A 503 (restart window) classifies as gateway_restarting (exit 6) with an `ign wait restart` hint"
    - "A 404 on a named resource classifies as not_found (exit 6)"
    - "A Basic credential used against /data routes emits a stderr warning naming API tokens as the only viable 8.3 auth"
    - "ign version against the live rig (when IGNITION_LIVE_URL set) exits 0 and reports the real ignitionVersion"
  artifacts:
    - path: "crates/ignition-core/src/client/classify.rs"
      provides: "Response classifier (status → content-type → redirect) + Jetty HTML sniffer"
      min_lines: 60
    - path: "crates/ignition-core/src/client/query.rs"
      provides: "ListQuery / ListEnvelope<T> / ListMetadata — the standard 8.3 list envelope"
      min_lines: 40
    - path: "crates/ignition-core/src/client/version.rs"
      provides: "GatewayInfo with ignitionVersion serde alias + LicenseInfo + live-string test rows"
      contains: "alias = \"version\""
    - path: "crates/ignition-core/src/client/mod.rs"
      provides: "redirect(Policy::none) client, single-site auth helper, get_json/post_empty pipeline"
      contains: "Policy::none"
    - path: "crates/ignition-core/tests/common/mod.rs"
      provides: "IgnitionMock wiremock builder (list_json / html_error / redirect helpers)"
      min_lines: 50
    - path: "crates/ignition-core/tests/live_gateway.rs"
      provides: "#[ignore]-gated live-gateway suite (env-gated; grows in later plans)"
      min_lines: 40
  key_links:
    - from: "crates/ignition-core/src/client/mod.rs (pipeline helpers)"
      to: "crates/ignition-core/src/client/classify.rs"
      via: "classify() runs BEFORE any .json() call on every response"
      pattern: "classify"
    - from: "crates/ignition-core/src/error.rs (enumerated test)"
      to: "README.md exit-code table"
      via: "new slugs added to BOTH homes in the same commit"
      pattern: "gateway_not_commissioned|gateway_restarting|not_found"
    - from: "ReqwestGatewayApi auth helper"
      to: "Secret::expose()"
      via: "the single header-construction match (moved, not duplicated)"
      pattern: "expose"
---

<objective>
Re-align the Phase-1 client seam with live-gateway truth and build the error-classification foundation every Phase-2 capability rides on.

Purpose: Research proved real 8.3.6 gateways return `ignitionVersion` (not `version`) — Phase 1's GatewayInfo fails deserialization against every real gateway. It also proved /data errors are Jetty HTML pages, uncommissioned gateways 302 everything to /welcome, and 503 marks the restart window. This plan fixes the model, builds the status → content-type → redirect classifier mapping into the LOCKED taxonomy (with three ADDITIVE slugs), adds the IgnitionMock test harness, and lands the live-gateway suite that closes STATE.md's flagged auth-verification gap.

Output: Corrected GatewayInfo, classify.rs, query.rs (ListEnvelope), 3 new CoreError variants, IgnitionMock helper, #[ignore] live suite, updated README taxonomy.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gateway-health-inspection/02-RESEARCH.md
@.planning/phases/02-gateway-health-inspection/openapi-8.3.6-phase2-extract.json
@.planning/phases/01-foundation-agentic-contracts/01-04-SUMMARY.md
@crates/ignition-core/src/client/mod.rs
@crates/ignition-core/src/error.rs
@crates/ignition-core/src/client/version.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classifier + additive taxonomy + GatewayInfo live-shape fix</name>
  <files>crates/ignition-core/src/client/classify.rs, crates/ignition-core/src/error.rs, crates/ignition-core/src/client/mod.rs, crates/ignition-core/src/client/version.rs, crates/ignition-cli/src/render.rs, crates/ignition-core/tests/gateway_info_contract.rs, README.md</files>
  <action>
    **error.rs — three ADDITIVE target-state variants (contract addition, never reshaping; codes 1–7 unchanged):**
    - `GatewayNotCommissioned { endpoint: Option<String> }` — slug `gateway_not_commissioned`, exit 6, Display: "gateway at {endpoint} is not commissioned", hint: "open http://<host>:<port>/welcome in a browser and complete the commissioning wizard"
    - `GatewayRestarting { endpoint: Option<String> }` — slug `gateway_restarting`, exit 6, Display: "gateway is restarting (webserver up, services down)", hint: "wait for readiness with `ign wait restart` or retry in ~1 minute"
    - `NotFound { endpoint: Option<String> }` — slug `not_found`, exit 6, Display: "resource not found on the gateway", hint: "check the id/path; a 404 JSON 'No route match' body can also mean a pre-8.3 gateway"
    - Wire all three into `code()`, `exit_code()`, `hint()`, `endpoint()`. Add rows to `exit_code_mapping_enumerated`. Update the class table doc-comment (exit 6 row now lists four slugs) and the README exit-code table — the SAME commit touches both homes.
    - Make `Auth::hint()` status-aware (messages/slugs unchanged; hints are CORE-05 content, not frozen slugs):
      - 401: "token not recognized — the X-Ignition-API-Token header must be the FULL `name:key` string from the gateway UI (Platform→Security→API Keys); Basic auth does not work on 8.3 /data routes — create an API token"
      - 403: keep the substring "three parts": "token recognized but under-permitted — Ignition token setup is three parts: (1) token holds an adequate security level, (2) gateway read/write permissions include that level, (3) 'Require secure connections' is unchecked for http gateways; run `ign doctor` for a diagnosis"
    - Update the two in-repo tests that pin hint strings (`hints_are_actionable_for_config_auth_target_state` 403 case keeps "three parts"; `error_envelope_locked_shape_and_endpoint` 401 case gets the new hint string — message/endpoint/order unchanged).

    **client/classify.rs (new):**
    - `pub(crate) async fn classify(resp: reqwest::Response, url: &str) -> Result<reqwest::Response, CoreError>` — dispatch order (research §Error-Body Sniffing): 2xx → Ok; 3xx + Location header containing "/welcome" → GatewayNotCommissioned; other 3xx (e.g. /idp) → Auth{status, endpoint}; 401/403 → Auth; 503 → GatewayRestarting; 404 → NotFound; everything else → Internal with the HTML-sniffed detail if any.
    - `fn html_error_parts(body: &str) -> Option<(u16, String)>` — pure substring scan for `<title>Error ` … `</title>` and `<th>MESSAGE:</th><td>` … `</td>` (fixed Jetty template; NO HTML crate — research Don't-Hand-Roll). Used ONLY to enrich the Internal fallback message; classified variants keep their fixed Display.
    - Golden test fixture: embed the EXACT captured Jetty HTML from 02-RESEARCH.md §Code Examples.

    **client/mod.rs:**
    - `build_client` adds `.redirect(reqwest::redirect::Policy::none())` — Pitfall 6: an uncommissioned gateway 302s everything to /welcome and reqwest's default follow would render the welcome HTML as a 200.
    - Extract the request pipeline into private helpers on ReqwestGatewayApi: `get_json<T>(&self, path, query: Option<&ListQuery>, auth: bool)` and `post_empty(&self, path, auth: bool)` — each: build URL → apply auth (when auth=true) → send → transport error → Network → `classify()` → `.json::<T>()`. The `auth: bool` param exists because `/StatusPing` must be fetched header-less (02-02).
    - Move the credential match into ONE private `fn apply_auth(&self, request) -> RequestBuilder` — the `Secret::expose()` confinement boundary MOVES here, not duplicates (grep audit: expose() call sites outside secret.rs confined to this one function). `gateway_info` body becomes a 1-line call to `get_json`.
    - When the credential is `Credential::Basic`, `apply_auth` emits `tracing::warn!` once per call: "Basic auth does not authenticate Ignition 8.3 /data routes (verified: valid credentials → 401); use an API token" (research Auth §2 — demote Basic loudly, never silently retry).

    **client/version.rs — the Phase-1 bug fix:**
    - Rename field `version` → `ignition_version` with `#[serde(alias = "version")]` (tolerates any 8.3.x still shipping the old name); add `name: Option<String>` and `license: Option<LicenseInfo>` (`mode: String`, `expiration_date: Option<String>`); add `redundancy_role`, `jvm_version` as optional passthrough; **REMOVE the `state` and `uptime` fields** (they do not exist on the real payload — running state/uptime come from /overview + /StatusPing in 02-02; keep the model truthful). Keep the `#[serde(skip)] endpoint` field.
    - Add `below_minimum` unit-test rows with the EXACT live strings: `("8.3.6 (b2026042713)", false)` (space-split already handled) and `("8.3.6", false)`.
    - render.rs (bin): drop the "unknown state" fragment from the version human line — print `gateway {ignition_version} ({edition})`; repin affected version goldens if the line shape changes.

    **tests/gateway_info_contract.rs:** add the live-capture regression: mock `/data/api/v1/gateway-info` returning the EXACT live JSON from 02-RESEARCH.md §Status/info (ignitionVersion, name, license.mode "Trial", …) → `gateway_info()` returns Ok with `ignition_version == "8.3.6 (b2026042713)"`.
  </action>
  <verify>
    `cargo test -p ignition-core` green (new classifier + taxonomy + live-capture tests); `cargo test --workspace` green with any repinned version goldens; `SNAPSHOTS=overwrite cargo test -p ignition-cli` is a no-op on a second run; `grep -rn 'gateway_not_commissioned' README.md crates/ignition-core/src/error.rs` hits BOTH homes; `cargo clippy --workspace --all-targets -- -D warnings` clean.
  </verify>
  <done>
    All six classification truths hold under wiremock (HTML 401→Auth+name:key hint, HTML 403→Auth+three-parts hint, 302→welcome→GatewayNotCommissioned, 503→GatewayRestarting, 404→NotFound, unclassifiable→Internal with sniffed detail); the live-capture body deserializes; the enumerated test + README both list the four exit-6 slugs; expose() still confined to the single auth helper.
  </done>
</task>

<task type="auto">
  <name>Task 2: ListQuery/ListEnvelope + IgnitionMock harness + full classifier matrix</name>
  <files>crates/ignition-core/src/client/query.rs, crates/ignition-core/src/client/mod.rs, crates/ignition-core/src/lib.rs, crates/ignition-core/tests/common/mod.rs, crates/ignition-core/tests/gateway_info_contract.rs</files>
  <action>
    **client/query.rs (new):**
    - `ListQuery { limit: i64 (-1 = all, the UI convention), offset: i64, sort_by / search / filter: Option<String> }` with `Default` (limit=-1) and a `to_query_pairs()` that skips None values. Every 8.3 list endpoint takes this (verified catalog).
    - `ListEnvelope<T> { items: Vec<T>, metadata: ListMetadata }` (Deserialize bound on T) and `ListMetadata { total, matching, limit, offset: i64 }` — serde-tolerant (metadata may carry extra keys like `metrics` — use `#[serde(default)]` fields, no deny_unknown_fields).
    - Register `pub mod query;` in client/mod.rs (lib.rs module tree unchanged otherwise).

    **tests/common/mod.rs (new) — IgnitionMock:**
    - Wraps a `wiremock::MockServer`; helper builders returning configured `Mock`s the test registers: `list_json(method, path, json!({items, metadata}))` (200 + JSON body), `html_error(method, path, status)` (exact captured Jetty template with the right `<title>Error NNN</title>`), `redirect(path, location)` (302 + Location header), `status_json(method, path, status, body)` (generic), `literal_true(method, path)` (200 body `true` — restart shape, ready for 02-05).
    - IgnitionMock is a thin convenience (~100 lines) — later plans' tests become 3-liners per scenario.

    **Classifier matrix tests (extend tests/gateway_info_contract.rs):** run `gateway_info()` against each IgnitionMock scenario and assert `CoreError` class + slug + exit code + endpoint + hint substring: HTML 401, HTML 403, 302→/welcome, 302→/idp (→Auth), 503, 404 JSON `{"message":"No route match for path: …"}` (→NotFound), 500 HTML (→Internal with sniffed "Error 500" in the message).
  </action>
  <verify>
    `cargo test -p ignition-core` green including the 7-scenario classifier matrix; ListQuery serializes `limit=-1` and skips absent optionals (unit test); `cargo clippy -p ignition-core --all-targets -- -D warnings` clean.
  </verify>
  <done>
    One generic list envelope + one mock harness exist for every later capability plan; the full research status-code matrix (§Auth Model 4) is wiremock-enforced.
  </done>
</task>

<task type="auto">
  <name>Task 3: Live-gateway #[ignore] suite + Basic demotion docs</name>
  <files>crates/ignition-core/tests/live_gateway.rs, crates/ignition-core/src/client/mod.rs, README.md</files>
  <action>
    **tests/live_gateway.rs (new):** `#[ignore]`-gated suite skipped unless `IGNITION_LIVE_URL` is set (read the env at test start; `IGNORED` otherwise). 02-01 scope (later plans APPEND their checks to this file):
    - `live_gateway_info_parses`: build ReqwestGatewayApi::for_tests(url, token) → `gateway_info()` → Ok, `ignition_version` non-empty, `below_minimum(...) == false` — THE live regression for the ignitionVersion fix.
    - `live_token_auth_works`: with `IGNITION_LIVE_TOKEN` set (full `name:key` string), gateway-info returns 200 — empirically closing STATE.md's flagged "live-gateway auth verification" gap on whatever gateway the user points at.
    - `live_basic_is_rejected`: when `IGNITION_LIVE_USER`/`IGNITION_LIVE_PASSWORD` are also set, assert Basic → Auth error (documents the verified reality; skip when envs absent).
    - File-header comment carries the rig-recreation recipe from 02-RESEARCH.md §Test architecture (docker run inductiveautomation/ignition:8.3.6 → browser commissioning → API key with 'Require secure connections' UNCHECKED).
    - Mark the STATE.md gap CLOSED in the plan SUMMARY.

    **README.md:** add a Phase-2 auth section: token-only machine auth on 8.3 /data routes (`X-Ignition-API-Token: name:key`, full UI-copied string — key-only → 401), Basic kept only as an enum arm for future/legacy surface with the loud warning, 401-vs-403 semantics table, and the three new exit-6 slugs already in the table from Task 1.
  </action>
  <verify>
    `cargo test -p ignition-core -- --ignored` skips cleanly with no envs; against a live rig (if available) the two core live tests pass; `cargo test --workspace` green; README renders the 401/403 semantics.
  </verify>
  <done>
    The live suite exists and is green (skip-by-default); the auth gap has an executable proof path; docs tell users exactly how to create a working token.
  </done>
</task>

</tasks>

<verification>
- `cargo test --workspace` green; `cargo clippy --workspace --all-targets -- -D warnings` + `cargo fmt --check` clean; `cargo build -p ignition-cli --no-default-features` still green.
- Classifier runs before every `.json()` — no `.json()` on a response that skipped `classify()` (grep the pipeline helpers).
- `Secret::expose()` call sites outside secret.rs: exactly one function (the auth helper).
- Exit-code table consistency: enumerated test rows == README rows, including the three new slugs.
</verification>

<success_criteria>
Phase-1's deserialization bug is fixed and regression-pinned; every error shape a live 8.3 gateway can produce classifies into the LOCKED taxonomy with actionable hints; the mock + live harnesses that make 02-02..02-05 cheap are in place.
</success_criteria>

<output>
After completion, create `.planning/phases/02-gateway-health-inspection/02-01-SUMMARY.md`
</output>
