---
phase: 02-gateway-health-inspection
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - crates/ignition-core/src/client/mod.rs
  - crates/ignition-core/src/client/status.rs
  - crates/ignition-core/src/client/metrics.rs
  - crates/ignition-core/src/actions/mod.rs
  - crates/ignition-core/src/actions/inspect.rs
  - crates/ignition-core/tests/status_contract.rs
  - crates/ignition-core/tests/live_gateway.rs
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/src/render.rs
  - crates/ignition-cli/tests/contract_status.rs
  - README.md
autonomous: true

must_haves:
  truths:
    - "User runs `ign status` and sees gateway name, version+revision (ignitionVersion), platform (java vendor/version, os), uptime, running state (from StatusPing), CPU/memory/disk, and license state incl. trial remaining"
    - "User runs `ign modules` and sees every module with id, name, version, state, licenseState; `--quarantined` swaps to the quarantined list"
    - "User runs `ign metrics` and sees current CPU percent + heap/max memory and thread execution counts (running/waiting/timedWaiting/blocked); `--history` adds historic chart datapoints"
    - "StatusPing is fetched WITHOUT any auth header (wiremock header-absence proof) so it works when auth is broken or absent"
    - "All three commands honor the envelope (json/compact/human) with [profile: NAME] header and [..]-elided goldens"
  artifacts:
    - path: "crates/ignition-core/src/client/status.rs"
      provides: "Overview / StatusPing / ModuleInfo / models + GatewayApi impls for overview(), status_ping(), modules()"
      min_lines: 80
    - path: "crates/ignition-core/src/client/metrics.rs"
      provides: "CurrentGauges / PerformanceCharts / ThreadCounts models + metrics trait impls"
      min_lines: 50
    - path: "crates/ignition-core/src/actions/inspect.rs"
      provides: "status/modules/metrics actions (serde models out, no printing — TUI rides this in Phase 6)"
      min_lines: 80
    - path: "crates/ignition-cli/tests/contract_status.rs"
      provides: "Snapbox goldens for status/modules/metrics in all three render modes"
      min_lines: 60
  key_links:
    - from: "GatewayApi trait (client/mod.rs)"
      to: "capability impl bodies"
      via: "trait method per capability; impl bodies delegate to the 02-01 get_json pipeline (coarse-method lock honored)"
      pattern: "async fn (overview|status_ping|modules|metrics_)"
    - from: "status action"
      to: "gateway_info + overview + status_ping"
      via: "merged into one StatusResult data payload"
      pattern: "status_ping"
    - from: "client/metrics.rs"
      to: "systemPerformance endpoints"
      via: "exact verified paths currentGauges / charts / threads (NOT /system/metrics — invented path, 404)"
      pattern: "systemPerformance"
---

<objective>
Ship the read-only inspection surface: `ign status`, `ign modules`, `ign metrics` (HLTH-01, HLTH-02, HLTH-07).

Purpose: The single best status call is `/data/api/v1/overview` (platform, uptime, CPU/mem/disk, license) plus unauthenticated `/StatusPing` for running state — these become the primitives every downstream phase reuses. Modules and metrics complete the "webpage status pages replaced" story.

Output: 6 new GatewayApi methods + models, 3 actions, 3 subcommands with goldens, live-suite checks.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gateway-health-inspection/02-RESEARCH.md
@.planning/phases/02-gateway-health-inspection/openapi-8.3.6-phase2-extract.json
@.planning/phases/02-gateway-health-inspection/02-01-SUMMARY.md
@crates/ignition-core/src/client/mod.rs
@crates/ignition-core/src/client/query.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: overview / status_ping / modules capabilities</name>
  <files>crates/ignition-core/src/client/status.rs, crates/ignition-core/src/client/mod.rs, crates/ignition-core/tests/status_contract.rs</files>
  <action>
    **client/status.rs (new)** — models straight from the live captures in 02-RESEARCH.md (§Status/info, §Modules), each with `#[serde(flatten)] extra: BTreeMap<String, serde_json::Value>` passthrough so `--json` stays complete as gateway responses evolve:
    - `Overview` — `version`, `redundancy { role, activity_level, project_state }` (optional), `java { version, vendor, name }`, `os { name, arch, version }`, `uptime: i64` (epoch **ms** — document in the field comment), `memory: Vec<i64>` ([used, max]), `cpu: f64` (0–1 **fraction** — name/comment it explicitly; gauges are percent — no silent conversion), `disk { total, used }`, `license { state, trial_remaining_s }`.
    - `StatusPing { state: String }` — observed RUNNING / STARTING; unknown states surface as-is (treated as not-ready by wait in 02-05).
    - `ModuleInfo { id, name, version, state, license_state, vendor_name, startup_time }` + passthrough.
    - GatewayApi growth (add to the trait in client/mod.rs, impl bodies in status.rs calling the 02-01 pipeline):
      - `async fn overview(&self) -> Result<Overview, CoreError>` — GET `/data/api/v1/overview`, authed.
      - `async fn status_ping(&self) -> Result<StatusPing, CoreError>` — GET `/StatusPing` **with auth=false** (the unauthenticated readiness anchor — works mid-restart and with broken auth).
      - `async fn modules(&self, quarantined: bool, query: &ListQuery) -> Result<ListEnvelope<ModuleInfo>, CoreError>` — GET `/data/api/v1/modules/healthy` or `/modules/quarantined` with query params.
    - NOTE: `/data/api/v1/overview` is NOT in the 46-path openapi extract but IS live-verified (research §Status/info) — trust the research table over the extract.
  </action>
  <verify>
    `cargo test -p ignition-core` green. tests/status_contract.rs (wiremock, IgnitionMock fixtures): overview parses the exact live-captured shape; **status_ping sends NO auth headers** (recorded-request header-absence proof, 01-04 pattern); modules parses the healthy-list capture incl. `state: "ACTIVE"`; modules passes `limit=-1`; HTML 401 on overview → Auth.
  </verify>
  <done>
    Three capabilities live on the LOCKED coarse-method trait with wiremock-proven paths, shapes, and the StatusPing header-less guarantee.
  </done>
</task>

<task type="auto">
  <name>Task 2: metrics capabilities</name>
  <files>crates/ignition-core/src/client/metrics.rs, crates/ignition-core/src/client/mod.rs, crates/ignition-core/tests/status_contract.rs</files>
  <action>
    **client/metrics.rs (new)** — exact verified paths under `/data/api/v1/systemPerformance/` (research §Metrics; the ignition-mcp `/data/api/v1/system/metrics` path is an invention — 404 — do NOT use it):
    - `CurrentGauges { cpu: f64 (percent), heap_memory: i64, max_memory: i64 }` — GET `currentGauges`.
    - `PerformanceCharts { cpu_datapoints: Vec<Datapoint>, heap_memory_datapoints, non_heap_memory_datapoints }` with `Datapoint { hist_id, timestamp: i64 (epoch ms), value: f64 }` — GET `charts` (captures use `cpuChartDatapoints` / `heapMemoryDatapoints` / `nonHeapMemoryDatapoints` — serde-rename to snake_case fields).
    - `ThreadCounts { running, waiting, timed_waiting, blocked: i64 }` — GET `threads`.
    - Trait: `async fn metrics_current()`, `async fn metrics_historic()`, `async fn metrics_threads()` — all authed GETs through the pipeline.
    - Keep the cpu-percent vs overview-cpu-fraction distinction explicit in field names/comments (research: "normalize in the model, not in users' eyes" — the normalization is HONEST NAMING, not silent conversion).
  </action>
  <verify>
    Wiremock tests in status_contract.rs: each of the three endpoints parses its exact live capture (cpu 4.88 percent; threads {running:32, waiting:39, timedWaiting:51, blocked:0}; a charts body with one datapoint each).
  </verify>
  <done>
    HLTH-07's server side is complete: current + historic + thread-execution metrics as typed models with passthrough.
  </done>
</task>

<task type="auto">
  <name>Task 3: `ign status` / `ign modules` / `ign metrics` commands + goldens</name>
  <files>crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/main.rs, crates/ignition-cli/src/render.rs, crates/ignition-core/src/actions/inspect.rs, crates/ignition-core/src/actions/mod.rs, crates/ignition-cli/tests/contract_status.rs, crates/ignition-core/tests/live_gateway.rs, README.md</files>
  <action>
    **actions/inspect.rs (new; actions/mod.rs registers it)** — serde-model-out, no printing (Phase-6 TUI rides this layer):
    - `status(api) -> StatusResult` — merges gateway_info + overview + status_ping; data shape: `{ gateway: {name, ignition_version, edition, license}, state: "RUNNING", overview: {java, os, uptime_ms, memory, cpu_fraction, disk, license: {state, trial_remaining_s}} }`. Tolerate overview/ping failures how? — NO: status is a read of a healthy gateway; a failed sub-call is an error (exit per taxonomy). Fields the gateway omits are `skip_serializing_if`-none.
    - `modules(api, quarantined) -> ModulesResult { items: Vec<ModuleInfo>, quarantined: bool }` (query default ListQuery = limit -1).
    - `metrics(api, include_history: bool) -> MetricsResult { current: CurrentGauges, threads: ThreadCounts, history: Option<PerformanceCharts> }`.
    - Unit-test actions with a FakeApi double (01-04 pattern; CoreErrors constructed lazily via the dead-port trick).

    **cli.rs** — `Commands::Status`, `Commands::Modules { #[arg(long)] quarantined: bool }`, `Commands::Metrics { #[arg(long)] history: bool }` (default output = current + threads; `--history` adds charts).

    **main.rs** — dispatch arms constructing ReqwestGatewayApi post-overlay (established version-arm pattern); credential REQUIRED for these commands (they are authed reads — resolve_secret, not resolve_secret_opt: no secret → SecretUnavailable exit 3, correct for a command that cannot work unauthenticated). New ActionOutput variants Status/Modules/Metrics with render_json arms (monomorphic per-variant).

    **render.rs (human)** — `[profile: NAME]` header (CORE-01), then plain rows (Phase-1 style, no table crate):
    - status: identity line (`name  ignitionVersion  edition`), state line, platform line (java vendor+version, os name/arch), uptime (humanized from ms), cpu/mem/disk line, **license line incl. trial countdown** (`license: trial, 1h57m remaining` — the research-recommended banner).
    - modules: one row per module `id  name  version  state  licenseState`.
    - metrics: gauges row (`cpu 4.9%  heap 229MB/1024MB`) + threads row; `--history` adds first/last datapoint summary lines.

    **contract_status.rs (new)** — snapbox goldens per the 01-02 harness (stdout_for_golden, isolated IGNITION_CLI_CONFIG, profile URL → wiremock base): status --json + --compact + human; modules human + --quarantined; metrics human; `[..]`-elide every dynamic value (uptime, timestamps, cpu, memory, trial remaining). Error golden: 401 HTML → exit 5 envelope.

    **live_gateway.rs** — append read-only live checks: overview parses, modules healthy non-empty, metrics_current parses (skip gracefully when no rig).

    **README.md** — command table rows for status/modules/metrics.
  </action>
  <verify>
    `cargo test --workspace` green; `SNAPSHOTS=overwrite` no-op on re-run; `ign status --json` against the mock emits the envelope with exactly the documented data keys; exit-5 error golden passes; clippy -D warnings + fmt clean.
  </verify>
  <done>
    HLTH-01, HLTH-02, HLTH-07 are user-observable: three commands, three render modes, goldens pinned, live checks in place.
  </done>
</task>

</tasks>

<verification>
- All three commands work end-to-end through the envelope in all render modes; every dynamic value in goldens is [..]-elided.
- StatusPing header-less proof test green (the liveness anchor must survive broken auth).
- No new CoreError variants (this plan rides 02-01's taxonomy exclusively).
</verification>

<success_criteria>
A user replaces the gateway webpage's Status Overview, Config > Modules, and Performance pages with `ign status`, `ign modules`, `ign metrics` — including the trial countdown — with machine-readable output for agents.
</success_criteria>

<output>
After completion, create `.planning/phases/02-gateway-health-inspection/02-02-SUMMARY.md`
</output>
