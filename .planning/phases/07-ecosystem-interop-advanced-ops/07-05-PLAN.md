---
phase: 07-ecosystem-interop-advanced-ops
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ignition-core/src/client/eam.rs
  - crates/ignition-core/src/actions/eam.rs
  - crates/ignition-core/src/client/classify.rs
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/tests/contract_eam.rs
  - README.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "ign eam history against the live 8.3.3 controller (profile uat, :9088) exits 0 and lists the cli-research-backup (forced) entry with its Failed level and GNET not-connected detail as DATA"
    - "ign eam task new uat-backup-demo eam_backup against the live controller CREATES the definition (exit 0) and ign eam tasks lists it"
    - "a gateway 422 on a /data/api/v1/resources/ path surfaces as invalid_input (exit 2) carrying the gateway's own message text, never internal_error"
    - "ign eam task new --help enumerates the three type classes with full token lists and a worked example — a first-time user can form a valid command without trial and error"
  artifacts:
    - path: "crates/ignition-core/src/client/eam.rs"
      provides: "EamHistoryItem.task_id as String (wire-faithful UUID)"
      contains: "pub task_id: String"
    - path: "crates/ignition-core/src/actions/eam.rs"
      provides: "create composition with config.settings (targetGateways/targetGroups + K=V + --definition overlay)"
      contains: "config.settings"
    - path: "crates/ignition-core/src/client/classify.rs"
      provides: "path-scoped 422 arm on config-resource paths mapping to InvalidInput"
      contains: "UNPROCESSABLE_ENTITY"
    - path: "crates/ignition-cli/src/cli.rs"
      provides: "TYPE doc enumerating benign/mutating/refused classes with full token lists + worked example"
    - path: "crates/ignition-cli/tests/contract_eam.rs"
      provides: "UUID-string taskId fixtures, live-shape create body re-pin, 422 classification contract, raw-capture history fixture"
  key_links:
    - from: "crates/ignition-core/src/actions/eam.rs eam_task_create"
      to: "config.settings.targetGateways"
      via: "composed create body"
      pattern: "targetGateways.*targetGroups"
    - from: "crates/ignition-core/src/client/classify.rs"
      to: "CoreError::InvalidInput"
      via: "422 arm scoped to /data/api/v1/resources/"
      pattern: "UNPROCESSABLE_ENTITY.*resources"
    - from: "crates/ignition-cli/src/cli.rs EamTaskCommand::New"
      to: "task_create_guard taxonomy"
      via: "TYPE doc enumeration matching REFUSED_TYPES/MUTATING_TYPES exactly"
      pattern: "eam_restoreBackup.*eam_installModules.*eam_remoteUpgrade"
---

<objective>
Close UAT gaps 1, 2, and 3 (07-UAT.md): the EAM history decode is wire-faithful
(UUID taskIds), `eam task new` composes the live 8.3.3 controller's required
`config.settings` shape (422 eliminated), a config-resource 422 classifies
honestly as `invalid_input`, and the TYPE help enumerates the guard-ladder
taxonomy so a first-time user forms a correct command without fumbling.

Purpose: Phase 7's EAM family must work against the real controller gateway
the user tests on — the three blockers all make working features fail live.
Output: fixed client model + create composition + classify arm + help UX, all
contract-pinned to the live-captured shapes, with live verification against
gateway A (http://localhost:9088).
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ecosystem-interop-advanced-ops/07-UAT.md

Live evidence (the wire truth these fixes are pinned to):
@.planning/debug/eam-history-raw.json
@.planning/debug/eam-create-422.md
@.planning/debug/eam-working-definition.json

Source truth:
@crates/ignition-core/src/client/eam.rs
@crates/ignition-core/src/actions/eam.rs
@crates/ignition-core/src/client/classify.rs
@crates/ignition-cli/tests/contract_eam.rs

Rig (UP, disposable verification target):
- Gateway A = profile `uat` → http://localhost:9088 (8.3.3, controller-mode
  EAM, has cli-research-backup). Do NOT reset/destroy A.
- Token (both gateways): `uatdiff:526MTlRs7STxfEgWGQ9_aSzQQJndIKLfepmwFeVSj_k`
  via `IGNITION_TOKEN` env (profile auth_kind token_env).
</context>

<tasks>

<task type="auto">
  <name>Task 1: EAM history decode is wire-faithful — task_id is a UUID string (gap 1)</name>
  <files>crates/ignition-core/src/client/eam.rs, crates/ignition-cli/tests/contract_eam.rs</files>
  <action>
The real 8.3.3 controller serializes `taskId` as a UUID STRING
(`.planning/debug/eam-history-raw.json`: `"taskId":"a2f4dab1-9a8f-4feb-9306-29e261f60453"`),
not an i64. `#[serde(default)]` cannot rescue a type mismatch, so reqwest `.json()`
decode fails → InternalError. Fix the model, then fix every fixture that carries
the wrong image:

1. `crates/ignition-core/src/client/eam.rs`:
   - `EamHistoryItem.task_id`: `i64` → `String` (keep the `#[serde(rename = "taskId", default)]`;
     `default` now yields `String::default()` — still tolerant). Update the field doc:
     "the run's id — a UUID STRING on 8.3.3 controllers (wire-faithful)".
   - Fix the unit test fixtures: `history_item_parses_the_live_shape` uses
     `"taskId": 42` and the sparse-shape case uses `"taskId": 43` — replace with
     UUID strings (use the literal captured value
     `"a2f4dab1-9a8f-4feb-9306-29e261f60453"` for the first; any well-formed
     UUID string for the second) and assert `item.task_id == "a2f4dab1-..."`.
     These fixtures currently prove the WRONG wire image — they are the gap's
     unit-test half.
2. `crates/ignition-cli/tests/contract_eam.rs`:
   - `mount_history`: `"taskId": 2` / `"taskId": 1` → distinct UUID strings.
     Re-pin the JSON golden (`eam_history_json_golden`) — `taskId` values become
     JSON strings (serde_json maps are key-sorted; the key order in the golden
     does not change). The human golden has no id column — verify it stays
     byte-identical (if it moves, re-pin).
   - `task_force_success_golden`: the history mock uses `"taskId": 9` → UUID
     string; re-pin both its compact and human goldens.
   - Add `eam_history_decodes_the_raw_capture`: mount a wiremock response whose
     body is EXACTLY the captured raw shape (single item with UUID taskId,
     `"taskName": "cli-research-backup (forced)"`, `"level": "Failed"`, the
     GNET-not-connected detail, `{items, metadata:{total,matching,limit,offset}}`
     envelope — use `set_body_json` with the structure from
     `.planning/debug/eam-history-raw.json`, fresh epoch-ms values) and assert
     `ign eam history --compact` exits 0 with the entry passthrough. This is the
     contract fixture the gap demanded — shaped like the raw capture, not the
     old numeric image.

Do NOT touch `EamTaskRecord` (definitions decode fine — `eam tasks` passed UAT).
  </action>
  <verify>
`cargo test -p ignition-core client::eam && cargo test -p ignition-cli --test contract_eam`
green. Live probe (gateway A, up):
`IGNITION_TOKEN=uatdiff:526MTlRs7STxfEgWGQ9_aSzQQJndIKLfepmwFeVSj_k ign --profile uat eam history`
→ exit 0, lists `cli-research-backup (forced)` with `[Failed]` and the
"Gateway network for agent '_controller' is currently not connected" detail
as DATA. Capture the run in the SUMMARY.
  </verify>
  <done>
EamHistoryItem decodes UUID-string taskIds; the raw-capture-shaped contract
test exists and passes; the live `ign eam history` probe exits 0 with the
Failed/GNET entry visible.
  </done>
</task>

<task type="auto">
  <name>Task 2: eam task new composes config.settings; 422 classifies as invalid_input (gap 3)</name>
  <files>crates/ignition-core/src/actions/eam.rs, crates/ignition-core/src/client/classify.rs, crates/ignition-cli/tests/contract_eam.rs, README.md</files>
  <action>
The live controller requires `config.settings` non-null ("Settings cannot be
null", HTTP 422) and expects `targetGateways` INSIDE `config.settings` — see
`.planning/debug/eam-working-definition.json`:
`config.profile={type,isSuspended,scheduleMode}`,
`config.settings={targetGateways,targetGroups,concurrentBackups,forceBackups}`.
The README §Settings forms example ALREADY documents the settings shape — the
composition just landed it in the wrong node.

1. `crates/ignition-core/src/actions/eam.rs` — `eam_task_create` composition:
   - `config.profile` = `{type, scheduleMode}` ONLY (do NOT add `isSuspended` —
     unverified for create; the server defaults it on read-back).
   - `config.settings` = `{targetGateways, targetGroups}` + every `--setting K=V`
     (auto-typed scalars — `parse_setting`/`auto_type` unchanged) + the
     `--definition` overlay deep-merged over the composed SETTINGS object
     (`deep_merge` unchanged — it now merges onto settings, not profile).
   - Defaults per the live shape: when NO `--target` is given,
     `targetGateways: ["_controller"]` (the controller itself — the
     live-captured zero-config default on a controller-mode gateway; explicit
     `--target` values replace it wholesale); `targetGroups: []` always (no
     --group flag exists).
   - The composed record stays `{"name": name, "config": {"profile": …,
     "settings": …}}`; `EamTaskCreateResult.definition` keeps echoing it
     verbatim. Update the module-level composition doc comment and the
     function's doc to describe profile/settings split.
   - Unit tests: move/extend the composition pins — bare create (no --target)
     yields `targetGateways == ["_controller"]`; with `--target gw-a` yields
     `["gw-a"]`; a K=V lands in `config.settings` (assert `settings` key, NOT
     `profile`); the `deep_merge` semantics tests stay as-is (pure fn). The
     guard ladder (`task_create_guard`) is UNTOUCHED — this fix must not
     disturb the CLI/TUI/action guard ladder.
2. `crates/ignition-core/src/client/classify.rs` — honest 422 classification:
   - Add a path-scoped arm BEFORE the Internal fallback:
     `S::UNPROCESSABLE_ENTITY if is_config_resource_url(url)` where
     `is_config_resource_url(url)` is `url.contains("/data/api/v1/resources/")`
     (pure helper + unit tests: the EAM create path
     `/data/api/v1/resources/com.inductiveautomation.eam/eam-tasks` matches;
     `/data/eam/...` runtime paths and gateway-info do NOT).
   - In the arm, read the body (`resp.text().await`): if it parses as JSON with
     a `messages` array (the live shape: `{"messages":["Settings cannot be
     null"],"fieldMessages":[]}`), join the messages into the reason; else use
     the raw body text; else a bare "HTTP 422" note. Return
     `CoreError::InvalidInput { reason: format!("gateway rejected the resource
     body (HTTP 422 from {url}): {text}") }` — exit 2 `invalid_input`, honest
     for a client-composed body the server rejected. (Body-reading precedent:
     the EamNotController arm reads body text.)
3. `crates/ignition-cli/tests/contract_eam.rs`:
   - Re-pin `task_new_backup_ondemand_fires_unguarded`: the golden's
     `definition` now shows `config.profile` (type/scheduleMode only) and
     `config.settings` (concurrentBackups/forceBackups from the two `--setting`
     flags + targetGateways `["gw-a"]` + targetGroups `[]`); the request-body
     assertions move to `body[0]["config"]["settings"]["concurrentBackups"]`
     etc. NOTE: this test passes `--target gw-a`, so no `_controller` default
     appears — add a second create test (or extend) pinning the no-target
     default: `ign eam task new t2 eam_backup --compact` with an empty-`--target`
     body assertion `targetGateways == ["_controller"]`.
   - Add `task_new_422_classifies_invalid_input`: mount the create POST → 422
     with body `{"messages":["Settings cannot be null"],"fieldMessages":[]}`
     (`set_body_raw` with content-type application/json — NOT set_body_string,
     which forces text/plain). Assert exit 2, stderr envelope slug
     `invalid_input`, message contains "Settings cannot be null" and names the
     endpoint. Never internal_error.
4. `README.md`: §Settings forms — "deep-merges over the composed profile" →
   "over the composed `config.settings`"; the command table's `eam task new`
   row gains the zero-target default (`targetGateways` defaults to
   `["_controller"]` when no `--target` is given) and the profile/settings
   split one-liner. No new slug this task — `invalid_input` already exists in
   the exit table (two-place rule satisfied; no table change).
  </action>
  <verify>
`cargo test -p ignition-core && cargo test -p ignition-cli --test contract_eam`
green; `cargo fmt --all && cargo clippy --all-targets -- -D warnings` clean.
Live probe (gateway A): `IGNITION_TOKEN=uatdiff:... ign --profile uat eam task
new uat-backup-demo eam_backup` → exit 0 (CREATE). Then `ign --profile uat
eam tasks` lists `uat-backup-demo type=eam_backup schedule=OnDemand`.
The created definition is an OnDemand backup (never auto-fires) — leaving it
on the devops rig is safe; note it in the SUMMARY. If the gateway still 422s,
the stderr must now be exit-2 `invalid_input` carrying the gateway message —
capture either outcome in the SUMMARY.
  </verify>
  <done>
Create body matches the live `config.settings` shape (wiremock-pinned incl.
the `_controller` default); the 422 contract proves exit-2 invalid_input with
the gateway's message; live create on gateway A succeeds and the definition is
visible in `ign eam tasks`.
  </done>
</task>

<task type="auto">
  <name>Task 3: TYPE help enumerates the guard-ladder taxonomy (gap 2)</name>
  <files>crates/ignition-cli/src/cli.rs</files>
  <action>
The user fumbled three invocations because TYPE is a bare String positional
whose doc says only "the openapi taxonomy: eam_backup, eam_restart, …".
Fix the help text — do NOT change argument order, names, or the contract
(locked: NAME then TYPE positionals):

1. In `EamTaskCommand::New` (crates/ignition-cli/src/cli.rs ~line 995):
   - `name` doc: "Task definition name (any resource name — e.g. nightly-backup)".
   - `r#type` doc: enumerate the three classes with FULL token lists and their
     guard consequences, matching `actions/eam.rs`'s `REFUSED_TYPES`/
     `MUTATING_TYPES` EXACTLY:
       - benign: `eam_backup` — unguarded with the default OnDemand schedule;
       - mutating (need `--yes`): `eam_restart`, `eam_sendProject`,
         `eam_sendResource`, `eam_sendTags`, `eam_activateLicense`,
         `eam_updateLicense`, `eam_unactivateLicense`;
       - refused (exit 6 `eam_task_type_refused`): `eam_restoreBackup`,
         `eam_installModules`, `eam_remoteUpgrade`.
   - Add a worked example line to the doc text:
     `ign eam task new nightly-backup eam_backup --target gw-a`.
   - Consider a cheap clap affordance (`value_name = "TYPE"` is already
     effective via the field name; do NOT add `possible_values` — unknown
     future types must stay accepted and fail-safe to the `--yes` rung).
2. Check whether any snapbox golden pins `eam task new --help` output
   (grep the tests/ dir for the old doc text) — re-pin any moved golden.
3. TUI: this is a doc-string-only change — no new command/flag shape, so the
   tui_coverage clap-walk row set is unchanged (`task_create_guard` and the
   TUI Confirm gating are untouched). State this in the SUMMARY.
  </action>
  <verify>
`cargo run -- eam task new --help` shows the three classes with full token
lists and the example; `cargo test -p ignition-cli --test tui_coverage` green;
workspace suite green.
  </verify>
  <done>
A first-time user reading `--help` can form a valid command without trial and
error; all three token lists match the guard-ladder constants exactly.
  </done>
</task>

</tasks>

<verification>
1. `cargo fmt --all` and `cargo clippy --all-targets -- -D warnings` clean.
2. Full workspace suite green (`cargo test --workspace`) — ~841+ tests
   including the re-pinned EAM goldens and the new 422/raw-capture contracts.
3. Live probes against gateway A (http://localhost:9088,
   `IGNITION_TOKEN=uatdiff:526MTlRs7STxfEgWGQ9_aSzQQJndIKLfepmwFeVSj_k`,
   profile `uat`), captured in the SUMMARY:
   - `ign eam history` → exit 0, cli-research-backup (forced) [Failed] GNET
     detail as DATA (gap 1 closed).
   - `ign eam task new uat-backup-demo eam_backup` → exit 0; `ign eam tasks`
     lists it (gap 3 closed).
   - `ign eam task new --help` → the enumerated taxonomy + example (gap 2
     closed).
   - If any create still fails server-side: exit 2 `invalid_input` with the
     gateway's message (never internal_error) — the honest-classification
     floor.
4. Guard ladder regression: `ign eam task new x eam_restoreBackup` still
   refuses exit 6 `eam_task_type_refused`; a mutating type without --yes
   still refuses exit 2 pre-resolution with zero network (the
   `task_new_guard_ladder_refusals_do_zero_work` contract stays green).
</verification>

<success_criteria>
All three UAT gaps (1, 2, 3) closed and live-verified against the real 8.3.3
controller; wiremock contracts re-pinned to the live-captured shapes (UUID
taskIds, config.settings composition, 422 → invalid_input); taxonomy additive
(no new slugs this plan); the guard ladder and TUI Confirm parity untouched;
fmt/clippy clean; workspace suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ecosystem-interop-advanced-ops/07-05-SUMMARY.md`
</output>
