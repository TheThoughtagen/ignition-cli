---
phase: 07-ecosystem-interop-advanced-ops
plan: "03"
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - crates/ignition-core/src/actions/script.rs
  - crates/ignition-core/src/actions/mod.rs
  - crates/ignition-core/src/error.rs
  - crates/ignition-core/tests/script_contract.rs
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/tests/contract_script.rs
  - crates/ignition-tui/src/routes.rs
  - crates/ignition-tui/src/state.rs
  - crates/ignition-tui/src/update.rs
  - README.md
autonomous: true
must_haves:
  truths:
    - "User can run `ign script run --code '2+2'` on a profile with scriptExec deployed and see {stdout, result, elapsedMs} in data (all keys always)"
    - "All three input forms work: --code STR, --file PATH, --file - (stdin)"
    - "Without scriptExec deployed (no persisted webdev_secret) the verb exits 6 with the additive script_exec_not_configured slug whose hint names `ign webdev deploy --with-script-exec`"
    - "No --yes guard exists on script run (the opt-in is structural — the deploy flag)"
    - "The secret never appears in any output mode (redaction canary extended)"
  artifacts:
    - path: "crates/ignition-core/src/actions/script.rs"
      provides: "script_run action: secret resolution + scriptExec precondition + exec POST"
      contains: "pub async fn script_run"
    - path: "crates/ignition-core/src/error.rs"
      provides: "script_exec_not_configured additive exit-6 slug"
      contains: "script_exec_not_configured"
    - path: "crates/ignition-cli/src/cli.rs"
      provides: "Script subcommand with Run leaf"
      contains: "Script(ScriptArgs)"
    - path: "crates/ignition-tui/src/routes.rs"
      provides: "routes row for script run (the pre-recorded Phase 6 instance)"
      contains: "\"script run\""
  key_links:
    - from: "crates/ignition-core/src/actions/script.rs"
      to: "GatewayApi::webdev_route_call"
      via: "exec POST with the secret header on the scriptExec route (200-BODY envelope is the only success oracle)"
      pattern: "webdev_route_call\\("
    - from: "crates/ignition-core/src/actions/script.rs"
      to: "profile.webdev_secret"
      via: "secret resolution; None → script_exec_not_configured naming the deploy flag"
      pattern: "webdev_secret"
    - from: "crates/ignition-cli/src/main.rs"
      to: "actions::script::script_run"
      via: "dispatch arm reading --code/--file/--file- input before the action"
      pattern: "script_run\\("
---

<objective>
`ign script run` (SCRPT-01) — the smallest plan: one verb over the already-shipped, already-secured scriptExec route.

Purpose: agents get non-interactive gateway-side Python execution; the opt-in is STRUCTURAL (route deployed only via `--with-script-exec`, fail-closed secret gate — Phase 5's LOCKED posture). No new config gate, no --yes guard.
Output: `ign script run` with three input forms, one additive slug, TUI input/result modal flow, README threat-model pointer.

Planner decisions (research Open Questions resolved):
- **All three input forms**: `--code STR`, `--file <path>`, `--file -` (stdin — the agent pipe path). Providing both --code and --file = InvalidInput exit 2 pre-resolution.
- **No --yes** (research rec adopted): the deploy flag is the opt-in; the verb is the route's entire purpose; agents need it non-interactive.
- **Timeout honesty**: no route-side timeout exists in v1.0.0 — the client rides the existing per-request class; README notes a long-running script holds the HTTP connection.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ecosystem-interop-advanced-ops/07-RESEARCH.md
@.planning/phases/07-ecosystem-interop-advanced-ops/07-02-SUMMARY.md
@.planning/phases/05-webdev-backend-tag-operations/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: script_run action — secret resolution + precondition + exec + contract</name>
  <files>crates/ignition-core/src/actions/script.rs, crates/ignition-core/src/actions/mod.rs, crates/ignition-core/src/error.rs, crates/ignition-core/tests/script_contract.rs</files>
  <action>
**Action** (`actions/script.rs` NEW; register in mod.rs) — reuse the constants and route-call machinery that already exist (actions/webdev.rs `SCRIPT_EXEC_ROUTE = "scriptExec"` and `SECRET_HEADER`; make them `pub(crate)` if not already):
1. `pub async fn script_run(api: &dyn GatewayApi, config: &Config, profile_name: &str, project: &str, code: &str) -> Result<ScriptRunResult, CoreError>`:
   - Resolve `webdev_secret` from the profile (the same lookup actions/webdev.rs uses). `None` → additive `CoreError` slug **`script_exec_not_configured`** (exit 6) whose hint names `ign webdev deploy --with-script-exec` verbatim.
   - Precondition handshake: `webdev_route_call(project, "scriptExec", [(SECRET_HEADER, secret)], {"action": "version"})` — success = 200-BODY `{ok, data}` (the ONLY oracle); a denial body `secret_required`/`secret_mismatch` surfaces honestly (mismatch means deployed-elsewhere/stale — the message already says redeploy or `--rotate-secret`; map to the existing webdev error family, no new slug).
   - Exec: POST `{"action": "exec", "code": <code>}` with the secret header → `ScriptRunResult { stdout: String, result: Value, elapsed_ms: u64 }` serialized under unit-explicit keys `{stdout, result, elapsedMs}` — ALL keys always (family convention). Route error body (`{ok:false, error{code,message,traceback?}}` at HTTP 200) → the existing denial-mapping shape with the traceback surfaced (the 05-08 pattern).
2. Pure helper `pub fn read_script_input(code: Option<&str>, file: Option<&str>) -> Result<String, CoreError>` — both given → InvalidInput; `--file -` reads stdin; file read errors → InvalidInput (the 03-03 put convention). Unit-tested separately from the async path.

**Error table** (`error.rs`): `script_exec_not_configured` additive exit-6 slug in CoreError + README + the `exit_code_mapping_enumerated` test (two-place rule).

**Contract** (`tests/script_contract.rs` NEW, wiremock over the ReqwestGatewayApi): (a) success round — version probe then exec POST, both carrying `X-Ignition-CLI-Secret` with the profile's stored secret; body shapes pinned (recorded-request proofs — assert the exec JSON body's `action`/`code` exactly); (b) missing secret → the slug, ZERO HTTP requests; (c) secret_mismatch denial → the honest error, no exec POST; (d) route error body with traceback → surfaced in the message. Redaction canary: no output path contains the secret hex (extend the test to assert over the rendered result Debug/JSON).
  </action>
  <verify>cargo test -p ignition-core --test script_contract && cargo test -p ignition-core error && cargo clippy -p ignition-core -- -D warnings</verify>
  <done>script_run proven at the action layer: probe+exec sequence, three-input pure reader, slug + redaction pinned; exit-table enumerated test extended</done>
</task>

<task type="auto">
  <name>Task 2: CLI surface + goldens + TUI input/result modal + README</name>
  <files>crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/main.rs, crates/ignition-cli/tests/contract_script.rs, crates/ignition-tui/src/routes.rs, crates/ignition-tui/src/state.rs, crates/ignition-tui/src/update.rs, README.md</files>
  <action>
**CLI** (`cli.rs`): top-level `Script(ScriptArgs)` with `enum ScriptCommand { Run { #[arg(long, value_name = "PY")] code: Option<String>, #[arg(long, value_name = "PATH")] file: Option<String>, #[arg(long, default_value = "ign-cli", value_name = "NAME")] project: String } }` — leaf `script run` (`script` requires a subcommand → no row). NO `--yes` arg.

**main.rs**: dispatch arm reads input via `read_script_input` (usage errors lead — exit 2 before resolution) → resolve profile/secret/client through the standard single-profile path → `script_run`. Human render: stdout block (verbatim), `result:` line, `elapsed:` line.

**Goldens** (`contract_script.rs` NEW): success JSON (`{stdout, result, elapsedMs}` with `[..]` elision on elapsedMs), success human, missing-secret refusal golden (exit 6, slug + deploy-flag hint), both-inputs InvalidInput golden (exit 2). Isolated `IGNITION_CLI_CONFIG` with a fixture profile carrying a fake secret; the webdev double rides a wiremock server (the established contract pattern).

**TUI** (the pre-recorded Phase 6 instance): row `"script run"` → `Screen(Screen::Dashboard)`. Dashboard ACTIONS menu gains `script run` (06-02 pattern): Input modal (multi-line code — the rig-restore form's textarea shape if one exists, else a single Input; stdin refused per the crossterm rule — no --file form in TUI, code-only), fire via one-shot `spawn_action`, result in the shared pretty-JSON Result modal. Ungated (CLI parity — no --yes exists).

**README**: extend the scriptExec posture section (already written Phase 5) with the verb contract: input forms, the structural opt-in, `{stdout, result, elapsedMs}` shape, no-timeout honesty, redaction guarantee, and the shared-secret threat-model cross-reference.
  </action>
  <verify>cargo test -p ignition-cli --test contract_script --test tui_coverage && cargo test --workspace && cargo fmt --all --check && cargo clippy --workspace -- -D warnings</verify>
  <done>`ign script run` works end-to-end in all three input forms (golden + contract proven); tui_coverage green with the script run row; README posture complete</done>
</task>

</tasks>

<verification>
- `cargo test --workspace` green; fmt + clippy clean
- SCRPT-01 truth: script run executes via scriptExec with the persisted secret; not-deployed → actionable exit 6
- Secret redaction extended to the new output paths
- The one new TUI row lands in the SAME plan (tui_coverage CI gate satisfied)
</verification>

<success_criteria>
SCRPT-01 delivered: opt-in (deploy-structural), non-interactive script execution with the frozen envelope discipline. One additive slug. Zero new dependencies. Route source untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ecosystem-interop-advanced-ops/07-03-SUMMARY.md`. Execution updates STATE.md (the pre-recorded tui_coverage instance satisfied; slug added to the table).
</output>
