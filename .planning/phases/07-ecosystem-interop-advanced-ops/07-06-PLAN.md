---
phase: 07-ecosystem-interop-advanced-ops
plan: 06
type: execute
wave: 2
depends_on: ["07-05"]
files_modified:
  - crates/ignition-core/src/error.rs
  - crates/ignition-core/src/client/classify.rs
  - crates/ignition-core/src/client/webdev.rs
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/tagConfig/doPost.py
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/tags/doPost.py
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/alarms/doPost.py
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/tagHistory/doPost.py
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/scriptExec/doPost.py
  - webdev/routes/VERSION
  - crates/ignition-core/src/webdev/mod.rs
  - crates/ignition-cli/tests/contract_eam.rs
  - crates/ignition-cli/tests/contract_tags.rs
  - README.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "ign eam task force on a task whose '(forced)' run occupies the slot exits 6 as eam_task_in_flight carrying the gateway's page text, never internal_error"
    - "ign tags export default and ign tags config get [default] (provider-ROOT forms) exit 6 as provider_root_unsupported naming the subtree-path workaround"
    - "subtree tag paths (e.g. [default]folder) still export/configure fine after the route redeploy — no regression"
    - "the webdev route bundle is version-bumped so a stale 1.0.0 deployment is refused with route_version_mismatch (redeploy hint) until ign webdev deploy runs"
  artifacts:
    - path: "crates/ignition-core/src/error.rs"
      provides: "EamTaskInFlight + ProviderRootUnsupported additive exit-6 slugs (two-place exit-table rule)"
      contains: "EamTaskInFlight"
    - path: "crates/ignition-core/src/client/classify.rs"
      provides: "path-scoped 409 arm on /data/eam/api/v1/eam-tasks/force/"
      contains: "eam-tasks/force/"
    - path: "webdev/routes/com.inductiveautomation.webdev/resources/cli/tagConfig/doPost.py"
      provides: "provider_root_unsupported honest denial (pre-call bracket detection + RpcContext translation)"
      contains: "provider_root_unsupported"
    - path: "crates/ignition-core/src/client/webdev.rs"
      provides: "denial_to_error arm mapping provider_root_unsupported to the new slug"
      contains: "provider_root_unsupported"
  key_links:
    - from: "crates/ignition-core/src/client/classify.rs 409 arm"
      to: "CoreError::EamTaskInFlight"
      via: "path-scoped force-URL match + Jetty page sniff"
      pattern: "CONFLICT.*eam-tasks/force"
    - from: "webdev/routes/.../tagConfig/doPost.py"
      to: "CoreError::ProviderRootUnsupported"
      via: "200-body denial code provider_root_unsupported mapped in denial_to_error"
      pattern: "provider_root_unsupported.*ProviderRootUnsupported"
    - from: "webdev/routes/VERSION"
      to: "crates/ignition-core/src/webdev/mod.rs ROUTE_BUNDLE_VERSION"
      via: "equality-enforced bundle version 1.1.0 (all five route files carry the same ROUTE_VERSION)"
      pattern: "1\\.1\\.0"
---

<objective>
Close UAT gaps 4 and 5 (07-UAT.md): the EAM force route's 409 state conflict
surfaces as an additive exit-6 `eam_task_in_flight` refusal (the
session_not_prunable precedent, force-route edition), and provider-ROOT tag
paths (`[default]` alone / bare `default`) get an honest
`provider_root_unsupported` refusal naming the subtree workaround instead of a
misleading route_error — via a route-level fix (option c: pre-call detection +
RpcContext translation) plus the mandated route-bundle version bump so stale
deployments cannot serve the old behavior.

Purpose: two honesty gaps where genuine gateway state/limitations masqueraded
as internal errors. Output: two additive taxonomy slugs wired through the
established seams (classify 409 arm; denial_to_error arm), a version-bumped
route bundle, contract pins, README sync, and live verification against
gateway A (whose leftover '(forced)' run is the gap-4 verification asset).
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ecosystem-interop-advanced-ops/07-UAT.md
@.planning/phases/07-ecosystem-interop-advanced-ops/07-05-SUMMARY.md

Live evidence:
@.planning/debug/eam-history-raw.json

Source truth:
@crates/ignition-core/src/error.rs
@crates/ignition-core/src/client/classify.rs
@crates/ignition-core/src/client/webdev.rs
@crates/ignition-core/src/webdev/mod.rs
@webdev/routes/com.inductiveautomation.webdev/resources/cli/tagConfig/doPost.py

Rig (UP): Gateway A = profile `uat` → http://localhost:9088 (8.3.3
controller; the leftover `cli-research-backup (forced)` run IS the gap-4 live
fixture — do NOT delete/complete it, do NOT reset A). Gateway B = profile
`uat-b` → http://localhost:9188 (disposable clone; trial may have expired —
if a B probe fails on trial, use A). Token both:
`uatdiff:526MTlRs7STxfEgWGQ9_aSzQQJndIKLfepmwFeVSj_k` via `IGNITION_TOKEN`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: force-route 409 → additive eam_task_in_flight refusal (gap 4)</name>
  <files>crates/ignition-core/src/error.rs, crates/ignition-core/src/client/classify.rs, crates/ignition-cli/tests/contract_eam.rs, README.md</files>
  <action>
The force route's 409 (a leftover '(forced)' run occupies the slot; gateway
page text: "Task 'X (forced)' already exists! It must be completed or deleted
before another task of this type can be force executed.") falls through
classify() to InternalError. Follow the 06-07 session_not_prunable precedent
EXACTLY — path-scoped 409 arm, additive exit-6 slug, two-place exit-table rule:

1. `crates/ignition-core/src/error.rs` — new additive variant (place beside
   `EamTaskTypeRefused`):
   `EamTaskInFlight { task: String, detail: String, endpoint: Option<String> }`
   with Display like
   `#[error("EAM task {task} has a run in flight — the gateway refused the force: {detail}")]`
   (`detail` carries the gateway's Jetty page message verbatim when sniffed;
   constructor/classify falls back to "the previous '(forced)' run must be
   completed or deleted first" when the page text is absent). `code()` →
   `"eam_task_in_flight"`, `exit_code()` → 6 (target state), `endpoint()`
   plumb-through, `hint()` names the resolution (complete or delete the
   leftover run — via the EAM console; no CLI verb deletes runs). Extend the
   `exit_code_mapping_enumerated` unit test with the new variant (the
   two-place exit-table rule: error.rs + README table row in step 4).
2. `crates/ignition-core/src/client/classify.rs` — new arm in the match,
   modeled on the designer-prune 409 arm:
   `S::CONFLICT if is_eam_force_url(url) => { read body text; sniff the Jetty
   page via html_error_parts → detail message; fallback detail; extract the
   task name; Err(CoreError::EamTaskInFlight { task, detail, endpoint }) }`.
   Helpers (pure, unit-tested like `is_designer_prune_url`/`designer_prune_id`):
   - `is_eam_force_url(url)` → `url.contains("/data/eam/api/v1/eam-tasks/force/")`
   - `eam_force_task_name(url)` → the LAST path segment after `force/`
     (the URL is `/eam-tasks/force/{owner}/{name}` — split on the force-route
     prefix, then take the final `/`-separated segment; query-safe like
     `designer_prune_id`).
   Unit tests: the force path matches; `/data/eam/api/v1/eam-tasks/history`
   and the config-resource list path do NOT; the name extractor pulls
   `cli-research-backup` from `/data/eam/api/v1/eam-tasks/force/eam/cli-research-backup`.
3. `crates/ignition-cli/tests/contract_eam.rs` — add
   `task_force_conflict_refusal_golden`: mount the find (owner=eam) + the
   force POST → 409 with the CAPTURED Jetty error page body
   (`set_body_raw` of the HTML page, content-type text/html — NOT
   set_body_string; the page text is in 07-UAT.md test 7). Assert: exit 6,
   stderr envelope slug `eam_task_in_flight`, message contains "already
   exists" and the task name, profile null in the envelope (error shape).
   The force-success golden from 07-05 stays green (204 path unchanged).
4. `README.md` — exit-code table gains the `eam_task_in_flight` row (exit 6,
   target state: the task's previous run occupies the force slot); note in
   the `eam task force` semantics section that a leftover run refuses with
   this slug carrying the gateway's page text.

Guard-ladder/TUI note: this is a TRANSPORT-level fix — `task_create_guard`,
CLI pre-resolution guards, and TUI Confirm gating are untouched; no new CLI
command/flag shape, so tui_coverage needs no row change.
  </action>
  <verify>
`cargo test -p ignition-core && cargo test -p ignition-cli --test contract_eam`
green (incl. the new 409 golden + the 07-05 force-success golden); fmt/clippy
clean. LIVE (gateway A — the leftover run is the asset):
`IGNITION_TOKEN=uatdiff:... ign --profile uat eam task force cli-research-backup --yes`
→ exit 6, slug `eam_task_in_flight`, message carries the gateway's
"already exists" page text. Capture the envelope in the SUMMARY. Do NOT
remediate the gateway state.
  </verify>
  <done>
The force 409 classifies as exit-6 `eam_task_in_flight` with the gateway page
text, contract-pinned and live-proven on A; taxonomy additive-only; exit
table synced in both places; guard ladder untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: provider-ROOT tag paths refuse honestly — provider_root_unsupported (gap 5, option c)</name>
  <files>webdev/routes/com.inductiveautomation.webdev/resources/cli/tagConfig/doPost.py, webdev/routes/com.inductiveautomation.webdev/resources/cli/tags/doPost.py, webdev/routes/com.inductiveautomation.webdev/resources/cli/alarms/doPost.py, webdev/routes/com.inductiveautomation.webdev/resources/cli/tagHistory/doPost.py, webdev/routes/com.inductiveautomation.webdev/resources/cli/scriptExec/doPost.py, webdev/routes/VERSION, crates/ignition-core/src/webdev/mod.rs, crates/ignition-core/src/error.rs, crates/ignition-core/src/client/webdev.rs, crates/ignition-cli/tests/contract_tags.rs, README.md</files>
  <action>
`system.tag.getConfiguration`/`exportTags` with a provider-ROOT path
(`[default]` alone, or a bare first segment that resolves to a provider) throw
IllegalStateException("No RpcContext available") on WebDev threads (8.3.3
b2026012009); leaf/subtree paths work (live-proven both directions, two
gateways). The route's bare-exect currently surfaces this as generic
`route_error` — a bug-shaped denial for a known platform limitation.

CHOSEN FIX (option c, the lightest honest one — CLI-side refusal (b) would
hide the limitation from every future consumer, and the recursive-browse
workaround (a) is unproven machinery inventing behavior the gateway may not
support): route-level honest refusal + Rust-side dedicated slug.

1. `webdev/routes/.../tagConfig/doPost.py` — ALL new logic nested INSIDE
   `doPost` (byte-0 rule: `def doPost` at byte 0, no module-level content;
   the 05-06 live-bisected contract):
   - A provider-root detector (bracket form): a path starting with `[` whose
     remainder after the closing `]` is empty (or only slashes) — e.g.
     `[default]`, `[prov]`. (The BARE form `default` CANNOT be pre-detected
     without provider-name knowledge — the gateway's TagPathParser resolves
     a leading provider-matching segment to the provider root.)
   - `getConfig`: if `tagPath` is bracket-provider-root → pre-call
     `err('provider_root_unsupported', 'provider-root tag paths are not
     supported on WebDev threads (no RpcContext) — use a subtree path like
     [provider]folder')` (zero gateway work — deterministic refusal).
   - `exportTags`: pre-flight scan `data['paths']`; if ANY path is
     bracket-provider-root → the same denial naming the offending path.
   - RpcContext translation for the bare form: wrap the
     `system.tag.getConfiguration(...)` and `system.tag.exportTags(...)`
     calls in a targeted `try/except`; if the formatted traceback contains
     `'No RpcContext'` → return the same `provider_root_unsupported`
     denial (the path resolved to a provider root inside the parser);
     otherwise re-`raise` into the outer bare-except (generic route_error
     semantics unchanged).
   - The outer bare-except / traceback envelope stays EXACTLY as-is for
     everything else.
2. Route-bundle version bump 1.0.0 → 1.1.0 (the route source changed; the
   equality-locked bundle must move so a stale 1.0.0 deployment is REFUSED —
   the designed staleness protection): edit `ROUTE_VERSION = '1.1.0'` in ALL
   FIVE route doPost.py files (tags, tagConfig, alarms, tagHistory,
   scriptExec template) + `webdev/routes/VERSION` → `1.1.0` +
   `ROUTE_BUNDLE_VERSION` in `crates/ignition-core/src/webdev/mod.rs` →
   `"1.1.0"`. The existing drift tests enforce consistency (they will fail
   until all three move together — that is the pin).
3. `crates/ignition-core/src/error.rs` — additive variant (beside
   `EamNotController`): `ProviderRootUnsupported { endpoint: Option<String> }`,
   Display: "provider-root tag paths are not supported by the deployed route
   (the gateway needs an RPC context WebDev threads don't carry) — target a
   subtree like [provider]folder". `code()` → `"provider_root_unsupported"`,
   exit 6, endpoint plumb-through, enumerated-test extension (two-place rule).
4. `crates/ignition-core/src/client/webdev.rs` — `denial_to_error` arm:
   `"provider_root_unsupported" => CoreError::ProviderRootUnsupported {
   endpoint: Some(endpoint) }` (the hint lives in the fixed Display — the
   route's message stays consistent with it). Extend the
   `denial_mapping_reuses_not_found_and_rides_the_rest` unit test with the
   new code → slug/exit assertions.
5. `crates/ignition-cli/tests/contract_tags.rs` — wiremock CANNOT execute
   the route's Python (no real gateway semantics), so pin BOTH halves:
   - Rust mapping: a tagConfig route mock answering HTTP 200 with the denial
     envelope `{"ok":false,"error":{"code":"provider_root_unsupported",
     "message":"..."}}` → `ign tags config get [default]` and an export-path
     variant exit 6, slug `provider_root_unsupported`, message names the
     subtree workaround.
   - Route-source pin: in `crates/ignition-core/src/webdev/mod.rs` tests
     (where route sources are already include_str-scanned for version
     equality), add an assertion that the tagConfig doPost source contains
     `provider_root_unsupported` — the route-side refusal cannot silently
     regress.
6. `README.md` — exit table row for `provider_root_unsupported`; in the tags
   export/config section, document the limitation honestly: provider-ROOT
   paths (`[default]` alone / a bare provider name) refuse on 8.3.3 (no
   RpcContext on WebDev threads); subtree paths (`[default]folder`) are the
   supported form.
  </action>
  <verify>
`cargo test --workspace` green (version-drift tests prove the bundle moved
together; the enumerated exit test carries both new slugs); fmt/clippy
clean. LIVE (gateway A, token as above, profile `uat`):
   1. Staleness proof (before redeploy): `ign --profile uat tags browse`
      (or any webdev-dependent tags command) → exit 6
      `route_version_mismatch` naming 1.0.0-deployed vs 1.1.0 — the designed
      refusal; capture it.
   2. `ign --profile uat webdev deploy` → routes redeployed at 1.1.0 (the
      ign-cli project is CLI-owned; redeploy is routine and unguarded by
      design).
   3. `ign --profile uat tags export default` → exit 6
      `provider_root_unsupported`, message names the subtree workaround
      (gap 5 closed, bare form via the RpcContext translation).
   4. `ign --profile uat tags config get [default]` → same refusal
      (bracket form via pre-call detection).
   5. Subtree regression probe: pick any real folder via
      `ign --profile uat tags browse` (e.g. a top-level folder F), then
      `ign --profile uat tags export [default]F` → exit 0 (no regression).
   Capture all five runs in the SUMMARY.
  </verify>
  <done>
Provider-root forms (bracket and bare) refuse exit 6 as
`provider_root_unsupported` with the subtree hint — live-proven on gateway A
after a routine redeploy; the route bundle is version-locked at 1.1.0 so stale
deployments refuse honestly; subtree paths regression-free; both new slugs
live in the two-place exit table; the limitation is README-documented.
  </done>
</task>

</tasks>

<verification>
1. `cargo fmt --all` + `cargo clippy --all-targets -- -D warnings` clean;
   full `cargo test --workspace` green.
2. Taxonomy audit: exactly two new slugs this plan (`eam_task_in_flight`,
   `provider_root_unsupported`), both exit 6, both in error.rs's enumerated
   test AND the README exit table (two-place rule); no existing slug/golden
   moved except the intended re-pins.
3. Live probes on gateway A captured in the SUMMARY: the force 409 refusal
   (task 1), the route_version_mismatch staleness proof, the redeploy, both
   provider-root refusals, and the subtree regression probe (task 2).
4. Gateway A's state is PRESERVED (the leftover '(forced)' run untouched —
   it remains a live verification asset; A is the standing devops rig).
</verification>

<success_criteria>
UAT gaps 4 and 5 closed: the force 409 and provider-root crash both surface
as honest additive exit-6 target-state refusals carrying the gateway's own
text and actionable workarounds; the route bundle version bump forces stale
deployments to refuse; every fix is contract- or source-pinned; live
verification against the real 8.3.3 gateways passes; workspace suite, fmt,
clippy all clean.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ecosystem-interop-advanced-ops/07-06-SUMMARY.md`
</output>
