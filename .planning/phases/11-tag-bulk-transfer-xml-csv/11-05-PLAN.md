---
phase: 11-tag-bulk-transfer-xml-csv
plan: 05
type: execute
wave: 4
depends_on: ["11-04"]
files_modified:
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/src/render.rs
  - crates/ignition-cli/tests/contract_tags.rs
  - README.md
autonomous: true

must_haves:
  truths:
    - "User passes --format xml|csv on `ign tags export` and `ign tags import`; default json behavior (flags, output files, goldens, stdout exception #4) is byte-identical to today"
    - "Before importing XML/CSV whose scan reports losses, the user sees the loss report as prose on STDERR and the command refuses with exit 2 invalid_input unless --yes — mirroring the require_confirmation/preview_then_confirm gate shape (10-04); with --yes the report rides the envelope as data.loss_report"
    - "CSV export warns-and-continues (stderr warning naming dropped fields) — generation is non-destructive, NO gate"
    - "XML/CSV stdout mode (-o -) writes RAW bytes in every render mode (the fourth-exception pattern extended); no pretty-printing, no added trailing newline — stdout purity harness stays green (zero stray bytes in --json mode)"
    - "README documents the CSV honesty ('the gateway cannot export CSV; --format csv output is CLI-generated and lossy: no alarms, legacy columns'), the loss-gate flow, and keeps the exit table in agreement (no new rows — invalid_input exists)"
  artifacts:
    - path: "crates/ignition-cli/src/cli.rs"
      provides: "--format json|xml|csv on tags Export + Import leaves (flag on existing arms — NO new tui_coverage rows)"
      contains: "--format"
    - path: "crates/ignition-cli/src/main.rs"
      provides: "Dispatch: input-byte reading, loss gate pre-resolution, format plumbing"
      contains: "loss"
    - path: "crates/ignition-cli/src/render.rs"
      provides: "Loss-report stderr rendering; raw-bytes xml/csv stdout; data.loss_report envelope field"
    - path: "README.md"
      provides: "CSV-honesty paragraph + loss-report section + command rows + exit-table sync"
      contains: "cannot export CSV"
    - path: "crates/ignition-cli/tests/contract_tags.rs"
      provides: "Goldens: json-unchanged pins, loss-gate refusal, --yes flow, xml/csv stdout"
  key_links:
    - from: "main.rs dispatch (import arm)"
      to: "tag_loss::scan_xml/scan_csv"
      via: "PRE-resolution scan of input bytes → gate decision before ANY wire work (zero requests on refusal)"
      pattern: "scan_xml|scan_csv"
    - from: "loss-gate refusal"
      to: "exit 2 invalid_input"
      via: "existing error class — NO new exit codes/slugs; hint names 're-run with --yes'"
      pattern: "invalid_input"
    - from: "render.rs (TagsExport xml/csv stdout)"
      to: "raw bytes on stdout"
      via: "the sanctioned-exception pattern (completions/logs-tail/tags-export-json precedent) — byte-exact, never pretty"
      pattern: "stdout"
    - from: "README CSV section"
      to: "11-LIVE-CAPTURES.md coverage table"
      via: "the documented lossy-field behavior cites the live-proven table"
      pattern: "lossy"
---

<objective>
Surface the transfer actions on the CLI with the TAGS-12 gate: `--format xml|csv` flags on tags export/import, a pre-resolution loss gate that shows the report and refuses without --yes (imports only — CSV generation warns-and-continues), raw-byte stdout/file handling, goldens, and the README's honest CSV contract.

Purpose: this is the user-visible half of the phase — TAGS-12's "user sees a loss-report warning and can abort" lives here, and the CLI contract (frozen envelope, stdout purity, Three-Place rules) must hold while it lands.
Output: wired CLI + gate + renderers + goldens + README rows.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tag-bulk-transfer-xml-csv/11-RESEARCH.md
@.planning/phases/11-tag-bulk-transfer-xml-csv/11-LIVE-CAPTURES.md
@crates/ignition-cli/src/cli.rs
@crates/ignition-cli/src/main.rs
@crates/ignition-cli/src/render.rs
@crates/ignition-cli/tests/contract_tags.rs
</context>

<planner_locks>
- **Gate shape lock (TAGS-12)**: ONE gate function in dispatch (the 10-04 preview_then_confirm lesson — one gate means the refusal shape cannot drift): read input bytes → scan (tag_loss) → if losses reported && format != json && !yes → render prose report to STDERR + refuse exit-2 invalid_input with hint "re-run with --yes to import anyway". Imports ONLY. CSV export = warn-and-continue (stderr), never gated. No new exit codes or slugs (invalid_input exists — Phase-1 taxonomy).
- **Envelope lock**: the frozen envelope is additive-only — success data gains `loss_report` (structured scan summary: facts + counts) for csv export and for xml/csv import WITH --yes; the refusal carries the prose inside the error message (rendered to stderr in every mode — the established error path). No envelope shape changes.
- **stdout purity lock (research Pitfall 7)**: loss-report prose NEVER touches stdout; the contract_stdout_purity harness must stay green with max diagnostics.
- **JSON lock**: default-format behavior — flags, `<last-segment>.json` default file name, trailing newline, pretty payload, goldens — byte-identical to today.
- **snapbox gotcha (research Pitfall 8)**: keep golden text simple; XML/CSV goldens with quotes/backslashes record escapes inline at the golden (03-02/05-05 convention).
- **Mixed-parent handling (probe 4)**: if 11-01 captured a hard mixed-parent failure, dispatch refuses pre-resolution exit-2 naming the offending parents — gating the NEW formats only if the constraint is new (JSON multi-path export behavior unchanged); if the gateway tolerates it, no check. Cite the capture in a comment.
</planner_locks>

<tasks>

<task type="auto">
  <name>Task 1: CLI flags + dispatch + loss gate + renderers</name>
  <files>crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/main.rs, crates/ignition-cli/src/render.rs</files>
  <action>
1. **cli.rs**: add `--format json|xml|csv` (default `json`, value_name FORMAT) to the existing `tags export` and `tags import` arms (TagsCommand::Export at ~cli.rs:591, TagsCommand::Import at ~:605) — flag-on-existing-leaf per the research architecture (zero new tui_coverage registry rows; the clap walk proves it).
2. **main.rs dispatch (export arm)**: format Json = today verbatim. Xml = call tags_export(format: Xml) — render writes file bytes / stdout raw. Csv = export the JSON interchange as today, then `generate_legacy_csv(subtrees)` → write bytes (NO trailing newline added beyond what csv emits) to file/stdout; render a stderr warning naming dropped_keys/coerced from the CsvGenerationReport (warn-and-continue, exit 0); data.loss_report rides the success envelope.
3. **main.rs dispatch (import arm)**: read the input file/stdin as RAW BYTES for xml/csv (new bytes reader beside the existing read_json_input — stdin `-` support symmetric to JSON). THE GATE (one function, e.g. `fn loss_gate(bytes, format, yes) -> Result<ScanSummary, CoreError>`): scan via tag_loss → if facts non-empty && !yes → render report prose to STDERR + return CoreError::InvalidInput { reason: <the report prose + "re-run with --yes to import anyway"> } (pre-resolution: zero wire work — NO webdev precondition, NO route call on refusal). With --yes: proceed, attach the structured scan summary to the result envelope as data.loss_report. Json format skips the gate entirely (byte-identical path).
4. **Mixed-parent pre-resolution** per the planner lock (cite 11-LIVE-CAPTURES.md Probe 4).
5. **render.rs**: TagsExportResult xml/csv stdout mode = write raw payload bytes to stdout in EVERY mode (extend the fourth-exception interception at render.rs:111); human mode adds a one-line summary (format, tag count, destination) to STDERR-adjacent human stdout header per existing human-render conventions — CAREFUL: human mode writes to stdout today for json; keep human-mode json untouched, xml/csv human mode = raw bytes + stderr summary line. TagsImportResult human mode gains a loss-report-aware line when data carries one.
6. ActionOutput variants extended additively (TagsExport/TagsImport results gain format + loss fields; serde skip_serializing_if on empties so legacy envelope consumers see no noise).
  </action>
  <verify>`cargo build --workspace`; manual smoke against wiremock not needed here (goldens in Task 2); `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo fmt --check` clean</verify>
  <done>Flags parse, gate refuses pre-resolution without --yes, csv export warns-and-continues, raw-byte stdout/file paths exist, envelope grows additively</done>
</task>

<task type="auto">
  <name>Task 2: Goldens + contract tests (json-unchanged pin, gate refusals, raw stdout)</name>
  <files>crates/ignition-cli/tests/contract_tags.rs</files>
  <action>
Following the file's wiremock+snapbox patterns (assert_cmd over a spawned binary against wiremock, isolating IGNITION_CLI_CONFIG per spawn):
1. **JSON-unchanged pin**: existing goldens MUST pass untouched — do not regenerate any json golden. If any json golden changed, that's a bug (the format default must be invisible).
2. **Loss-gate refusals**: import xml with a loss-bearing fixture (alarms/CompoundProperty present) and no --yes → assert exit 2, stderr contains the report prose + "re-run with --yes", stderr envelope (json mode) has error.code invalid_input, AND the wiremock recorded ZERO route calls (the precondition mount uses expect(0)-style absence — the pre-resolution proof). Repeat for csv (csv_no_alarms fact fires the gate).
3. **--yes flow**: same fixture with --yes → exit 0, envelope data.loss_report present (structured facts), importTagsFile REQUEST pin shows file_b64 == base64(fixture bytes) verbatim.
4. **CSV export warn-and-continue**: export --format csv over a canned JSON interchange → exit 0, file/stdout bytes == generate_legacy_csv output, stderr warning names the dropped keys, data.loss_report present.
5. **XML export raw stdout**: `-o -` --format xml → stdout == the canned payload_b64 decoded, EXACTLY (byte assert via Command output, not a snapbox golden — byte-exactness beyond what str! guarantees); file mode asserts NO trailing newline.
6. **stdout purity**: run the contract_stdout_purity harness suite — one stray byte under max diagnostics fails; the loss-report prose must appear only on stderr (if the harness needs a new scenario for tags import, ADD the scenario in its established pattern).
7. snapbox golden care: escapes recorded inline (Pitfall 8).
  </action>
  <verify>`cargo test -p ignition-cli --test contract_tags` green (old goldens untouched + new tests); `cargo test -p ignition-cli --test contract_stdout_purity` green; `cargo test --workspace` green</verify>
  <done>The CLI contract is pinned: refusals are pre-resolution and exit-2, --yes flows carry bytes verbatim with structured loss_report, raw stdout is byte-exact, json default is untouched</done>
</task>

<task type="auto">
  <name>Task 3: README contract reconciliation</name>
  <files>README.md</files>
  <action>
Following the Three-Place / two-places rules (08-executable-contract discipline):
1. Command rows for `tags export --format` and `tags import --format` (the established row format).
2. The CSV honesty paragraph, verbatim spirit per the lock: "The gateway cannot export tags to CSV (official Ignition 8.3 docs). `ign tags export --format csv` output is CLI-generated from the gateway's JSON interchange and is LOSSY: no alarm configurations, the legacy 47-column set only, numeric enum coercion. The command prints exactly what was dropped on stderr." Reference the documented lossy-field table (cite 11-LIVE-CAPTURES.md or inline the table if README convention prefers self-containment — match how v1.0 READMEs handled similar tables).
3. Loss-report section (TAGS-12): when importing xml/csv, the CLI scans and reports what would be dropped/coerced BEFORE the import; refusal exit 2 without --yes; structured loss_report in envelope data for agents; the scan is advisory — the gateway remains the parsing authority.
4. Collision-policy note: importTags on the gateway also supports Ignore ('i'); ign deliberately surfaces only abort/overwrite (the 05-05 lock).
5. Exit-table sync: NO new rows (invalid_input exists) — but verify the table still agrees (the readme_exit_table_agreement test enforces it; run it).
6. Streaming-exception documentation: extend the sanctioned-stdout-exceptions list (completions, logs -f, tags export json -o -) with tags export xml/csv -o - raw-bytes.
  </action>
  <verify>`cargo test -p ignition-cli readme_exit_table_agreement` (or the workspace equivalent) green; README rows render correctly in plain markdown; `cargo test --workspace` green</verify>
  <done>README states the CSV truth, the gate flow, and stays in machine-checked agreement with the exit taxonomy</done>
</task>

</tasks>

<verification>
- Default `ign tags export/import` invocations produce byte-identical stdout/files/requests to pre-phase (goldens prove it)
- `ign tags import --format xml FILE` without --yes on a loss-bearing file: exit 2, report on stderr, ZERO wire calls
- `--format csv` export: exit 0, warned losses, loss_report in envelope
- `cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --check` green
- stdout purity harness green (loss prose never on stdout)
</verification>

<success_criteria>
TAGS-12 is user-visible and agent-visible: the warning fires before the commit, abort is one missing flag away, and the honest CSV contract is written down where agents read it.
</success_criteria>

<output>
After completion, create `.planning/phases/11-tag-bulk-transfer-xml-csv/11-05-SUMMARY.md`
</output>
