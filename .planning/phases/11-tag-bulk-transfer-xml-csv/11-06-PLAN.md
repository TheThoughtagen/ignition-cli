---
phase: 11-tag-bulk-transfer-xml-csv
plan: 06
type: execute
wave: 5
depends_on: ["11-05"]
files_modified:
  - crates/ignition-cli/tests/e2e_webdev.rs
  - .planning/phases/11-tag-bulk-transfer-xml-csv/11-LIVE-GATE.md
autonomous: true

must_haves:
  truths:
    - "XML round-trip fidelity is proven on a LIVE multi-level UDT subtree from a real gateway: export file sha256 == route payload_b64 sha256 (transport fidelity) AND re-export-after-import sha256 == original sha256 (byte-identity oracle) — success criterion 1's live proof, not wiremock"
    - "CSV round-trip is proven live: generated CSV imports through importTagsFile and the re-exported JSON's field diff matches the documented lossy coverage table — success criterion 2"
    - "The loss gate refuses LIVE without --yes with zero gateway mutations, and proceeds with --yes — success criterion 3 on the real wire"
    - "All gates pass on BOTH rig generations (8.3.6 + 8.3.3) with the 1.2.0 bundle self-deployed, evidence recorded in 11-LIVE-GATE.md"
  artifacts:
    - path: "crates/ignition-cli/tests/e2e_webdev.rs"
      provides: "Env-gated live gates: xml fidelity round-trip, csv round-trip, loss-gate refusal (mutated-tag data OR --yes flows behind IGNITION_LIVE_MUTATIONS=1)"
      contains: "IGNITION_LIVE_MUTATIONS"
    - path: ".planning/phases/11-tag-bulk-transfer-xml-csv/11-LIVE-GATE.md"
      provides: "Recorded gate evidence: rig table, per-gate PASS/fail + timings, live-truth deltas, teardown state"
      contains: "PASS"
  key_links:
    - from: "live gate (new tests)"
      to: "ign webdev deploy"
      via: "the gate self-deploys the 1.2.0 bundle FIRST (live_webdev_deploy_status_scriptexec_loop pattern) — any new-format call before deploy hits the version-drift refusal"
      pattern: "deploy"
    - from: "sha256 A == sha256 B assertion"
      to: "the determinism answer from 11-LIVE-CAPTURES.md Probe 3"
      via: "byte-identity oracle if captures proved determinism; the recorded structural fallback otherwise"
      pattern: "sha256"
    - from: "11-LIVE-GATE.md evidence"
      to: "phase verification"
      via: "success criteria 1-3 close on the recorded runs, per the phase-9/10 gate-record discipline"
      pattern: "criteria"
---

<objective>
Prove the phase on real gateways: extend the env-gated live suite with the XML fidelity round-trip (the byte-identity oracle on a multi-level UDT subtree), the CSV round-trip with lossy-diff verification, and the live loss-gate refusal — then run everything on BOTH rigs and record the evidence.

Purpose: roadmap success criterion 1 explicitly demands "round-trip proven byte-faithful on a real multi-level UDT export from a live gateway, not just wiremock fixtures." Wiremock work (11-02/04/05) cannot close it; this plan can.
Output: three new live gates + recorded 11-LIVE-GATE.md evidence + the final full-workspace verification.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tag-bulk-transfer-xml-csv/11-RESEARCH.md
@.planning/phases/11-tag-bulk-transfer-xml-csv/11-LIVE-CAPTURES.md
@.planning/phases/09-agent-surface-api-diagnostics/09-RIG-NOTES.md
@crates/ignition-cli/tests/e2e_webdev.rs
</context>

<planner_locks>
- **Live-gate conventions** (e2e_webdev.rs header contract, verbatim): env-gated skip-by-default (`IGNITION_LIVE_URL` + `IGNITION_LIVE_TOKEN`, green no-op without them); anything that MUTATES the gateway additionally requires `IGNITION_LIVE_MUTATIONS=1`; the gate self-deploys the bundle first (the redeploy-sequencing lesson, research Pitfall 5).
- **Mutation safety**: the UDT subtree lives under a phase-namespaced path (`[default]P11Live...`) and cleanup deletes it in the gate's teardown; the scratch import target is its own folder. Refusal gates must prove ZERO mutations (pre-resolution live).
- **Oracle selection**: the byte-identity assertion follows 11-LIVE-CAPTURES.md Probe 3's determinism answer — byte-identity if the capture proved export determinism + import-reexport identity; otherwise transport-sha256 + structural comparison, per the recorded fallback. The gate code comments cite the capture.
- **No user setup**: rigs are disposable, images cached, recipe proven three times (09/10 and 11-01 this phase).
</planner_locks>

<tasks>

<task type="auto">
  <name>Task 1: Write the three live gates</name>
  <files>crates/ignition-cli/tests/e2e_webdev.rs</files>
  <action>
Extend e2e_webdev.rs following its established patterns (LiveEnv gathering, MUTATIONS gate helper, spawned-binary steps, cleanup-on-exit):

1. `live_tags_xml_fidelity_roundtrip` (MUTATIONS=1):
   a. Self-deploy the bundle (`ign webdev deploy`) — assert the version handshake reads 1.2.0 (the redeploy-sequencing pin).
   b. Seed a multi-level UDT structure via the CLI: UDT type `MotorType` under `[default]_types_` (parameter + OPC child with an alarm + expression child), then folder `P11Live` with two instances + nested `P11Live/Sub` third instance (mirror 11-01's probe-3 construction; config files via tempfile).
   c. `ign tags export [default]P11Live --format xml -o <tmp>/a.xml` → sha256(file) must equal sha256(decoded payload_b64) — capture the route response for the transport assertion (spawn with --json so the envelope is parseable, or assert via a second export to stdout; pick the cleaner and comment it).
   d. `ign tags import [default]P11LiveRoundtrip --file <tmp>/a.xml --format xml` WITHOUT --yes → expect exit 2 refusal, then assert the seeded subtree is UNCHANGED (zero-mutation proof via a read-back), then re-run WITH --yes → exit 0.
   e. `ign tags export [default]P11LiveRoundtrip/P11Live --format xml -o <tmp>/b.xml` — hmm, path discipline: import lands the subtree under the target basePath; export the imported top-level name and byte-compare against the ORIGINAL export per the oracle-selection lock (adjust the exported path to whatever the import actually landed — the gate asserts on the same logical subtree). Assert the determinism-capture-selected oracle (byte-identity A==B, or the structural fallback).
   f. Cleanup: deleteTags the seeded paths (P11Live, P11LiveRoundtrip) + the UDT type; assert cleanup.
2. `live_tags_csv_roundtrip` (MUTATIONS=1): export a seeded subtree as JSON → generate CSV via `ign tags export --format csv` (from a second seed set or reuse) → `ign tags import --format csv --yes` into a scratch folder → export JSON back → assert the imported model matches the documented lossy expectations (alarms absent, legacy-mapped fields landed) — the live diff IS the coverage table verification. Cleanup.
3. `live_tags_loss_gate_refusal` (read-only — NO mutations env needed): craft a small loss-bearing XML fixture inline (CompoundProperty present), attempt `ign tags import --format xml <fixture>` against the deployed gateway with NO --yes → assert exit 2 + report prose on stderr + ZERO route calls (nothing to read back — the pre-resolution refusal is provable by the absence of any envelope/error from the route layer and by asserting the seeded state untouched IF seeding ran; keep this gate self-contained: no seeding required, assert exit 2 + stderr only).
4. Every test reads env at start and returns quietly when absent (green no-op); `#[ignore = "opt-in e2e: ..."]` attributes matching the file's existing ones; doc-comment the environment table addition (same vars, no new ones).
  </action>
  <verify>`cargo test -p ignition-cli --test e2e_webdev` compiles green as a no-op suite (envs absent → all pass quietly); `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo fmt --check` clean</verify>
  <done>Three live gates exist, compile, and honor the env-gated conventions; no gate can touch a gateway without the explicit mutations opt-in (except the read-only refusal gate)</done>
</task>

<task type="auto">
  <name>Task 2: Run the gates on BOTH rigs + record evidence</name>
  <files>.planning/phases/11-tag-bulk-transfer-xml-csv/11-LIVE-GATE.md</files>
  <action>
Spin/commission/provision both rigs (11-01's Task-1 recipe, fresh containers ign-p11g-836 / ign-p11g-833 or the kept-alive 11-01 rigs if teardown was deferred — state which in the doc):
1. Rig A (8.3.6 @18188): run the full e2e_webdev ignored suite (the existing deploy/status/scriptexec gates + the three new ones) with URL/TOKEN/MUTATIONS=1. Record per-gate PASS with wall times.
2. Rig B (8.3.3 @19188): same.
3. Record in 11-LIVE-GATE.md: rig table (image/port/token NAME/commission timestamps), per-gate results, any live-truth delta discovered (e.g. XML bytes differing between 8.3.3/8.3.6, importTags quirks the probes didn't catch — record verbatim and cite which gate caught it), the sha256 values from the fidelity oracle (A and B), and the teardown state (torn down clean / kept alive).
4. If a gate FAILS on a live-truth delta: fix the CODE (never weaken the gate to pass silently unless the delta proves the assertion wrong — then update gate + captures doc together, citing evidence), re-run, record both the failure and the fix.
  </action>
  <verify>11-LIVE-GATE.md shows all gates PASS on both rigs with sha256 evidence; zero ign-p11* residue after teardown (docker ps / docker volume ls); the two-rig run commands are recorded verbatim in the doc</verify>
  <done>Success criteria 1-3 each have a recorded live PASS on both gateway generations; any version-behavior delta between 8.3.3/8.3.6 is documented</done>
</task>

<task type="auto">
  <name>Task 3: Final phase verification + success-criteria checklist</name>
  <files>.planning/phases/11-tag-bulk-transfer-xml-csv/11-LIVE-GATE.md</files>
  <action>
1. Full verification battery: `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --check`, lean build (`cargo build -p ignition-cli --no-default-features`), and the stdout-purity + readme-agreement suites specifically.
2. Append the phase success-criteria checklist to 11-LIVE-GATE.md, each criterion mapped to its closing evidence:
   - SC-1 (XML byte-faithful round-trip on a real multi-level UDT export, live) → live_tags_xml_fidelity_roundtrip PASS lines + sha256 A==B evidence
   - SC-2 (CSV transfer with documented lossy behavior) → live_tags_csv_roundtrip + README CSV-honesty section + coverage table
   - SC-3 (loss-report warning + abort before import) → live_tags_loss_gate_refusal + contract goldens + the gate's zero-mutation proof
3. Note the requirement mapping (TAGS-10/11/12) and anything deferred for the milestone (none expected; record if found).
  </action>
  <verify>All suites green in one run; the checklist in 11-LIVE-GATE.md names a concrete artifact per criterion; git log for the phase shows atomic per-task commits across all six plans</verify>
  <done>The phase is verifiably done: every roadmap success criterion has recorded, reproducible evidence — live where the roadmap demanded live</done>
</task>

</tasks>

<verification>
- Three new live gates exist in e2e_webdev.rs honoring the env-gated conventions (green no-ops without envs)
- 11-LIVE-GATE.md records BOTH-rig PASS evidence incl. sha256 fidelity numbers and teardown state
- `cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --check` green
- Roadmap SC-1/2/3 each map to recorded evidence in the checklist
</verification>

<success_criteria>
The phase closes on live-gateway proof: XML bytes round-trip byte-identically through a real multi-level UDT subtree, CSV moves with documented losses, and the loss gate refuses before committing — on both 8.3.3 and 8.3.6.
</success_criteria>

<output>
After completion, create `.planning/phases/11-tag-bulk-transfer-xml-csv/11-06-SUMMARY.md`
</output>
