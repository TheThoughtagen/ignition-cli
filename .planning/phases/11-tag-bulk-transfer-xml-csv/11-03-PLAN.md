---
phase: 11-tag-bulk-transfer-xml-csv
plan: 03
type: tdd
wave: 2
depends_on: ["11-01"]
files_modified:
  - Cargo.toml
  - crates/ignition-core/Cargo.toml
  - crates/ignition-core/src/actions/tag_loss.rs
  - crates/ignition-core/src/actions/mod.rs
  - crates/ignition-core/tests/fixtures/udt-multilevel.xml
autonomous: true

must_haves:
  truths:
    - "scan_xml counts Tag elements, collects type-attribute values, detects CompoundProperty (alarms), extracts top-level names and root attributes (MinVersion/locale), and flags a partial/advisory parse — it NEVER refuses a file the gateway might accept"
    - "scan_csv detects the # version=1 marker row, tallies columns seen, counts rows, flags numeric TagType/DataType/AccessRights enum coercions, and extracts top-level names from Path/Name — never refuses"
    - "Both scans are validated against the REAL captured UDT XML from the live gateway (11-01 fixture) — the roadmap's 'validate the loss-report design against real exports' flag is closed by a passing test over real bytes"
    - "quick-xml is added WITHOUT the `serialize` (serde-derive) feature — the deliberate rejection recorded in workspace deps"
  artifacts:
    - path: "crates/ignition-core/src/actions/tag_loss.rs"
      provides: "scan_xml + scan_csv pure functions + XmlScan/CsvScan/LossFact structs"
      exports: ["scan_xml", "scan_csv"]
      min_lines: 150
    - path: "crates/ignition-core/src/actions/mod.rs"
      provides: "pub mod tag_loss wiring"
      contains: "tag_loss"
    - path: "Cargo.toml"
      provides: "workspace.dependencies: quick-xml (no serialize feature) + csv with rationale comments"
      contains: "quick-xml"
    - path: "crates/ignition-core/tests/fixtures/udt-multilevel.xml"
      provides: "The real captured gateway XML, verbatim from 11-01"
  key_links:
    - from: "tests/fixtures/udt-multilevel.xml"
      to: "scan_xml tests"
      via: "include_str! fixture — the scan is validated on live-captured bytes, not hand-written samples"
      pattern: "include_str!"
    - from: "scan output (top_level_names)"
      to: "11-04 tags_import abort pre-check"
      via: "collision pre-check names come from the scan, not a re-parse"
      pattern: "top_level_names"
    - from: "LossFact entries"
      to: "11-05 loss report rendering"
      via: "the structured facts the CLI prints to stderr and embeds as envelope data.loss_report"
      pattern: "LossFact"
---

<objective>
Build the TAGS-12 loss-report scan as pure advisory functions in ignition-core: `scan_xml` (quick-xml Event-loop, NO serde) and `scan_csv` (csv crate), TDD'd with the real captured multi-level UDT XML from 11-01 as the anchor fixture.

Purpose: transfer is passthrough — parsing exists ONLY to warn. The scan must be lenient (never refuse a file the gateway would accept) and must know the documented lossy facts (CSV: no alarms, legacy 47 columns, numeric enums; XML: export-shape "edited properties only"). Red-Green discipline pins this behavior before the actions layer consumes it.
Output: tag_loss.rs with tested pure fns + the two new workspace dependencies (quick-xml 0.41 default features only, csv 1.x).
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tag-bulk-transfer-xml-csv/11-RESEARCH.md
@.planning/phases/11-tag-bulk-transfer-xml-csv/11-LIVE-CAPTURES.md
@.planning/phases/11-tag-bulk-transfer-xml-csv/artifacts/udt-multilevel.xml
</context>

<feature>
  <name>Loss-report advisory scans (scan_xml + scan_csv)</name>
  <files>crates/ignition-core/src/actions/tag_loss.rs, crates/ignition-core/src/actions/mod.rs, Cargo.toml, crates/ignition-core/Cargo.toml, crates/ignition-core/tests/fixtures/udt-multilevel.xml</files>
  <behavior>
    Pure functions over raw bytes; no I/O, no gateway, no network. Public shape (adjust field names only if the tests demand it — the REPORT CONTENT is the contract):

    ```rust
    pub struct LossFact { pub code: &'static str, pub detail: String }  // e.g. code "csv_no_alarms", "csv_legacy_columns_only", "csv_numeric_enum", "xml_export_edited_only", "xml_parse_partial"
    pub struct XmlScan { pub tag_count: usize, pub types_seen: Vec<String>, pub has_compound_property: bool, pub top_level_names: Vec<String>, pub root_attrs: Vec<(String, String)>, pub partial: bool, pub facts: Vec<LossFact> }
    pub struct CsvScan { pub has_version_marker: bool, pub columns: Vec<String>, pub row_count: usize, pub top_level_names: Vec<String>, pub numeric_enum_count: usize, pub facts: Vec<LossFact> }
    pub fn scan_xml(raw: &[u8]) -> XmlScan;
    pub fn scan_csv(raw: &[u8]) -> CsvScan;
    ```

    Cases (write ALL as failing tests first):

    scan_xml:
    - docs-sample XML (inline const, 11-RESEARCH.md §Code Examples verbatim: `<Tags MinVersion="8.0.0" locale="en_US">` + AtomicTag with CompoundProperty alarms) → tag_count 1, types_seen == ["AtomicTag"], has_compound_property true, top_level_names == ["Amps"], root_attrs contain MinVersion + locale, facts include the "export-shaped file carries only edited properties" advisory (code `xml_export_edited_only`, triggered by a MinVersion root attr).
    - THE REAL FIXTURE (include_str!("../tests/fixtures/udt-multilevel.xml") — note: the fixture lands at crates/ignition-core/tests/fixtures/, so the path from src/actions/ tests via include_str! is `../../tests/fixtures/udt-multilevel.xml`; prefer putting the scan unit tests in the same file's #[cfg(test)] mod and use the relative include path): parse SUCCEEDS (partial == false), tag_count > 0, has_compound_property true (alarms were seeded in 11-01), top_level_names non-empty and matching the UDT subtree created in 11-01. This test is the roadmap flag's closure — if the real bytes break the event loop, FIX THE SCAN, not the fixture.
    - truncated/malformed XML (inline const, e.g. `<Tags><Tag name="X"` cut mid-element) → partial == true, whatever was seen before the error is reported, NO Err return. Advisory posture: an unparseable file is the gateway's error to make.
    - empty input → zero-value scan, no panic.

    scan_csv:
    - docs-sample CSV (inline const with `# version=1` marker row + memory-tag row + folder row) → has_version_marker true, columns == the 47 documented legacy headers (assert a representative subset: Path, Name, TagType, DataType, AccessRights, UDTParentType, SourceTagPath), row_count 2 (data rows, marker row excluded), top_level_names derive from Path/Name (folder membership rides the `Path` column: `P11Csv/` prefix ⇒ top-level name `P11Csv`), facts include `csv_no_alarms` (UNCONDITIONAL on import — alarms cannot arrive in CSV) and `csv_legacy_columns_only`.
    - a row with numeric enums (TagType 0 / DataType 2) → numeric_enum_count > 0, facts include a `csv_numeric_enum` entry naming the column and the raw value.
    - CSV WITHOUT the marker row → has_version_marker false + a fact noting it (the gateway may reject; advisory).
    - embedded-newline cell (a quoted Expression value containing \n — the official sample's multi-line cell class) → parsed without breaking row_count (the csv crate owns RFC-4180 quoting; this test pins that hand-rolled parsing was NOT used).
    - empty input → zero-value scan, no panic.

    Both scans: deterministic, no allocation of parsed tag models — tallies and names only (the parse authority is the gateway).
  </behavior>
  <implementation>
    1. RED: add workspace deps first (quick-xml = "0.41" DEFAULT features only — an explicit comment in Cargo.toml records the serde-derive rejection: dynamic `<Property name="...">` attribute VALUES can't map to struct fields and a strict schema parser would refuse files the gateway accepts; csv = "1"). Copy 11-01's captured XML verbatim to crates/ignition-core/tests/fixtures/udt-multilevel.xml. Write every behavior case above as a failing test (`cargo test -p ignition-core tag_loss` → compile errors are the first RED; then assert failures). Commit: `test(11-03): add failing loss-scan tests over real UDT capture + docs samples`.
    2. GREEN: implement scan_xml as a quick-xml Reader event loop (research §Code Examples shape: read_event_into with a reused buf; match Event::Start|Event::Empty on b"Tag"/b"CompoundProperty"/b"Property"; track depth to distinguish TOP-LEVEL Tag elements from nested ones — top_level_names come from Tag elements directly under the root; on Err set partial=true and break). Implement scan_csv with the csv crate Reader (flexible records; first record = header row unless it starts with `# version=1`, in which case the marker row is skipped and the NEXT record is the header; rows tally from the header onward; numeric-enum detection = TagType/DataType/AccessRights column values that parse as integers). Add the facts per the behavior table. Commit: `feat(11-03): implement advisory xml/csv loss scans`.
    3. REFACTOR if needed (naming, fact-code constants), keeping tests green. Commit: `refactor(11-03): tidy loss-scan module` (only if something actually changed).
    Do NOT wire the scans into tags_import/dispatch — that's 11-04/11-05. No GatewayApi involvement: these are pure fns.
  </implementation>
</feature>

<planner_locks>
- **Library lock**: quick-xml 0.41.x Event-loop WITHOUT the serialize/serde feature + csv 1.x — both in ignition-core via workspace.dependencies with rationale comments (research Standard Stack; serde-derive REJECTED).
- **Advisory posture lock**: the scan NEVER returns a hard parse error for content it half-understands — partial facts ride the report; refusals are the gateway's job.
- `# version=1` marker row + numeric enums (TagType 0=OPC, 1=Expression-class, 2=Client, 6=Folder, 10=UDT Instance, 13=Derived; DataType 0..9) come from the official docs tables cited in 11-RESEARCH.md and the 11-01 probe-5 captures — use the captures where they differ from the docs.
</planner_locks>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Dependencies + fixture + failing scan tests</name>
  <files>Cargo.toml, crates/ignition-core/Cargo.toml, crates/ignition-core/tests/fixtures/udt-multilevel.xml, crates/ignition-core/src/actions/tag_loss.rs</files>
  <action>
Add the two workspace dependencies (root Cargo.toml [workspace.dependencies] with rationale comments, then crates/ignition-core/Cargo.toml references — the established single-sourcing pattern). Copy the 11-01 captured XML byte-verbatim into tests/fixtures/. Create tag_loss.rs with the struct skeletons + stub fns (todo!() or minimal defaults) and write ALL behavior cases from the feature block as tests inside the module's #[cfg(test)] mod + the fixture-driven test. Run and confirm RED (compile-then-assert failures).
  </action>
  <verify>`cargo test -p ignition-core tag_loss` FAILS (assertions, not just compile errors — the fixture test must reach its asserts); `cargo build --workspace` succeeds (deps resolve); git commit lands as the RED commit</verify>
  <done>Every behavior case exists as a failing test anchored on the real captured XML and the docs samples</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN + REFACTOR): Implement the scans until every test passes</name>
  <files>crates/ignition-core/src/actions/tag_loss.rs, crates/ignition-core/src/actions/mod.rs</files>
  <action>
Wire `pub mod tag_loss;` into actions/mod.rs. Implement scan_xml (quick-xml event loop, depth-tracked top-level Tag detection) and scan_csv (csv crate, marker-row handling, numeric-enum tally) per the implementation notes. Iterate until the full suite is green. If the REAL fixture exposes an event-loop gap (encodings, self-closing elements, namespace-ish attrs), fix the scan and note it — never edit the fixture. REFACTOR pass: extract fact-code constants, doc-comments on the advisory posture.
  </action>
  <verify>`cargo test -p ignition-core tag_loss` green (every behavior case); `cargo test --workspace` green; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo fmt --check` clean; GREEN + (optional) REFACTOR commits follow the TDD commit sequence</verify>
  <done>scan_xml and scan_csv are implemented, tested against REAL live-captured UDT XML, and never refuse partial input — ready for 11-04's import pre-check and 11-05's loss report</done>
</task>

</tasks>

<verification>
- `cargo test -p ignition-core tag_loss` green — including the real-fixture test (the roadmap flag's closure)
- quick-xml dependency has NO serialize feature (grep Cargo.toml + `cargo tree -p ignition-core -i quick-xml` shows default features)
- Scan output shapes match the contract the later plans consume: top_level_names, has_compound_property, facts[]
- Commits follow RED → GREEN → (REFACTOR) with the documented messages
</verification>

<success_criteria>
Pure advisory scans exist and are proven against live-gateway bytes. The loss-report's intelligence is pinned by tests before any consumer exists.
</success_criteria>

<output>
After completion, create `.planning/phases/11-tag-bulk-transfer-xml-csv/11-03-SUMMARY.md`
</output>
