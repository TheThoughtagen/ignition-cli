---
phase: 11-tag-bulk-transfer-xml-csv
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ignition-core/src/actions/tag_loss.rs
  - README.md
  - .planning/STATE.md
  - .planning/phases/11-tag-bulk-transfer-xml-csv/11-LIVE-CAPTURES.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "The xml_udt_type_definition fact text states live truth: definitions DO import via this command's provider-root path and route to [provider]_types_/Name; the verbatim refusal is a folder-basePath behavior this command cannot hit; cross-provider udtParentType stays source-qualified and instance parameter overrides drop when the target type is unresolvable (8.3.3; 8.3.6 preserves)"
    - "The fact still FIRES unconditionally for any file containing type=\"UdtType\" — detection unchanged, only the text corrected (existing has_fact pins pass without modification)"
    - "No live artifact repeats the falsified unconditional-refusal claim: the README loss-gate section, the two STATE.md decision lines, and the captures doc all carry scoped/corrected, dated wording"
  artifacts:
    - path: "crates/ignition-core/src/actions/tag_loss.rs"
      provides: "corrected fact detail + codes doc comment + test comment; code XML_UDT_TYPE_DEFINITION UNCHANGED (codes are stable consts, never renamed)"
      contains: "_types_"
    - path: "README.md"
      provides: "loss-gate section (TAGS-12) with scoped UDT-definition truth instead of 'refuses outright'"
      contains: "folder"
    - path: ".planning/STATE.md"
      provides: "dated [CORRECTED 2026-09-14, 11-08] markers appended to the two falsified decision lines (134/143), original text preserved"
      contains: "CORRECTED 2026-09-14"
    - path: ".planning/phases/11-tag-bulk-transfer-xml-csv/11-LIVE-CAPTURES.md"
      provides: "dated scope notes on §Probe 3 finding (b)(1) and oracle item 4, mirroring the Probe-5 correction precedent (line 245 format)"
      contains: "SCOPE-CORRECTED"
  key_links:
    - from: "tag_loss.rs fact detail"
      to: "11-LIVE-CAPTURES.md §Probe 3 scope note + .planning/debug/udt-type-fact-falsified.md"
      via: "the same conditioned truth in code, docs, and planning record"
      pattern: "folder-basePath"
---

<objective>
Close UAT test 4 (Phase 11): the `xml_udt_type_definition` loss fact claims importTags refuses UDT type definitions and "the import lands nothing" — falsified live. On the CLI's provider-root import path (the only form it exposes), definitions DO import and route to `[provider]_types_/Name`.

Purpose: the advisory fact is the agent-facing contract for UdtType-bearing imports; its text currently misleads every user/agent that reads it. The 11-01 capture was TRUE but scope-wrong (folder basePath only) — the correction must scope the claim, not delete it, and mirror everywhere the claim propagated.

Output: corrected fact text in tag_loss.rs (const/detection untouched) + mirrored corrections in README, STATE.md (dated), and 11-LIVE-CAPTURES.md (dated scope notes).
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/debug/udt-type-fact-falsified.md
@.planning/phases/11-tag-bulk-transfer-xml-csv/11-UAT.md
@.planning/phases/11-tag-bulk-transfer-xml-csv/11-LIVE-CAPTURES.md
@crates/ignition-core/src/actions/tag_loss.rs
@README.md

**Root cause (debug session, verified live ×3 on 8.3.6 + UAT):** the 11-01 probe-3(b) capture proved the verbatim refusal ONLY for FOLDER-basePath imports (`[default]P11Roundtrip`); the CLI always sends PROVIDER-ROOT basePath (`format!("[{provider}]")`, tags.rs:2210) where the gateway ACCEPTS UdtType definitions, routing each to `[provider]_types_/Name` regardless of position in the file. Overgeneralization path: 11-01-SUMMARY → STATE.md:134/143 → tag_loss.rs detail (172-180) → README ~1118. Required truth elements for the reword: (a) definitions DO import and land under `[provider]_types_/Name`; (b) the verbatim refusal ("Udt definitions can only be imported in the UDT Definitions tab") is folder-basePath-only (11-01 capture) and unreachable via this command; (c) exported `udtParentType` is provider-qualified — cross-provider imports keep pointing at the SOURCE provider — and instance parameter overrides silently drop when the referenced type is unresolvable in the target provider (8.3.3; 8.3.6 preserves).

**Binding constraints:** keep the fact FIRING unconditionally (detection is correct; the transfer genuinely isn't lossless — the caveats in (c) are real losses). Do NOT rename `codes::XML_UDT_TYPE_DEFINITION` (codes are stable consts). Do NOT edit historical SUMMARYs, the UAT file, or the debug sessions. No exit-slug, envelope, or gate-behavior change (the fact's firing and the gate are pinned and must stay green untouched).

**Doc-correction precedents:** captures doc line 245 — `**[CORRECTED live 2026-09-14 by the 11-06 gate, Rig A 8.3.6 — see 11-LIVE-GATE.md §Deltas]**` followed by the corrected truth, original text preserved (landed in commit 361d6b4). STATE.md precedent — corrections recorded as dated, source-cited annotations (STATE.md:131: "wording corrections live in the docs").

**Golden check (debug session):** grep found NO pin on the detail string outside tag_loss.rs itself (`lands nothing` appears only in tag_loss.rs plus unrelated CSV asserts at tags_contract.rs:1768-1769). Re-verify with grep before and after the edit.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reword the falsified fact (tag_loss.rs) — text only, const and detection untouched</name>
  <files>crates/ignition-core/src/actions/tag_loss.rs</files>
  <action>
Three text edits, zero behavioral change:

1. **Fact `detail`** (tag_loss.rs:175-177) — replace with (single line, watch the `\` line-continuation whitespace rules):

```rust
detail: "file contains UDT type definition(s) (type=\"UdtType\") — this import \
         DOES land them: provider-root imports route each definition to \
         [provider]_types_/Name regardless of its position in the file; the \
         gateway's refusal (\"Udt definitions can only be imported in the UDT \
         Definitions tab\") is a FOLDER-basePath behavior (11-01 capture) this \
         command cannot hit; real caveats: exported udtParentType is \
         provider-qualified so cross-provider imports keep pointing at the \
         SOURCE provider, and instance parameter overrides silently drop when \
         the referenced type is unresolvable in the target provider (8.3.3; \
         8.3.6 preserves)"
```

2. **`codes::XML_UDT_TYPE_DEFINITION` doc comment** (tag_loss.rs:27-30) — replace the two falsified lines with the scoped truth:

```rust
/// File contains `type="UdtType"` definition(s): they DO import via the CLI's
/// provider-root path, routed to `[provider]_types_/Name`; the verbatim
/// refusal ("Udt definitions can only be imported in the UDT Definitions
/// tab") is folder-basePath-only (11-01 probe 3) — unreachable via this
/// command. Fires unconditionally: the transfer is not lossless
/// (definition routing + cross-provider udtParentType / parameter-override
/// caveats — see the fact detail).
```

3. **Test comment** (tag_loss.rs:439-440) — replace "importTags REFUSES type definitions and lands nothing" with: "the scan must surface UdtType presence — the transfer is not lossless (definitions route to _types_; the folder-basePath refusal of 11-01 probe 3 is unreachable via the CLI's provider-root import)". The `has_fact` assertion (441) stays EXACTLY as-is.

4. Grep gates:
   - `rg -n "lands nothing|refuses outright|Udt definitions can only" crates/` after the edit → remaining hits ONLY: tags_contract.rs:1768/1769 (unrelated CSV capture asserts) and the two mentions inside the NEW detail text (the verbatim refusal quote is retained, scoped). No other source/test repeats the unconditional claim.
   - `rg -n "lands nothing|importTags refuses" crates/ignition-cli/tests/ crates/ignition-core/tests/` → no golden/snapshot pin on the old detail text (expected: none; if one surfaces, update THAT pin to the new text in this task and note it in the SUMMARY).
  </action>
  <verify>`cargo test -p ignition-core` green — the tag_loss suite including the untouched `has_fact(XML_UDT_TYPE_DEFINITION)` pins; `cargo clippy -p ignition-core -- -D warnings` clean; both grep gates pass.</verify>
  <done>The fact fires identically (detection pins untouched and green); its text states the live, conditioned truth with all three required elements (a/b/c).</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the correction — README loss-gate section, STATE.md dated markers, captures-doc scope notes</name>
  <files>README.md, .planning/STATE.md, .planning/phases/11-tag-bulk-transfer-xml-csv/11-LIVE-CAPTURES.md</files>
  <action>
1. **README.md §"The loss gate (imports, TAGS-12)"** (~1117-1121): replace the clause "UDT type definitions the gateway refuses outright," with the scoped truth, keeping the paragraph's flow and length similar:

```
… reports what the import would drop or coerce — UDT type definition
transfer caveats (definitions DO import on this command's provider-root
path, routing to `[provider]_types_/Name`; but exported `udtParentType`
stays provider-qualified, so cross-provider imports keep pointing at the
source provider, and instance parameter overrides silently drop when the
referenced type is unresolvable in the target provider on 8.3.3),
gateway-export-shaped files carrying only edited properties, and (for
CSV, unconditionally) the no-alarms/legacy-columns facts.
```

The README is the agent contract — state what the command actually does, not the folder-path behavior it cannot reach.

2. **.planning/STATE.md lines 134 and 143**: keep the original decision text and APPEND a dated correction marker to each (provenance over silent rewrites):

Line 134 appends:
` **[CORRECTED 2026-09-14, 11-08]**: the refusal is FOLDER-basePath-only ([default]P11Roundtrip capture condition); the CLI's provider-root import (tags.rs:2210) accepts UdtType definitions and routes them to [provider]_types_/Name — live-proven ×3 on 8.3.6 + the UAT import itself (.planning/debug/udt-type-fact-falsified.md)`

Line 143 appends:
` **[CORRECTED 2026-09-14, 11-08]**: the quoted 'refuses type definitions, lands nothing' claim is folder-basePath-only — provider-root imports land definitions in _types_ (see the 11-01 line correction above)`

3. **11-LIVE-CAPTURES.md** — dated scope notes in the line-245 precedent format (bold bracket marker, original text preserved):
   - After §Probe 3 finding (b)(1) (line 153), insert as a new paragraph:
     `**[SCOPE-CORRECTED live 2026-09-14 by the Phase-11 UAT — see .planning/debug/udt-type-fact-falsified.md]** This refusal was observed with a FOLDER basePath ([default]P11Roundtrip). PROVIDER-ROOT imports — the only form the shipped CLI exposes (\`tags_import_bulk\` sends \`[{provider}]\`, tags.rs:2210) — ACCEPT UdtType definitions, routing each to \`[provider]_types_/Name\` regardless of position in the file (live-proven ×3 on 8.3.6: type-only file, mixed fixture with target type absent, re-import with type present; plus the UAT import itself). The capture text above remains true for folder-basePath imports, which this command cannot express.`
   - On oracle item 4 (line 161), append:
     ` **[SCOPE NOTE 2026-09-14, 11-08]** the UdtType refusal behind this exclusion is folder-basePath-only; provider-root imports land definitions in \`[provider]_types_\`. The exclusion stays correct for the 11-06 oracle's design (instance-only seeded XML + config-surfaced type) — the fidelity guarantee never exercised the folder path.`

4. Do NOT touch: 11-01-SUMMARY.md / 11-03-SUMMARY.md (historical execution records), 11-UAT.md, the debug sessions, the ROADMAP.
  </action>
  <verify>`rg -n "refuses outright" README.md .planning/` → zero hits (the only occurrence was README ~1119); `rg -n "lands nothing" .planning/STATE.md` → only inside corrected-marker context or zero hits; `cargo test --workspace` still green — especially `readme_exit_table_agreement` (include_str! parses a SECTION-scoped README region; this edit is in the loss-gate prose section, but prove it) and the tag_loss pins.</verify>
  <done>No live artifact repeats the unscoped claim; all three corrections are dated, scoped, and traceable to the debug session; the frozen README exit table and all code pins are untouched and green.</done>
</task>

</tasks>

<verification>
1. `cargo test --workspace` — all green (tag_loss detection pins untouched; README agreement tests unaffected).
2. `rg -n "refuses outright" README.md .planning/` — zero hits.
3. `rg -n "XML_UDT_TYPE_DEFINITION" crates/` — the const name is byte-identical everywhere (codes are stable).
4. The UAT gap's truth is testable by a human: `ign tags import --format xml <udt-fixture>` shows the fact with the corrected text; `--yes` lands the import (already live-proven — no live-rig work required for THIS plan; the phase re-verification re-runs UAT tests 3/4).
</verification>

<success_criteria>
- The advisory fact reads as true live guidance on the only import path the CLI exposes, and the verbatim refusal is correctly attributed to folder-basePath imports.
- README, STATE.md, and the captures doc carry the same scoped truth with dated provenance.
- Fact code, firing behavior, gate behavior, and all detection pins are byte-identical to before.
</success_criteria>

<output>
After completion, create `.planning/phases/11-tag-bulk-transfer-xml-csv/11-08-SUMMARY.md`
</output>
