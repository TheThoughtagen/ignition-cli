---
phase: 11-tag-bulk-transfer-xml-csv
plan: 04
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - Cargo.toml
  - crates/ignition-core/Cargo.toml
  - crates/ignition-core/src/actions/tags.rs
  - crates/ignition-core/tests/tags_contract.rs
autonomous: true

must_haves:
  truths:
    - "tags_export with format=xml decodes the route's payload_b64 and returns RAW gateway bytes — zero parse/normalize/re-serialize between the envelope and the caller (json path byte-identical to today)"
    - "tags_import with format=xml|csv reads the input file (or stdin) as RAW bytes, base64-encodes them, and sends them verbatim to the route's importTagsFile — the CLI-side CSV→JSON conversion trap is structurally absent"
    - "The abort collision pre-check for xml/csv imports uses scan-derived top-level names against a target browse — the 05-05 zero-write pre-check shape, fed by tag_loss scans"
    - "generate_legacy_csv converts the JSON interchange to the legacy 47-column CSV with the # version=1 marker row and numeric enums, dropping/coercing exactly per the 11-01 coverage table"
    - "The exportTags route call carries an explicit timeout override (300s, pinned by unit test at birth — the 09-05 BUNDLE_DOWNLOAD_TIMEOUT pattern) so large exports cannot die at the 30s client default"
  artifacts:
    - path: "crates/ignition-core/src/actions/tags.rs"
      provides: "ExportFormat, format-aware tags_export/tags_import, generate_legacy_csv"
      contains: "generate_legacy_csv"
    - path: "Cargo.toml"
      provides: "workspace base64 dependency"
      contains: "base64"
    - path: "crates/ignition-core/tests/tags_contract.rs"
      provides: "Request pins for format=xml export + importTagsFile action calls + csv-generation fixtures"
  key_links:
    - from: "tags_export(format: Xml)"
      to: "route payload_b64"
      via: "base64 decode → raw bytes; sha256 asserted equal in tests — the transport-fidelity seam"
      pattern: "payload_b64"
    - from: "tags_import(format: Xml|Csv)"
      to: "route importTagsFile"
      via: "input bytes → base64 → file_b64 — bytes reach the gateway's own parser unmutated"
      pattern: "importTagsFile"
    - from: "tags_import abort pre-check"
      to: "tag_loss::scan_xml/scan_csv top_level_names"
      via: "scan feeds the effective-names browse comparison (no re-parse)"
      pattern: "top_level_names"
    - from: "generate_legacy_csv"
      to: "11-LIVE-CAPTURES.md §CSV-Coverage"
      via: "the generator's drop/coerce behavior matches the live-proven coverage table"
      pattern: "csv_no_alarms|coverage"
---

<objective>
Build the core transfer layer: format-aware `tags_export` (XML = raw-byte passthrough OUT) and `tags_import` (XML/CSV = raw-byte passthrough IN via importTagsFile, with the scan-fed abort pre-check), plus `generate_legacy_csv` — the one sanctioned CLI-side conversion (gateway cannot export CSV; official docs), honest about every dropped field.

Purpose: this is the byte-faithfulness seam. Everything the CLI shows the user (11-05) and the live gate proves on the rig (11-06) rides these actions. The invariant: no codec in the transfer path — base64 is the only transformation, and it is byte-exact by construction.
Output: format-aware actions + csv generation + wiremock pins + the 300s export timeout.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tag-bulk-transfer-xml-csv/11-RESEARCH.md
@.planning/phases/11-tag-bulk-transfer-xml-csv/11-LIVE-CAPTURES.md
@crates/ignition-core/src/actions/tags.rs
@crates/ignition-core/tests/tags_contract.rs
</context>

<planner_locks>
- **Passthrough lock (binding, milestone anti-scope)**: XML download = decode payload_b64, write bytes. XML/CSV upload = read bytes, encode file_b64, send. NO XML/CSV parsing in either direction (parsing exists only in tag_loss for the advisory report). CLI-side CSV→JSON conversion on import is forbidden (research Anti-Patterns).
- **JSON behavior byte-identical**: format=Json keeps today's parse→normalize→pretty→trailing-newline pipeline untouched. XML/CSV file mode writes NO trailing newline (research Pitfall 3 — the sha256 oracle breaks otherwise). stdout-mode raw-bytes handling lands in 11-05's render; these actions return the decoded payload through the result struct.
- **CSV generation lock (Open Question #1 resolution)**: `--format csv` download is CLI-generated from the gateway's JSON interchange and is DOCUMENTED lossy — no alarms, legacy columns only, numeric enums. The generator's fidelity claims must match 11-LIVE-CAPTURES.md §CSV-Coverage exactly.
- **base64 crate** added to workspace deps (engine::general_purpose::STANDARD); Jython side already used base64 in 11-02.
- **Collision matrix**: abort (pre-check) / overwrite (no pre-check) only — the 03-02/05-05 lock, unchanged for new formats.
</planner_locks>

<tasks>

<task type="auto">
  <name>Task 1: tags_export format param — XML raw-byte passthrough + export timeout</name>
  <files>Cargo.toml, crates/ignition-core/Cargo.toml, crates/ignition-core/src/actions/tags.rs, crates/ignition-core/tests/tags_contract.rs</files>
  <action>
1. Add `base64 = "0.22"` to workspace.dependencies (comment: byte-faithful carrier through the JSON envelope, research Pattern 1) + ignition-core's Cargo.toml.
2. Add `pub enum ExportFormat { Json, Xml }` (clap-value derivation happens in 11-05; core keeps a FromStr or plain enum). Extend `tags_export` (tags.rs:1691) with a `format: ExportFormat` parameter:
   - `Json`: EXACTLY today's body — unchanged (existing unit tests + wiremock pins keep passing untouched).
   - `Xml`: call `webdev_route_call` with body `{"action":"exportTags","paths":...,"format":"xml"}`; read `data.payload_b64` (missing key → Internal honesty error naming the route, mirroring the existing payload-missing arm); `base64::decode` → raw bytes. File mode (`out: Some(path)`): `std::fs::write(path, &raw)` — NO trailing newline, no transformation; stdout mode: carry the decoded payload in the result (TagsExportResult gains `format` + the raw string for xml; render decides byte-writing in 11-05). Result tag_count for xml comes from the envelope (the route doesn't count — set tag_count from a quick tag_loss::scan_xml tally, reusing 11-03 — do NOT build a JSON model).
3. **Export timeout (research Pitfall 6, the 09-05 pattern)**: check `webdev_route_call`'s timeout posture; parameterize the exportTags call site with `TAGS_EXPORT_TIMEOUT: Duration = Duration::from_secs(300)` applied via the per-request override mechanism (mirror how logs_download got its 120s and download_to_file its 300s), with a unit test at birth pinning the constant's value.
4. Wiremock pins in tags_contract.rs: format=xml request pin (asserts the recorded request carried `format:"xml"` + paths) responding with a canned payload_b64; assert decode(file bytes) == canned XML exactly (the transport-fidelity sha256/byte-equality assertion).
  </action>
  <verify>`cargo test -p ignition-core tags_export` green (json tests untouched + new xml pins); byte-equality assertion proves decode == canned bytes; `cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --check` green</verify>
  <done>XML export decodes gateway bytes with zero transformation; JSON export byte-identical to today; the 300s timeout is pinned by a unit test</done>
</task>

<task type="auto">
  <name>Task 2: tags_import format param — XML/CSV byte-faithful upload via importTagsFile</name>
  <files>crates/ignition-core/src/actions/tags.rs, crates/ignition-core/tests/tags_contract.rs</files>
  <action>
1. Add `ImportFormat { Json, Xml, Csv }`. Extend `tags_import` (tags.rs:1813) with `format: ImportFormat` and change the payload parameter to a raw-bytes source (`input: &std::path::Path` or pre-read bytes — the CLI reads stdin/file in dispatch in 11-05; core accepts `&[u8]`):
   - `Json`: EXACTLY today's body (parse JSON → effective_top_level_names → browse pre-check → configure) — unchanged.
   - `Xml|Csv`: (a) scan the bytes with `tag_loss::scan_xml`/`scan_csv` → `top_level_names` (the scan output REPLACES a re-parse; store the scan in the result for 11-05's report); (b) the abort pre-check reuses the existing browse-`[provider]`-and-compare shape with scan-derived names (`_types_` structural exemption unchanged); (c) call `webdev_route_call` with `{"action":"importTagsFile","file_b64":base64::encode(bytes),"basePath":"[{provider}]","collisionPolicy":"a"|"o"}`; (d) parse `data.results` QualityCode strings with the existing parse_results helper; imported count = scan top_level_names.len().
   - Return TagsImportResult extended with `format` + the scan summary fields 11-05 needs (facts + names — keep it additive; envelope data grows new keys, never renames).
2. basePath form: `[{provider}]` provider-root — NOTE 11-01 Probe 2 recorded whether importTags tolerates provider-root from a route/script thread; if the captures show provider-root refusing (the RpcContext class), target `[{provider}]` anyway in core but let the route's provider_root_unsupported translation surface honestly, and record the constraint in the result/docs handoff for 11-05. Cite the captures in a code comment either way.
3. Wiremock pins: importTagsFile REQUEST pin (file_b64/basePath/collisionPolicy verbatim, `expect(1)`), abort pre-check pins (browse → collision refusal → ZERO importTagsFile calls — the zero-write proof), overwrite no-pre-check pin, provider_root_unsupported error-envelope pin.
  </action>
  <verify>`cargo test -p ignition-core tags_import` green; the zero-write collision-refusal pin proves no importTagsFile call on abort-collision; base64 round-trip pin proves bytes verbatim; full workspace suite + clippy + fmt green</verify>
  <done>XML/CSV uploads carry input bytes unmutated to the gateway's parser; the collision matrix is enforced with the scan as the name source; scan output rides the result for the loss report</done>
</task>

<task type="auto">
  <name>Task 3: generate_legacy_csv — the honest JSON-interchange → legacy CSV conversion</name>
  <files>crates/ignition-core/src/actions/tags.rs, crates/ignition-core/tests/tags_contract.rs</files>
  <action>
Implement `pub fn generate_legacy_csv(subtrees: &[serde_json::Value]) -> Result<Vec<u8>, CoreError>` (pure fn in tags.rs, unit-tested):
1. Header row: the exact 47 legacy columns from the official docs table (11-RESEARCH.md §Legacy CSV skeleton has the verbatim column list — use it byte-for-byte), then the `# version=1,,,,,,,,,` marker row.
2. Row emission from the JSON interchange subtrees (the tags export shape): folder membership rides the `Path` column (nested folders build slash-prefixed paths, trailing `/`); UDT TYPE definitions emit as `_types_` rows (Path `_types_/`, TagType 13 per the docs enum — verify against the 11-01 captures); UDT INSTANCES emit TagType 10 with UDTParentType = the instance's `type` and SourceTagPath empty; leaf tags map: TagType numeric per the docs enum (AtomicTag→by valueSource: opc=0, expression/query/db=1, client=2; Folder=6), DataType numeric (docs 0..9 table — map the JSON string forms: Boolean/Int4/Float8/etc.), AccessRights from the JSON value or default Read_Write, Enabled TRUE/FALSE, Value verbatim.
3. LOSSY handling (the honesty core): fields with NO legacy column (tagGroup, tag groups bindings, modern alarm configs, UDT parameters beyond SourceTagPath, value bindings beyond OPC writeback) are DROPPED — return them in the result (`CsvGenerationReport { rows, dropped_keys: Vec<String>, coerced: Vec<String> }`) so 11-05 can warn-and-continue truthfully; the dropped-keys tally must match 11-LIVE-CAPTURES.md §CSV-Coverage — reconcile any delta in favor of the captures.
4. Write via the `csv` crate (embedded newlines/commas/quotes in Expression/Documentation cells are exactly what the crate owns — hand-rolling is the research-rejected trap).
5. Unit tests: a fixture JSON interchange (folder + memory + opc + expression + UDT type + instance — mirror the probe-5 rig CSV) → expected CSV bytes; an alarms-bearing tag → alarms dropped + reported in the report; the numeric-enum assertions (TagType/DataType cells are integers, not strings).
  </action>
  <verify>`cargo test -p ignition-core generate_legacy_csv` green including the lossy-drop report assertions; csv crate quoting proven by the embedded-newline fixture; full workspace suite + clippy + fmt green</verify>
  <done>The CSV generator produces marker-rowed, numeric-enum, 47-column legacy files and reports every field it dropped or coerced — matching the live-proven coverage table</done>
</task>

</tasks>

<verification>
- Byte-fidelity seams proven at the unit layer: decode(payload_b64) == canned bytes; file_b64 round-trips input bytes; ZERO parse calls in either transfer path (grep: tags.rs transfer arms contain no quick_xml/csv Reader usage — csv Reader appears ONLY inside scan_csv and generate_legacy_csv)
- `cargo test --workspace` + clippy -D warnings + fmt --check green
- Wiremock REQUEST pins assert format/file_b64/basePath/collisionPolicy on the wire
- JSON paths (export + import) produce byte-identical requests/responses to pre-phase pins
</verification>

<success_criteria>
The core layer moves bytes both ways with the gateway as the only semantic actor, and generates legacy CSV honestly (documented, reported losses). Ready for the CLI contract layer.
</success_criteria>

<output>
After completion, create `.planning/phases/11-tag-bulk-transfer-xml-csv/11-04-SUMMARY.md`
</output>
