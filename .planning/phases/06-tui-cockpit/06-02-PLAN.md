---
phase: 06-tui-cockpit
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - crates/ignition-tui/src/lib.rs
  - crates/ignition-tui/src/ui/dashboard.rs
  - crates/ignition-tui/src/ui/profiles.rs
  - crates/ignition-tui/src/workers/refresh.rs
  - crates/ignition-tui/src/state.rs
  - crates/ignition-tui/src/update.rs
  - crates/ignition-tui/src/event.rs
  - crates/ignition-tui/src/routes.rs
autonomous: true
must_haves:
  truths:
    - "User sees status, modules, metrics, and sessions panels updating every 5 seconds without any keystroke"
    - "Panels show Loading/Loaded/Error states — a dead gateway shows errors, never a frozen or blank UI"
    - "User can trigger version / wait gateway|restart|module / doctor / restart from a dashboard actions menu and see results in a scrollable modal"
    - "User can switch profiles from within the TUI (p key) and all workers re-target the new gateway; stale-era results from the old profile are dropped"
    - "Sessions panel allows selecting a session and terminating it via a confirm modal (modal accept ≡ --yes)"
  artifacts:
    - path: crates/ignition-tui/src/ui/dashboard.rs
      provides: "Four-panel dashboard render (status/modules/metrics/sessions) + actions menu + result modal content"
      min_lines: 60
    - path: crates/ignition-tui/src/ui/profiles.rs
      provides: "Profile switcher modal (list + add form) render"
    - path: crates/ignition-tui/src/workers/refresh.rs
      provides: "Interval refresh worker composing inspect::status/modules/metrics + sessions::sessions"
      contains: "actions::"
    - path: crates/ignition-tui/src/event.rs
      provides: "Refresh(Snapshot) + ProfileChanged(String) + ActionDone variants"
  key_links:
    - {from: "crates/ignition-tui/src/workers/refresh.rs", to: "ignition-core actions", via: "free fns over &dyn GatewayApi", pattern: "(inspect|sessions)::(status|modules|metrics|sessions)\\(&"}
    - {from: "crates/ignition-tui/src/ui/profiles.rs", to: "profile switch", via: "sync use_profile call in update", pattern: "profile::use_profile"}
    - {from: "state era", to: "worker results", via: "stale-era drop", pattern: "era"}
    - {from: "dashboard actions menu", to: "guarded verbs", via: "confirm modal before worker spawn", pattern: "Modal::Confirm"}
---

<objective>
First live screen: the status dashboard (status/modules/metrics/sessions panels, 5s periodic refresh via a worker task), a dashboard actions menu hosting the global verbs (version, wait×3, doctor, restart), session terminate off the sessions panel, and the in-TUI profile switcher with era-based worker re-targeting.

Purpose: Delivers Success Criterion 2 (live dashboard + profile switch) and establishes the two patterns every remaining screen copies: the interval/poll worker and the one-shot action + result-modal flow.
Output: A dashboard you can leave open against a live gateway; profile switch re-spawns workers against the new client.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tui-cockpit/06-01-SUMMARY.md
@crates/ignition-core/src/actions/inspect.rs
@crates/ignition-core/src/actions/sessions.rs
@crates/ignition-core/src/actions/version.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh worker + dashboard panels with async states</name>
  <files>crates/ignition-tui/src/lib.rs, crates/ignition-tui/src/workers/refresh.rs, crates/ignition-tui/src/ui/dashboard.rs, crates/ignition-tui/src/state.rs, crates/ignition-tui/src/update.rs, crates/ignition-tui/src/event.rs</files>
  <action>
  - Add `AppEvent::Refresh(Snapshot)` to event.rs; `Snapshot` (status/modules/metrics/sessions as `Option<T>` per RESEARCH Pattern 2 — per-call `.ok()` so one failing endpoint degrades a panel, not the whole dashboard) lives in workers/refresh.rs.
  - `workers/refresh.rs`: `pub async fn refresh_worker(api: Arc<ReqwestGatewayApi>, tx: mpsc::UnboundedSender<AppEvent>, mut shutdown: watch::Receiver<bool>, era: u64, period: Duration)` — LOCKED period 5s. Compose the existing action fns AS-IS (`inspect::status(&*api)`, `inspect::modules(&*api, false)`, `inspect::metrics(&*api, ...)` — check the real signature for the second arg, `sessions::sessions(&*api, None)`); send `Refresh(snap)` each tick; `select!` against the shutdown watch so profile-switch/screen teardown stops it; exit when `tx.send` errors. The worker is spawned by lib.rs/update on startup — add the spawn site + `dashboard_busy` guard so keystrokes can't stack duplicate refreshes.
  - state.rs: `DashboardData { snapshot: Option<Snapshot>, last_refresh: Option<Instant>, per-panel error strings }`; wire into AppState.
  - ui/dashboard.rs (replace 06-01's placeholder): four panels in a 2x2-ish layout — status (gateway version, uptime, cpu, memory essentials), modules (table: name/state/license), metrics (key gauges), sessions (table: id/type/user, selectable rows via TableState). Every panel renders its own Loading/Loaded/Error state (RESEARCH async-github LoadingState pattern) — never blank, never blocking. Keep formatting minimal and derived from the typed results; serde-pretty is NOT needed here (human summary fields).
  - update.rs: `Refresh(snap)` arm stores snapshot + clears busy + records last_refresh; render shows "N s ago" from the tick clock (250ms tick makes this live).
  - Tests: TestBackend render of a fixture-snapshot dashboard (assert panel titles + a known module/session row); update unit (Refresh fills DashboardData, busy clears); worker unit with a FakeApi-style double if one exists in core tests (else a stub via `for_tests` constructor — check client/mod.rs) proving shutdown watch terminates the loop.
  </action>
  <verify>cargo test -p ignition-tui && cargo clippy --workspace --all-targets -- -D warnings</verify>
  <done>Dashboard renders four populated panels from a fixture snapshot; refresh worker composes the four action fns unmodified; shutdown watch ends the worker; busy guard prevents stacking.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard actions menu + one-shot result modal + session terminate</name>
  <files>crates/ignition-tui/src/ui/dashboard.rs, crates/ignition-tui/src/update.rs, crates/ignition-tui/src/state.rs</files>
  <action>
  - Actions menu (`a` key on Dashboard): list the global verbs — `version`, `connections` (one-shot read of the 02-03 connections family — database+opc lists — via the existing actions fn AS-IS; result modal), `wait gateway`, `wait restart`, `wait module`, `doctor`, `restart`. Selecting one spawns a one-shot worker (the pattern: `tokio::spawn` with current era + client Arc → call the action fn → send an ActionDone event). `wait` variants are long-running (poll.rs backs them with deadlines) — they run in the worker with NO UI block; a subtle "running: wait gateway" status line shows while in flight (busy guard per action). `restart` is `--yes`-guarded in the CLI (main.rs:566) — selecting it opens the Confirm modal FIRST; accept ≡ `--yes` (the TUI owns its own confirmation; the action fn itself is called unguarded), cancel spawns nothing.
  - LOCKED result display: the result modal renders `serde_json::to_string_pretty(&result)` of the typed action result in a scrollable pane (PgUp/PgDn) — one mechanism for every future action, no per-verb formatting this phase.
  - Sessions panel terminate: `t` (or Enter) on a selected session opens Confirm (guarded verb — main.rs:404), accept spawns `sessions::terminate_session`. Result modal shows the outcome; next refresh reflects it.
  - Add routes.rs rows for the paths these cover: `version`, `status`, `modules`, `metrics`, `connections`, `sessions` (BARE form — SessionsArgs.command is Option<SessionsCmd> at cli.rs:835, so bare `ign sessions` IS the list action; there is no `sessions list` leaf), `sessions terminate` → Screen(Dashboard). Verify the exact leaf-path spellings against the clap tree — walk `Cli`'s Subcommand enums in cli.rs and use the real names.
  - Tests: update units — confirm-cancel spawns nothing (state has no in-flight action), accept moves to in-flight, ActionDone closes to result modal; TestBackend render of the actions menu + result modal with a small pretty-JSON fixture.
  </action>
  <verify>cargo test -p ignition-tui</verify>
  <done>Global verbs triggerable from the dashboard with confirm-gating on restart and terminate; results in a scrollable pretty-JSON modal; long waits never block input; routes rows added for covered leaves.</done>
</task>

<task type="auto">
  <name>Task 3: Profile switcher modal (use_profile + client rebuild + era bump)</name>
  <files>crates/ignition-tui/src/ui/profiles.rs, crates/ignition-tui/src/update.rs, crates/ignition-tui/src/state.rs, crates/ignition-tui/src/event.rs, crates/ignition-tui/src/routes.rs</files>
  <action>
  - `p` key (any screen) opens the profile switcher modal: list from `config::load(config::config_path()).profiles` keys with the active one marked. Selecting a profile runs the LOCKED switch sequence synchronously in update() (all local, milliseconds — file I/O is not gateway I/O): `actions::profile::use_profile(config_path, name)` (sync fn — persists active) → `context::rebuild(name)` → bump `state.era` → signal shutdown to old workers → re-spawn refresh worker with the new Arc → `AppEvent::ProfileChanged(name)` banner ("profile: NAME" in the status line). A failed rebuild shows the Error modal and KEEPS the old client/era (switch is atomic from the user's perspective).
  - Profile add inside the modal (`a`): small Input-modal form for the common fields (name, url; leave auth refs to the CLI form per the LOCKED modal-depth decision — show "ign profile add --help equivalent" hint line). Calls `actions::profile::add` (check its real signature in actions/profile.rs), refreshes the list. Add routes rows: `profile use` → Screen(profiles modal), `profile list` → Screen(profiles modal), `profile add` → Screen(profiles modal) (exact leaf spellings from cli.rs ProfileCmd).
  - Workers re-target proof: era test — spawn (simulated) worker with era N, bump state era to N+1, assert `is_current` false and update drops a Refresh event stamped with the old era (Pitfall 9 — no log lines from the previous profile).
  - Tests: update units for the switch state machine (happy path bumps era + respawns; failed rebuild keeps old era); era-drop unit; TestBackend render of the modal listing two profiles with active marker.
  </action>
  <verify>cargo test -p ignition-tui && cargo clippy --workspace --all-targets -- -D warnings</verify>
  <done>Profile switch works end-to-end in state-machine terms: persisted active profile, rebuilt client, era bump, workers re-spawned, stale events dropped; profile family routes rows present.</done>
</task>

</tasks>

<verification>
- cargo test --workspace green; clippy -D warnings clean
- Against a live rig (optional, #[ignore]-style or manual): `ign tui` shows live-refreshing panels; `p` switches profile and panels re-populate from the new gateway
- grep workers/refresh.rs calls only action fns (no client re-implementation)
</verification>

<success_criteria>
TUI-02 and the profile half of TUI-06 delivered: live dashboard with periodic refresh, non-blocking async states, global verbs reachable, guarded verbs confirm-gated, profile switch with clean worker re-targeting.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tui-cockpit/06-02-SUMMARY.md`
</output>
