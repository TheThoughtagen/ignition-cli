---
phase: 06-tui-cockpit
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - crates/ignition-tui/src/ui/tags.rs
  - crates/ignition-tui/src/workers/watch.rs
  - crates/ignition-tui/src/state.rs
  - crates/ignition-tui/src/update.rs
  - crates/ignition-tui/src/event.rs
  - crates/ignition-tui/src/routes.rs
autonomous: true
must_haves:
  truths:
    - "User can browse the tag tree provider-by-provider and open a tag's detail / current value"
    - "User can toggle live watch on selected tags and see values/qualities update periodically in a watch table"
    - "User can write a tag value from the TUI (JSON-scalar rule surfaced in the input hint)"
    - "Provider/config/export/import tag verbs are reachable from the Tags screen actions menu, with confirm modals on the guarded ones (provider delete, config delete, import overwrite)"
  artifacts:
    - path: crates/ignition-tui/src/ui/tags.rs
      provides: "Tag tree browser + detail pane + live-watch table + tags actions menu"
      min_lines: 80
    - path: crates/ignition-tui/src/workers/watch.rs
      provides: "tag watch worker: interval tags_read poll over the watched path set (2s)"
      contains: "tags_read"
  key_links:
    - {from: "crates/ignition-tui/src/workers/watch.rs", to: "tag values", via: "interval tags_read over watched set", pattern: "tags_read\\(&"}
    - {from: "crates/ignition-tui/src/ui/tags.rs", to: "browse action", via: "one-shot worker", pattern: "tags_browse"}
    - {from: "watch toggle", to: "worker path set", via: "watch set state -> worker param", pattern: "watched"}
---

<objective>
The Tags screen: tree browsing via `tags_browse`, on-demand reads, a live-watch table (2s `tags_read` poll over the watched set), value writes with the JSON-scalar rule, and the full tags actions menu (providers create/delete, config edit, export/import) with confirm-gating matching the CLI's --yes set.

Purpose: Delivers TUI-04 (browse + live watch) and completes the tags family's registry coverage — the largest CLI family — reusing 06-02's one-shot/result-modal and 06-03's poll-worker patterns.
Output: One new screen with live data; routes rows for every tags leaf except alarms (done in 06-03).
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tui-cockpit/06-01-SUMMARY.md
@.planning/phases/06-tui-cockpit/06-03-SUMMARY.md
@crates/ignition-core/src/actions/tags.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tag browser — providers, tree browse, detail + read</name>
  <files>crates/ignition-tui/src/ui/tags.rs, crates/ignition-tui/src/state.rs, crates/ignition-tui/src/update.rs, crates/ignition-tui/src/event.rs</files>
  <action>
  - Object-list → detail navigation (k9s style, RESEARCH Pattern 4): screen opens with a provider list (the providers listing — check tags.rs for the native-REST providers list fn; the CLI's `tags providers list` equivalent), Enter descends into that provider's tag tree via `tags_browse` (its fullPath tree — render as an indented path list exactly like the CLI's human browse mode conceptually), Enter on a tag opens the detail pane, Esc ascends level by level.
  - Detail pane: node info + on-demand current value (`tags_read` one-shot over the selected path) shown as value/quality/timestamp. Loading/Error states per 06-02's pattern; require_routes denials surface as the Error state with the action's hint text (route preconditions are handled INSIDE the actions layer — TUI inherits for free).
  - state.rs: `TagsData { providers, tree: browse result, selection path stack, detail: Loading/Loaded/Error }`; TableState/ListState per list (per RESEARCH Pattern 4).
  - Tests: update units for descend/ascend navigation state machine; TestBackend render of a seeded tree + detail pane (assert a known fullPath row).
  </action>
  <verify>cargo test -p ignition-tui</verify>
  <done>Provider list → tree browse → tag detail + on-demand read all navigate with Enter/Esc and render seeded fixtures headlessly.</done>
</task>

<task type="auto">
  <name>Task 2: Live watch table</name>
  <files>crates/ignition-tui/src/workers/watch.rs, crates/ignition-tui/src/ui/tags.rs, crates/ignition-tui/src/state.rs, crates/ignition-tui/src/update.rs, crates/ignition-tui/src/event.rs</files>
  <action>
  - `w` on a selected tag toggles it into the watched set (state: `BTreeSet<String>` of paths; visual marker on watched rows). While the set is non-empty, a watch worker runs: `pub async fn tag_watch_worker(api, tx, shutdown, era, paths, period=2s)` in workers/watch.rs — interval `tags_read(&*api, provider, paths)` over the WHOLE watched set (check the action's provider param shape), send `AppEvent::TagWatch(rows)`. Worker restarts when the set changes (shutdown + respawn with new set, era-stamped); stops (empty set / screen exit / profile switch).
  - ui/tags.rs: watch table (split pane or toggled view — pick the simpler): path, value, quality, timestamp, updated-indicator. Quality strings are DATA, never parsed into errors (Bad_NotNotFound shows exit-0-style — 05-04 convention).
  - Tests: watch-set toggle unit; worker respawn on set change (assert old worker's era is stale — 06-02's is_current); TagWatch event updates table; TestBackend render of a seeded watch table.
  </action>
  <verify>cargo test -p ignition-tui</verify>
  <done>Live watch shows periodically-updating values for the watched set, restarts on set changes, and stops cleanly on screen exit.</done>
</task>

<task type="auto">
  <name>Task 3: Tags actions menu — write, providers, config, export/import, udt</name>
  <files>crates/ignition-tui/src/ui/tags.rs, crates/ignition-tui/src/update.rs, crates/ignition-tui/src/routes.rs</files>
  <action>
  - Actions menu (`a`) hosting the remaining tags verbs, each as one-shot worker + result modal, with Confirm modals where the CLI is --yes-guarded (grep main.rs `require_confirmation` call sites for the tags family to get the exact guarded set — per STATE it includes provider delete, config delete, and import overwrite):
    - `write` — value Input modal on the selected path; hint text states the JSON-scalar rule (bare string stays string; arrays/objects are invalid_input — 05-04 decision).
    - `providers` — list (inline panel/menu), create (common-fields Input: name + hint for the rest), delete (Confirm — guarded).
    - `config` — edit form modal (common scalar fields + "press ? for the CLI form" hint per the LOCKED modal-depth decision), delete (Confirm — guarded).
    - `export` — file-path Input modal (default `<last-segment>.json` naming per 05-05; the `-o -` stdout pipe form is CLI-only — a FLAG VALUE, not a distinct leaf, so it gets NO registry row: the `tags export` leaf maps Screen(Tags) and the exception is documented in a routes.rs comment per 06-06's OutOfBand rule).
    - `import` — file-path Input + collision policy choice (abort default); overwrite path is Confirm-gated (05-05).
    - `udt` — whatever TagsUdtCommand exposes (list/get-style reads): menu entries + result modal.
  - routes.rs: rows for every remaining tags leaf — `tags browse`, `tags read`, `tags write`, `tags providers *`, `tags config *`, `tags export`, `tags import`, `tags udt *` (EXACT leaf spellings from cli.rs TagsCommand/TagsProviderCommand/TagsConfigCommand/TagsUdtCommand); `tags export -o -`-style stdout exception → OutOfBand (verify how the `-o -` case is shaped — if it's a flag value rather than a distinct leaf, keep the leaf as Screen(Tags) and note it in a routes.rs comment; the coverage test walks leaf PATHS only).
  - Tests: confirm-gating units (provider delete / config delete / import overwrite refuse without accept); write-modal hint presence; menu render test.
  </action>
  <verify>cargo test -p ignition-tui && cargo clippy --workspace --all-targets -- -D warnings</verify>
  <done>Every non-alarm tags verb reachable from the Tags screen with correct confirm-gating; tags family rows complete in the registry.</done>
</task>

</tasks>

<verification>
- cargo test --workspace green; clippy -D warnings clean
- grep watch.rs: tag watch reuses tags_read action (no client re-implementation)
- Navigation honesty: Esc always ascends exactly one level (provider list ← tree ← detail)
</verification>

<success_criteria>
TUI-04 delivered: tag browse + detail + live watch + write, with the whole tags family reachable and the registry rows to prove it.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tui-cockpit/06-04-SUMMARY.md`
</output>
