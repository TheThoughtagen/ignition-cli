---
phase: 06-tui-cockpit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/ignition-tui/Cargo.toml
  - crates/ignition-tui/src/lib.rs
  - crates/ignition-tui/src/event.rs
  - crates/ignition-tui/src/state.rs
  - crates/ignition-tui/src/update.rs
  - crates/ignition-tui/src/context.rs
  - crates/ignition-tui/src/routes.rs
  - crates/ignition-tui/src/ui/mod.rs
  - crates/ignition-tui/src/workers/mod.rs
  - crates/ignition-core/src/actions/logs.rs
  - crates/ignition-cli/src/main.rs
autonomous: true
must_haves:
  truths:
    - "`ign tui` opens a fullscreen cockpit with a tab bar over every screen name; placeholder panes render without any gateway"
    - "User quits via q / Esc-when-no-modal / Ctrl-C and the terminal is fully restored (raw mode off, alt screen exited)"
    - "`ign tui` with piped stdout exits with a usage-class CoreError (no panic)"
    - "Tab / Shift+Tab cycles screens; keystrokes never trigger gateway I/O in the update path"
    - "Workspace gains exactly two new runtime deps: ratatui 0.30.2 and crossterm 0.29 (event-stream)"
  artifacts:
    - path: crates/ignition-tui/src/lib.rs
      provides: "run(profile_flag) entry: context resolve, ratatui::init, select loop, ratatui::restore"
      contains: "ratatui::init"
    - path: crates/ignition-tui/src/event.rs
      provides: "AppEvent enum (Input/Tick/Error + refresh/data variants) + era + shutdown-watch types"
    - path: crates/ignition-tui/src/state.rs
      provides: "AppState (Elm model): Screen enum (all variants), Focus, Modal enum, should_quit, era"
    - path: crates/ignition-tui/src/update.rs
      provides: "Pure sync update(): key handling (quit, tab nav, key-press-kind filter), modal Esc"
    - path: crates/ignition-tui/src/context.rs
      provides: "profile flag -> Arc<ReqwestGatewayApi> via public ignition_core::config fns (no main.rs refactor)"
    - path: crates/ignition-tui/src/routes.rs
      provides: "Mapping {Screen,Streamed,OutOfBand} + CliRoute + static routes() scaffold (tui, completions rows)"
    - path: crates/ignition-tui/src/workers/mod.rs
      provides: "Worker lifecycle helpers: era stamping, tokio::sync::watch shutdown channel"
    - path: crates/ignition-tui/src/ui/mod.rs
      provides: "render(state, frame): tab bar + active-screen placeholder + modal infra (centered Rect + Clear)"
  key_links:
    - {from: "crates/ignition-cli/src/main.rs", to: "crates/ignition-tui/src/lib.rs", via: "Tui dispatch arm", pattern: "ignition_tui::run"}
    - {from: "crates/ignition-cli/src/main.rs", to: "stdout", via: "TTY pre-check", pattern: "is_terminal"}
    - {from: "crates/ignition-tui/src/lib.rs", to: "terminal lifecycle", via: "init/restore pair", pattern: "ratatui::(init|restore)"}
    - {from: "crates/ignition-tui/src/update.rs", to: "nothing async", via: "purity", pattern: "^(?!.*(\\.await)).*$"}
    - {from: "crates/ignition-core/src/actions/logs.rs", to: "tokio::spawn-able tail", via: "Send sink", pattern: "dyn FnMut\\(&LogEntry\\) \\+ Send"}
    - {from: "crates/ignition-tui/src/context.rs", to: "ignition_core::config", via: "public fns only", pattern: "config::(load|apply_env_overlay|resolve_selection)"}
---

<objective>
Fill the ignition-tui stub with the phase's foundation: the event-driven async cockpit loop (AppEvent mpsc + crossterm EventStream + tokio::select!), Elm-style AppState/update, profile→client context resolution, UI chrome with modal infrastructure, the coverage-registry scaffold, and the two codebase seam fixes (logs::tail sink `+ Send`; minimal `ign tui` dispatch arm).

Purpose: Every later screen plan (06-02..06-06) plugs into this loop, state, modal infra, and registry — this plan IS the architecture. Get the lifecycle and purity rules right here and screens become additive.
Output: A runnable `ign tui` cockpit shell (tab bar + placeholders), headless-testable via TestBackend, with zero gateway calls wired yet.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tui-cockpit/06-RESEARCH.md
@crates/ignition-core/src/actions/mod.rs
@crates/ignition-core/src/config/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deps, tail-sink Send fix, and profile→client context</name>
  <files>Cargo.toml, crates/ignition-tui/Cargo.toml, crates/ignition-core/src/actions/logs.rs, crates/ignition-tui/src/context.rs</files>
  <action>
  - Workspace `Cargo.toml` `[workspace.dependencies]`: add `ratatui = "0.30.2"` (floor-pinned at the MSRV-1.88 release; add the research's one-line comment about 0.30.1's patch MSRV bump) and `crossterm = { version = "0.29", features = ["event-stream"] }`. These are the ONLY two new deps in the whole phase — no tokio-stream, no tokio-util, no tui-input.
  - `crates/ignition-tui/Cargo.toml`: replace the zero-dep stub block with `ignition-core`, `ratatui`, `crossterm`, `tokio`, `futures-util` (all `{ workspace = true }`); update the crate description (no longer a stub). tokio already has `rt-multi-thread,macros,time,sync` workspace-enabled — do NOT add features.
  - `crates/ignition-core/src/actions/logs.rs`: change the `tail` sink parameter (and the `TailState.sink` field it feeds) from `&mut dyn FnMut(&LogEntry)` to `&mut (dyn FnMut(&LogEntry) + Send)` — the rig.rs sinks already use this convention; without it the tail future cannot cross `tokio::spawn` on the multi-thread runtime (RESEARCH Pattern 2b / Pitfall 6). Existing callers pass concrete Send closures and the concrete-local tests at the bottom of logs.rs keep compiling — if any test local is genuinely non-Send, fix the test local, never the signature direction.
  - Create `crates/ignition-tui/src/context.rs`: `pub fn resolve(profile_flag: Option<&str>) -> Result<(String, Arc<ReqwestGatewayApi>), CoreError>` composing ONLY public building blocks in this exact order (mirror main.rs's private `resolve_profile_context` semantics — read it first for reference): `config::config_path()` → `config::load` → `config::apply_env_overlay(&mut config, selected)` → `config::resolve_selection(flag, &config)` (None = ProfileNotFound-style usage error naming how to add one) → `config::secret::resolve_secret` for the selected profile → `ReqwestGatewayApi::new(profile, credential)`. Also `pub fn rebuild(profile_name: &str) -> Result<(String, Arc<ReqwestGatewayApi>), CoreError>` (load+overlay+resolve_secret for a NAMED profile) — 06-02's profile switcher reuses it. Secrets stay confined: context.rs passes `Credential` into `ReqwestGatewayApi::new` and never formats/stores it (Secret::expose confinement is in the client's header site only).
  - Add a unit test with an isolated `IGNITION_CLI_CONFIG`-style temp config proving resolve picks flag > active and errors cleanly on no-profile. Follow the repo's env-test isolation conventions (in-process env tests need config::ENV_LOCK — check how ignition-core tests do it).
  </action>
  <verify>cargo test -p ignition-core logs && cargo check --workspace && cargo tree -i crossterm 2>&1 | grep -c "crossterm v0.29" (must resolve ONE crossterm major)</verify>
  <done>Workspace compiles with ratatui/crossterm added; tail sink is Send; context::resolve/rebuild unit-tested against a temp config; `cargo tree -i crossterm` shows a single 0.29 in the graph.</done>
</task>

<task type="auto">
  <name>Task 2: AppEvent loop, AppState/update, run() entry, minimal Tui arm</name>
  <files>crates/ignition-tui/src/event.rs, crates/ignition-tui/src/state.rs, crates/ignition-tui/src/update.rs, crates/ignition-tui/src/workers/mod.rs, crates/ignition-tui/src/lib.rs, crates/ignition-cli/src/main.rs</files>
  <action>
  - `event.rs`: `pub enum AppEvent { Input(crossterm::event::Event), Tick, Error(String), ActionDone(...) }` — define the variant set from RESEARCH Pattern 1, but payload types that belong to later screens (Refresh snapshot, LogLine, TagWatch, Alarms, ProfileChanged) get added by THEIR plans; here define only what the shell needs. Include `pub type Shutdown = tokio::sync::watch::Receiver<bool>` convention.
  - `state.rs`: `AppState { should_quit, screen: Screen, focus: Focus, modal: Option<Modal>, era: u64, ... }`. Define `Screen` with ALL phase variants NOW (Dashboard, Logs, Tags, Alarms, Projects, Rig) so later plans never edit the enum, `Focus { Normal, Table, Detail, Modal }`, and a `Modal` enum with the three infrastructure shapes later plans extend: `Confirm { title, body }`, `Input { title, buffer }`, `Result_ { title, lines }`. Per-screen data structs are added by their plans.
  - `update.rs`: `pub fn update(state: &mut AppState, event: AppEvent)`. PURE AND SYNC — this file must never contain `.await` (gateway I/O lives only in `workers/*`; RESEARCH anti-pattern #1). Handle: quit keys — `q` when no modal, `Ctrl-C` ALWAYS (raw mode disables ISIG — Pitfall 4), Esc closes modal else quits; Tab/Backtab cycle screens; ignore non-Press key events via `event.as_key_press_event()` (Pitfall 5). Input/confirm-modal keystrokes route to the modal buffer (hand-rolled char-append/backspace — NO tui-input dep).
  - `workers/mod.rs`: lifecycle helpers every worker uses — `pub fn new_era(state) -> u64` (bumps + returns), `pub fn shutdown_channel() -> (watch::Sender<bool>, watch::Receiver<bool>)`, and an era-check helper `pub fn is_current(state_era, worker_era) -> bool`. Workers attach their spawn-era to results; update drops stale-era events (Pitfall 9).
  - `lib.rs`: replace the panicking stub with `pub async fn run(profile_flag: Option<String>) -> Result<(), CoreError>`: `context::resolve` → `ratatui::init()` → the select loop → `ratatui::restore()` on EVERY path (Ok, Err, and via init's panic hook) → return the app result. The loop (RESEARCH Pattern 1, adapted from ratatui's async-github example): `tokio::select!` over (a) a 250ms `tokio::time::interval` tick, (b) `crossterm EventStream` next() matched EXHAUSTIVELY — `Some(Ok(e)) => update(Input), Some(Err(e)) => return Err(wrapped), None => break` (a bare `Some(Ok(e)) =` arm permanently kills input on stream error — Pitfall 3), (c) `events.recv()` AppEvents. LOCKED render cadence: draw after EVERY processed arm (per-event), tick is the 250ms staleness floor — not tick-only.
  - `main.rs` Tui arm (choke-file discipline — stays ~5 lines + comment): `#[cfg(feature = "tui")] Commands::Tui => { if !stdout().is_terminal() { return (None, Err(CoreError::Usage-class "ign tui requires a terminal (stdout is not a TTY)")); } ignition_tui::run(cli.profile.clone()).await.map(|()| ActionOutput::TuiExited) }`. Check how CoreError spells the usage-class variant (exit 2) and use the real one. `ActionOutput::TuiExited`'s render arm renders NOTHING in every mode (LOCKED stdout decision: `ign tui` prints nothing on success; errors after restore flow the normal stderr envelope + exit taxonomy — frozen contract untouched). Keep `#[cfg(not(feature = "tui"))]` path compiling (lean build).
  </action>
  <verify>cargo build --workspace && cargo clippy --workspace --all-targets -- -D warnings && echo "ign tui" | cargo run -p ignition-cli -- tui; echo "exit=$?" (expect usage-class exit code 2, clean stderr envelope, exit=2 — NOT a panic)</verify>
  <done>`ign tui` compiles, non-TTY invocation returns exit 2 with a usage-class envelope on stderr; the loop matches EventStream exhaustively; update.rs contains zero `.await`; lean build (--no-default-features) still compiles.</done>
</task>

<task type="auto">
  <name>Task 3: UI chrome, modal infra, routes scaffold, headless tests</name>
  <files>crates/ignition-tui/src/ui/mod.rs, crates/ignition-tui/src/routes.rs</files>
  <action>
  - `ui/mod.rs`: `pub fn render(state: &AppState, frame: &mut Frame)` — top tab bar (all Screen variants, active highlighted) + active-screen body. Each screen's body dispatches to `ui::dashboard::render`-style per-screen fns — create per-screen ui modules as tiny placeholders NOW (`ui/dashboard.rs` etc. rendering a "not yet wired" Block) so later plans OWN whole files instead of editing mod.rs dispatch. Modal infra: when `state.modal` is Some, render over `frame.area().centered(Constraint::Ratio(1,2), Constraint::Length(5))`-style rect (0.30 helpers — don't hand-roll rect math) with `Clear` underneath; render Confirm/Input/Result_ shapes.
  - `routes.rs`: `pub enum Mapping { Screen(Screen), Streamed, OutOfBand }`, `pub struct CliRoute { pub path: &'static str, pub mapping: Mapping }`, `pub fn routes() -> &'static [CliRoute]`. Seed with the shell-known rows: `"tui" -> Screen(entry)`, `"completions" -> OutOfBand`. Later plans append their families' rows; 06-06 completes the table and lights the CI test. Keep paths as space-separated leaf paths exactly matching clap's subcommand chain ("logs loggers set" style).
  - Tests (in ignition-tui, `#[cfg(test)]` modules): (1) TestBackend chrome render — 80x24 buffer asserts tab bar + placeholder pane (ratatui `TestBackend` + `Buffer::with_lines` + `assert_buffer`, RESEARCH test pattern; NO insta dep); (2) update units: q quits, Ctrl-C quits even with modal open, Esc pops modal before quitting, Tab cycles wrap-around, non-Press key events ignored; (3) modal input buffer: chars append, backspace pops, Esc cancels. 
  </action>
  <verify>cargo test -p ignition-tui && cargo test -p ignition-cli 2>&1 | tail -5 (existing goldens unmoved)</verify>
  <done>TestBackend test proves the chrome renders; keymap units prove quit/modal/tab semantics; routes scaffold compiles with Mapping kinds; all pre-existing ignition-cli tests green (no golden moved).</done>
</task>

</tasks>

<verification>
- cargo test --workspace green; cargo clippy --workspace --all-targets -- -D warnings clean
- `cargo tree -i crossterm` shows exactly one crossterm 0.29 (no dual-major — Pitfall 7)
- `echo | cargo run -p ignition-cli -- tui` exits 2 with usage-class stderr envelope (TTY guard works headless)
- grep: update.rs contains no `.await`; main.rs Tui arm ≤ ~10 lines; logs.rs sink has `+ Send`
</verification>

<success_criteria>
Cockpit shell opens/quits cleanly, restores the terminal, renders headlessly via TestBackend, and the two seam fixes (+Send sink, thin context) are in — every 06-02..06-06 plan can now purely ADD screen modules.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tui-cockpit/06-01-SUMMARY.md`
</output>
