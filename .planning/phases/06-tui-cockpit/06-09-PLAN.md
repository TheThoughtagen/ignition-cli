---
phase: 06-tui-cockpit
plan: 09
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - crates/ignition-tui/src/update.rs
  - crates/ignition-tui/src/workers/watch.rs
  - crates/ignition-tui/src/ui/tags.rs
autonomous: true
must_haves:
  truths:
    - "Pressing r on the Tags screen re-fires the visible level's read (provider list at root, current browse level inside the stack, open detail pane at depth) — stale error states clear without Esc+Enter re-navigation"
    - "Tags browse/read error panes hint the recovery path (r to refresh)"
    - "A successful tags write refreshes the open matching detail pane without manual refire"
  artifacts:
    - path: crates/ignition-tui/src/update.rs
      provides: "'r' handler in tags_keys + tags-write ActionDone refresh trigger"
    - path: crates/ignition-tui/src/ui/tags.rs
      provides: "Error-pane recovery hint text"
  key_links:
    - {from: "tags_keys 'r'", to: "workers::watch spawn fns", via: "current-level refire", pattern: "KeyCode::Char\('r'\)"}
    - {from: "ActionDone 'tags write'", to: "detail read refire", via: "alarms-ack trigger pattern (update.rs:222)", pattern: "tags write"}
---

<objective>
Close two Tags-screen freshness gaps from 06-UAT.md: (1) stale browse results persist with no invalidation path [major, test 10], (2) detail pane shows a stale value after a successful write [minor, test 11].

Purpose: The Tags screen must reflect the gateway's current state — the UAT caught a trial-reset leaving a stale 402 error on screen, and a successful write leaving a stale displayed value.
Output: An 'r' refresh key, recovery hints on error panes, and a write→detail refresh trigger, all test-pinned.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tui-cockpit/06-UAT.md

@crates/ignition-tui/src/update.rs
@crates/ignition-tui/src/workers/watch.rs
@crates/ignition-tui/src/ui/tags.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 'r' refresh on the Tags screen (current-level refire)</name>
  <files>crates/ignition-tui/src/update.rs, crates/ignition-tui/src/workers/watch.rs</files>
  <action>
  Gap 06-UAT test 10 [major]. Root cause: one-shot browse results persist in the Tags navigation stack with no invalidation path — set_screen(Tags) (update.rs:463-464, 565-566) re-fires only spawn_providers_once, never the in-stack browse pane; tags_keys (update.rs:784) has no refresh binding. Recovery currently requires non-obvious Esc+Enter re-navigation.

  Add a `KeyCode::Char('r')` handler to tags_keys that re-fires the CURRENT level's read, deepest-first:
  - detail pane open (TagsData.detail, state.rs ~:762) → bump detail_seq and refire the detail read (the same spawn the open/Enter path uses)
  - stack non-empty (TagsData.stack, top = current level) → refire the current browse read for the top level's path
  - otherwise → refire spawn_providers_once (respecting the providers_busy guard)
  Also re-fire on screen entry: make the set_screen(Tags) arm (and the profile-switch re-entry arm at :565) re-fire the deepest visible one-shot, not just the provider list. Clear the error fields (providers_error / browse error / detail error) when arming the refire so the stale 402-style pane visibly reloads.

  Add unit tests mirroring the existing update-test style: (a) 'r' at provider level re-arms providers (busy guard respected), (b) 'r' with a stacked browse level refires that level, (c) 'r' with an open detail refires the read under a NEW detail_seq, (d) screen re-entry with a populated stack refires the deepest level.
  </action>
  <verify>cargo test -p ignition-tui</verify>
  <done>'r' re-fires the deepest visible read at every Tags depth; screen re-entry invalidates stale stack results; tests pin all four cases</done>
</task>

<task type="auto">
  <name>Task 2: tags-write ActionDone refreshes the open detail</name>
  <files>crates/ignition-tui/src/update.rs</files>
  <action>
  Gap 06-UAT test 11 [minor]. Root cause: no ActionDone label trigger for "tags write" — the detail pane's read is purely on-demand. The established pattern is the alarms-ack trigger at update.rs:222 (`if label == "alarms ack"` arms a re-poll), test-pinned at update.rs:4146.

  In the ActionDone arm (update.rs:207), on a SUCCESSFUL `label == "tags write"` result: refire the open detail read when the written path matches the open detail's path (bump detail_seq, spawn the read — Task 1's refire helper), mirroring the alarms-ack pattern. The write modal knows the target path (the form at update.rs:1047-1096) — carry it into the trigger comparison. Also nudge the watch table: if the written path is in the watched set, the 2s poll refreshes it naturally — no extra work needed there; note this in a comment.

  Add a unit test in the alarms-ack-trigger style (update.rs:4146 precedent): a landed "tags write" ActionDone for the open detail's path refires the read (new detail_seq, busy state); a write for a DIFFERENT path leaves the pane untouched.
  </action>
  <verify>cargo test -p ignition-tui</verify>
  <done>Write→read-back round-trip is visible in the TUI without manual refire; matching-path refire and non-matching no-op both test-pinned</done>
</task>

<task type="auto">
  <name>Task 3: Recovery hints on Tags error panes</name>
  <files>crates/ignition-tui/src/ui/tags.rs</files>
  <action>
  Gap 06-UAT test 10 (missing item: "error panes should hint the recovery path"). In crates/ignition-tui/src/ui/tags.rs, append a hint line to the browse/provider/detail error renders (the honest-error states): e.g. "press r to refresh" (match the existing error-pane text conventions — lowercase, muted style if one exists). Keep it to the Tags screen's error states; do not touch other screens.

  Verify with the existing render tests style (TestBackend): an errored browse pane renders the hint line.
  </action>
  <verify>cargo test -p ignition-tui</verify>
  <done>Every Tags error pane (providers/browse/detail) shows the refresh hint; render test pins it</done>
</task>

</tasks>

<verification>
- cargo test -p ignition-tui (all existing 172+ tests plus the new ones green)
- cargo test --workspace --features tui && cargo fmt --all --check && cargo clippy --workspace --features tui --all-targets -- -D warnings
</verification>

<success_criteria>
- All three must_have truths hold, each pinned by tests added in this plan
- update() stays pure-sync (awaits confined to spawned async blocks) — the Elm constraint from 06-01
</success_criteria>

<output>
After completion, create `.planning/phases/06-tui-cockpit/06-09-SUMMARY.md`
</output>
