---
phase: 06-tui-cockpit
plan: 08
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - crates/ignition-core/src/client/resources.rs
  - crates/ignition-core/tests/resources_contract.rs
autonomous: true
must_haves:
  truths:
    - "ign resource put <project> <root-level-file> (e.g. perspective-properties.json) lands on the gateway and reads back via resource get"
    - "The surgery output structure for root-level file members is pinned by gateway-free unit/contract tests (CI-green without a rig)"
    - "Nested-member puts (views/root/view.json) keep landing — no regression to the 05-07 working path"
  artifacts:
    - path: crates/ignition-core/src/client/resources.rs
      provides: "Root-level file member surgery: correct descriptor/entry shape that survives the gateway's import validation"
    - path: crates/ignition-core/tests/resources_contract.rs
      provides: "Structure-pinning tests for root-level member surgery output"
  key_links:
    - {from: "crates/ignition-core/src/client/resources.rs", to: "zip import validation", via: "descriptor + folder-flag shape for project-root members", pattern: "folder|descriptor"}
---

<objective>
Close the 06-UAT test 6 [major] gap: `ign resource put` fails for project-ROOT file members (HTTP 500, gateway log "Project import failed: module folder must have folder flag set") while nested members land fine.

Purpose: Root-level project files (perspective-properties.json, etc.) are the remaining writable surface — without this, Perspective project configuration cannot be provisioned via the CLI.
Output: Fixed member surgery + structure-pinning tests + live-rig confirmation.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tui-cockpit/06-UAT.md

@crates/ignition-core/src/client/resources.rs
@crates/ignition-core/tests/resources_contract.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the failure — structure tests for root-level member surgery</name>
  <files>crates/ignition-core/tests/resources_contract.rs, crates/ignition-core/src/client/resources.rs</files>
  <action>
  Gap 06-UAT test 6 [major]. Repro: `ign resource put pterm perspective-properties.json --file <any json>` → HTTP 500 from projects/import, gateway log: "Project import failed: module folder must have folder flag set" (IllegalArgumentException, gateway.ProjectRoutes).

  The UAT layout matrix (manual surgery experiments on pterm.zip, live-store verified) — use this as the hypothesis space:
  - WORKS: nested member (views/root/view.json) + parent-folder descriptor chain from the export zip
  - A) module-root file (com.inductiveautomation.perspective/resources/general-properties.json) + synthesized resource.json → 500 "resource already exists" (implicit node collision)
  - A2) same file bare, no descriptor → import succeeds, file SILENTLY not adopted (05-07 basename rule)
  - B) module root + descriptor → 500
  - C) project-root perspective-properties.json + root resource.json → succeeds, silently no-ops
  - PerspectiveProjectProps (from jar-cache strings): module=com.inductiveautomation.perspective, RESOURCE_TYPE='general-properties'

  Write failing-first tests that run the surgery for a project-root file member and assert the resulting zip structure: which members exist, the resource.json descriptor shape, and folder-flag bits on every directory entry (the gateway validation that fires). Assert the CURRENT broken output first (RED), so the fix has a structural pin. These tests must run without a gateway (build the surgery output in-memory, inspect the zip).
  </action>
  <verify>cargo test -p ignition-core --test resources_contract</verify>
  <done>Structure tests enumerate root-level surgery output members/descriptors/flags and initially fail against the current broken shape</done>
</task>

<task type="auto">
  <name>Task 2: Fix the surgery so root-level file members land</name>
  <files>crates/ignition-core/src/client/resources.rs, crates/ignition-core/tests/resources_contract.rs</files>
  <action>
  Fix the member-surgery helpers (resources.rs: `member_path`, `user_path`, `merge_descriptor_member`, `synthesized_descriptor`, `parent_of`) so a project-root file member produces an import the gateway accepts and adopts. The landing rule (05-07, live-proven): an overwrite-import LANDS a NEW file member only when its immediate parent folder's descriptor names it — a project-root file has no parent chain in the zip, so the fix must synthesize the correct root-level structure (descriptor shape and/or the module-folder flag the gateway validates — the exact combination is what Task 1's tests now pin). Iterate against Task 1's structure tests until GREEN.

  Then verify end-to-end against the live rig (all CLI-automatable): `ign rig status` (or `ign rig up` if down) → export a scratch project (`ign project export`) → `ign resource put <proj> perspective-properties.json --file <minimal json>` → confirm non-error envelope → `ign resource get <proj> perspective-properties.json` reads the written content back → restore the project via import from the pristine export. If the rig is unreachable after one `ign rig up` attempt, record the wiremock/structure-test evidence as the floor and note the live check as pending for /gsd-verify-work — do not block the plan on it.

  Guard: no behavior change for nested members — the existing nested-member contract tests must stay green untouched.
  </action>
  <verify>cargo test -p ignition-core --test resources_contract && ign resource put <rig-project> perspective-properties.json --file /tmp/pp.json && ign resource get <rig-project> perspective-properties.json</verify>
  <done>Root-level put round-trips on the rig (or structure tests green + live check explicitly deferred); nested puts unregressed</done>
</task>

</tasks>

<verification>
- cargo test --workspace --features tui (no regressions)
- cargo fmt --all --check && cargo clippy --workspace --features tui --all-targets -- -D warnings
- Root-level put round-trip evidence (live rig output pasted into the SUMMARY, or explicit deferral note)
</verification>

<success_criteria>
- All three must_have truths hold; the fix is structure-pinned so CI guards it without a gateway
</success_criteria>

<output>
After completion, create `.planning/phases/06-tui-cockpit/06-08-SUMMARY.md`
</output>
