---
phase: 06-tui-cockpit
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - crates/ignition-tui/src/ui/logs.rs
  - crates/ignition-tui/src/ui/alarms.rs
  - crates/ignition-tui/src/workers/tail.rs
  - crates/ignition-tui/src/workers/watch.rs
  - crates/ignition-tui/src/state.rs
  - crates/ignition-tui/src/update.rs
  - crates/ignition-tui/src/event.rs
  - crates/ignition-tui/src/routes.rs
autonomous: true
must_haves:
  truths:
    - "User can tail gateway logs live in the Logs screen with a level filter, and scroll back through history"
    - "A weekend-long tail cannot OOM the process — the display buffer is a ring capped at 10,000 entries"
    - "Leaving the Logs screen stops the tail worker; the UI never blocks on gateway I/O while entries stream"
    - "User can view active alarms in a table showing full UUIDs, browse alarm history, and acknowledge alarms via a username-required modal"
    - "Short-prefix alarm ids can be acked from the TUI (the action layer expands them — 05-08 behavior inherited)"
  artifacts:
    - path: crates/ignition-tui/src/ui/logs.rs
      provides: "Log stream pane: ring-backed lines, level filter indicator, scrollback"
      min_lines: 60
    - path: crates/ignition-tui/src/ui/alarms.rs
      provides: "Active-alarms table + history view + ack input modal"
      min_lines: 50
    - path: crates/ignition-tui/src/workers/tail.rs
      provides: "tail worker: actions::logs::tail with channel-sending Send sink + watch shutdown"
      contains: "actions::logs::tail"
    - path: crates/ignition-tui/src/workers/watch.rs
      provides: "alarms_active interval poll worker (5s)"
  key_links:
    - {from: "crates/ignition-tui/src/workers/tail.rs", to: "AppEvent channel", via: "sink closure sends LogLine (sync send on unbounded)", pattern: "send\\(AppEvent::LogLine"}
    - {from: "logs screen exit", to: "tail worker", via: "watch shutdown signal", pattern: "shutdown"}
    - {from: "crates/ignition-tui/src/ui/alarms.rs", to: "ack action", via: "username modal -> worker", pattern: "tags_alarms_ack"}
    - {from: "ring buffer", to: "display", via: "render-side level filter over retained entries", pattern: "VecDeque"}
---

<objective>
The monitoring pair: the Logs screen (live tail via the Send-fixed sink, 10k ring buffer, render-side level filtering, scrollback, loggers level management) and the Alarms screen (5s active-alarm poll, full-UUID table, history browse, ack flow with the required --username modal).

Purpose: Delivers Success Criterion 3 (tail + level filter, zero UI blocking) and TUI-05 (alarm panel view + ack). Also proves the two streaming patterns (channel-sink tail, interval poll) that 06-04's tag watch reuses.
Output: Two new live screens; registry rows for the logs and tags-alarms families.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tui-cockpit/06-01-SUMMARY.md
@.planning/phases/06-tui-cockpit/06-02-SUMMARY.md
@crates/ignition-core/src/actions/logs.rs
@crates/ignition-core/src/actions/tags.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Logs screen — tail worker, ring buffer, level filter, scrollback</name>
  <files>crates/ignition-tui/src/workers/tail.rs, crates/ignition-tui/src/ui/logs.rs, crates/ignition-tui/src/state.rs, crates/ignition-tui/src/update.rs, crates/ignition-tui/src/event.rs</files>
  <action>
  - event.rs: `AppEvent::LogLine(ignition_core::…::LogEntry)` (use the real LogEntry path from actions/logs.rs).
  - `workers/tail.rs`: spawn on entering the Logs screen (and on profile change via era) — `actions::logs::tail(&*api, logger, min_level, since_ms, interval, None /*deadline: runs until shutdown*/, sink)` where `sink = &mut |e: &LogEntry| { let _ = tx.send(AppEvent::LogLine(e.clone())); }` (sync `send` on the unbounded channel is legal in a sync closure — LOCKED transport decision). Wrap the whole tail future in a `select!` against the screen's watch-shutdown so leaving the screen stops it even between entries. Era-stamp nothing per-line (ring turnover is fine); the worker exits on send-error OR shutdown.
  - state.rs: `LogsData { ring: VecDeque<LogEntry> (CAP 10_000, evict front), filter: LogLevelFilter (All..Error — an enum over the gateway levels), scroll_offset, follow: bool (auto-scroll to bottom unless user scrolled up), dropped: usize }`. LOCKED: ring cap 10_000 entries; level filter applies AT RENDER over the retained ring (filtering the query alone would hide already-received entries — RESEARCH anti-pattern), AND as `min_level` when the tail (re)starts. Changing the filter key (`l` cycles) restarts the tail worker with the new min_level (shutdown + respawn).
  - ui/logs.rs: full-width stream pane; each line `LEVEL timestamp logger message` (level color-coded: ERROR red, WARN yellow, INFO default, DEBUG dim); status row shows filter, follow state, ring size; PgUp/PgDn/j/k scroll (scroll disables follow; `G`/End re-enables); `f` toggles follow.
  - update.rs: LogLine arm pushes to ring (with cap eviction); Tick arm nothing (render is per-event per 06-01 cadence).
  - Tests: ring eviction unit (push 10_100, assert len 10_000 and oldest dropped); render-side filter unit (ring with mixed levels, filter=Warn renders only Warn+); follow/scroll state machine unit; TestBackend render of a seeded ring (assert a known line + filter indicator).
  </action>
  <verify>cargo test -p ignition-tui</verify>
  <done>Logs screen streams entries into a capped ring, level filter works retroactively over the ring and on restart, scrollback + follow behave, and leaving the screen stops the worker.</done>
</task>

<task type="auto">
  <name>Task 2: Loggers actions + logs-family route rows</name>
  <files>crates/ignition-tui/src/ui/logs.rs, crates/ignition-tui/src/update.rs, crates/ignition-tui/src/routes.rs</files>
  <action>
  - Actions menu (`a`) on the Logs screen hosting the loggers family: `loggers get` (optional logger filter via Input modal), `loggers set` (logger + level via two Input fields or one "logger level" line — gated by Confirm: `logs loggers set` and `logs loggers reset` are --yes-guarded, main.rs:529/542), `loggers reset`. One-shot workers + the 06-02 result modal (pretty-JSON) show outcomes; the tail keeps streaming independently (separate worker).
  - routes.rs rows for the logs family with EXACT leaf paths from cli.rs LogsCmd/LoggersCmd: the tail screen maps `logs` (BARE form — LogsArgs.command is Option<LogsCmd> at cli.rs:888 and `follow` is a FLAG on the bare logs command, not a subcommand; there is NO `logs follow` leaf — bare `ign logs` covers list/tail) → Screen(Logs). Everything the Logs screen shows is Screen(Logs); reserve Streamed for raw-pane cases like rig logs in 06-06. Loggers rows: `logs loggers` (BARE form — LoggersArgs.command is Option<LoggersCmd> at cli.rs:910, so bare `ign logs loggers` is an invocable action) plus its `get`/`set`/`reset` leaves → Screen(Logs). Walk the LogsCmd tree and map any other leaf that exists.
  - Tests: update units for the set/reset confirm-gating (cancel spawns nothing); menu render test.
  </action>
  <verify>cargo test -p ignition-tui && grep -c "logs" crates/ignition-tui/src/routes.rs (family rows present)</verify>
  <done>Loggers verbs reachable from the Logs screen with confirm-gating on the guarded two; logs family fully mapped in the registry.</done>
</task>

<task type="auto">
  <name>Task 3: Alarms screen — active poll, history, ack modal</name>
  <files>crates/ignition-tui/src/workers/watch.rs, crates/ignition-tui/src/ui/alarms.rs, crates/ignition-tui/src/state.rs, crates/ignition-tui/src/update.rs, crates/ignition-tui/src/event.rs, crates/ignition-tui/src/routes.rs</files>
  <action>
  - `workers/watch.rs`: `pub async fn alarms_worker(api, tx, shutdown, era, period=5s)` — interval poll `actions::tags::tags_alarms_active` (check its real signature — it carries the WebDev-route precondition internally, inherited for free); send `AppEvent::Alarms(Vec<…row type…>)` each tick; era-stamp + shutdown-select like refresh_worker.
  - state.rs: `AlarmsData { active: Option<Vec<AlarmRow>>, last_poll, ack_form, view: Active|History }` — reuse whatever row type the action returns (don't re-map); history is fetched on demand (one-shot worker over `tags_alarms_active`'s sibling `tags_alarms_history` — check tags.rs for the history fn + its args).
  - ui/alarms.rs: table of active alarms showing the FULL UUID (05-08 convention — copy-paste-verbatim), name, state, timestamp; header shows poll age; `h` toggles a history view for the selected alarm; empty state says "no active alarms" (not a crash).
  - Ack flow (`a` on a selected alarm): Input modal collecting `username` (REQUIRED — the 3-arg wire form needs it; block OK until non-empty) and optional `note`; accept spawns a one-shot worker calling `tags_alarms_ack(api, project, ids, note, username)` — take `project` and the exact arg shapes from the TagsAlarmsCommand dispatch in main.rs / tags.rs signature. Selected-row ids pass through AS SHOWN (full UUID or prefix — the ACTION expands short prefixes, 05-08; ambiguous-prefix errors surface in the result modal). Ack is NOT confirm-gated (matches CLI — it is not a --yes verb).
  - routes.rs: rows for the tags-alarms leaves (`tags alarms active`, `tags alarms history`, `tags alarms ack` — exact spellings from cli.rs TagsAlarmsCommand) → Screen(Alarms).
  - Tests: update units — poll result fills table, ack form requires username (OK disabled on empty), ack result refreshes active list trigger; TestBackend render of a two-alarm table asserting the full UUID is displayed.
  </action>
  <verify>cargo test -p ignition-tui && cargo clippy --workspace --all-targets -- -D warnings</verify>
  <done>Alarms screen polls active alarms every 5s, displays full UUIDs, browses history, and acks via a username-required modal with prefix ids passing through to the expanding action.</done>
</task>

</tasks>

<verification>
- cargo test --workspace green; clippy -D warnings clean
- Ring cap proven by unit test; update.rs still contains zero `.await`
- Manual/#[ignore] live check (optional): open Logs against a rig, see entries stream while navigating tabs (no input lag), leave screen, worker stops
</verification>

<success_criteria>
TUI-03 delivered (tail + level filter + zero blocking, memory-bounded) and TUI-05 delivered (alarm view + ack). The two streaming worker patterns (channel-sink, interval-poll) are proven for 06-04 to reuse.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tui-cockpit/06-03-SUMMARY.md`
</output>
