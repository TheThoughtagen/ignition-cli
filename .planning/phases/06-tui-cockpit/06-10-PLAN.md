---
phase: 06-tui-cockpit
plan: 10
type: execute
wave: 2
depends_on: ["06-09"]
gap_closure: true
files_modified:
  - crates/ignition-tui/src/ui/mod.rs
  - crates/ignition-tui/src/update.rs
  - crates/ignition-tui/src/state.rs
autonomous: true
must_haves:
  truths:
    - "Every modal fits its content — the 'Enter to run · Esc to cancel' footer is fully visible on Actions and LogsActions menus"
    - "Modals resize with the terminal — heights clamp to the frame (never clip on small terminals) and grow with content within bounds"
    - "j/k/g/G/Ctrl-d/Ctrl-u navigate every menu/list/result modal, matching the screen-level keymaps"
    - "The dashboard menu reads as actions ('wait for gateway up', 'wait for restart complete', 'wait for module ready') while routes.rs keeps clap-exact rows"
    - "The Projects actions menu groups verbs by noun (project / resource / webdev) with human-readable labels and consequence descriptions"
  artifacts:
    - path: crates/ignition-tui/src/ui/mod.rs
      provides: "Content-driven, frame-clamped modal geometry + grouped menu rendering"
    - path: crates/ignition-tui/src/update.rs
      provides: "Vim-motion key arms in all modal handlers"
    - path: crates/ignition-tui/src/state.rs
      provides: "Display-prose ACTIONS labels + noun-grouped PROJECT_ACTIONS"
  key_links:
    - {from: "ui/mod.rs modal_height", to: "frame area", via: "content height clamped to frame", pattern: "min|clamp"}
    - {from: "update.rs modal arms", to: "selection/scroll state", via: "j/k/g/G/Ctrl-d/Ctrl-u", pattern: "KeyCode::Char\('j'\)"}
    - {from: "state.rs menu consts", to: "update.rs executor arms", via: "label strings match on both sides", pattern: "wait for"}
---

<objective>
Close five TUI modal/menu UX gaps from 06-UAT.md test 5 + test 12: footer-hint clipping [cosmetic], fixed-size modals [minor], missing vim motions in modals [minor], confusing wait-prefix menu labels [minor], and the flat CLI-jargon Projects menu [minor].

Purpose: The cockpit's modals and menus are the primary interaction surface — the UAT found clipped hints, non-resizing geometry, arrows-only navigation, and labels that read as CLI jargon.
Output: A modal system that fits and resizes, vim motions everywhere, and menus that read as human prose.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tui-cockpit/06-UAT.md

@crates/ignition-tui/src/ui/mod.rs
@crates/ignition-tui/src/state.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Content-driven, frame-clamped modal geometry</name>
  <files>crates/ignition-tui/src/ui/mod.rs</files>
  <action>
  Gaps 06-UAT test 5 [cosmetic + minor]. Root cause (code-arithmetic verified): ui/mod.rs:73 `Modal::Actions => ACTIONS.len()+3 capped .min(11)` = 10 rows but content needs 11 (7 entries + blank + hint + 2 borders) — the footer clips; LogsActions has the same off-by-one (3+3=6 vs 7). Line 84: `centered(Ratio(1, 2), Length(height.max(5)))` is fixed — clipped on small terminals, small on large ones.

  1. Fix every modal height formula to content + chrome: entries + hint + blank separator + 2 borders (len + 4 for menu modals; equivalent exact counts for Confirm/Result/Input modals — count their actual rendered rows). Drop the separate .min caps that caused the clipping.
  2. Clamp to the frame: at the render site (line 84), bound the height by the frame area (min(content_height, frame.height - 2)) and keep the width ratio — so modals never clip on small terminals and use space on large ones. For Result modals with long content, the existing scroll mechanism handles overflow within the clamped box.
  3. Update/extend the modal render tests (TestBackend, existing style at ui/mod.rs:460-503): (a) Actions modal at a LARGE terminal shows the full footer line, (b) the same modal at a small frame (e.g. 12 rows) still renders without panic and with visible borders.
  </action>
  <verify>cargo test -p ignition-tui</verify>
  <done>Footer hints fully visible on all menu modals; no modal clips at any terminal size (clamped); tests pin large and small frames</done>
</task>

<task type="auto">
  <name>Task 2: Vim motions in every modal</name>
  <files>crates/ignition-tui/src/update.rs</files>
  <action>
  Gap 06-UAT test 5 [minor] + UX feedback "vim motions everywhere". Root cause: modal nav arms are arrows-only for menus (update.rs ~2473-2548: Up/Down per menu kind) and PgUp/PgDn-only for the Result modal; screens already have j/k.

  Add to EVERY menu/list modal handler (Actions, LogsActions, TagActions, ProjectActions, RigActions, and any list-bearing modal like profiles): `j`/`k` as Down/Up aliases, `g`/`G` as home/end of the list. Add to the Result modal (and any scrollable modal): `j`/`k` line scroll, `Ctrl-d`/`Ctrl-u` half-page scroll, `g`/`G` top/bottom — operating on the existing scroll offset, matching the screen-level scroll conventions (Logs screen is the reference implementation). Keep arrow keys and PgUp/PgDn working unchanged.

  Mirror the existing modal key test style: one test per motion family asserting selection/offset movement (e.g. j advances selection like Down; Ctrl-d moves the result offset by half the visible height; G bottoms out).
  </action>
  <verify>cargo test -p ignition-tui</verify>
  <done>j/k/g/G/Ctrl-d/Ctrl-u work in all menu and result modals; arrows/PgUp/PgDn unregressed; motions test-pinned</done>
</task>

<task type="auto">
  <name>Task 3: Display-prose menu labels + noun-grouped Projects menu</name>
  <files>crates/ignition-tui/src/state.rs, crates/ignition-tui/src/update.rs, crates/ignition-tui/src/ui/mod.rs</files>
  <action>
  Gaps 06-UAT test 5 [minor] + test 12 [minor] + UX feedback ("wait is a weird word...", "the resource put is confusing. i dont get delete vs resource delete").

  1. state.rs ACTIONS (line 336): change the three wait labels to display prose — "wait for gateway up", "wait for restart complete", "wait for module ready" — leaving version/connections/doctor/restart as-is. Labels are display-side per the documented LOG_ACTIONS precedent ("Labels are display-side; the route rows in crate::routes carry the clap-exact spellings"). CRITICAL: the executor arms that match on these strings (update.rs:2148 `ACTIONS.get(index)` dispatch, and any `match` on the label text) must be updated to the SAME strings on both sides — and routes.rs must NOT change (the tui_coverage test asserts routes() ↔ clap-tree equality and must stay green untouched).
  2. state.rs PROJECT_ACTIONS (line 379): restructure to noun-grouped entries with human labels + one-line consequence descriptions. Recommended shape: a grouped const (e.g. `PROJECT_ACTION_GROUPS: [(&str, &[ProjectAction]); 3]` with sections "project" / "resource" / "webdev"), each entry carrying a display label and a description string — e.g. project: "new — create an empty project", "delete — remove the whole project (irreversible)"; resource: "put — create or replace one file", "delete — remove one file from the project"; webdev: "deploy — publish the project to the gateway", "status". Keep the flat selection indexing coherent (either flatten groups into one index space or track (group, idx) — pick the simpler one). Update the executor dispatch (update.rs:1805), the modal render (menu with section headers and dimmed one-line descriptions), the height formula from Task 1 (description rows count as content), and the selection handler (update.rs:2523) to the new structure. Confirm-gating per verb must remain EXACT (the gated_cli_verb parity test walks every PendingAction — it must stay green).
  3. Update menu-render tests for the new labels/grouping; update any tests that assert the old label strings.
  </action>
  <verify>cargo test -p ignition-tui && cargo test -p ignition-cli --features tui --test tui_coverage</verify>
  <done>Dashboard menu reads as prose actions; Projects menu grouped by noun with descriptions; routes.rs and the coverage test untouched and green; confirm parity test green</done>
</task>

</tasks>

<verification>
- cargo test --workspace --features tui (all green, including tui_coverage 3/3 and the gated-verb parity test)
- cargo fmt --all --check && cargo clippy --workspace --features tui --all-targets -- -D warnings
</verification>

<success_criteria>
- All five must_have truths hold
- routes.rs NOT modified (clap-exact rows preserved — the coverage test proves it)
- Confirm-gating per verb unchanged (parity test proves it)
</success_criteria>

<output>
After completion, create `.planning/phases/06-tui-cockpit/06-10-SUMMARY.md`
</output>
