---
phase: 06-tui-cockpit
plan: 05
type: execute
wave: 5
depends_on: ["06-04"]
files_modified:
  - crates/ignition-tui/src/ui/projects.rs
  - crates/ignition-tui/src/workers/ops.rs
  - crates/ignition-tui/src/state.rs
  - crates/ignition-tui/src/update.rs
  - crates/ignition-tui/src/event.rs
  - crates/ignition-tui/src/routes.rs
autonomous: true
must_haves:
  truths:
    - "User can browse the project list, open a project's detail, and drill into its resources (list → get with content preview)"
    - "User can trigger project actions from the TUI — create, set, delete, import, export — with confirm modals on the guarded ones (delete, import-overwrite)"
    - "User can trigger resource actions — put, delete — both confirm-gated (05-02 decision), and webdev deploy/status from the same screen"
    - "Rich-arg actions (import file, export path, resource put file) accept the common field via modal and show the equivalent CLI form via a ? hint"
  artifacts:
    - path: crates/ignition-tui/src/ui/projects.rs
      provides: "Project list → project detail → resource browser + project/resource/webdev action menus"
      min_lines: 80
    - path: crates/ignition-tui/src/workers/ops.rs
      provides: "One-shot workers for project/resource/webdev verbs (browse, get, create, set, delete, import, export, put, deploy, status)"
      contains: "actions::projects"
  key_links:
    - {from: "crates/ignition-tui/src/workers/ops.rs", to: "ignition-core actions", via: "project/resource/webdev free fns", pattern: "actions::(projects|resources|webdev)::"}
    - {from: "project delete/import-overwrite/resource put/resource delete", to: "Confirm modal", via: "accept ≡ --yes before spawn", pattern: "Modal::Confirm"}
---

<objective>
The Projects screen: project list → detail → resource drill-down (get with content preview), plus the action menus for the whole project family (create/set/delete/import/export), the resource family (put/delete), and webdev deploy/status (which is a project-zip import into the CLI-owned ign-cli project — a natural citizen of this screen).

Purpose: Delivers Success Criterion 5 (browse projects/resources + trigger project actions) and completes registry coverage for the project/resource/webdev families ahead of 06-06's coverage proof.
Output: One browser screen with two drill-down levels and confirm-gated destructive actions.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tui-cockpit/06-01-SUMMARY.md
@.planning/phases/06-tui-cockpit/06-02-SUMMARY.md
@crates/ignition-core/src/actions/projects.rs
@crates/ignition-core/src/actions/resources.rs
@crates/ignition-core/src/actions/webdev.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project browser — list, detail, resource drill-down</name>
  <files>crates/ignition-tui/src/ui/projects.rs, crates/ignition-tui/src/workers/ops.rs, crates/ignition-tui/src/state.rs, crates/ignition-tui/src/update.rs, crates/ignition-tui/src/event.rs</files>
  <action>
  - Create `workers/ops.rs` for one-shot project/resource/webdev workers (the 06-02 one-shot pattern, named per-family: `project_list`, `project_get`, `resource_list`, `resource_get`, …) — each takes `(Arc<client>, tx, era)`, calls the action fn AS-IS, sends a tagged done-event. NO gateway awaits outside workers.
  - state.rs: `ProjectsData { list, selected detail (Loading/Loaded/Error), resources list, resource detail }` with the object-list → detail navigation stack (project list → Enter → project detail + resources list → Enter → resource detail; Esc ascends).
  - ui/projects.rs: project table (name, title, enabled-ish summary from the action's list shape); project detail pane (the get result's fields); resources list (path + kind from the resource family's list shape — remember resources ride export-zip surgery in the actions layer, invisible here); resource detail = the flat `{project, path, content_kind, content}` shape with a SCROLLABLE content preview (text content renders; binary is fenced by the action's exit-6 — surface as Error state verbatim).
  - Tests: navigation state machine units; TestBackend render of a seeded project list + detail; resource detail scroll unit.
  </action>
  <verify>cargo test -p ignition-tui</verify>
  <done>Two-level browse works headlessly: projects → detail+resources → resource get with scrollable preview, Loading/Error states per panel.</done>
</task>

<task type="auto">
  <name>Task 2: Project + resource + webdev action menus with confirm gating</name>
  <files>crates/ignition-tui/src/ui/projects.rs, crates/ignition-tui/src/update.rs, crates/ignition-tui/src/routes.rs</files>
  <action>
  - Project actions menu (`a` at list or detail level): `create` (name + common-field Inputs: title/defaultDb-ish per the real ProjectModify shape — keep to common fields + CLI hint), `set` (field=value style Inputs on the selected project, common fields only), `delete` (Confirm — guarded, main.rs:807), `import` (file-path Input + collision policy; the overwrite policy arm is Confirm-gated — main.rs:857; abort policy needs no confirm), `export` (file-path Input, runs the streaming export to that path — completion lands in the result modal).
  - Resource actions (at resource level): `put` (path + file-path Inputs + Confirm — guarded since 05-02), `delete` (Confirm — guarded). ResourceBinary-style fencing surfaces as the action's error in the result modal.
  - Webdev menu: `deploy` (Confirm NOT required — webdev deploy is deliberately unguarded, 05-03 decision: the ign-cli project is CLI-owned; do NOT add a confirm here), `status` (result modal shows the sweep degradation data). Map the WebdevCommand leaves here.
  - Every menu entry: one-shot worker + 06-02 result modal (pretty-JSON). Rich-arg modals carry the "press ? for the CLI form" hint (LOCKED modal-depth decision) — `?` opens a result-modal-style pane with the equivalent `ign …` command line.
  - routes.rs: rows for every project/resource/webdev leaf (EXACT spellings from cli.rs ProjectCommand/ResourceCommand/WebdevCommand) → Screen(Projects).
  - Tests: confirm-gating units (delete/import-overwrite/put/resource-delete spawn nothing on cancel; deploy needs NO confirm); menu + CLI-hint render tests.
  </action>
  <verify>cargo test -p ignition-tui && cargo clippy --workspace --all-targets -- -D warnings</verify>
  <done>All project/resource/webdev verbs reachable with the exact CLI confirm-parity (guarded ones gated, deploy ungated); family rows complete in the registry.</done>
</task>

<task type="auto">
  <name>Task 3: Destructive-verb confirm-parity audit</name>
  <files>crates/ignition-tui/src/update.rs, crates/ignition-tui/src/routes.rs</files>
  <action>
  - Audit task (cheap, catches drift): grep ALL `require_confirmation` call sites in main.rs and cross-check every one whose verb family is registry-mapped so far (sessions, logs loggers set/reset, restart, project delete/import-overwrite, resource put/delete, tags provider/config delete + import overwrite) has a Confirm modal in the TUI flow that gates the worker spawn. Fix any misses.
  - Assert in a unit test that the set of confirm-gated TUI actions (a static list in the test) matches the expected guarded families — a soft parity tripwire until 06-06's structural test lands.
  - Sanity: verify no TUI path constructs a `Credential` or reads a `Secret` outside context.rs (grep) — secrets stay confined.
  </action>
  <verify>cargo test -p ignition-tui && grep -c "Modal::Confirm" crates/ignition-tui/src/update.rs >/dev/null && echo confirm-flow-present</verify>
  <done>Every CLI --yes-guarded verb reachable so far is TUI-confirm-gated; no secret handling outside context.rs.</done>
</task>

</tasks>

<verification>
- cargo test --workspace green; clippy -D warnings clean
- grep secrets confinement: `Secret|Credential` appears in ignition-tui ONLY in context.rs
- Esc navigation pops exactly one level at every depth
</verification>

<success_criteria>
Success Criterion 5 and the project half of TUI-06 delivered: full project/resource browse + act surface with CLI confirm-parity, ready for 06-06's structural coverage proof.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tui-cockpit/06-05-SUMMARY.md`
</output>
