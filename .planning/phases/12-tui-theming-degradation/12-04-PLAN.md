---
phase: 12-tui-theming-degradation
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "The CI grep pipeline demonstrably FAILS on a planted Color:: literal outside theme.rs and PASSES on the clean tree"
    - "Human confirms on a real terminal: theme selection works (default/dark/light/mono), the degradation ladder is readable at every tier, unknown theme name warns + starts, and the tab indicator is clearly visible with no parked cursor"
  artifacts: []
  key_links: []
---

<objective>
Prove the enforcement and verify the experience: negatively test the CI grep (plant a violation, watch it fail, revert), then put a human in front of the cockpit across the full capability ladder — the part no buffer assertion can judge (contrast, readability, aesthetics).

Purpose: success criterion 3's enforcement must be demonstrated, not assumed; success criteria 1 and 2 say "verified in capability-limited terminals" — that verification is inherently human (TUIX-03/04 acceptance).
Output: enforcement proof recorded in the summary; human approval of the degradation ladder and theme selection.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-tui-theming-degradation/12-03-SUMMARY.md
@.planning/phases/12-tui-theming-degradation/12-RESEARCH.md (theme set table + degradation rationale)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Negative-test the tokenization grep (plant → fail → revert)</name>
  <files></files>
  <action>
Prove the CI step actually bites (a grep that silently passes on violations is worse than no grep):

1. Plant: append a line containing `Color::Red` to a NON-theme source file — e.g. add `// #[cfg(dead_code)] fn _tokenization_probe() { let _ = ratatui::style::Color::Red; }` at the end of `crates/ignition-tui/src/ui/tags.rs` (comment-form is enough — the grep is textual; no compile needed).
2. Run the exact pipeline from ci.yml locally:
   `grep -rn -e 'Color::' -e 'style::Color' -e 'Color as' crates/ --include='*.rs' | grep -v 'src/ui/theme.rs'`
   EXPECT: the planted line appears → the pipeline is non-empty → ci.yml's step would exit 1.
3. Revert the planted line (`git checkout -- crates/ignition-tui/src/ui/tags.rs` or delete the line) — confirm `git status` shows no modification.
4. Re-run the pipeline on the clean tree. EXPECT: empty output, exit 0.

Record both runs (planted-offender output + clean output) in the summary. Commit NOTHING in this task — the tree must be byte-identical before and after.
  </action>
  <verify>Planted run prints the tags.rs offender line; clean run prints nothing; `git status --porcelain` empty at task end</verify>
  <done>Enforcement demonstrated in both directions: violation → fail, clean tree → pass; no residue</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — theme selection + degradation ladder on a real terminal</name>
  <files>none (verification only)</files>
  <action>
Pause for the user to verify the full theming system on a real terminal (steps below). No code changes in this task; fix-and-retest if the user reports issues.
  </action>
  <verify>User confirms all 8 steps: default-theme no-regression, dark/light theme selection applies consistently across screens, NO_COLOR mono remains readable with visible selection (REVERSED) and errors (BOLD), TERM=xterm and TERM=xterm-256color step-downs keep layout + contrast, theme="banana" warns-and-starts, active tab clearly highlighted, no parked cursor block</verify>
  <done>User types "approved" (optionally with hue-adjustment notes — values are adjustable post-UAT) or all reported issues are fixed and re-verified</done>
  <what-built>
The complete Phase 12 theming system: `[ui].theme` config selection over four named themes (default/mono/dark/light), per-tier authored palettes (truecolor/256/16/mono) with env-based tier detection (NO_COLOR/COLORTERM/TERM), token migration of all screens, themed tab-bar highlight, hidden cursor, and CI tokenization enforcement. All automated gates green (tests, clippy, fmt, lean build, grep).
  </what-built>
  <how-to-verify>
Use the established UAT rig setup (the uat profile / live-gateway tunnel recipe from prior phase UATs — e.g. 08-05's live WHK checkpoint). For each step, note what you SEE, not just that it runs:

1. **No regression (default theme):** config WITHOUT a `[ui].theme` key → `ign tui` → the cockpit looks as it did before Phase 12 (near-monochrome, red error labels, gray selection).
2. **Theme selection applies:** set `theme = "dark"` in the config's `[ui]` table → restart `ign tui` → dashboard AND logs screens visibly take the dark palette (colored borders/accents; error labels red; log levels colored); flip through all screens — the palette is consistent everywhere.
3. **Light theme:** `theme = "light"` viewed on a light-background terminal profile → readable (no vanishing DarkGray-on-white selection).
4. **Mono degradation:** `NO_COLOR=1 ign tui` (any theme in config) → fully monochrome cockpit where you can still: tell the selected table row apart (REVERSED), spot error states (BOLD), read everything. No invisible selection, no contrast collapse.
5. **16/256 step-down:** run under `TERM=xterm` (16-color) and `TERM=xterm-256color` → no broken layout, no unreadable contrast at either tier.
6. **Lenient unknown name:** `theme = "banana"` → a stderr warning appears before the alternate screen AND the cockpit starts with the default theme (never a crash/exit).
7. **Tab indicator (09-UAT Gap 2):** active tab is clearly highlighted; the terminal cursor does NOT park as a stuck block on the tab bar while navigating screens.
8. Palette aesthetics are explicitly adjustable post-UAT — note any hue you'd change, but structural failures (invisible/unreadable) are the blockers.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally with hue-adjustment notes) or describe the issues</resume-signal>
</task>

</tasks>

<verification>
- Task 1's both-direction grep proof recorded in the summary
- Checkpoint approval recorded with per-step observations; any hue notes captured as follow-up candidates (values are adjustable without breaking tests — tests pin slots, not literals)
</verification>

<success_criteria>
- CI tokenization enforcement proven working in both directions
- Human-verified: theme selection consistent across screens, readable at every capability tier (truecolor/256/16/mono), lenient unknown-name handling, visible tab indicator, hidden cursor
- Phase success criteria 1-3 all demonstrably TRUE
</success_criteria>

<output>
After completion, create `.planning/phases/12-tui-theming-degradation/12-04-SUMMARY.md`
</output>
