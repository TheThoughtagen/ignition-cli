---
phase: quick/260921-96c
plan: 01
subsystem: api
tags: [mcp, clap, rust, guarded-ops, workspace]

# Dependency graph
requires: []
provides:
  - "workspace push registered in GUARDED_OPS with a dispatch-site marker"
  - "MCP catalog derives an optional confirm boolean for workspace_push"
  - "workspace_push accepts confirm as a known wire argument; smuggled yes still refuses -32602"
affects: [mcp-server, workspace-push, guarded-ops-registry]

# Actuals (#2632)
actuals:
  tokens: 3723
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Dynamic-prose GUARDED_OPS entry pinned by dispatch-site marker alone (no main.rs require_confirmation literal — the gate lives in ignition-core)"

key-files:
  created: []
  modified:
    - crates/ignition-cli/src/main.rs
    - crates/ignition-cli/src/mcp.rs
    - README.md

key-decisions:
  - "RED was verified by temporarily removing the GUARDED_OPS tuple (Edit tool, not git stash) while keeping the dispatch-site marker, confirming both new mcp.rs tests fail, then restoring the tuple to its committed Task 1 state before GREEN — avoids any git stash/reset that could disturb the worktree"

requirements-completed: [QUICK-96c]

coverage:
  - id: D1
    description: "ign mcp serve tools/list advertises workspace_push with an optional confirm boolean (absent from required)"
    requirement: QUICK-96c
    verification:
      - kind: unit
        ref: "crates/ignition-cli/src/mcp.rs#guarded_leaves_carry_optional_confirm_and_readonly_leaves_do_not"
        status: pass
      - kind: integration
        ref: "crates/ignition-cli/tests/contract_mcp.rs#catalog_sample_round_trips_through_clap_parse"
        status: pass
      - kind: manual_procedural
        ref: "cargo run -p ignition-cli --bin ign -- mcp serve — tools/list spot-check confirmed workspace_push confirm in properties, absent from required; rig_down carries no confirm property"
        status: pass
    human_judgment: false
  - id: D2
    description: "A tools/call of workspace_push carrying confirm is accepted as a known argument (no -32602) and dispatches with --yes appended; a smuggled yes key still refuses -32602"
    requirement: QUICK-96c
    verification:
      - kind: unit
        ref: "crates/ignition-cli/src/mcp.rs#workspace_push_accepts_confirm_and_refuses_smuggled_yes"
        status: pass
    human_judgment: false
  - id: D3
    description: "The GUARDED_OPS drift test stays green in both directions with the new workspace push entry, no test loosening"
    requirement: QUICK-96c
    verification:
      - kind: unit
        ref: "crates/ignition-cli/src/main.rs#tests::guarded_ops_registry_tracks_every_dispatch_site"
        status: pass
    human_judgment: false

duration: 15min
completed: 2026-09-21
status: complete
---

# Quick Task 260921-96c: Register workspace push in GUARDED_OPS Summary

**Registered the `workspace push` clap leaf in `GUARDED_OPS` (one dynamic-prose entry + one dispatch-site marker), restoring SC-2 parity — `ign mcp serve` now advertises an optional `confirm` boolean for `workspace_push` and accepts `confirm: true` as the MCP-native confirmation, with the conflict-refusal path and `rig down` left untouched.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-09-21T06:40:00-06:00 (approx)
- **Completed:** 2026-09-21T06:54:30-06:00
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments
- `GUARDED_OPS` in `crates/ignition-cli/src/main.rs` now carries a `("workspace push", ...)` pair with representative `render_push_preview`-style prose, plus the `// guarded:workspace push` dispatch-site marker above the `workspace_push(...)` call in `WorkspaceCommand::Push` — the single const that both the MCP catalog and `execute`'s fail-closed argument set derive from
- `crates/ignition-cli/src/mcp.rs` gained an explicit `workspace_push` regression pin inside `guarded_leaves_carry_optional_confirm_and_readonly_leaves_do_not` (confirm present as boolean, absent from `required`) and a new `#[tokio::test]` (`workspace_push_accepts_confirm_and_refuses_smuggled_yes`) proving `confirm` is a known wire argument (argv parses, dispatch runs, `--yes` reaches `cli.yes`) and that a smuggled `yes` key still refuses `-32602`
- `README.md` documents the MCP-native `confirm: true` confirmation for `workspace push` in three places: the command-row table, the "Push is guarded, spliced, and honest" paragraph, and the MCP guarded-writes bullet (which now also names `rig down` as intentionally unguarded on both surfaces)
- Live spot-check against `cargo run -p ignition-cli --bin ign -- mcp serve` confirms `workspace_push` now lists `confirm` (optional) alongside `delete`/`path`, and `rig_down` still lists none

## Task Commits

1. **Task 1: Register workspace push end-to-end — registry entry + dispatch-site marker** - `02cbda8` (feat)
2. **Task 2: Per-leaf MCP pins — optional confirm in the schema, accepted on the wire, smuggled yes still refused** - `907e103` (test)
3. **Task 3: Document the MCP confirmation for workspace push** - `7800ba6` (docs)

_Task 2 is test-only: Task 1's registry entry already wired the schema and dispatch bridge generically (no code change needed in mcp.rs per the plan's interface notes), so Task 2 adds the regression pins without a separate feat/green commit. RED was verified before committing (see Decisions Made) rather than via a persisted failing-test commit._

## Files Created/Modified
- `crates/ignition-cli/src/main.rs` - `GUARDED_OPS` entry for `workspace push` (dynamic prose) + `// guarded:workspace push` dispatch-site marker in the `WorkspaceCommand::Push` arm
- `crates/ignition-cli/src/mcp.rs` - explicit `workspace_push` schema pin in the guarded-leaves test; new `workspace_push_accepts_confirm_and_refuses_smuggled_yes` test
- `README.md` - three surgical prose edits naming the MCP `confirm` field for `workspace push` and the `rig down` unguarded-by-design note

## Decisions Made
- RED verification for Task 2's TDD gate was performed by temporarily editing `main.rs` to remove the `("workspace push", ...)` tuple (keeping the dispatch-site marker) via the Edit tool, running only the two new `mcp::tests::*` targets to confirm both failed with the expected messages, then restoring the tuple to its exact Task-1-committed text (verified via `git diff` showing no residual change) before re-running to confirm GREEN. `git stash`/`git checkout --` were deliberately avoided per the destructive-git prohibition; the Edit-tool round-trip achieves the same isolation without touching git state.
- No architectural changes were needed — Task 1's single registry entry + marker lit up the entire MCP path (catalog schema, fail-closed argument set, `--yes` dispatch) with zero code changes to `mcp.rs` logic, exactly as the plan's interface notes predicted.

## Deviations from Plan

None - plan executed exactly as written. No Rule 1-4 auto-fixes were needed; all three tasks' `<verify>` and `<done>` criteria passed on the first attempt after implementation.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Verification Evidence

- `cargo test -p ignition-cli` — **294 passed, 0 failed, 27 ignored** (all ignored are opt-in live-gateway e2e tests gated on `IGNITION_LIVE_URL`/`IGNITION_LIVE_TOKEN`, unrelated to this change)
- `cargo clippy --workspace --all-targets -- -D warnings` — clean, zero warnings
- `cargo fmt --all -- --check` — clean, no diff
- `cargo test -p ignition-cli --bin ign guarded` — `guarded_ops_registry_tracks_every_dispatch_site` and `guarded_leaves_carry_optional_confirm_and_readonly_leaves_do_not` both pass
- `cargo test -p ignition-cli --test contract_mcp catalog_sample_round_trips_through_clap_parse` — passes, now sampling `workspace_push` in its wire-guarded set
- Manual spot-check via `cargo run -p ignition-cli --bin ign -- mcp serve` (never the installed `~/.cargo/bin/ign`): `workspace_push` carries `confirm` in `properties`, absent from `required`; `rig_down` carries no `confirm` property at all

## Next Phase Readiness
- No blockers. This closes the SC-2 parity gap for `workspace push` specifically; no other guarded verbs were touched.
- `rig down` remains deliberately unguarded on both CLI and MCP surfaces, as instructed — untouched by this task.

---
*Phase: quick/260921-96c*
*Completed: 2026-09-21*
