# Requirements Archive: v1.0 MVP

**Archived:** 2026-08-30
**Status:** SHIPPED

All v1 requirements below shipped in v1.0. A fresh `.planning/REQUIREMENTS.md` is created by `/gsd-new-milestone` for the next milestone.

---

# Requirements: ignition-cli

**Defined:** 2026-08-20
**Core Value:** One binary that lets a developer (or an AI agent) fully operate and inspect an Ignition 8.3+ gateway — health, projects, tags, rigs — without opening the gateway webpage or Designer.

## v1 Requirements

Requirements for initial release. Each maps to roadmap phases.

### Core Plumbing

- [x] **CORE-01**: User can configure multiple gateway profiles (add/use/list) with URL, auth, and label, selected via `--profile` flag or `IGNITION_PROFILE` env, with the active profile name visible in every output
- [x] **CORE-02**: User can authenticate via API token (preferred) or basic auth, supplied through env vars or config, with secrets never echoed in JSON output or logs
- [x] **CORE-03**: Every subcommand supports `--json` with stable field names, plus `--compact` one-line JSON for agents
- [x] **CORE-04**: Every command exits with documented stable exit codes (0 ok / 2 usage / distinct config, auth, network, target-state codes)
- [x] **CORE-05**: Errors render as a machine-readable JSON envelope with code, message, endpoint, and actionable hint (e.g., "WebDev route missing → run ign webdev deploy")
- [x] **CORE-06**: Commands are non-interactive by default; destructive operations require `--yes` or `IGNITION_YES` env
- [x] **CORE-07**: User can generate shell completions (bash/zsh/fish)
- [x] **CORE-08**: `ign version` checks gateway minimum version and refuses cleanly on <8.3.1

### Gateway Health

- [x] **HLTH-01**: User can view gateway info and status (version, platform, revision, running state)
- [x] **HLTH-02**: User can view module health for all modules (state, versions)
- [x] **HLTH-03**: User can list, fetch, download, and tail (`-f`) gateway logs
- [x] **HLTH-04**: User can get and set logger levels per-logger
- [x] **HLTH-05**: User can view database connection status
- [x] **HLTH-06**: User can view OPC connection status
- [x] **HLTH-07**: User can view system metrics (CPU, memory, historic + current, thread execution)
- [x] **HLTH-08**: User can view connected sessions: designers, Perspective sessions (incl. terminate), Vision sessions
- [x] **HLTH-09**: User can restart the gateway with optional `--wait` for ready
- [x] **HLTH-10**: User can run `ign doctor` — connectivity, auth, read/write permission, WebDev-route presence, and rig detection preflight
- [x] **HLTH-11**: User can wait/poll for gateway up, restart complete, or module ready

### Projects

- [x] **PROJ-01**: User can list projects with inheritance/parent info
- [x] **PROJ-02**: User can create, delete, copy, and rename projects
- [x] **PROJ-03**: User can export a project to file (full, with collision policy abort/overwrite on import)
- [x] **PROJ-04**: User can import a project from file or stdin
- [x] **PROJ-05**: User can list, get, put, and delete individual resources within a project (surgical edit loop)

### WebDev Backend

- [x] **WEB-01**: User can deploy the CLI's own versioned WebDev routes to a gateway via `ign webdev deploy` and check status via `ign webdev status`
- [x] **WEB-02**: WebDev routes version-negotiate: CLI refuses WebDev-dependent commands with an actionable error on route mismatch

### Tags

- [x] **TAGS-01**: User can list tag providers and create/delete providers
- [x] **TAGS-02**: User can browse tags as a tree with filtering
- [x] **TAGS-03**: User can read tag values (single or batch)
- [x] **TAGS-04**: User can write a tag value
- [x] **TAGS-05**: User can get, create, edit, and delete tag configs (JSON in/out)
- [x] **TAGS-06**: User can list UDT types and get UDT definitions
- [x] **TAGS-07**: User can view active alarms, query alarm history, and acknowledge alarms
- [x] **TAGS-08**: User can query tag history
- [x] **TAGS-09**: User can bulk export/import tag providers (json/xml/csv) with collision policy (default abort)

### Rig Lifecycle

- [x] **RIG-01**: User can run `rig up/down/status/reset` driven by compose-file discovery (git-module/WHK-Global conventions), with port-mapping awareness and wait-for-commissioned
- [x] **RIG-02**: User can view rig logs (passthrough) and trial status
- [x] **RIG-03**: User can reset trial state (spike decides: wrap existing Playwright resetter vs native headless HTTP+CSRF flow)
- [x] **RIG-04**: User can snapshot a rig (gwbk download + project/tag exports) and restore it to a repeatable state

### TUI Cockpit

- [x] **TUI-01**: User can open a ratatui cockpit (`ign tui`) exposing every CLI action through object-list → detail navigation (k9s/lazygit style)
- [x] **TUI-02**: User can view a live status dashboard (modules, sessions, metrics) with periodic refresh
- [x] **TUI-03**: User can tail gateway logs with level filtering in the TUI
- [x] **TUI-04**: User can browse tags and live-watch tag values in the TUI
- [x] **TUI-05**: User can view and acknowledge alarms in an alarm panel
- [x] **TUI-06**: User can browse projects/resources and switch profiles in the TUI

### Advanced Operations

- [x] **SYNC-01**: User can diff two gateways' projects at resource level (`ign project diff <profile-a> <profile-b>`)
- [x] **SYNC-02**: User can selectively sync resources between gateways (dev→test→prod promotion)
- [x] **SCRPT-01**: User can execute gateway scripts via `ign script run` (opt-in, disabled by default)
- [x] **BKUP-01**: User can download and restore gateway backups (gwbk) via native API
- [x] **BKUP-02**: User can list EAM task history and create EAM tasks (read-heavy; write scope guarded)

### Ecosystem Interop

- [x] **INTR-01**: User can export a project with `--decode-scripts` (emit `.py` alongside JSON) and import with `--encode-scripts` for round-trip editing in nvim/VS Code
- [x] **INTR-02**: User can run `ign lint` which delegates to ignition-lint on PATH, passing through exit codes and JSON reports (actionable install hint if absent)
- [x] **INTR-03**: User can run `ign tag browse --from-export <path>` to browse git-module tag exports offline

## v2 Requirements

Deferred to future release. Tracked but not in current roadmap.

### API Escape Hatch & Extensions

- **EXT-01**: `ign api call --method --path` raw passthrough for uncurated endpoints
- **EXT-02**: License status, diagnostics bundle generation/download, redundancy status, gateway-network/agent status as curated commands
- **EXT-03**: EAM write operations beyond guarded basics
- **EXT-04**: MCP-server transport mode (thin shim over the stable JSON contract)

### TUI Extensions

- **TUIX-01**: Configurable polling cadence per profile (prod vs dev)
- **TUIX-02**: TUI theming

## Out of Scope

Explicitly excluded. Documented to prevent scope creep.

| Feature | Reason |
|---------|--------|
| Ignition 8.1.x support | API variance not worth carrying; git-module v2 precedent; 8.3.1+ only |
| LSP / completions / hover / go-to-def | ignition-nvim + ignition-lsp own editing |
| Lint engine (rules, severities, suppression) | ignition-lint owns it; we delegate only |
| Designer-side Git integration | ignition-git-module owns it |
| Perspective/Vision view editing semantics | Editors + Designer own semantics; resource get/put covers raw-JSON edits |
| Web UI | TUI is the human interface; web UI recreates the webpage problem |
| Daemon / background service | Single binary constraint; poll-based wait/watch |
| OpenAPI-spec discovery subsystem | Scope creep; curated surface is the product (escape hatch in v2) |
| Vision project bin→XML auto-export | git-module roadmap owns it; CLI consumes results |
| gwbk authoring/parsing offline | Kindling/nvim territory; CLI treats gwbk as opaque bytes via native API |

## Traceability

Which phases cover which requirements. Updated during roadmap creation.

| Requirement | Phase | Status |
|-------------|-------|--------|
| CORE-01 | Phase 1 | Shipped (v1.0) |
| CORE-02 | Phase 1 | Shipped (v1.0) |
| CORE-03 | Phase 1 | Shipped (v1.0) |
| CORE-04 | Phase 1 | Shipped (v1.0) |
| CORE-05 | Phase 1 | Shipped (v1.0) |
| CORE-06 | Phase 1 | Shipped (v1.0) |
| CORE-07 | Phase 1 | Shipped (v1.0) |
| CORE-08 | Phase 1 | Shipped (v1.0) |
| HLTH-01 | Phase 2 | Shipped (v1.0) |
| HLTH-02 | Phase 2 | Shipped (v1.0) |
| HLTH-03 | Phase 2 | Shipped (v1.0) |
| HLTH-04 | Phase 2 | Shipped (v1.0) |
| HLTH-05 | Phase 2 | Shipped (v1.0) |
| HLTH-06 | Phase 2 | Shipped (v1.0) |
| HLTH-07 | Phase 2 | Shipped (v1.0) |
| HLTH-08 | Phase 2 | Shipped (v1.0) |
| HLTH-09 | Phase 2 | Shipped (v1.0) |
| HLTH-10 | Phase 2 | Shipped (v1.0) |
| HLTH-11 | Phase 2 | Shipped (v1.0) |
| PROJ-01 | Phase 3 | Shipped (v1.0) |
| PROJ-02 | Phase 3 | Shipped (v1.0) |
| PROJ-03 | Phase 3 | Shipped (v1.0) |
| PROJ-04 | Phase 3 | Shipped (v1.0) |
| PROJ-05 | Phase 3 | Shipped (v1.0) |
| RIG-01 | Phase 4 | Shipped (v1.0) |
| RIG-02 | Phase 4 | Shipped (v1.0) |
| RIG-03 | Phase 4 | Shipped (v1.0) |
| RIG-04 | Phase 4 | Shipped (v1.0) |
| WEB-01 | Phase 5 | Shipped (v1.0) |
| WEB-02 | Phase 5 | Shipped (v1.0) |
| TAGS-01 | Phase 5 | Shipped (v1.0) |
| TAGS-02 | Phase 5 | Shipped (v1.0) |
| TAGS-03 | Phase 5 | Shipped (v1.0) |
| TAGS-04 | Phase 5 | Shipped (v1.0) |
| TAGS-05 | Phase 5 | Shipped (v1.0) |
| TAGS-06 | Phase 5 | Shipped (v1.0) |
| TAGS-07 | Phase 5 | Shipped (v1.0) |
| TAGS-08 | Phase 5 | Shipped (v1.0) |
| TAGS-09 | Phase 5 | Shipped (v1.0) |
| TUI-01 | Phase 6 | Shipped (v1.0) |
| TUI-02 | Phase 6 | Shipped (v1.0) |
| TUI-03 | Phase 6 | Shipped (v1.0) |
| TUI-04 | Phase 6 | Shipped (v1.0) |
| TUI-05 | Phase 6 | Shipped (v1.0) |
| TUI-06 | Phase 6 | Shipped (v1.0) |
| SYNC-01 | Phase 7 | Complete |
| SYNC-02 | Phase 7 | Complete |
| SCRPT-01 | Phase 7 | Complete |
| BKUP-01 | Phase 7 | Complete |
| BKUP-02 | Phase 7 | Complete |
| INTR-01 | Phase 7 | Complete |
| INTR-02 | Phase 7 | Complete |
| INTR-03 | Phase 7 | Complete |

**Coverage:**
- v1 requirements: 53 total (previous footer said 46 — corrected 2026-08-21; actual REQ-ID count is 53)
- Mapped to phases: 53
- Unmapped: 0 ✓

---
*Requirements defined: 2026-08-20*
*Last updated: 2026-08-21 after roadmap creation (traceability filled, count corrected)*

## Requirement Outcomes (v1.0 close-out)

All 44 atomic v1 requirements (CORE, HLTH, PROJ, WEB, TAGS, RIG, TUI, SYNC, SCRPT, BKUP, INTR) shipped and live-verified across phases 1-7.

Outcome notes (validated with adjustments):

- **TAGS-09 (adjusted)**: Shipped JSON-native only. XML/CSV deferred to backlog — 05-RESEARCH live-proved JSON is the native interchange format (round-trip verified on a real 8.3.3 rig); xml/csv added format-surface cost without ecosystem demand.
- **PROJ-05 (adjusted mechanism)**: The roadmap's per-resource REST routes DO NOT EXIST on real 8.3 gateways (triple-verified in 05-02). Shipped as export → zip-member surgery → overwrite-import; UX contract (list/get/put/delete) unchanged, live-rig round-trip proven.
- **RIG-03 (spike resolved)**: Native Rust OIDC+CSRF trial reset (tier 1) — Playwright delegation rejected at 04-RESEARCH (Node+chromium runtime, broke across the 8.3.3 UI rewrite). Live-proven on 8.3.3 (expired→active) and 8.3.6.
- **BKUP-02 (scope honored)**: EAM family shipped with the guarded create ladder (backup tasks unguarded, mutating types `--yes`, restore/install/upgrade refused) — live-verified against the WHK controller rig; EAM write ops beyond guarded basics remain EXT-03 (v2).
- **SCRPT-01 (posture locked)**: Opt-in is structural — scriptExec deploys only via `ign webdev deploy --with-script-exec`; a missing secret = exit 6 with zero HTTP.
- **TUI-01 (exceeded)**: Every CLI action has a TUI mapping, CI-enforced by the tui_coverage clap-tree bidirectional walk — not aspirational, structurally proven.

Dropped: none.
