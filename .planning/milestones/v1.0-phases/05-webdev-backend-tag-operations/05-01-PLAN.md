---
phase: 05-webdev-backend-tag-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webdev/README.md
  - webdev/routes/project.json
  - webdev/routes/VERSION
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/tags/resource.json
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/tags/config.json
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/tags/doPost.py
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/tagConfig/resource.json
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/tagConfig/config.json
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/tagConfig/doPost.py
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/alarms/resource.json
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/alarms/config.json
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/alarms/doPost.py
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/tagHistory/resource.json
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/tagHistory/config.json
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/tagHistory/doPost.py
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/scriptExec/resource.json
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/scriptExec/config.json
  - webdev/routes/com.inductiveautomation.webdev/resources/cli/scriptExec/doPost.py
  - crates/ignition-core/src/webdev/mod.rs
  - crates/ignition-core/src/lib.rs
autonomous: true

must_haves:
  truths:
    - "Five route folders exist under webdev/routes with the Designer-native layout and every action from the research's corrected inventory answers its action-dispatch contract"
    - "Every route answers {\"action\":\"version\"} with the same bundle version; the version string appears in every doPost.py"
    - "The scriptExec route refuses ALL actions (including version) when its secret is unconfigured — fail-closed"
    - "Route sources are embedded in ignition-core via a manifest a later plan's deploy zips — no runtime file dependency"
  artifacts:
    - path: "webdev/routes/com.inductiveautomation.webdev/resources/cli/tags/doPost.py"
      provides: "tags route: version/browse/read/write"
      min_lines: 60
    - path: "webdev/routes/com.inductiveautomation.webdev/resources/cli/tagConfig/doPost.py"
      provides: "tagConfig route: version/getConfig/configure/deleteTags/listUDTTypes/getUDTDefinition/exportTags"
      min_lines: 80
    - path: "webdev/routes/com.inductiveautomation.webdev/resources/cli/alarms/doPost.py"
      provides: "alarms route: version/active/history/acknowledge"
      min_lines: 60
    - path: "webdev/routes/com.inductiveautomation.webdev/resources/cli/tagHistory/doPost.py"
      provides: "tagHistory route: version/query"
      min_lines: 40
    - path: "webdev/routes/com.inductiveautomation.webdev/resources/cli/scriptExec/doPost.py"
      provides: "scriptExec route: secret-gated version/exec"
      min_lines: 60
    - path: "crates/ignition-core/src/webdev/mod.rs"
      provides: "embedded route bundle manifest + ROUTE_BUNDLE_VERSION"
      exports: ["ROUTE_BUNDLE_VERSION", "ROUTE_FILES", "SCRIPT_EXEC_TEMPLATE"]
  key_links:
    - from: "crates/ignition-core/src/webdev/mod.rs"
      to: "webdev/routes/**"
      via: "include_str! per manifest entry"
      pattern: "include_str!"
    - from: "crates/ignition-core/src/lib.rs"
      to: "crates/ignition-core/src/webdev/mod.rs"
      via: "pub mod webdev"
      pattern: "pub mod webdev"
---

<objective>
Ship the CLI's own WebDev route sources — five action-dispatch routes (tags, tagConfig, alarms, tagHistory, scriptExec) with the versioned handshake contract — as repo files under `webdev/routes/` AND embedded into ignition-core so `ign webdev deploy` (05-03) can zip them from the binary with no source checkout.

Purpose: This is the gateway-side half of the phase. Every tag operation (TAGS-02..09) rides these routes; the version handshake (WEB-02) hangs off the shared `version` action. Research live-proved every scripting-API call these routes make — the corrected probe route is the skeleton.
Output: `webdev/routes/**` (5 route folders + project.json + VERSION), `crates/ignition-core/src/webdev/mod.rs` (embedded manifest), static contract tests.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-webdev-backend-tag-operations/05-RESEARCH.md

# The live-proven probe route — the skeleton for tags+tagConfig (copy the corrected call forms verbatim):
@.planning/phases/05-webdev-backend-tag-operations/artifacts/probe-route/com.inductiveautomation.webdev/resources/cli/probe/doPost.py
@.planning/phases/05-webdev-backend-tag-operations/artifacts/probe-route/com.inductiveautomation.webdev/resources/cli/probe/config.json

# WHK-Global's production auth module — the pattern to port into scriptExec (dual-header extract, sha256 constant-time, fail-closed):
@../data/projects/WHK-Global/ignition/script-python/core/util/webdev_auth/code.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: tags + tagConfig route sources</name>
  <files>webdev/routes/project.json, webdev/routes/VERSION, webdev/routes/com.inductiveautomation.webdev/resources/cli/tags/{resource.json,config.json,doPost.py}, webdev/routes/com.inductiveautomation.webdev/resources/cli/tagConfig/{resource.json,config.json,doPost.py}</files>
  <action>
Create the route scaffold at `webdev/routes/` mirroring the research's verified Designer-native layout. `project.json` = `{"title": "ign-cli", "description": "ign CLI WebDev routes (deployed by `ign webdev deploy` — do not edit)", "enabled": true, "inheritable": false, "parent": null}`. `VERSION` = the single line `1.0.0` (ROUTE_BUNDLE_VERSION). Each route folder gets `resource.json` exactly per the research's verified format (scope "G", version 1, restricted false, overridable true, files ["config.json", "doPost.py"], lastModification attributes) and `config.json` = the probe artifact's gates verbatim (python-resource, doPost enabled, require-auth FALSE — planner lock: all four always-on routes are open at the WebDev layer; the CLI's own credential still reaches the gateway).

Each doPost.py is SELF-CONTAINED (no cross-resource imports — WebDev route folders are independent; duplicating the ~25-line shared core across routes is deliberate, note it in a header comment). Shared core copied from the probe: `isinstance(data, (str, unicode))` re-parse (Pitfall 3), the `jv()` depth-capped walker (Pitfall: jsonEncode stack-overflows on Java objects), bare `except:` with `traceback.format_exc()` (catches Java Throwables), and the body envelope: success `{'ok': True, 'data': {...}}`, failure `{'ok': False, 'error': {'code': '<machine_code>', 'message': '<human>', 'traceback': <opt>}}` — NEVER rely on `'status'` (WebDev ignores it; denials ride HTTP 200).

Route `tags` actions (research "Action inventory"):
- `version` → `{'routeVersion': '1.0.0', 'minCli': '1.0'}` (same constants in every route — define `ROUTE_VERSION = '1.0.0'` and `MIN_CLI = '1.0'` at the top of each file).
- `browse {path=''}` → `system.tag.browse(path, {})` → results as dicts with keys fullPath/name/tagType/hasChildren/dataType (discriminator is `tagType`: Provider/Folder/AtomicTag/UdtType/UdtInstance/Property — pass ALL entries through including Property; filtering is the CLI's display decision).
- `read {paths:[...]}` → `system.tag.readBlocking(paths)` → per-path `{path, value (native JSON type), quality (str), timestamp (str)}`.
- `write {path, value}` → `system.tag.writeBlocking([path],[value])` → `{path, quality: str(qv[0])}` (Quality has no `.quality` attr — prior-art defect 9-adjacent).

Route `tagConfig` actions — every ignition-mcp defect corrected:
- `version` (same shape).
- `getConfig {tagPath}` → `system.tag.getConfiguration(data['tagPath'], False)` — STRING first arg, never a list (defect: list's `[` poisons TagPathParser). jv()-walk the result. Include an optional `recursive` bool (default False) for the UDT-definition case.
- `configure {basePath, tags, collisionPolicy='m'}` → `system.tag.configure(basePath, tags, collisionPolicy)` — basePath is `'[default]'`-form, NOT a provider name; children NEST under Folder/UdtType entries (slash-names rejected); alarms are a LIST of dicts; `tagType` (not `type`) is the discriminator. Return `{'results': [str(x) for x in result]}` — quality strings verbatim ('Good' / 'Bad_NotFound(...)' / 'Error_Configuration(...)' / 'Bad_Unsupported(...)').
- `deleteTags {paths:[...]}` → `system.tag.deleteTags(paths)` → `{'deleted': len(paths)}` (returns nothing; count echoes request length).
- `listUDTTypes {provider='default'}` → `system.tag.browse('[%s]_types_' % provider, {})` → same entry shape as browse.
- `getUDTDefinition {provider, name}` → `getConfiguration('[%s]_types_/%s' % (provider, name), True)` recursive + jv() walk.
- `exportTags {paths:[...]}` → `system.tag.exportTags(tagPaths=data['paths'])` — KWARGS ONLY (positional fails) → `{'payload': <json string>}`.
  </action>
  <verify>`cat webdev/routes/VERSION` prints 1.0.0; `python3 -c "import json;[json.load(open(p)) for p in ['webdev/routes/project.json'] + __import__('glob').glob('webdev/routes/**/*.json', recursive=True)]"` parses every JSON file; grep confirms `tagPaths=` (kwargs) and `data\['tagPath'\]` (string arg) forms.</verify>
  <done>tags + tagConfig doPost.py files exist with all 4 + 7 actions dispatching, both config.json files match the probe gates, resource.json files list exactly [config.json, doPost.py].</done>
</task>

<task type="auto">
  <name>Task 2: alarms + tagHistory + secret-gated scriptExec routes</name>
  <files>webdev/routes/com.inductiveautomation.webdev/resources/cli/alarms/{resource.json,config.json,doPost.py}, webdev/routes/com.inductiveautomation.webdev/resources/cli/tagHistory/{resource.json,config.json,doPost.py}, webdev/routes/com.inductiveautomation.webdev/resources/cli/scriptExec/{resource.json,config.json,doPost.py}</files>
  <action>
Same scaffold (resource.json/config.json identical to Task 1's — scriptExec included: its gate is IN-CODE, not config.json; see posture lock below).

Route `alarms`:
- `version` (same shape).
- `active {source?, priority?, state?}` → `system.alarm.queryStatus()` (pass supported filters as kwargs when present) → iterate to a list of `{eventId: str(e), source: str, state: str, priority: str, name: str}` (eventId is a UUID object — stringify; state strings look like 'Active, Unacknowledged').
- `history {startDateMs, endDateMs, ...}` → `system.alarm.queryJournal(...)` inside its own try/except: when the raised error names the missing journal ('No alarm journal profile specified'), return the STRUCTURED denial `{'ok': False, 'error': {'code': 'no_alarm_journal', 'message': <original text>}}` — the CLI maps this code to an actionable slug. Other errors ride the generic envelope.
- `acknowledge {eventIds:[...], note, username}` → `system.alarm.acknowledge([str(i) for i in eventIds], note, username)` — the gateway-scope 3-arg form (2-arg fails; UUID objects don't coerce to String[]). Return the list of UNacknowledged ids the call yields (8.3 behavior) as `{'unacknowledged': [...]}`.

Route `tagHistory`:
- `version`.
- `query {paths:[...], startDateMs, endDateMs, aggregationMode='LastValue', returnSize=N}` → `system.tag.queryTagHistory(paths=..., aggregationMode=..., returnSize=..., startDate=Date(long(startDateMs)), endDate=Date(long(endDateMs)))` — `long()` wrap is mandatory (`Date(float)` fails coercion, Pitfall 12). Convert the returned dataset to `{columns: [str,...], rows: [[jv(cell),...],...]}` — the time column is `t_stamp` (NOT 'Timestamp', defect 8); tag columns are provider-relative paths. jv() every cell.

Route `scriptExec` — PLANNER-LOCKED security posture (user decision LOCKED: own auth, never wide-open):
- config.json stays require-auth FALSE / user-source "" — the secret gate is THE auth mechanism (API tokens get 401 on require-auth routes, so a Basic layer would lock the CLI's own token-authed calls out; user-source:"default" breaks on renamed IdPs — research Open Question 3 resolved to secret-only, rationale in 05-RESEARCH "Recommended mechanism" layer 1).
- Module-level `SECRET = None` with a loud comment: the deploy step (05-03) generates doPost.py from this template by substituting `__IGN_CLI_SECRET__` → the generated hex secret. Unconfigured (None or placeholder left in place) = reject EVERY action including version — fail-closed (WHK `_UNCONFIGURED` precedent).
- Port WHK-Global's ~40-line gate, adapted: `extract` from either `x-ignition-cli-secret` or `authorization: Bearer` header (lowercase the header dict first — WebDev preserves sent case); constant-time compare via sha256-both-sides (`java.security.MessageDigest` — Jython 2.7 lacks `hmac.compare_digest`); denial = the body envelope `{'ok': False, 'error': {'code': 'secret_required'|'secret_mismatch'}}` at HTTP 200.
- `version` + `exec {code}` actions: exec runs `code` capturing stdout via the OutputCapture pattern (`system.util.executeAs?` NO — plain eval/exec with redirected stdout: `from java.io import ByteArrayOutputStream; ... System.setOut` or python `StringIO` redirect — use whichever the probe's session used; if neither was probed, use `exec(code, globals_dict)` with a `sys.stdout = StringIO()` capture and restore, plus `elapsed = time.time()` around it). Return `{'stdout': str, 'result': jv(result if any), 'elapsedMs': int}`. Log `sha256(code)[:12] + elapsed` per invocation (audit pattern) via `system.util.logger('ign-cli-scriptexec')`.
  </action>
  <verify>grep scriptExec/doPost.py for `SECRET = None` (fail-closed default), `MessageDigest` (constant-time), and both header names; grep alarms/doPost.py for the 3-arg acknowledge call and `no_alarm_journal`; grep tagHistory/doPost.py for `long(` (Date coercion) and `t_stamp` passthrough.</verify>
  <done>All five route folders complete; scriptExec refuses with structured body when SECRET is None; alarms acknowledge uses (ids, note, username); tagHistory wraps dates with long() and passes t_stamp through verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Embedded bundle module + static contract tests</name>
  <files>crates/ignition-core/src/webdev/mod.rs, crates/ignition-core/src/lib.rs</files>
  <action>
Create `crates/ignition-core/src/webdev/mod.rs`:
- `pub const ROUTE_BUNDLE_VERSION: &str = "1.0.0";` and `pub const MIN_CLI: &str = "1.0";`
- `pub const ROUTE_FILES: &[(&str, &str)]` — (zip_member_path, contents) pairs via `include_str!("../../../../webdev/routes/...")` for the ALWAYS-ON set: project.json + the four route folders (tags/tagConfig/alarms/tagHistory × resource.json/config.json/doPost.py). Zip member paths use forward slashes exactly as the research's folder format: `project.json`, `com.inductiveautomation.webdev/resources/cli/tags/doPost.py`, etc.
- `pub const SCRIPT_EXEC_TEMPLATE: &str = include_str!("../../../../webdev/routes/com.inductiveautomation.webdev/resources/cli/scriptExec/doPost.py");` — kept SEPARATE from ROUTE_FILES (deploy substitutes the secret before packing it; it must be impossible to deploy scriptExec unsubstituted by accident).
- A `#[cfg(test)]` static-contract block asserting: (1) every ROUTE_FILES entry's declared doPost.py contains `ROUTE_VERSION = '1.0.0'` and `minCli`-equivalent constants matching the Rust consts (string-containment — Jython isn't parseable here); (2) no ROUTE_FILES member contains the `__IGN_CLI_SECRET__` placeholder (scriptExec only, template-only); (3) SCRIPT_EXEC_TEMPLATE contains `__IGN_CLI_SECRET__` exactly once and `SECRET = None`; (4) no embedded member name contains a backslash; (5) the manifest lists exactly 13 members (1 project.json + 4×3).
Register `pub mod webdev;` in lib.rs.
  </action>
  <verify>`cargo test -p ignition-core webdev` green; `cargo clippy --workspace --all-targets -- -D warnings` clean.</verify>
  <done>Embedded manifest compiles, static contract tests pass, scriptExec is template-only (not in ROUTE_FILES), lib.rs registers the module.</done>
</task>

</tasks>

<verification>
- All 5 route folders + project.json + VERSION exist under webdev/routes/ with the exact Designer-native layout (folders `com.inductiveautomation.webdev/resources/cli/<route>/`).
- Every JSON file parses; every resource.json lists exactly ["config.json", "doPost.py"].
- The embedded manifest's static tests pass in CI with zero gateway dependency.
- Route sources encode ONLY live-verified call forms (spot-check the 9 defect corrections from 05-RESEARCH are absent: no list-arg getConfiguration, no provider-arg configure, no dict alarms, no 2-arg acknowledge, no 'Timestamp' column, no importTags).
</verification>

<success_criteria>
- Five self-contained action-dispatch routes covering the research's full action inventory (parity with the 21-tool MCP surface's route-side needs).
- Version handshake constants identical across all routes and the Rust embed.
- scriptExec fail-closed without a baked secret; template placeholder verified by test.
- ignition-core embeds the always-on bundle; a later plan zips it without touching the filesystem.
</success_criteria>

<output>
After completion, create `.planning/phases/05-webdev-backend-tag-operations/05-01-SUMMARY.md`
</output>
