---
phase: 06-tui-cockpit
plan: 06
type: execute
wave: 6
depends_on: ["06-05"]
files_modified:
  - crates/ignition-tui/src/ui/rig.rs
  - crates/ignition-tui/src/workers/rig_stream.rs
  - crates/ignition-tui/src/state.rs
  - crates/ignition-tui/src/update.rs
  - crates/ignition-tui/src/event.rs
  - crates/ignition-tui/src/routes.rs
  - crates/ignition-cli/tests/tui_coverage.rs
  - README.md
autonomous: true
must_haves:
  truths:
    - "User can view rig status and trigger rig actions (up/down/reset/restore/status/logs/trial/snapshot) from a Rig screen with confirm modals on exactly the CLI-guarded ones (reset, restore, trial reset)"
    - "A CI test walks the live clap CommandFactory tree and asserts bidirectional equality with the TUI route registry: every CLI leaf has a mapping, no orphan rows — full coverage, structurally proven"
    - "Out-of-band mappings are exactly the leaf-representable sanctioned stdout exceptions — the `completions` leaf only; the flag-value/stream-form exceptions (`logs -f` NDJSON, `tags export -o -`) are not distinct leaves and are documented in a routes.rs comment"
    - "README documents ign tui: interactive-only, silent stdout on success, errors-after-restore to stderr"
  artifacts:
    - path: crates/ignition-tui/src/routes.rs
      provides: "COMPLETE static registry: one row per CLI leaf command, no gaps, no orphans"
    - path: crates/ignition-cli/tests/tui_coverage.rs
      provides: "The structural-completeness CI proof (clap tree walk vs routes())"
      contains: "CommandFactory"
      min_lines: 40
    - path: crates/ignition-tui/src/ui/rig.rs
      provides: "Rig status render + action menu + raw logs stream pane"
  key_links:
    - {from: "crates/ignition-cli/tests/tui_coverage.rs", to: "clap tree", via: "CommandFactory::command() leaf walk", pattern: "Cli::command\\(\\)"}
    - {from: "crates/ignition-cli/tests/tui_coverage.rs", to: "routes registry", via: "bidirectional set equality", pattern: "ignition_tui::routes::routes\\(\\)"}
    - {from: "rig logs pane", to: "compose stream", via: "run_streaming sink -> ring", pattern: "rig"}
---

<objective>
Final plan: the Rig screen (status + guarded action menu + raw compose-logs stream pane), completion of the routes registry to FULL CLI-leaf coverage, the tui_coverage.rs CI test that structurally proves Success Criterion 1, and the README's TUI documentation.

Purpose: The phase's defining claim — "structurally complete because TUI and CLI share the same actions layer" — becomes machine-enforced: adding a future CLI command without a TUI mapping FAILS CI.
Output: Last screen, complete registry, the coverage proof, docs.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tui-cockpit/06-01-SUMMARY.md
@crates/ignition-core/src/actions/rig.rs
@crates/ignition-cli/src/cli.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rig screen — status, guarded action menu, logs stream pane</name>
  <files>crates/ignition-tui/src/ui/rig.rs, crates/ignition-tui/src/workers/rig_stream.rs, crates/ignition-tui/src/state.rs, crates/ignition-tui/src/update.rs, crates/ignition-tui/src/event.rs</files>
  <action>
  - Rig screen (docker-side, no gateway client): on entry, a one-shot worker calls the rig status action (check actions/rig.rs for the status/compose-state shape) and renders a summary (containers + state). Actions menu (`a`) hosts the RigCommand verbs: up/down/reset/restore/status/logs/trial/snapshot (check the real leaf set in cli.rs RigCommand/TrialCommand). CLI confirm-parity (LOCKED accept ≡ --yes rule — match main.rs:1359-1364 EXACTLY): `reset`, `restore`, and `trial reset` are the ONLY confirm-gated rig verbs — they open Confirm modals first, accept ≡ --yes, cancel spawns nothing. `up`/`down`/`status`/`logs`/`trial status`/`snapshot` run DIRECT with no modal — in particular `down` is deliberately UNGUARDED in the CLI (RigCommand::Down dispatches without require_confirmation at main.rs:1415; compose down keeps volumes) and must NOT acquire a TUI gate. Rig actions ride the same one-shot worker + result-modal pattern; results render as pretty-JSON.
  - `workers/rig_stream.rs`: rig logs pane — the rig logs action's streaming sink (`dyn FnMut(&str) + Send` — the rig sinks ALREADY have Send) sends each compose line as an AppEvent into a second ring buffer (reuse the 06-03 ring pattern; raw lines, cap 10_000). Map the `rig logs` route row as Streamed (raw-pane case — this is what the Streamed mapping kind exists for). Shutdown on leaving the pane/screen via the watch channel.
  - routes.rs: rows for every rig leaf (exact spellings from RigCommand/TrialCommand) — Screen(Rig) except `rig logs` → Streamed.
  - Tests: menu/confirm units; ring reuse unit; TestBackend render of the status summary + a seeded logs pane.
  </action>
  <verify>cargo test -p ignition-tui</verify>
  <done>Rig family fully reachable: status view, guarded verbs confirm-gated, live raw logs pane, family rows in the registry.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the registry + the tui_coverage.rs CI proof</name>
  <files>crates/ignition-tui/src/routes.rs, crates/ignition-cli/tests/tui_coverage.rs</files>
  <action>
  - Complete routes(): walk cli.rs's full Subcommand tree (Commands + every nested enum: SessionsCmd, LogsCmd/LoggersCmd, WaitCmd, ProjectCommand, ResourceCommand, WebdevCommand, TagsCommand + its nested Provider/Config/Udt/Alarms/History enums, RigCommand/TrialCommand, ProfileCmd, Connections, Completions, Tui) and ensure one row per row-requiring node (rule below). Explicitly include the dashboard-family top-level leaves — `status`, `modules`, `metrics` (Dashboard panels) and `connections` (cli.rs:82 — its TUI home is the Dashboard actions-menu one-shot read + pretty-JSON result modal built in 06-02) → Screen(Dashboard). The bare-form rows from 06-02/06-03 must be present: `sessions` → Screen(Dashboard), `logs` → Screen(Logs), `logs loggers` → Screen(Logs). Path format = space-joined clap names exactly as the tree walk will see them.
  - Coverage rule (used BOTH for writing routes() and by the CI test): a clap node REQUIRES a registry row when (a) its subcommands minus auto-generated `help` are empty (a true leaf), OR (b) `!cmd.is_subcommand_required_set()` (public clap API) — the Option-subcommand case where the bare form is itself an invocable action (`SessionsArgs.command: Option<SessionsCmd>` cli.rs:835, `LogsArgs.command: Option<LogsCmd>` :888, `LoggersArgs.command: Option<LoggersCmd>` :910 → bare `ign sessions`, `ign logs`, `ign logs loggers`). Nodes that REQUIRE a subcommand (e.g. `rig trial` — TrialCommand is not Option) get no row of their own; only their children do.
  - Create `crates/ignition-cli/tests/tui_coverage.rs` (RESEARCH Pattern 5): `fn walk(cmd: &clap::Command, prefix: &mut String, out: &mut Vec<String>)` recursing `get_subcommands()` and SKIPPING any subcommand named `help` (clap auto-generates `help` under every node — `disable_help_subcommand` is set nowhere — which would otherwise pollute the leaf set); a node is emitted per the coverage rule above (no non-help subcommands, OR `!is_subcommand_required_set()`), recursing into children regardless. The test walks `Cli::command()` (clap `CommandFactory` — the same mechanism clap_complete uses), compares against `ignition_tui::routes::routes()` paths, and asserts BOTH directions: `missing` (CLI row-requiring nodes with no TUI mapping — must be empty) and `orphans` (registry rows with no CLI node — must be empty). Include a Mapping-kind sanity check: the OutOfBand row set is exactly `["completions"]` — the only leaf-representable sanctioned stdout exception (STATE.md's four: the `completions` leaf; `logs -f` NDJSON and `tags export -o -` are FLAG VALUES on Screen-mapped leaves, not distinct leaves; `rig logs` raw maps as Streamed). Document the flag-value exceptions in a routes.rs comment so the four-exception STATE list stays traceable.
  - The test runs under default features (`tui` is default-on; ignition-tui is a normal dep for the test). Verify it FAILS meaningfully when a row is deleted (temporarily remove one row, see the orphan/missing assert fire, restore) — this validates the test actually bites.
  - Run the full workspace suite — this test now guards every prior plan's registry rows.
  </action>
  <verify>cargo test -p ignition-cli --test tui_coverage && cargo test --workspace</verify>
  <done>Bidirectional coverage test green: every row-requiring CLI node mapped (true leaves + the bare-invocable `sessions`/`logs`/`logs loggers` forms), zero orphans, OutOfBand = exactly the `completions` leaf; test proven to fail on registry drift.</done>
</task>

<task type="auto">
  <name>Task 3: README TUI section + phase verification sweep</name>
  <files>README.md, crates/ignition-tui/src/routes.rs</files>
  <action>
  - README: add a short TUI section documenting: `ign tui` is interactive-only (TTY pre-check, exit 2 otherwise); silent stdout on success (LOCKED decision — the envelope contract is the CLI's, the alt screen owns display); errors after restore render to stderr per the frozen taxonomy; keybindings summary (q/Ctrl-C quit, Tab cycle, Enter/Esc navigate, a actions, p profiles); the coverage guarantee (CI-enforced CLI↔TUI mapping).
  - Phase verification sweep: (1) cargo test --workspace + clippy -D warnings + cargo fmt --check; (2) `cargo tree -i crossterm` single 0.29; (3) grep audits — update.rs has no `.await`; `Secret|Credential` confined to context.rs in ignition-tui; main.rs Tui arm still ≤ ~10 lines; (4) optional #[ignore]-style live smoke (needs IGNITION_LIVE_URL/TOKEN per repo convention — quiet skip without env): open-and-quit against the rig if a pty harness is cheap, otherwise document the manual smoke steps in the SUMMARY.
  - routes.rs: final read-through — every row's Mapping kind matches where the action actually lives (Screen assignments honest vs the implemented screens).
  </action>
  <verify>cargo test --workspace && cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --check</verify>
  <done>All five phase success criteria machine-checkable or documented: cockpit + coverage test (SC1), dashboard+profile (SC2), tail+filter+non-blocking (SC3), tags/alarms (SC4), projects (SC5); README current.</done>
</task>

</tasks>

<verification>
- cargo test --workspace green INCLUDING tui_coverage.rs (the SC1 proof)
- clippy -D warnings + fmt clean; single crossterm major
- grep audits all pass (update purity, secret confinement, choke-file discipline)
</verification>

<success_criteria>
Phase 6 structurally complete: every CLI action TUI-mapped and CI-enforced, all six TUI requirements delivered, terminal lifecycle safe, README documents the interface.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tui-cockpit/06-06-SUMMARY.md`
</output>
