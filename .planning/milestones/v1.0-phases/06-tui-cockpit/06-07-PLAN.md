---
phase: 06-tui-cockpit
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - crates/ignition-core/src/client/metrics.rs
  - crates/ignition-core/src/client/classify.rs
  - crates/ignition-core/src/error.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/src/render.rs
  - crates/ignition-tui/src/ui/dashboard.rs
  - README.md
autonomous: true
must_haves:
  truths:
    - "ign metrics (gauges) succeeds on 8.3.3 gateways that serialize heapMemory/maxMemory as Java doubles in scientific notation (e.g. 2.85746728E8)"
    - "The TUI dashboard metrics panel populates on the same 8.3.3 gateways"
    - "Designer prune of a live session exits 6 with slug session_not_prunable and a 'close the Designer first' hint — not exit 1 internal-error"
    - "ign tui refused under a pipe shows a TTY-relevant hint ('run ign tui in an interactive terminal'), not the --file/stdin resource-put hint"
  artifacts:
    - path: crates/ignition-core/src/client/metrics.rs
      provides: "CurrentGauges heap/max as f64 + exponent-form decode fixture test"
      contains: "f64"
    - path: crates/ignition-core/src/client/classify.rs
      provides: "409 Conflict mapping for the designer-prune route to an exit-6 target-state slug"
      contains: "CONFLICT"
    - path: crates/ignition-core/src/error.rs
      provides: "session_not_prunable slug + exit 6 + actionable hint; TTY-contextual InvalidInput hint mechanism"
  key_links:
    - {from: "crates/ignition-core/src/client/metrics.rs", to: "wire exponent forms", via: "f64 serde decode + fixture", pattern: "E8|f64"}
    - {from: "crates/ignition-core/src/client/classify.rs", to: "CoreError exit-6 slug", via: "409 arm", pattern: "session_not_prunable"}
    - {from: "crates/ignition-cli/src/main.rs", to: "error.rs hint", via: "TTY raise site carries contextual hint", pattern: "interactive terminal"}
---

<objective>
Close three ignition-core gaps from 06-UAT.md: (1) metrics decode failure on 8.3.3 gateways [major], (2) designer-prune 409 surfacing as exit-1 internal [minor], (3) misleading --file/stdin hint on the `ign tui` TTY refusal [minor].

Purpose: The CLI and TUI must work against 8.3.3 gateways (the version actually in the field), and error surfaces must be actionable.
Output: Fixed wire models, a new additive exit-6 slug, a contextual TTY hint — all pinned by tests.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tui-cockpit/06-UAT.md

@crates/ignition-core/src/client/metrics.rs
@crates/ignition-core/src/client/classify.rs
@crates/ignition-core/src/error.rs
@crates/ignition-cli/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CurrentGauges decode for exponent-form Java doubles</name>
  <files>crates/ignition-core/src/client/metrics.rs, crates/ignition-cli/src/render.rs, crates/ignition-tui/src/ui/dashboard.rs</files>
  <action>
  Gap 06-UAT test 4 [major]. Root cause (wire-verified 2026-08-28): 8.3.3 (b2026012009) serializes the heap gauge as a Java double in scientific notation — raw body: {"cpu":1.2755618546264424,"heapMemory":2.85746728E8,"maxMemory":1073741824} — and serde_json refuses exponent/decimal forms for i64.

  1. In `crates/ignition-core/src/client/metrics.rs` (lines ~34-38): change `heap_memory: i64` and `max_memory: i64` to `f64` in BOTH the wire struct and the domain `CurrentGauges`. Byte counts <= ~9e15 are exact in f64, so no precision loss at JVM heap scales. `cpu` is already f64; charts (`Datapoint.value`) already f64; threads are plain integers — leave those alone.
  2. Add a unit test fixture with the exact raw 8.3.3 body above (exponent-form heapMemory) pinning the decode — extend the existing tests at metrics.rs:145 that currently only feed integer forms. Cover: exponent form, integer form, and a decimal form (e.g. 2.8574672E8) all decoding.
  3. Audit sibling models in metrics.rs for other integer-typed gauge fields that could arrive exponent-form on other builds — the wire structs are the audit surface; document the audit conclusion in a comment.
  4. Update display consumers for f64: `crates/ignition-cli/src/render.rs:363` (`human_bytes(gauges.heap_memory)` — make human_bytes take f64 or convert at call site, keeping existing golden output for integer-valued bytes) and `crates/ignition-tui/src/ui/dashboard.rs:285` (`fn fmt_mib(bytes: i64)` → accept f64; callers at :73-74 and :142-143). Existing snapbox goldens for `ign metrics` must stay green — the rendered output for whole-byte values must not change format.
  </action>
  <verify>cargo test -p ignition-core metrics && cargo test -p ignition-cli && cargo test -p ignition-tui</verify>
  <done>Exponent-form fixture test passes; gauges decode from the raw 8.3.3 body; CLI + TUI display formatters consume f64; existing goldens green</done>
</task>

<task type="auto">
  <name>Task 2: Map designer-prune 409 to an exit-6 target-state slug</name>
  <files>crates/ignition-core/src/client/classify.rs, crates/ignition-core/src/error.rs, README.md</files>
  <action>
  Gap 06-UAT test 6 [minor]. Root cause (wire-verified): DELETE /data/api/v1/designer/{id} against a live Designer session returns 409 empty body; `classify()` (crates/ignition-core/src/client/classify.rs:30) has no 409 arm, so it falls through to CoreError::Internal (exit 1, "internal errors are bugs" hint). 409 means prune-refused-for-live-designer — a target-state condition.

  1. Add an additive CoreError variant in the exit-6 target-state class (follow the existing refusal-slug pattern), slug `session_not_prunable`, exit 6, hint: "close the Designer first — prune removes stale entries only". Frozen-taxonomy constraint applies to changing existing slugs/codes — an ADDITIVE slug in an existing exit class is the established growth pattern.
  2. Wire the 409 arm in classify() for the designer-prune route (scope the mapping to that route so other routes' 409s keep current behavior).
  3. Also distinguish the perspective terminate 404 ("No valid sessions found to close" — id-vs-scope mismatch) from generic not_found if it can be done additively and route-scoped; if the empty 404 body makes this undetectable, skip it and note why in a comment.
  4. Update the README exit-code table (the table lives in exactly TWO places — CoreError::exit_code() + README — enforced by the enumerated unit test) and the enumerated test with the new slug.
  </action>
  <verify>cargo test -p ignition-core && cargo test -p ignition-cli</verify>
  <done>A 409 on designer prune classifies to exit 6 / session_not_prunable with the actionable hint; README table + enumerated unit test updated; all existing slugs/codes unchanged</done>
</task>

<task type="auto">
  <name>Task 3: Contextual hint for the ign tui TTY refusal</name>
  <files>crates/ignition-core/src/error.rs, crates/ignition-cli/src/main.rs</files>
  <action>
  Gap 06-UAT test 2 [minor]. Root cause: the TTY guard at main.rs:1642-1645 rides CoreError::InvalidInput whose hint (error.rs:400) is baked per-variant: "fix the input source — a readable file path via --file, or `-` to pipe the content on stdin" — the resource-put hint, meaningless for `ign tui | cat`.

  Constraint: NO new taxonomy variant (frozen); slug and exit code (2, usage class) must not change; the existing --file/stdin hint must keep firing for the resource-put raise sites (error.rs:964/976, main.rs:1923/1928).

  Implement a hint override at the raise site or a hint-parameterized constructor for InvalidInput — pick whichever is least invasive while keeping every existing snapbox golden green. The TTY refusal must render a hint like: "run ign tui in an interactive terminal" (wording flexible, meaning fixed). Add/extend a snapbox golden pinning `ign tui` under a pipe: exit 2, correct envelope, the new hint, no alt-screen escapes.
  </action>
  <verify>cargo test -p ignition-cli</verify>
  <done>`ign tui | cat` golden shows the interactive-terminal hint; resource-put InvalidInput goldens unchanged; exit code and slug unchanged</done>
</task>

</tasks>

<verification>
- cargo test --workspace --features tui (all 698+ tests green)
- cargo fmt --all --check && cargo clippy --workspace --features tui --all-targets -- -D warnings
- The exponent-form fixture, the 409 classify test, and the TTY golden are all present and green
</verification>

<success_criteria>
- All four must_have truths hold, each pinned by a test added in this plan
- No existing slug, exit code, envelope shape, or golden output changed (additive only)
</success_criteria>

<output>
After completion, create `.planning/phases/06-tui-cockpit/06-07-SUMMARY.md`
</output>
