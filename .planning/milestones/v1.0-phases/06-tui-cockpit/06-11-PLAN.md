---
phase: 06-tui-cockpit
plan: 11
type: execute
wave: 3
depends_on: ["06-09", "06-10"]
gap_closure: true
files_modified:
  - crates/ignition-tui/src/ui/rig.rs
  - README.md
autonomous: true
must_haves:
  truths:
    - "The Rig status summary renders as a readable, structured panel (grouped key-value rows with clear section separation) instead of the cramped layout the UAT called out"
    - "The README TUI section documents every key added by gap closure (r on Tags, vim motions in modals) alongside the existing keymap"
  artifacts:
    - path: crates/ignition-tui/src/ui/rig.rs
      provides: "Readable structured rig status summary render"
    - path: README.md
      provides: "TUI keymap documentation synchronized with gap-closure keys"
  key_links:
    - {from: "ui/rig.rs summary_lines", to: "render_summary", via: "structured layout", pattern: "summary_lines"}
    - {from: "README TUI section", to: "new keys", via: "keymap rows", pattern: "vim|refresh"}
---

<objective>
Close the final 06-UAT gap (test 13 [minor]: "rig status doesn't show well" — display quality, root cause TBD at diagnosis) and synchronize the README's TUI keymap with every key the gap-closure plans added.

Purpose: The Rig screen is the operator's fixture-control surface; its status pane must be scannable, and new keys are only real if documented.
Output: A readable rig summary render + a README keymap that matches the shipped cockpit.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tui-cockpit/06-UAT.md

@crates/ignition-tui/src/ui/rig.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix the rig status summary render</name>
  <files>crates/ignition-tui/src/ui/rig.rs</files>
  <action>
  Gap 06-UAT test 13 [minor]: "rig status doesn't show well. Otherwise pass." The gap's root cause is TBD — diagnose first: read render_summary (ui/rig.rs:75) and summary_lines (:113) against the RigStatusResult fields and identify the readability defects (candidate culprits from the UAT context: dense unseparated lines, missing labels/alignment, values running together at 80 cols, no visual hierarchy — the user's monochrome feedback amplifies all of these).

  Then fix: restructure summary_lines into a scannable layout — grouped sections (identity / state / trial / snapshot) with blank-line separation, aligned key-value pairs, and state prominence (e.g. RUNNING / DOWN on its own line). Use the existing style conventions from the other screens (dashboard panels are the reference); avoid inventing new color usage beyond what the codebase already does — this is layout and grouping, not a color overhaul (color remains backlog).

  Add/extend a TestBackend render test: render the Rig screen with a populated RigStatusResult (both RUNNING and DOWN shapes) at 80x24 and assert the summary contains the section separators and the state line (string-contains assertions on the rendered buffer, existing test style).
  </action>
  <verify>cargo test -p ignition-tui</verify>
  <done>Rig summary renders grouped, separated, aligned sections for both RUNNING and DOWN states; render test pins the structure</done>
</task>

<task type="auto">
  <name>Task 2: Sync the README TUI keymap with gap-closure keys</name>
  <files>README.md</files>
  <action>
  Plans 06-07 through 06-10 added user-visible keys and surfaces; the README TUI section (written in 06-06) predates them. Update it to document: the Tags 'r' refresh (06-09), vim motions in modals — j/k/g/G/Ctrl-d/Ctrl-u (06-10), and the noun-grouped Projects menu + prose dashboard labels if the section lists menu entries (06-10). Also verify the exit-code table already reflects session_not_prunable (06-07 Task 2 owns that row — cross-check only, fix if missing). Keep the README's existing voice and table formats; no new sections beyond what the keys need.
  </action>
  <verify>rg -n "session_not_prunable|Ctrl-d" README.md && cargo test --workspace --features tui</verify>
  <done>README TUI keymap matches the shipped keys; exit table cross-checked; full workspace still green</done>
</task>

</tasks>

<verification>
- cargo test --workspace --features tui (final gap-closure sweep: all suites green)
- cargo fmt --all --check && cargo clippy --workspace --features tui --all-targets -- -D warnings
- README keymap and exit table reflect the shipped behavior
</verification>

<success_criteria>
- Both must_have truths hold
- The phase's 13 UAT gaps are all addressed or explicitly owned elsewhere (UX themes like color are backlog, not gap closure)
</success_criteria>

<output>
After completion, create `.planning/phases/06-tui-cockpit/06-11-SUMMARY.md`
</output>
