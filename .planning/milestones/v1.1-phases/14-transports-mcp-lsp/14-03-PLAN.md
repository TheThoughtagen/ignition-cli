---
phase: 14-transports-mcp-lsp
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - crates/ignition-cli/Cargo.toml
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/src/lsp.rs
  - crates/ignition-tui/src/routes.rs
  - crates/ignition-cli/tests/tui_coverage.rs
autonomous: true

must_haves:
  truths:
    - "ign lsp serves LSP over stdio via lsp-server 0.10 (Content-Length framing + handshake owned by the crate; WE own the sync dispatch loop on the main thread)"
    - "Declared capabilities are NARROW BY DESIGN: completion + hover + full-text sync ONLY — never definition/codeAction/workspace symbols (Python ignition-lsp owns statics; composition, never replacement)"
    - "Gateway truth enters ONLY through a TTL-cached snapshot written by a background refresher thread; LSP request handlers take a read lock and return — zero network inside any request (SC-3 structural property)"
    - "The refresher soft-degrades: Session/gateway failure marks the snapshot healthy=false and keeps retrying — editing offline never kills the server"
    - "`ign lsp` maps OutOfBand in routes.rs in the SAME commit as the clap command; pinned set extends to exactly [api call, completions, edit, mcp, lsp]"
  artifacts:
    - path: "crates/ignition-cli/src/lsp.rs"
      provides: "lsp-server sync loop, narrow ServerCapabilities, GatewayCache (Arc<RwLock<Arc<Snapshot>>>) + background refresher thread (own tokio runtime, block_on legal there)"
      min_lines: 200
    - path: "crates/ignition-cli/Cargo.toml"
      provides: "lsp-server 0.10 + lsp-types 0.97 — the phase's ONLY new dependencies"
      contains: "lsp-server"
    - path: "crates/ignition-cli/src/cli.rs"
      provides: "hidden Commands::Lsp unit variant"
      contains: "Lsp"
    - path: "crates/ignition-tui/src/routes.rs"
      provides: "OutOfBand row for `lsp` with written justification"
      contains: "lsp"
    - path: "crates/ignition-cli/tests/tui_coverage.rs"
      provides: "pinned OutOfBand set [api call, completions, edit, mcp, lsp]"
      contains: "lsp"
  key_links:
    - from: "crates/ignition-cli/src/main.rs"
      to: "lsp::serve"
      via: "main-level sync seam (no block_on — the loop is crossbeam-sync)"
      pattern: "Commands::Lsp"
    - from: "crates/ignition-cli/src/lsp.rs refresher thread"
      to: "ignition-core::Session"
      via: "Session::resolve once at startup, then TTL populate loop — ONE client construction site (CORE-09)"
      pattern: "Session::resolve"
    - from: "crates/ignition-cli/src/lsp.rs"
      to: "Arc<RwLock<Arc<Snapshot>>>"
      via: "handlers read-lock a cloned Arc snapshot; lock held microseconds; refresher is the only writer"
      pattern: "RwLock"
---

<objective>
Stand up `ign lsp` — the lsp-server-based stdio LSP with the sync dispatch loop and the TTL GatewayCache — completing the transport pattern the MCP slice proved: OutOfBand seam, Session-only auth, cache-only handlers.

Purpose: SC-3 server half (data plane + the no-blocking-network structural property) and the `lsp` OutOfBand atomic landing. Handlers (completions/hover/diagnostics) are 14-04; contract suite is 14-04 too.
Output: `crates/ignition-cli/src/lsp.rs` (loop + cache + refresher) + hidden `ign lsp` + OutOfBand row + the phase's only two new dependencies.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-transports-mcp-lsp/14-RESEARCH.md
@crates/ignition-cli/src/main.rs
@crates/ignition-cli/src/lsp.rs
@crates/ignition-cli/Cargo.toml
@crates/ignition-tui/src/routes.rs

Research: Pattern 4 (the roadmap-flag verification, RESOLVED: lsp-server 0.10 Connection API verbatim; sync loop; refresher thread owns the tokio runtime — block_on is legal THERE, never in a request handler; init_tracing/stderr reuse), Anti-Pattern 5 (narrow capabilities), Don't-Hand-Roll table (lsp-server owns framing/handshake/shutdown), Pitfall 6 (atomic row landing). Snapshot fields per research Pattern 4: providers, tag_trees, named_queries, projects, fetched_at, healthy.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cargo deps + hidden `ign lsp` command + OutOfBand row + lsp-server sync loop scaffold</name>
  <files>crates/ignition-cli/Cargo.toml, crates/ignition-cli/src/cli.rs, crates/ignition-tui/src/routes.rs, crates/ignition-cli/tests/tui_coverage.rs, crates/ignition-cli/src/main.rs, crates/ignition-cli/src/lsp.rs</files>
  <action>
    1. **Cargo.toml** — add exactly `lsp-server = "0.10"` and `lsp-types = "0.97"` (research-verified versions; crossbeam-channel arrives transitively — never a direct dep). Nothing else.
    2. **cli.rs** — hidden unit variant (research Pattern 1 sketch): `/// Serve LSP over stdio (hidden; for ignition-nvim) \n #[command(hide = true)] \n Lsp,`. Leaf path is exactly `lsp`.
    3. **routes.rs + tui_coverage.rs** — ATOMIC with step 2 (13-08/14-01 discipline): OutOfBand row `"lsp"` with a justification comment in the established genre (the LSP protocol stream IS the product; composition with ignition-lsp — OutOfBand means "not the envelope render path"); extend the pinned set to exactly `["api call", "completions", "edit", "mcp", "lsp"]` and update the routes.rs:430-441 section comment (`lsp` joins the set; 08-06 reservation FULFILLED).
    4. **main.rs seam** — beside the Mcp seam: `if let Commands::Lsp = cli.command { let profile_flag = cli.profile.clone(); return lsp::serve(profile_flag.as_deref()); }` — a PLAIN SYNC return (no block_on; lsp-server's loop is crossbeam-sync; the roadmap flag's block_on question resolves as: the runtime lives ONLY in the refresher thread, Task 2).
    5. **lsp.rs scaffold** (research Pattern 4 API, verbatim shape): `Connection::stdio()`; `ServerCapabilities` narrowed BY DESIGN — `completion_provider` (trigger_characters ["/", "."]), `hover_provider: Simple(true)`, `text_document_sync: Kind(FULL)`, everything else default (NEVER claim definition/codeAction/workspace symbols — ignition-lsp's statics, research Anti-Pattern 5); `connection.initialize(caps)?` handshake; the sync loop `for msg in &connection.receiver` handling Request (via `connection.handle_shutdown(&req)?` for shutdown → return Ok), Notification, Response; `io_threads.join()?` at the end. `init_tracing` is reused verbatim by main (already runs before the seam) — stderr writer doubles as the LSP log channel; no new logging surface.
    6. Handler stubs in this task: completion/hover return empty/default results; notifications are no-ops. Handlers get REAL bodies in 14-04 — keep the loop shape final so 14-04 only fills function bodies.
  </action>
  <verify>`cargo build -p ignition-cli` clean; `cargo test -p ignition-cli --test tui_coverage` green with the [.., mcp, lsp] pin; manual smoke: `printf 'Content-Length: 43\r\n\r\n{"jsonrpc":"2.0","id":1,"method":"initialize"}' | IGNITION_CLI_CONFIG=<tmp>/config.json target/debug/ign lsp | head -c 400` → a Content-Length-framed initialize response (server_capabilities present, no completion-less caps); `cargo clippy --workspace --all-targets -- -D warnings` clean.</verify>
  <done>`ign lsp` speaks LSP base protocol end to end (handshake + framed response + clean shutdown); capabilities deliberately narrow; OutOfBand row + command + pin atomic; exactly two new deps.</done>
</task>

<task type="auto">
  <name>Task 2: GatewayCache TTL snapshot + background refresher thread (soft-degrade, Session-only)</name>
  <files>crates/ignition-cli/src/lsp.rs</files>
  <action>
    1. **Snapshot type** (research Pattern 4): `struct Snapshot { providers: Vec<String>, tag_trees: BTreeMap<String, Vec<String>> /* provider → cached tag paths */, named_queries: BTreeMap<String, Vec<String>> /* project → paths */, projects: Vec<String>, fetched_at: Instant, healthy: bool }` behind `Arc<RwLock<Arc<Snapshot>>>` (`GatewayCache::snapshot(&self) -> Arc<Snapshot>` clones the inner Arc — read path holds the lock microseconds).
    2. **Refresher thread** (`GatewayCache::spawn(profile_flag: Option<&str>, kick_rx)`): `std::thread::spawn` building its OWN small tokio `current_thread` Runtime INSIDE the thread; the roadmap flag resolves here — `runtime.block_on(...)` is legal on this thread because it is NOT an LSP request; the LSP loop itself never touches a runtime. Resolution: `Session::resolve` (effective profile = ambient flag; IGNITION_PROFILE already folded by main) ONCE at startup; on failure → publish an empty `healthy:false` snapshot and KEEP RETRYING each cycle (degrade-soft: editing offline must not kill the server).
    3. **Populate** rides the CLI's own trusted machinery — NO new endpoint surface: providers list; per-provider tag browse → flattened tag paths into `tag_trees`; named queries via the export/`resource_members` path per project (research OQ3, planner-locked); project list. Wrap each populate step so one failing section doesn't zero the others (partial snapshot better than none; keep `healthy` = all-sections-ok).
    4. **Loop**: immediate first populate → publish, then `std::thread::sleep(LSP_CACHE_TTL)` per cycle with `const LSP_CACHE_TTL: Duration = Duration::from_secs(30)` (documented const; UAT may tune later). A `kick_rx` (std mpsc or crossbeam channel) accepts refresh kicks (14-04's didSave) — drain kicks each cycle so a burst collapses to one refresh. After each publish, send the new snapshot version over a channel the main loop can poll/select (14-04 consumes it for diagnostics publishing — define the channel now, consume later).
    5. Unit tests (cfg(test) in lsp.rs, pure — no network): snapshot swap semantics (readers see old until publish), TTL const pinned, degrade-soft state machine via a fake populate closure (healthy false on failure, recovers on success). Cache trait/closure seam: parameterize populate behind a small `trait SnapshotSource` or fn pointer so tests inject fakes without a gateway.
  </action>
  <verify>`cargo test -p ignition-cli lsp` green (swap/degrade/TTL pins); anti-sabotage: make the fake source fail → healthy:false asserted → restore; `cargo clippy --workspace --all-targets -- -D warnings` clean. Manual smoke still passes: with an unreachable gateway config, `ign lsp` initializes and answers shutdown (server alive, snapshot healthy:false).</verify>
  <done>TTL snapshot cache live: refresher owns the only network+runtime; handlers' data path is read-lock-only; offline editing degrades to healthy:false snapshots without killing the server; Session is the single construction site.</done>
</task>

</tasks>

<verification>
- `cargo test -p ignition-cli` green (lsp unit tests + tui_coverage extended pin); `cargo clippy --workspace --all-targets -- -D warnings` green
- Cargo.toml diff for the whole phase-so-far: exactly lsp-server 0.10 + lsp-types 0.97 beyond the 14-01 baseline (zero MCP deps)
- Handshake smoke over real binary: framed initialize response; shutdown handshake exits cleanly; unreachable gateway does not prevent serving
- routes.rs row + cli.rs command + tui_coverage pin in the same commit as a unit (08-06/13-08 atomicity)
</verification>

<success_criteria>
- SC-3 structural half: cache-only request path exists (handlers read snapshots; only the refresher touches the network) — 14-04's contract tests prove it behaviorally
- Composition posture: capabilities limited to completion/hover/full-sync — ignition-lsp's statics surface untouched by claim
- `lsp` OutOfBand atomic landing; pinned set now exactly [api call, completions, edit, mcp, lsp]
</success_criteria>

<output>
After completion, create `.planning/phases/14-transports-mcp-lsp/14-03-SUMMARY.md`
</output>
