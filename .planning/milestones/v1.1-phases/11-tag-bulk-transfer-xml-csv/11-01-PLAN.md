---
phase: 11-tag-bulk-transfer-xml-csv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-tag-bulk-transfer-xml-csv/11-LIVE-CAPTURES.md
  - .planning/phases/11-tag-bulk-transfer-xml-csv/11-RIG-NOTES.md
  - .planning/phases/11-tag-bulk-transfer-xml-csv/artifacts/udt-multilevel.xml
autonomous: true

must_haves:
  truths:
    - "All five roadmap-mandated probes are answered with verbatim rig evidence recorded in 11-LIVE-CAPTURES.md: (1) exportTags kwargs+xml return form, (2) importTags basePath/QualityCode/temp-perms from a script thread, (3) REAL multi-level UDT XML byte shape + re-export determinism, (4) mixed-parent exportTags behavior, (5) legacy CSV column coverage table"
    - "A real multi-level UDT XML export captured from a live gateway (not hand-written) is committed under the phase artifacts dir — the roadmap's explicit RESEARCH flag"
    - "The re-export determinism answer is recorded with a named fidelity-oracle strategy (byte-identity if deterministic, else the documented fallback)"
    - "Both rig generations (8.3.6 + 8.3.3) answered every probe — or the delta between them is recorded"
  artifacts:
    - path: ".planning/phases/11-tag-bulk-transfer-xml-csv/11-LIVE-CAPTURES.md"
      provides: "The five probe answers + CSV coverage table + verbatim exception strings"
      contains: "exportType"
    - path: ".planning/phases/11-tag-bulk-transfer-xml-csv/11-RIG-NOTES.md"
      provides: "Rig ops log (ports, containers, token NAMES only, teardown state)"
    - path: ".planning/phases/11-tag-bulk-transfer-xml-csv/artifacts/udt-multilevel.xml"
      provides: "Captured gateway XML bytes of a multi-level UDT subtree"
      contains: "UdtType"
  key_links:
    - from: ".planning/phases/11-tag-bulk-transfer-xml-csv/artifacts/udt-multilevel.xml"
      to: "11-03 scan-module test fixtures"
      via: "the loss-report scan MUST be validated against this real capture (roadmap flag)"
      pattern: "UdtType|CompoundProperty"
    - from: "11-LIVE-CAPTURES.md coverage table"
      to: "11-04 generate_legacy_csv honesty"
      via: "the documented lossy-field table comes from the probe-5 diff"
      pattern: "coverage"
---

<objective>
Run the five live-gateway probes the roadmap flags as MANDATORY before any Phase 11 code: pull a REAL multi-level UDT XML export and answer the five open questions (exportTags+xml return form, importTags wire behavior, UDT XML byte shape + determinism, mixed-parent constraint, legacy CSV coverage). Everything downstream (route design, scan design, CSV generation, fidelity oracle) cites these captures instead of guessing.

Purpose: the roadmap's success criterion 1 requires live-gateway proof, and research rates the exact XML bytes / return forms MEDIUM-LOW confidence — captures beat guesses (Phase 9/10 precedent).
Output: 11-LIVE-CAPTURES.md (five answers + coverage table), 11-RIG-NOTES.md (ops log), artifacts/udt-multilevel.xml (real captured bytes). Zero product code.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tag-bulk-transfer-xml-csv/11-RESEARCH.md
@.planning/phases/09-agent-surface-api-diagnostics/09-RIG-NOTES.md
@crates/ignition-core/webdev/routes/com.inductiveautomation.webdev/resources/cli/scriptExec/doPost.py
@crates/ignition-core/webdev/routes/com.inductiveautomation.webdev/resources/cli/tagConfig/doPost.py
</context>

<planner_locks>
No CONTEXT.md exists for this phase — no locked user decisions. Locks this plan operates under (binding on later plans):

- **Probe vehicle = scriptExec route** (deployed via `ign webdev deploy --with-script-exec`, secret persisted 0600): the 1.1.0 tagConfig route does NOT pass `exportType`, so probes ride raw HTTP against scriptExec's `exec` action with the `x-ignition-cli-secret` header (the e2e_webdev pin-4 pattern).
- **Documented caveat**: scriptExec runs on a script thread, NOT a WebDev thread — the 07-06 RpcContext constraint (provider-root refusals) may differ. Record probe answers as script-thread truth; later plans design the route with the SAME `No RpcContext` translation the existing exportTags action already carries, and the 11-06 live gate exercises the true WebDev-thread path as the final oracle.
- Docker rigs are disposable and spun by the executor (images cached locally) — no user setup.
</planner_locks>

<tasks>

<task type="auto">
  <name>Task 1: Spin, commission, provision, and deploy both rigs</name>
  <files>.planning/phases/11-tag-bulk-transfer-xml-csv/11-RIG-NOTES.md</files>
  <action>
Follow the 09-RIG-NOTES.md recipe VERBATIM (proven three times):
1. Pre-flight: confirm Docker is running (start OrbStack if not — Phase 5 lesson) and ports 18188/19188 free (`lsof -i :18188 -i :19188`).
2. Rig A: `docker run -d --name ign-p11-836 -p 18188:8088 -e ACCEPT_EULA=Y -e GATEWAY_ADMIN_PASSWORD=password -e IGNITION_EDITION=standard inductiveautomation/ignition:8.3.6`
3. Rig B: compose project in /tmp/ign-p11-rigs/ign-p11-833/ per the recipe (image 8.3.3, port 19188:8088, named volume).
4. Commission both headlessly via the wire recipe: GET /bootstrap → GET /get-step?step=license&name=eula → POST /post-step {id:license,step:eula,data:{accept:true}} (201) → POST /post-step {id:finished,step:finished,data:{startGateway:true}} (200) → poll /StatusPing until body is exactly {"state":"RUNNING"} (~2 min).
5. Provision an API token per rig with the /tmp/ign-p11-rigs/provision_token.sh URL USER PASS TOKENNAME recipe (names p11tok836 / p11tok833); stage keys IMMEDIATELY to /tmp/ign-p11-rigs/tokens.env (chmod 600, outside the repo) — the Phase 9 lesson about shell env not persisting between tool calls. Sanity: curl gateway-info with the token → 200 on both.
6. Scratch config: IGNITION_CLI_CONFIG=/tmp/ign-p11-rigs/config.toml; create a profile per rig (ign profile add with the rig URL; token via IGNITION_TOKEN env per spawn).
7. Deploy the current 1.1.0 bundle WITH scriptExec on each rig: `ign --profile <p> webdev deploy --with-script-exec`. Read the persisted secret location (the deploy output names it), export the secret value to the scratch env file.
8. Record everything (container ids, ports, token NAMES only, secret PATH never its value) in 11-RIG-NOTES.md. No secret material enters the repo.
  </action>
  <verify>curl -H "X-Ignition-API-Token: $T_836" http://localhost:18188/data/api/v1/gateway-info → 200 (same on 19188); POST to each rig's scriptExec route with action=version + the secret header returns ok:true with routeVersion 1.1.0</verify>
  <done>Both rigs RUNNING, token-authenticated, 1.1.0 bundle deployed with a working secret-gated scriptExec; 11-RIG-NOTES.md records the ops log</done>
</task>

<task type="auto">
  <name>Task 2: Run probes 1, 2, 4 via scriptExec on both rigs</name>
  <files>.planning/phases/11-tag-bulk-transfer-xml-csv/11-LIVE-CAPTURES.md</files>
  <action>
Probe via raw curl POST to /system/webdev/ign-cli/cli/scriptExec with {"action":"exec","code":"..."} and the x-ignition-cli-secret header. Record VERBATIM outputs (repr prefixes, exception strings, types) in 11-LIVE-CAPTURES.md §Probes — one section per probe, both rigs each.

First seed a small subtree for probing (statement form, per route's Jython-2.7 exec contract — use `_result` for the surfaced value):
```
_result = str(system.tag.configure('[default]', [{'name':'P11Seed','tagType':'Folder','tags':[{'name':'T1','tagType':'AtomicTag','valueSource':'memory','value':42},{'name':'T2','tagType':'AtomicTag','valueSource':'expression','expression':'1+1'}]}], 'a'))
```

**Probe 1 — exportTags kwargs+xml return form** (research Pitfall 1): run
- `system.tag.exportTags(tagPaths=['[default]P11Seed'])` (JSON kwargs — the live-proven baseline) and record return type + payload prefix.
- `system.tag.exportTags(tagPaths=['[default]P11Seed'], exportType='xml')` — record: does it return a string? Does it ERROR (record the exception verbatim)? Does the payload start with an XML declaration? Is it the full document or a fragment?
- Also `system.tag.exportTags('[default]P11Seed', ['[default]P11Seed'], True, 'xml')` — the documented positional form, for the fallback design.

**Probe 2 — importTags from a script thread**: export the seed XML, write a gateway temp file (java.io.File.createTempFile('ign-probe','.xml'), write bytes, close), then:
- `system.tag.importTags(tmpPath, '[default]P11Import', 'a')` (subfolder basePath) — record the returned list's element shape (str(QualityCode)? toString forms like `Good`?) and temp-dir path (proves JVM temp writability).
- `system.tag.importTags(tmpPath, '[default]', 'a')` (provider ROOT basePath) — record success or the verbatim exception (expect the No RpcContext class per 07-06; this decides route-side translation needs).
- collision-policy probe: import the same file twice with 'o' then 'a' — confirm 'a' errors on collision (record the exception), 'o' succeeds. Confirm temp file deleted in a finally (check the temp dir listing).
- Wrap ALL probes in try/finally so no temp file leaks.

**Probe 4 — mixed-parent exportTags**: `system.tag.exportTags(tagPaths=['[default]P11Seed/T1','[default]P11Seed/T2'], exportType='xml')` (same parent — baseline) vs `system.tag.exportTags(tagPaths=['[default]P11Seed/T1','[default]P11Import/T1'], exportType='xml')` (mixed parents). Record: hard error (verbatim) / silent partial / works. Also run the mixed-parent JSON baseline (no exportType) so the plan can decide whether any constraint handling is NEW-format-only or pre-existing.
  </action>
  <verify>11-LIVE-CAPTURES.md contains §Probe 1, §Probe 2, §Probe 4 with verbatim rig output from BOTH rigs (or an explicit per-rig delta note); every probe answer maps to one of the research's Open Questions 2/3/5</verify>
  <done>The exportTags+xml return form is KNOWN (string vs error), importTags basePath forms + QualityCode shape + collision semantics are KNOWN, mixed-parent behavior is KNOWN — each with captured evidence, not inference</done>
</task>

<task type="auto">
  <name>Task 3: Capture REAL multi-level UDT XML (probe 3) + legacy CSV coverage map (probe 5)</name>
  <files>.planning/phases/11-tag-bulk-transfer-xml-csv/11-LIVE-CAPTURES.md, .planning/phases/11-tag-bulk-transfer-xml-csv/artifacts/udt-multilevel.xml</files>
  <action>
**Probe 3 — the roadmap's explicit flag.** Build a REAL multi-level UDT structure on rig A via the deployed tagConfig route (the 05-05-proven `configure` path), NOT scriptExec, so the capture reflects CLI-realistic construction:
1. Create a UDT TYPE `MotorType` under `[default]_types_` with: a parameter `MotorNumber` (default 1), a child AtomicTag `Amps` (valueSource opc, opcItemPath binding using the parameter, e.g. `ns=1;s=[Dairy]Motor {MotorNumber}/Amps`), a child expression tag `Doubled` (expression using the parameter), and an ALARM on Amps (name `Low Amps`, priority 4, setpoint 25) — alarms matter: they exercise CompoundProperty in the XML. Use `ign tags config create [default]_types_/MotorType --file def.json` with the definition shaped per the 8.3 json-format docs (UDT shapes cited in 11-RESEARCH.md); verify with `ign tags udt def MotorType`.
2. Create a Folder `P11UDT` containing TWO UDT instances of MotorType (`M1`, `M2`) plus a nested Folder `P11UDT/Sub` containing a third instance `M3` — this makes the export MULTI-LEVEL (folder → instance → UDT children) and exercises the same-parent export list `['[default]P11UDT']`.
3. Export XML via scriptExec: `system.tag.exportTags(tagPaths=['[default]P11UDT'], exportType='xml')` — write the EXACT returned bytes to .planning/phases/11-tag-bulk-transfer-xml-csv/artifacts/udt-multilevel.xml (byte-faithful: do not pretty-print, re-indent, or edit). Also export JSON interchange alongside for cross-reference (into the captures doc, trimmed).
4. Confirm the capture contains: the root element with MinVersion/locale attrs, `type="UdtType"` or instance markers, nested Tag elements, parameter encoding, and a CompoundProperty alarms block. If ANY expected element is missing, record what the gateway ACTUALLY produced — the capture is ground truth, the expectation list is only a checklist.
5. Determinism check (decides the fidelity oracle): export the unchanged subtree 3× and byte-compare all pairs. Then import the XML into `[default]P11Roundtrip` via the probe-2 importTags pattern ('o' policy) and re-export → byte-compare against the original. Record BOTH answers: (a) unchanged-subtree export determinism, (b) import→re-export byte-identity. If byte-identity fails on (b), capture the exact differing bytes and record the structural-comparison fallback the live gate must use instead.
6. Repeat probe 3's export + determinism on rig B (8.3.3) — record any byte-level delta between gateway versions (header/declaration/indentation).

**Probe 5 — legacy CSV coverage map.** Hand-build a legacy CSV per the official docs sample (11-RESEARCH.md §Legacy CSV skeleton): `# version=1` marker row, one memory tag (TagType 1, DataType 7), one folder row (Path `P11Csv/` prefix, TagType 6), one OPC tag (TagType 0, DataType 2, OPCServer/OPCItemPath), and one UDT-instance row (TagType 10, UDTParentType `MotorType`, SourceTagPath). Import via `system.tag.importTags(csvPath, '[default]P11CsvImport', 'a')` (probe-2 temp-file pattern). Then re-export the import target as JSON (`system.tag.exportTags(tagPaths=['[default]P11CsvImport'])`) and diff field-by-field: for each of the 47 documented columns record LANDED (and the JSON key it became) / DROPPED / COERCED (e.g. numeric enum → what string). This diff IS the documented lossy-field table. Record it as a markdown table in 11-LIVE-CAPTURES.md §CSV-Coverage. Explicitly answer: are tagGroup / tag-group bindings / UDT parameters / modern properties outside the 47 columns droppable? (Check by adding one extra column the legacy table lacks, or note the importer's behavior for unknown columns.)
  </action>
  <verify>artifacts/udt-multilevel.xml exists, parses as XML, and contains a CompoundProperty alarms block + UDT nesting (grep UdtType|CompoundProperty); 11-LIVE-CAPTURES.md §Probe 3 records determinism answers (a) and (b) with a named oracle strategy; §CSV-Coverage contains a column-by-column table covering all 47 legacy columns</verify>
  <done>The real UDT XML bytes are committed as a fixture; export determinism is answered (byte-identity oracle or named fallback); the CSV lossy-field coverage table exists as the documented behavior 11-04's generator and 11-05's README will cite</done>
</task>

</tasks>

<verification>
- `ls .planning/phases/11-tag-bulk-transfer-xml-csv/artifacts/udt-multilevel.xml` exists and contains live-captured bytes (not hand-authored — provenance stated in the captures doc)
- 11-LIVE-CAPTURES.md answers all five research Open Questions (2, 3, 4, 5, and the CSV-coverage half of 6) with rig evidence from both 8.3.3 and 8.3.6
- 11-RIG-NOTES.md records teardown state (or intentional keep-alive for 11-06 — state which explicitly)
- Zero product code changed: `git status` shows only .planning/ files
</verification>

<success_criteria>
Five probes answered with verbatim evidence; the real multi-level UDT XML fixture committed; determinism + CSV coverage recorded. Later plans cite this document instead of re-deriving wire truth (the Phase-10 capture-first discipline).
</success_criteria>

<output>
After completion, create `.planning/phases/11-tag-bulk-transfer-xml-csv/11-01-SUMMARY.md`
</output>
