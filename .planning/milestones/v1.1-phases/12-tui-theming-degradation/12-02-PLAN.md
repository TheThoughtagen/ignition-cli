---
phase: 12-tui-theming-degradation
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - crates/ignition-tui/src/context.rs
  - crates/ignition-tui/src/state.rs
  - crates/ignition-tui/src/lib.rs
  - crates/ignition-tui/src/update.rs
autonomous: true

must_haves:
  truths:
    - "[ui].theme in config selects the cockpit palette at startup; theme = \"mono\" resolves Reset-only at ANY ambient terminal tier"
    - "Unknown theme name warns via tracing::warn! and falls back to the default theme — the cockpit still starts (Phase-8 lenient-degradation contract)"
    - "A wrong-typed [ui].theme value degrades via the existing lenient_ui deserializer — resolve still succeeds"
    - "AppState::new() carries the default theme at the Mono tier — all ~100 existing AppState::new() test callsites keep signatures and stay valid"
    - "Profile switch adopts ctx.palette alongside ctx.poll_interval (uniform ResolvedContext adoption — no silent drop of the 08-05 trap class)"
  artifacts:
    - path: "crates/ignition-tui/src/context.rs"
      provides: "ResolvedContext.palette field; resolve_palette(name, tier) warn+default fallback fn; build_context wires detect_tier over real env"
      contains: "resolve_palette"
    - path: "crates/ignition-tui/src/state.rs"
      provides: "AppState.palette field defaulting to default-theme @ Mono tier"
      contains: "pub palette"
    - path: "crates/ignition-tui/src/lib.rs"
      provides: "run_loop adoption: state.palette = ctx.palette in the existing one-block"
      contains: "state.palette = ctx.palette"
    - path: "crates/ignition-tui/src/update.rs"
      provides: "switch_profile adopts ctx.palette before respawning workers"
      contains: "ctx.palette"
  key_links:
    - from: "config.ui.theme"
      to: "resolve_palette → ResolvedContext.palette"
      via: "Theme::by_name + tracing::warn! + default fallback in build_context"
      pattern: "by_name"
    - from: "ResolvedContext.palette"
      to: "AppState.palette"
      via: "run_loop one-block adoption (lib.rs, alongside poll_interval)"
      pattern: "state\\.palette = ctx\\.palette"
    - from: "detect_tier"
      to: "real environment"
      via: "build_context injects |k| std::env::var(k).ok() — the ONLY real-env call site"
      pattern: "detect_tier"
---

<objective>
Wire `[ui].theme` from config to the cockpit's pixels-in-waiting: resolve the named theme at the detected capability tier in `context::build_context`, carry the resolved `Palette` on `ResolvedContext`, land it on `AppState.palette` in run_loop's adoption block, and adopt it on profile switch.

Purpose: this is the exact ride-along pattern 08-05 established for `poll_interval` (ResolvedContext carrier → one-block adoption → switch-chain adoption). Theme resolution must obey the Phase-8 lenient-degradation decision: an unknown theme NAME warns and falls back to default — it must never fail the load or kill startup.
Output: `ctx.palette` resolved from config at TUI startup; `state.palette` defaulted and adoptable; resolution tests pinning known/unknown/wrong-typed/mono-tier-independent behavior.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-tui-theming-degradation/12-RESEARCH.md
@.planning/phases/12-tui-theming-degradation/12-01-SUMMARY.md

# Source files (read targeted sections)
crates/ignition-tui/src/context.rs — ResolvedContext struct (lines 41-56), build_context (lines 88-104), resolve-level test fixtures (lines 320-430 show the TOML-fixture pattern)
crates/ignition-tui/src/state.rs — AppState struct head (line 1304), manual Default impl (lines ~1382-1415)
crates/ignition-tui/src/lib.rs — run() (lines 57-68), run_loop adoption block (lines 83-89)
crates/ignition-tui/src/update.rs — switch_profile (lines ~570-600; the ctx.poll_interval adoption is the template)
crates/ignition-core/src/config/profile.rs — UiConfig { theme: Option<String> } + lenient_ui deserializer (Phase 8)
</context>

<tasks>

<task type="auto">
  <name>Task 1: resolve_palette + ResolvedContext.palette + AppState.palette + both adoption sites</name>
  <files>crates/ignition-tui/src/context.rs, crates/ignition-tui/src/state.rs, crates/ignition-tui/src/lib.rs, crates/ignition-tui/src/update.rs</files>
  <action>
**context.rs:**
1. Import `crate::ui::theme::{detect_tier, resolve_palette_deps...}` — concretely `use crate::ui::theme::{Palette, Theme, Tier, detect_tier};` (names per what 12-01 actually exported).
2. Add a module-level (crate-visible is fine) resolution helper — the warn+default fallback, pure and fixed-tier-testable:
```rust
/// Resolve `[ui].theme` to a Palette at `tier`. Unknown names WARN and
/// fall back to the default theme — the lenient-degradation contract
/// (Phase 8: a typo in a NEW key must never fail the load), decided at
/// TUI-resolution time where load_for_tui already degrades new-schema
/// surface. Pure over (name, tier): 12-02 tests pin it at fixed tiers,
/// build_context supplies the real detected tier.
fn resolve_palette(name: Option<&str>, tier: Tier) -> Palette {
    let theme = Theme::by_name(name.unwrap_or("default")).unwrap_or_else(|| {
        tracing::warn!(
            theme = name.unwrap_or(""),
            "unknown [ui].theme — using default theme"
        );
        Theme::by_name("default").expect("default theme exists")
    });
    theme.resolve(tier)
}
```
3. Extend `ResolvedContext` with `pub palette: Palette` (doc: "the resolved theme palette — `[ui].theme` at the detected capability tier; adopted by run_loop and the profile switch like poll_interval").
4. In `build_context`, after `Session::resolve_loaded`: `let tier = detect_tier(&|k| std::env::var(k).ok());` and `let palette = resolve_palette(config.ui.theme.as_deref(), tier);` — include both in the returned struct. This is the ONLY real-environment detection call site.

**state.rs:** Add `pub palette: Palette` to `AppState` (doc: "the resolved theme palette (TUIX-03/04): `[ui].theme` at the detected tier. Defaults to the default theme at the MONO tier — deterministic, no env reads at construction; run_loop overwrites with the real detection"). In the manual `Default` impl add: `palette: crate::ui::theme::Theme::by_name("default").expect("default theme exists").resolve(crate::ui::theme::Tier::default()),` — placed next to the existing `poll_interval` line. Import at top of file if cleaner.

**lib.rs:** In run_loop's adoption block (lines 85-89, where client/profile/profile_url/poll_interval are adopted in one block) add `state.palette = ctx.palette;` — same block, same comment style.

**update.rs:** In `switch_profile`, adopt the palette in the SAME statement group as the existing `state.poll_interval = ctx.poll_interval;` adoption (line ~576): `state.palette = ctx.palette;` — theme is global config so the value is normally identical, but rebuild() re-resolves it and uniform adoption closes the silent-drop trap class 08-05 documented.
  </action>
  <verify>`cargo build -p ignition-tui` compiles; `cargo test -p ignition-tui` full suite green (existing ~195 tests unaffected — AppState::new() signatures unchanged); clippy -D warnings + fmt clean</verify>
  <done>ctx.palette flows config → build_context → run_loop → AppState; switch_profile adopts it; default state is default-theme @ Mono; zero signature changes to AppState::new() consumers</done>
</task>

<task type="auto">
  <name>Task 2: Resolution tests — known name, unknown-name fallback, wrong-typed degradation, mono tier-independence</name>
  <files>crates/ignition-tui/src/context.rs, crates/ignition-tui/src/state.rs</files>
  <action>
Extend the existing test modules, following the established resolve-level TOML-fixture pattern (context.rs tests set IGNITION_CLI_CONFIG to a fixture file under ENV_LOCK — copy the harness from `resolve_degrades_wrong_typed_poll_interval`).

**context.rs unit tests (pure, fixed tier — no env dependence):**
- `resolve_palette_known_name_fixed_tier`: `resolve_palette(Some("dark"), Tier::C16)` == `Theme::by_name("dark").unwrap().resolve(Tier::C16)` (same for "light" and "default" at any tier).
- `resolve_palette_unknown_name_falls_back_to_default`: `resolve_palette(Some("banana"), Tier::Mono)` == `resolve_palette(None, Tier::Mono)` == default theme's mono palette.
- `resolve_palette_none_is_default`: `resolve_palette(None, Tier::C256)` == default @ C256.

**context.rs resolve-level integration tests (TOML fixtures):**
- `[ui] theme = "mono"` fixture → `resolve(...)` Ok and `ctx.palette` is the all-Reset palette. This pin is TIER-INDEPENDENT by design (12-01 authored the mono theme Reset at all tiers) — it proves the config→palette wiring without depending on ambient COLORTERM/TERM in CI or dev shells.
- `theme = "banana"` fixture → Ok, and `ctx.palette` == `resolve_palette(None, detect_tier(&|k| std::env::var(k).ok()))` (both sides computed in-process under the same env — deterministic equality; proves the fallback actually flowed through build_context, not just the helper).
- Wrong-typed `theme = 42` fixture → Ok (the lenient_ui deserializer degrades it to None — mirrors the `poll_interval_secs = "banana"` degradation test); same computed-equality assertion as above.

**state.rs unit test:**
- `app_state_default_palette_is_mono_reset`: `AppState::new().palette` — every Color slot == `Color::Reset` (the pre-resolve default is the strictest tier; construction never reads env).
  </action>
  <verify>`cargo test -p ignition-tui` green — new tests pass AND the pre-existing context/state suites stay green; clippy -D warnings + fmt clean</verify>
  <done>Resolution contract pinned at both levels: pure resolve_palette (fixed tiers, fallback) and build_context integration (mono tier-independence, unknown-name fallback, wrong-typed degradation); AppState default proven Reset-only</done>
</task>

</tasks>

<verification>
- `cargo test -p ignition-tui` full suite green (existing + new)
- `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo fmt --all --check` clean
- `cargo build -p ignition-cli --no-default-features` clean (theme code lives in ignition-tui — lean build must stay unaffected)
- Structural: `grep -n "state.palette = ctx.palette" crates/ignition-tui/src/lib.rs crates/ignition-tui/src/update.rs` — both adoption sites present
</verification>

<success_criteria>
- `[ui].theme` resolves to a concrete Palette on ResolvedContext at startup, at the env-detected tier
- Unknown theme name → warn + default (cockpit starts); wrong-typed → lenient degradation (cockpit starts)
- AppState defaults to default-theme @ Mono with zero env reads; switch chain adopts ctx.palette
- All resolution contracts pinned by tests; existing suites untouched and green
</success_criteria>

<output>
After completion, create `.planning/phases/12-tui-theming-degradation/12-02-SUMMARY.md`
</output>
