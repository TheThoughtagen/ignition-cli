---
phase: 09-agent-surface-api-diagnostics
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ignition-tui/src/state.rs
  - crates/ignition-tui/src/update.rs
  - crates/ignition-tui/src/routes.rs
  - crates/ignition-cli/tests/tui_coverage.rs
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "The seven Phase 9 verbs are visible in the Dashboard actions menu (press `a`) and run on Enter: license status, redundancy status, gan status, diagnostics bundle generate/status/download/wait"
    - "Each menu verb dispatches through the SAME ignition-core action functions the CLI uses — no second construction, results render via the generic ActionDone pretty-JSON path"
    - "CI structurally enforces routes↔menu parity for the Dashboard: a Screen-mapped route row without a menu entry (or a menu entry without a route) fails the build — the 09-04/09-05 blind spot cannot recur"
  artifacts:
    - path: "crates/ignition-tui/src/state.rs"
      provides: "ACTIONS [&str; 22] — the 7 new clap-exact verbs with 09 provenance comments"
      contains: "diagnostics bundle wait"
    - path: "crates/ignition-tui/src/update.rs"
      provides: "Seven execute_menu_action arms spawning the ignition-core actions"
      contains: "license_status"
    - path: "crates/ignition-tui/src/routes.rs"
      provides: "menu_label(path) — the single registry-path → menu-prose alias seam"
      contains: "menu_label"
    - path: "crates/ignition-cli/tests/tui_coverage.rs"
      provides: "dashboard menu-parity test: bidirectional ACTIONS↔MENU_HOSTED equality + pinned Dashboard-route count"
      contains: "MENU_HOSTED"
  key_links:
    - from: "crates/ignition-tui/src/state.rs ACTIONS"
      to: "crates/ignition-tui/src/update.rs execute_menu_action"
      via: "label string == arm key (the locked-list contract)"
      pattern: "Some\\(\"(license status|redundancy status|gan status|diagnostics bundle)"
    - from: "crates/ignition-cli/tests/tui_coverage.rs"
      to: "crates/ignition-tui/src/routes.rs routes()"
      via: "MENU_HOSTED paths must exist as Mapping::Screen(Screen::Dashboard) rows"
      pattern: "Mapping::Screen\\(Screen::Dashboard\\)"
    - from: "crates/ignition-tui/src/update.rs"
      to: "ignition_core::actions::{license,redundancy,gan,diagnostics}"
      via: "spawn_action with the action fns (same ones main.rs dispatches)"
      pattern: "ignition_core::actions::"
---

<objective>
Close UAT Gap 1 (test 10): make the seven Phase 9 morning-check verbs runnable from the TUI Dashboard, and close the systemic blind spot that let them ship unreachable.

09-04/09-05 added `routes.rs` CliRoute rows (clap-walk green) but never extended the hardcoded `ACTIONS: [&str; 15]` menu const (state.rs:411) nor added executor arms in `update.rs` — registry rows without a reachable surface. The coverage contract enforced routes↔clap parity but nothing enforced routes↔menu parity. This plan adds the menu entries + dispatch arms, then installs the missing parity contract as CI.

Purpose: UAT test 10's user-visible failure — "actions menu shows only the 15 v1.0 verbs".

Output: ACTIONS 15→22 with seven dispatch arms; a `menu_label` alias seam; a bidirectional Dashboard menu-parity CI test; suite green.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-agent-surface-api-diagnostics/09-UAT.md
@.planning/phases/09-agent-surface-api-diagnostics/09-04-SUMMARY.md
@.planning/phases/09-agent-surface-api-diagnostics/09-05-SUMMARY.md
@crates/ignition-tui/src/state.rs
@crates/ignition-tui/src/update.rs
@crates/ignition-tui/src/routes.rs
@crates/ignition-cli/tests/tui_coverage.rs

Verified facts for this plan:
- `ui/mod.rs` renders the Actions modal and sizes it from `ACTIONS.len()` dynamically (lines 111, 203) — NO ui/mod.rs change is needed; the menu renders new entries automatically.
- `spawn_action<T: Serialize>` maps Ok results through the generic pretty-JSON → `ActionDone` → `Modal::Result_` path (update.rs:3972 test proves it) — all seven outputs (`LicenseStatusResult`, `RedundancyStatusResult`, `GanStatusResult`, `BundleStatusWire`, `BundleDownloadResult`) derive Serialize, so NO per-type render arms are needed.
- Action signatures: `license_status(api)`, `redundancy_status(api)`, `gan_status(api)` (actions/{license,redundancy,gan}.rs); `bundle_generate(api)`, `bundle_status(api)`, `bundle_wait(api, interval, timeout)`, `bundle_download(api, out: Option<&Path>)` (actions/diagnostics.rs).
- CLI wait defaults: `--interval 2 --timeout 300` (cli.rs Wait leaves) — the TUI arm mirrors these.
- The wait trio are the ONLY display-prose menu labels today ("wait for gateway up" etc.); every other ACTIONS entry is clap-exact. The 7 new labels use clap-exact spellings.
- tui_coverage.rs already does `use ignition_tui::routes::{Mapping, routes};` — the crate is reachable from the integration test.
</context>

<tasks>

<task type="auto">
  <name>Task 1: ACTIONS 15→22 + seven executor dispatch arms</name>
  <files>crates/ignition-tui/src/state.rs, crates/ignition-tui/src/update.rs</files>
  <action>
  **state.rs:**
  - Change `pub const ACTIONS: [&str; 15]` → `pub const ACTIONS: [&str; 22]`, appending after "lint" with provenance comments matching the existing style:
    `// 09-04: the curated morning-check reads (EXT-02) — labels clap-exact,` / `// matching the routes.rs rows.` then `"license status", "redundancy status", "gan status",` then `// 09-05: the diagnostics-bundle family (EXT-02) — gateway-support` / `// actions beside backup/restart.` then `"diagnostics bundle generate", "diagnostics bundle status", "diagnostics bundle download", "diagnostics bundle wait",`
  - Update the doc comment above ACTIONS if it pins the count in prose.

  **update.rs — seven arms in `execute_menu_action`, placed after the `"lint"` arm, before `_ => {}` (all DIRECT-spawn, ungated — the backup/eam-read pattern; no PendingAction variants, no Confirm/Input modals):**
  - `Some("license status")` → `workers::spawn_action(state, "license status", async move { ignition_core::actions::license::license_status(&*client).await })`
  - `Some("redundancy status")` → `...redundancy::redundancy_status(&*client)...`
  - `Some("gan status")` → `...gan::gan_status(&*client)...`
  - `Some("diagnostics bundle generate")` → `...diagnostics::bundle_generate(&*client)...`
  - `Some("diagnostics bundle status")` → `...diagnostics::bundle_status(&*client)...`
  - `Some("diagnostics bundle wait")` → `...diagnostics::bundle_wait(&*client, ignition_core::actions::restart::DEFAULT_INTERVAL, std::time::Duration::from_secs(300))...` with a comment: mirrors the clap defaults (2 s / 300 s — cli.rs Wait leaves); `DEFAULT_INTERVAL` is the single 2 s poll-interval source; the 300 s literal must NOT be `BUNDLE_DOWNLOAD_TIMEOUT` (that is the per-request download override, a different semantic).
  - `Some("diagnostics bundle download")` → `...diagnostics::bundle_download(&*client, None)...` with a comment: `None` = the default `.part`-rename naming (timestamped fallback — note this differs from backup download, which passes the profile stem).
  - Each arm follows the exact `if let Some(client) = client_arc(state)` guard shape of the existing arms. Worker labels = clap-exact action names (the "wait gateway"-style convention).
  - state.rs's existing ACTIONS unit tests (uniqueness etc., ~line 1331) must stay green — the 7 new labels are unique.
  </action>
  <verify>cargo test -p ignition-tui green; cargo build green; `rg -n 'Some\("(license status|redundancy status|gan status|diagnostics bundle)' crates/ignition-tui/src/update.rs` shows 7 arms, each under a matching ACTIONS label</verify>
  <done>All seven verbs are selectable in the Actions modal and dispatch through the shared ignition-core actions; the generic result-modal path renders their JSON; existing TUI tests untouched and green</done>
</task>

<task type="auto">
  <name>Task 2: routes↔menu parity contract (menu_label seam + CI test with negative proof)</name>
  <files>crates/ignition-tui/src/routes.rs, crates/ignition-cli/tests/tui_coverage.rs</files>
  <action>
  **routes.rs — the alias seam:**
  - Add `pub fn menu_label(path: &str) -> Option<&'static str>` — the SINGLE mapping from registry path to display-prose menu label. Entries: `"wait gateway"` → `"wait for gateway up"`, `"wait restart"` → `"wait for restart complete"`, `"wait module"` → `"wait for module ready"`; everything else returns `None` (menu label == clap-exact path). Doc comment: this is the 06-10 display-prose rule made structural — the parity test keys off it.

  **tui_coverage.rs — `dashboard_actions_menu_matches_registry` test:**
  - Define `MENU_HOSTED: &[&str]` — clap-exact route paths for every Dashboard action-menu verb (15 existing + 7 new). Derive the existing 15 by cross-referencing `routes()`'s `Mapping::Screen(Screen::Dashboard)` rows against the current ACTIONS entries (`version`, `connections`, `wait gateway`, `wait restart`, `wait module`, `doctor`, `restart`, `backup download`, `backup restore`, `eam history`, `eam tasks`, `eam task new`, `eam task force`, `script run`, `lint` — confirm each against the actual routes() rows). STOP AND RECORD A DEVIATION if any current ACTIONS entry cannot be traced to a route path — that would be a latent registry bug this test exists to surface; record it in the summary rather than silently aliasing it.
  - Justified exclusions in comments: `status`, `modules`, `metrics`, `sessions` (bare) = dashboard PANELS (in-screen data, not menu verbs); `tui` = the cockpit itself; `sessions terminate` if it is modal-driven rather than menu-listed — verify against routes()/ACTIONS and comment the disposition explicitly. (Scope note: this contract covers the Dashboard screen only — where the gap actually bit; per-screen extension to Logs/Tags/Projects/Rig is deliberately out of scope.)
  - Assertions (all in one test):
    (a) PINNED COUNT of `Screen(Dashboard)` route rows in routes() — a new Dashboard-mapped row changes this number and fails CI, forcing a conscious menu entry (the "pinned test is the pre-declaration" pattern, same as the OutOfBand set);
    (b) every `MENU_HOSTED` path exists in routes() as `Mapping::Screen(Screen::Dashboard)`;
    (c) `menu_label(path).unwrap_or(path)` ∈ ACTIONS for every MENU_HOSTED path;
    (d) reverse: every ACTIONS entry traces to exactly one MENU_HOSTED path via the same label function — no orphan verbs;
    (e) `ACTIONS.len() == MENU_HOSTED.len()`.
  - **Negative proof (required):** temporarily remove one ACTIONS entry (e.g. "lint") — the test MUST fail; restore. Then temporarily add a fake `CliRoute { path: "fake verb", mapping: Screen(Dashboard) }` row — the pinned count MUST fail; revert. Record both negative proofs in the summary.
  </action>
  <verify>cargo test -p ignition-cli --test tui_coverage green; both negative proofs demonstrated then reverted; `rg -n "menu_label" crates/ignition-tui/src/routes.rs` shows the alias seam with the wait-trio entries</verify>
  <done>The 09-04/09-05 blind spot is structurally closed: adding a Dashboard-mapped route without a menu entry (or a menu entry without a route/arm) fails CI; the pinned count forces same-change updates for future command families</done>
</task>

</tasks>

<verification>
1. `cargo test -p ignition-tui -p ignition-cli` — zero failures
2. `rg -n 'ACTIONS: \[&str; 22\]' crates/ignition-tui/src/state.rs` — 7 new clap-exact verbs present with 09 provenance comments
3. `rg -c 'Some\("diagnostics bundle|Some\("license status|Some\("redundancy status|Some\("gan status' crates/ignition-tui/src/update.rs` — 7 arms
4. tui_coverage.rs parity test present with pinned Dashboard-route count; negative proofs recorded in the summary
5. No ui/mod.rs change needed (menu + modal sizing read ACTIONS.len() dynamically) — verify by `rg -n "ACTIONS" crates/ignition-tui/src/ui/mod.rs` showing only .len()/iteration uses
</verification>

<success_criteria>
- Pressing `a` on the Dashboard lists 22 verbs including the seven Phase 9 verbs; Enter on each runs the same action the CLI runs and shows the result in the result modal (pretty JSON)
- The clap walk AND the new menu-parity test are both green; the parity test demonstrably fails on route-without-menu and menu-without-route mutations
- The 09-04/09-05 registry-rows-without-surface failure mode cannot recur silently
- UAT test 10's diagnosed gap is re-testable at verify-work (the tab-indicator visual issue is separately deferred to Phase 12 per the UAT diagnosis and is explicitly NOT this plan's scope)
</success_criteria>

<output>
After completion, create `.planning/phases/09-agent-surface-api-diagnostics/09-08-SUMMARY.md`
</output>
