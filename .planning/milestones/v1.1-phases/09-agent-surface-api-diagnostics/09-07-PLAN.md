---
phase: 09-agent-surface-api-diagnostics
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ignition-core/src/client/diagnostics.rs
  - crates/ignition-core/src/error.rs
  - crates/ignition-core/src/poll.rs
  - crates/ignition-core/src/actions/diagnostics.rs
  - crates/ignition-cli/tests/contract_diagnostics.rs
  - README.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "bundle wait observing Invalid exits IMMEDIATELY (no further polls), non-zero, naming the observed state and that a fresh generate is required"
    - "bundle wait's deadline message never says 'gateway unreachable' when the gateway answered with an observed state — the observation rides the message verbatim"
    - "Unknown (never-observed) states still keep polling until the deadline — honest unknowns preserved, exit 4 network_error unchanged"
    - "The new failure slug is Three-Place consistent: exit enum + literal (exit,slug) table + README exit table all agree"
  artifacts:
    - path: "crates/ignition-core/src/client/diagnostics.rs"
      provides: "Invalid in BUNDLE_CAPTURED_STATES with UAT provenance; BUNDLE_UNAVAILABLE_STATES + is_bundle_unavailable()"
      contains: "BUNDLE_UNAVAILABLE_STATES"
    - path: "crates/ignition-core/src/error.rs"
      provides: "BundleNotAvailable variant, slug bundle_not_available, exit 6; both pinned tables updated"
      contains: "bundle_not_available"
    - path: "crates/ignition-core/src/actions/diagnostics.rs"
      provides: "bundle_wait: unavailable → immediate error arm, before the captured-terminal Done arm"
      contains: "is_bundle_unavailable"
    - path: "crates/ignition-core/src/poll.rs"
      provides: "deadline error carries the observation; message lead distinguishes answered vs no-answer"
      contains: "observation"
    - path: "README.md"
      provides: "bundle wait semantics rewritten + exit-table row for bundle_not_available"
  key_links:
    - from: "crates/ignition-core/src/actions/diagnostics.rs"
      to: "is_bundle_unavailable"
      via: "probe arm ordered BEFORE the captured-terminal Done arm"
      pattern: "is_bundle_unavailable"
    - from: "crates/ignition-core/src/error.rs"
      to: "README.md exit table"
      via: "Three-Place slug rule (09-01 gateway_client_error pattern, atomic)"
      pattern: "bundle_not_available"
    - from: "crates/ignition-core/src/poll.rs"
      to: "CoreError::Network"
      via: "deadline() sets the observation field on the last observation"
      pattern: "observation"
---

<objective>
Close UAT Gap 3 (test 8): make `ign diagnostics bundle wait` honest about terminal states.

The 09-07 UAT observed, for the first time live: a `Valid` bundle decays to `Invalid` within ~2 minutes UNPROMPTED and stays `Invalid` — `Invalid` is the gateway's steady-state "no current bundle" answer, not a transient generation failure. Today wait treats `Invalid` as an unknown state and polls it until the 300 s deadline, then mislabels the outcome as "gateway unreachable" even though the gateway answered concretely. This plan encodes the observed truth: `Invalid` becomes a captured TERMINAL steady state, wait exits immediately on it with bundle-not-available semantics, and the deadline message can no longer claim "unreachable" when the last observation exists.

Purpose: the UAT's retest already proved the happy path (generate→wait exits 0 in seconds) — the gap is EXCLUSIVELY Invalid-state handling and deadline-message honesty.

Output: capture-encoded `Invalid` handling in the client vocabulary, a new `bundle_not_available` slug (Three-Place atomic), an honest deadline message, updated contract tests + README rows, full suite green.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-agent-surface-api-diagnostics/09-UAT.md
@.planning/phases/09-agent-surface-api-diagnostics/09-05-SUMMARY.md
@.planning/phases/09-agent-surface-api-diagnostics/09-01-SUMMARY.md
@crates/ignition-core/src/client/diagnostics.rs
@crates/ignition-core/src/actions/diagnostics.rs
@crates/ignition-core/src/poll.rs
@crates/ignition-core/src/error.rs

Capture-provenance facts to encode verbatim (from the UAT diagnosis, Gap 3):
- Observed 2026-09-07 on the 8.3.6 rig (ign-p9-836): `Valid` at min 4-5, `Invalid` at min 6 under PASSIVE status-only polling; `Invalid` persists (TTL probe evidence in 09-UAT.md).
- Secondary wire quirk (do NOT encode as behavior): the same Valid bundle reported fileSize 64708 on one poll and 73889 on others — the bundle file appears non-static while Valid. The CLI reports faithfully at every hop; note it in the model docs as an observation, nothing more.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode Invalid as a captured TERMINAL steady state (client vocabulary)</name>
  <files>crates/ignition-core/src/client/diagnostics.rs</files>
  <action>
  - Add `"Invalid"` to `BUNDLE_CAPTURED_STATES` with a per-element provenance comment: `// 8.3.6 rig ign-p9-836, 2026-09-07 UAT (09-UAT.md Gap 3): Valid decays to Invalid within ~2 minutes UNPROMPTED (passive status-only polling; TTL probe) and stays Invalid — the steady-state "no current bundle" answer. TERMINAL.`
  - Add alongside the existing consts:
    `pub const BUNDLE_UNAVAILABLE_STATES: &[&str] = &[ /* provenance comment as above */ "Invalid" ];`
    and `pub fn is_bundle_unavailable(state: &str) -> bool { BUNDLE_UNAVAILABLE_STATES.contains(&state) }` (case-sensitive membership, same shape as `is_generating`).
  - Rewrite the module doc-comment header: the current "exactly `Generating` and `Valid` were observed... failure vocabularies remain unenumerated" paragraph is now FALSIFIED — three states observed; `Invalid` is a captured terminal steady state (unavailable), not an unknown. Document the fileSize non-static observation (64708 vs 73889 across polls of the same Valid bundle) as a gateway-side quirk the CLI reports faithfully.
  - Unit tests: `Invalid` ∈ BUNDLE_CAPTURED_STATES; `is_generating("Invalid")` false; `is_bundle_unavailable("Invalid")` true; `is_bundle_unavailable("Valid")` false; a genuinely unknown state ("Mystery") is neither.
  </action>
  <verify>cargo test -p ignition-core --lib diagnostics (client module tests green); `rg -n "BUNDLE_UNAVAILABLE_STATES" crates/ignition-core/src/` hits client/diagnostics.rs with the provenance comment</verify>
  <done>Invalid is capture-encoded with live provenance as a terminal steady state, distinguishable from generating (pending), valid (terminal success), and unknown (keep polling)</done>
</task>

<task type="auto">
  <name>Task 2: bundle_not_available slug + immediate-exit wait + honest deadline message</name>
  <files>crates/ignition-core/src/error.rs, crates/ignition-core/src/poll.rs, crates/ignition-core/src/actions/diagnostics.rs</files>
  <action>
  **error.rs — new variant, atomic Three-Place landing (the 09-01 `gateway_client_error` pattern):**
  - Add `BundleNotAvailable { state: String }`: Display = `no diagnostics bundle available (gateway reports state {state:?}) — run \`ign diagnostics bundle generate\` first; polling cannot change this state`. Precedent: `ImportDenied` (exit 6, slug `import_denied`) — a gateway-answered refusal, target-state class.
  - `code()` => `"bundle_not_available"`; `exit_code()` => `6`; `hint()` arm naming the generate command.
  - Update BOTH pinned tables in this file (`exit_code_mapping_enumerated` triples AND the duplicated literal `(exit, slug)` table) with `(6, "bundle_not_available")` in the same change — the tables must compile-and-pass together.
  - **Construction-site note:** adding a field to `Network` (below) touches ~15 literal constructions (client/mod.rs ~7, idp.rs ~4, poll.rs, version.rs, error.rs helper/tests). Mechanical `observation: None` insertion at each; match arms using `..` are untouched (the 09-04 mock-stub chore pattern).

  **poll.rs — honest deadline:**
  - Add `observation: Option<String>` to `CoreError::Network`.
  - `deadline()` sets `observation: Some(...)` carrying the last-observation text when one exists.
  - The Network Display must distinguish: when `observation` is Some → the lead must NOT contain "unreachable" (e.g. `no terminal state at {url}` + `; last observation: {obs}`); when None → today's `gateway unreachable at {url}{source_note}` preserved. thiserror named-arg expressions can do this; if the derive fights the expression, fall back to a manual Display impl for the variant's message via a small helper method — the CONTRACT is the wording, not the mechanism. Exit 4 / `network_error` unchanged.
  - Keep poll.rs's own deadline tests passing: "timed out" named, last observation carried, `Network { source: None, .. }` matches.

  **actions/diagnostics.rs — wait semantics:**
  - Probe arm ORDER (critical): `is_generating` → `Pending("state … — still generating")`; then `is_bundle_unavailable` → return `Err(CoreError::BundleNotAvailable { state })` IMMEDIATELY (zero further polls — only a fresh generate changes this state); then `BUNDLE_CAPTURED_STATES` contains → `Done` (Valid = terminal success); else → `Pending("unknown state … — still waiting")`. Invalid is in BUNDLE_CAPTURED_STATES after Task 1, so the unavailable arm MUST precede the captured-Done arm.
  - Update the module doc (the 09-05 "unknown states keep polling" paragraph): Invalid no longer rides the unknown path; deadline wording no longer claims unreachability when an observation exists.
  - Scripted WaitRig tests: `["Generating", "Invalid"]` → Err after EXACTLY 2 probes (no deadline wait), exit 6, slug `bundle_not_available`, message contains `Invalid` and `generate`; the existing unknown-state deadline test now additionally asserts the message does NOT contain "unreachable" (observation present) while still asserting exit 4 + `network_error`.
  </action>
  <verify>cargo test -p ignition-core (lib + integration green, incl. both pinned slug tables); the new Invalid rig test asserts `exit_code() == 6`, `code() == "bundle_not_available"`, and `rig.calls() == 2`</verify>
  <done>Wait on Invalid exits immediately with exit-6 bundle-not-available semantics naming the observed state; deadline-with-observation never says "unreachable"; honest-unknown polling to deadline (exit 4) preserved; Three-Place tables agree</done>
</task>

<task type="auto">
  <name>Task 3: Binary contract test + README rows (exit table + bundle wait semantics)</name>
  <files>crates/ignition-cli/tests/contract_diagnostics.rs, README.md</files>
  <action>
  - contract_diagnostics.rs: update the existing deadline contract test for the new wording (assert exit 4, observation text present, "unreachable" ABSENT when observation exists). Add a wiremock contract test: mount bundle status answering `{"state":"Invalid"}` → `ign diagnostics bundle wait` exits 6, the envelope slug is `bundle_not_available`, and the error names the observed state + the generate command. (The 09-05 test file is the declared shared home — extend, don't fork.)
  - README: rewrite the `ign diagnostics bundle wait` command-reference row — the captured vocabulary now includes `Invalid` as a TERMINAL steady state ("no current bundle"); wait exits IMMEDIATELY exit 6 `bundle_not_available` on it (a fresh generate is required; polling cannot change the state); unknown states keep polling to the deadline (exit 4) and the deadline message carries the last observation instead of claiming unreachability. Add `bundle_not_available` to the README exit-code table's exit-6 row in the same format as `import_denied`/`not_found` (Three-Place third place).
  - Keep every other pinned README claim intact (envelope data `{state, fileSize}`; defaults 2s/300s; download rows untouched).
  </action>
  <verify>cargo test -p ignition-cli --test contract_diagnostics green; `rg -n "bundle_not_available" README.md crates/ignition-core/src/error.rs` — all Three-Place sites present; full workspace suite green (note: ~50 min wall-clock — run in background, zero failures required)</verify>
  <done>Contract tests pin the Invalid immediate-exit and the honest deadline at the binary level; README exit table and command rows agree with the exit enum (Three-Place complete)</done>
</task>

</tasks>

<verification>
1. `rg -n '"Invalid"' crates/ignition-core/src/client/diagnostics.rs` — present in BUNDLE_CAPTURED_STATES and BUNDLE_UNAVAILABLE_STATES with the 2026-09-07 UAT provenance comment
2. `rg -n "bundle_not_available" crates/ignition-core/src/error.rs README.md` — variant slug + both pinned tables + README exit row
3. `rg -n "unreachable" crates/ignition-core/src/poll.rs` — the observation-bearing deadline path does not produce "unreachable" (lead-word switch verified by test)
4. cargo test -p ignition-core -p ignition-cli — zero failures (full workspace in background if time allows)
5. UAT Gap 3's three missing items each have a landed counterpart (vocabulary+provenance / immediate exit / deadline distinction)
</verification>

<success_criteria>
- `ign diagnostics bundle wait` against a gateway answering `Invalid` exits promptly (seconds, not 300s) with exit 6 `bundle_not_available`, naming the state and the generate command
- A wait that polls unknown states to the deadline exits 4 with the observation in the message and no "unreachable" claim when the gateway answered
- Happy path unchanged: Generating → Valid still exits 0 with the wire envelope (existing tests prove it)
- Workspace suite green; Three-Place slug rule holds (exit enum = literal table = README table)
- UAT test 8's diagnosed failure mode is re-testable at verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/09-agent-surface-api-diagnostics/09-07-SUMMARY.md`
</output>
