---
phase: 13-composite-engine-workspace-historian-edit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-composite-engine-workspace-historian-edit/13-LIVE-CAPTURES.md
  - .planning/phases/13-composite-engine-workspace-historian-edit/13-RIG-NOTES.md
  - .planning/phases/13-composite-engine-workspace-historian-edit/artifacts/config-before-836.json
  - .planning/phases/13-composite-engine-workspace-historian-edit/artifacts/config-after-836.json
  - .planning/phases/13-composite-engine-workspace-historian-edit/artifacts/designer-diff-836.txt
  - .planning/phases/13-composite-engine-workspace-historian-edit/artifacts/config-before-833.json
  - .planning/phases/13-composite-engine-workspace-historian-edit/artifacts/config-after-833.json
  - .planning/phases/13-composite-engine-workspace-historian-edit/artifacts/designer-diff-833.txt
autonomous: false

must_haves:
  truths:
    - "A Designer-written history binding's tags config shape is captured verbatim from BOTH trial rigs (8.3.3 + 8.3.6) as committed before/after JSON artifacts + a diff — the roadmap-mandated Designer-diff oracle"
    - "The spike verdict is recorded either way per the roadmap's loose done-definition: replay-closure (with the EXACT diff-captured field set that produces data) OR documented-limitation (with the diff evidence + the bounded provider-config probe result) — both legitimate"
    - "Whether 05-06's `historicalProvider` was the typo (vs the documented `historyProvider`) is ANSWERED by the diff, not inferred"
    - "All live activity is budgeted INSIDE resettable 2h trial windows: rig commissioning, provisioning, and every capture are scripted so the window's only interactive step is the Designer binding"
    - "Replay tests ONLY diff-captured key names — `historicalScanclass` or any other guess is never pre-committed to a pinned artifact"
  artifacts:
    - path: ".planning/phases/13-composite-engine-workspace-historian-edit/13-LIVE-CAPTURES.md"
      provides: "Spike record: rig/window log, before/after shapes, the replay procedure + outcome, the verdict line (closure path or documented-limitation), provider-probe result if run"
      contains: "designer-diff"
      min_lines: 40
    - path: ".planning/phases/13-composite-engine-workspace-historian-edit/13-RIG-NOTES.md"
      provides: "Rig ops log: containers, ports, provider names (unique per run), token NAMES only, trial-window timestamps, teardown state"
    - path: ".planning/phases/13-composite-engine-workspace-historian-edit/artifacts/designer-diff-836.txt"
      provides: "The unified diff of tags config get before/after the Designer binding (8.3.6 rig)"
  key_links:
    - from: "artifacts/designer-diff-*.txt"
      to: "13-04 TAGS-14 implementation"
      via: "the closure plan implements/pins ONLY field names that appear in this diff (Pitfall 12: no guessed shapes)"
      pattern: "historyProvider|historyEnabled|sampleMode"
    - from: "13-LIVE-CAPTURES.md verdict line"
      to: "SC-4 done-state selection"
      via: "closure recipe (13-04 pins it) OR documented-limitation-with-evidence (13-04 ships it) — both legitimate per roadmap"
      pattern: "verdict"
---

<objective>
Run the roadmap-MANDATED time-boxed both-rig Designer-diff spike for the historian slice: capture a Designer-written tag↔historian binding's tags config shape as ground truth, diff it against the CLI-created shape, replay the binding via the tag write path using ONLY diff-captured names, and record the verdict — binding closure achieved, or documented-limitation-now-with-evidence. The roadmap deliberately holds the historian done-definition loose; THIS plan's record is the branch point 13-04 implements from.

Purpose: TAGS-14 is spike-gated by the roadmap. The 05-06 spike's five failed candidates all omitted the Designer's actual field set; research supplies a leading hypothesis (doc-corrected `historyProvider` vs 05-06's `historicalProvider`) and a field checklist, but the Designer diff is the oracle that names the real keys.
Output: 13-LIVE-CAPTURES.md (spike record + verdict), 13-RIG-NOTES.md, six committed capture artifacts (before/after JSON + diff per rig). ZERO product code.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/workflows/spike.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-composite-engine-workspace-historian-edit/13-RESEARCH.md
@.planning/phases/05-webdev-backend-tag-operations/05-06-SUMMARY.md
@.planning/phases/11-tag-bulk-transfer-xml-csv/11-RIG-NOTES.md
@crates/ignition-cli/tests/e2e_webdev.rs
@README.md
</context>

<planner_locks>
Locked decisions binding this plan (no CONTEXT.md; source: orchestrator planning context + STATE.md):

- **USER (2026-09-14):** Historian rig access CONFIRMED via resettable 2h trial windows (11-06 ops mechanism: image trial = 2h, fresh rig ~4 min, trials reset as necessary — but NEVER count on a reset mid-activity). Budget every live activity inside one window.
- **ROADMAP:** this plan IS the spike; the Designer-diff is the oracle; documented-limitation-with-evidence is a legitimate outcome.
- **Research field checklist** (hypothesis-with-strong-evidence, NOT a substitute for the diff): `historyEnabled` (Boolean), `historyProvider` (String — 05-06 sent `historicalProvider`, absent from the official table), `sampleMode` (OnChange|Periodic|TagGroup), Historical Tag Group (exact key the diff names — do NOT pre-commit `historicalScanclass` anywhere pinned), `historicalDeadbandMode`, `historicalDeadband`. Dataset tags can't bind — spike tag is Int4 (fine).
- Unique provider names per run (11-06 discipline). The Designer is a desktop app on the host Mac against port-forwarded containers — its install/start is budgeted INSIDE the window.
</planner_locks>

<tasks>

<task type="auto">
  <name>Task 1: Stage both trial rigs — commission, license, provision historian + tag, script the captures</name>
  <files>.planning/phases/13-composite-engine-workspace-historian-edit/13-RIG-NOTES.md</files>
  <action>
Follow the 11-RIG-NOTES.md / 09-02 recipe VERBATIM (fifth-generation-proven, ~4 min per rig):
1. Pre-flight: Docker running; ports 18188/19188 free (`lsof -i :18188 -i :19188`). Note the wall-clock trial-window START time for each rig in 13-RIG-NOTES.md (2h budget).
2. Rig A: `docker run -d --name ign-p13-836 -p 18188:8088 -e ACCEPT_EULA=Y -e GATEWAY_ADMIN_PASSWORD=password -e IGNITION_EDITION=standard inductiveautomation/ignition:8.3.6`. Rig B: same shape as 8.3.3 on 19188 (compose recipe per 11-RIG-NOTES).
3. Commission headlessly (bootstrap → eula-accept → start-gateway → poll /StatusPing to RUNNING). Provision an API token per rig via the provision_token.sh recipe; stage keys to /tmp/ign-p13-rigs/tokens.env (chmod 600, outside the repo). Scratch config at /tmp/ign-p13-rigs/config.toml; one profile per rig.
4. Deploy the current WebDev bundle WITH script-exec on each rig (`ign --profile <p> webdev deploy --with-script-exec`) — the tagConfig route is the create/edit vehicle.
5. Provision the historian per the e2e_webdev.rs:868 native-REST shape with UNIQUE names: `p13hist836` / `p13hist833` (add a run suffix if re-running after an expired window — never reuse a name against a fresh gateway). Then create the spike tag via the CLI: `ign tags config create '[default]P13H/T1'` with `{"tagType":"AtomicTag","dataType":"Int4","value":0}` (research's Code Example shape).
6. Run the BEFORE capture per rig and commit the bytes: `ign tags config get '[default]P13H/T1' --json` → artifacts/config-before-836.json / config-before-833.json. Also stage a capture SCRIPT at /tmp/ign-p13-rigs/capture.sh (before + after + diff via `jq -S`) so the post-Designer step is one command — the window's only interactive step must be the Designer.
7. Record containers, ports, provider names, token NAMES only, window start times, and the exact commands in 13-RIG-NOTES.md. No secret material in the repo.
  </action>
  <verify>curl gateway-info → 200 on both rigs; `ign tags config get '[default]P13H/T1'` exits 0 on both; artifacts/config-before-*.json exist and parse as JSON; capture.sh exists and is executable; 13-RIG-NOTES.md records window start times</verify>
  <done>Both trial rigs RUNNING with historian + spike tag provisioned, BEFORE shapes committed, and the post-Designer capture fully scripted</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Designer step — hand-write the history binding on the spike tag (both rigs)</name>
  <files>.planning/phases/13-composite-engine-workspace-historian-edit/artifacts/.gitkeep</files>
  <what-built>
Both trial rigs are live with an InternalHistorian provider (p13hist836 / p13hist833) and an unbound Int4 tag `[default]P13H/T1`. The BEFORE config shape is captured. This step creates ONE binding by hand in the Designer — the ground truth no scripted path can produce (a non-Designer path would defeat the diff oracle).
  </what-built>
  <action>
ALL scripted preparation is automated BEFORE this step (Task 1 staged both rigs, the historian, the spike tag, and /tmp/ign-p13-rigs/capture.sh — the window's only interactive step is this one).

Per rig (8.3.6 first, then 8.3.3):
1. Open http://localhost:18188 (then http://localhost:19188) in a browser → download/launch the Designer (first launch installs Ignition Designer — this is the install time budgeted inside the 2h window).
2. Log into the Designer, open/create any project, find tag `[default]P13H/T1` in the Tag Browser, open its tag editor.
3. In the History section: check **History Enabled**; set **Storage Provider** = `p13hist836` (or `p13hist833` on rig B); set **Sample Mode** = `Tag Group`; pick the historical tag group (the default `Historical` group if offered); leave deadband defaults.
4. Save the tag. That's the whole step — one Designer-written binding per rig.
5. If the 2h window expired while this task waited: say so — Claude will re-spin the rig (~4 min), re-provision with a NEW unique provider name, and you repeat only the Designer step on the fresh rig.
  </action>
  <verify>After resume: `ign tags config get '[default]P13H/T1' --json` on each rig shows the history keys present (the binding took) — this check opens Task 3's after-capture</verify>
  <done>A Designer-written history binding exists on `[default]P13H/T1` on BOTH rigs, ready for the scripted AFTER capture</done>
  <resume-signal>Type "done" once the tag is saved in the Designer on BOTH rigs (or name which rig is done if doing them one at a time)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: AFTER captures + Designer-diff + replay via the tag write path → spike verdict</name>
  <files>.planning/phases/13-composite-engine-workspace-historian-edit/13-LIVE-CAPTURES.md, .planning/phases/13-composite-engine-workspace-historian-edit/artifacts/config-after-836.json, .planning/phases/13-composite-engine-workspace-historian-edit/artifacts/designer-diff-836.txt, .planning/phases/13-composite-engine-workspace-historian-edit/artifacts/config-after-833.json, .planning/phases/13-composite-engine-workspace-historian-edit/artifacts/designer-diff-833.txt</files>
  <action>
1. Run the staged capture script per rig: `ign tags config get '[default]P13H/T1' --json` → artifacts/config-after-{836,833}.json; `diff <(jq -S . config-before-X.json) <(jq -S . config-after-X.json) > artifacts/designer-diff-X.txt`. COMMIT the diff files (the designer-diff oracle artifacts — do not hand-edit them; if jq normalizes something meaningful, note it in the captures doc instead).
2. Record the diff analysis in 13-LIVE-CAPTURES.md §Designer-Diff: which keys appeared (expect historyEnabled + historyProvider + sampleMode + the historical-group key — capture the EXACT name), any nested shapes (objects vs scalars), any keys the Designer REMOVED. Explicitly answer: was 05-06's `historicalProvider` the typo? Does the diff confirm the research's vocabulary table?
3. **Replay (closure attempt)** on rig A: create a SECOND tag `[default]P13H/T2` and configure it with EXACTLY the diff-captured keys (values mirroring the Designer's — provider name, sample mode, group), via `ign tags config create`/`edit` (the existing tagConfig route — passthrough posture, no new CLI surface this plan). Then: `ign tags write '[default]P13H/T2' --value 42` (a few values, ~1s apart, per the 05-06 write cadence) → `ign tags history query '[default]P13H/T2' --start <now-5m> --end <now+5m>`. Repeat on rig B.
4. **Verdict**: if the query returns DATA rows (not null values) on a rig — SC-4 closure is achievable via the tag write path; pin the EXACT working field set in 13-LIVE-CAPTURES.md (this becomes 13-04's implementation contract). If NO data: run the bounded provider-config probe (research OQ4): export/probe the historian provider config + tag-group config via the native REST surface to check whether the Designer wrote binding state OUTSIDE the tag config — record what the probe shows, then the verdict is documented-limitation-WITH-evidence (cite the diff + probe files). NEVER iterate new guessed keys beyond the diff-captured set.
5. Cleanup per rig (self-cleaning): `ign tags config delete` both spike tags, delete the historian provider (e2e_webdev.rs:899 shape). Record teardown + window end times in 13-RIG-NOTES.md.
6. Close 13-LIVE-CAPTURES.md with a one-line **verdict** and a **field-set table** (key → type → Designer UI label) built ONLY from captured names — the artifact 13-04 implements from.
  </action>
  <verify>artifacts/config-after-*.json + designer-diff-*.txt exist for BOTH rigs and the diffs are non-empty; 13-LIVE-CAPTURES.md contains §Designer-Diff, §Replay, and a verdict line that is exactly one of `SPIKE VERDICT: closure — field set: [...]` or `SPIKE VERDICT: documented-limitation — evidence: diff + provider-probe`; no key name appears in the captures doc that is absent from the committed diffs; rig tags/historian cleaned up (verify with a final get/browse)</verify>
  <done>The Designer-diff oracle is captured from both rigs, the replay is attempted with diff-captured names only, and the spike verdict (either legitimate done-state) is recorded as the branch point for 13-04</done>
</task>

</tasks>

<verification>
- Six capture artifacts committed (before/after/diff × 2 rigs); diffs are machine-generated, not hand-authored
- 13-LIVE-CAPTURES.md answers the research's Open Question 1 (typo hypothesis) and OQ4 (provider-level state) with rig evidence
- The verdict + field-set table cite ONLY diff-captured names (`historicalScanclass` appears nowhere unless the diff itself contained it)
- 13-RIG-NOTES.md records both trial windows' start/end times — evidence every live step fit the 2h budget (or documents the reset+re-spin that happened)
- Zero product code: `git status` shows only .planning/ files
</verification>

<success_criteria>
The mandated Designer-diff spike is complete: a Designer-written binding's config shape captured on both rigs, the diff committed as the oracle artifact, the tag-write-path replay attempted with diff-captured names, and the verdict recorded — SC-4's done-definition is no longer loose.
</success_criteria>

<output>
After completion, create `.planning/phases/13-composite-engine-workspace-historian-edit/13-01-SUMMARY.md`
</output>
