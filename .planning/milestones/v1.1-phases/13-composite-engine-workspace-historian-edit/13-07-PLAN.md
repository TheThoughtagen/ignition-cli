---
phase: 13-composite-engine-workspace-historian-edit
plan: 07
type: execute
wave: 4
depends_on: ["13-06"]
files_modified:
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-cli/src/render.rs
  - crates/ignition-tui/src/routes.rs
  - crates/ignition-cli/tests/tui_coverage.rs
  - crates/ignition-cli/tests/contract_workspace.rs
  - README.md
autonomous: true

must_haves:
  truths:
    - "A user runs `ign workspace checkout|status|push` — three verbs, one family in the clap tree — each dispatching through the Session seam (no second construction) with the global flags honored"
    - "The new clap nodes have routes() rows (normal envelope verbs — NOT OutOfBand) so the bidirectional tui_coverage walk stays green — workspace family rows land in the SAME tasks as their clap commands"
    - "The workspace verbs SURVIVE the stdout-purity byte-scan harness over the real spawned binary (Phase-8 contract: new stdout-writing surfaces are covered) — a single stray stdout byte under max diagnostics fails"
    - "Contract goldens pin the agent-facing behavior: status render (push-relative rows), the --yes-less push refusal whose message IS the blast radius, the conflict refusal, the checkout refusals"
    - "README documents the workspace contract honestly: project-resources scope (tag values NEVER in the tree — tags remain `ign tags` verbs), manifest committed / codec artifacts gitignored, push-relative status semantics, --delete opt-in, conflict resolution is manual"
  artifacts:
    - path: "crates/ignition-cli/src/cli.rs"
      provides: "Commands::Workspace(WorkspaceArgs) → Checkout/Status/Push subcommands with --decode-scripts / --yes / --delete / project / target-dir args"
      contains: "Workspace"
    - path: "crates/ignition-cli/tests/contract_workspace.rs"
      provides: "snapbox goldens + assert_cmd binary tests: three render modes for status, refusal goldens, envelope data shapes"
      min_lines: 60
    - path: "crates/ignition-cli/tests/tui_coverage.rs"
      provides: "routes() rows for workspace checkout/status/push + the clap walk green"
      contains: "workspace"
  key_links:
    - from: "crates/ignition-cli/src/main.rs"
      to: "ignition-core::Session"
      via: "dispatch resolves through Session only (Phase-8 seam rule)"
      pattern: "Session::resolve"
    - from: "crates/ignition-cli/tests/contract_workspace.rs refusal goldens"
      to: "13-06 gate refusal message"
      via: "the golden anchors on the stable refusal-message prefixes from the action layer (the preview IS the message)"
      pattern: "conflict|confirmation_required"
    - from: "stdout purity harness"
      to: "workspace CLI family"
      via: "the byte-scan covers the new verbs — envelope dispatch stays pure"
      pattern: "workspace"
---

<objective>
Surface the workspace slice: the `ign workspace checkout|status|push` CLI family with dispatch through the Session seam, render arms for the three modes, TUI routes rows landing with the clap commands, stdout-purity coverage for the new verbs, binary-level contract goldens, and the README's workspace contract section.

Purpose: SC-1's user-facing half — a user (or agent) drives checkout/drift/guarded-push as commands. The contract-test + coverage rituals (routes rows with clap, purity scan, goldens) are what make the surface agentic, not just runnable.
Output: CLI family + dispatch + render + coverage rows + purity coverage + contract_workspace.rs + README section.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-composite-engine-workspace-historian-edit/13-RESEARCH.md
@.planning/phases/13-composite-engine-workspace-historian-edit/13-03-SUMMARY.md
@.planning/phases/13-composite-engine-workspace-historian-edit/13-06-SUMMARY.md
@crates/ignition-cli/src/cli.rs
@crates/ignition-cli/src/main.rs
@crates/ignition-cli/src/render.rs
@crates/ignition-tui/src/routes.rs
@crates/ignition-cli/tests/tui_coverage.rs
@.planning/phases/09-agent-surface-api-diagnostics/09-03-PLAN.md
@README.md
</context>

<planner_locks>
- **Workspace verbs are normal envelope verbs** (NOT OutOfBand): stdout stays envelope/data-only; every refusal renders as the contract error envelope. Only `ign edit` (13-08) is OutOfBand.
- **Envelope additions are additive-only:** CheckoutOutcome/WorkspaceStatus/PushOutcome ride `data` with skip-serialize-at-defaults; pre-existing envelopes byte-identical (grep-able pins where goldens exist).
- **Atomicity:** routes.rs rows + tui_coverage expectations land in the SAME task as each clap node (the walk fails an orphan either way — Phase-8 executable contract).
- **No new exit slugs.** All refusals ride existing classes (InvalidInput exit 2 / confirmation_required exit 2 / config exit 3).
</planner_locks>

<tasks>

<task type="auto">
  <name>Task 1: clap family + Session-seam dispatch + three-mode render</name>
  <files>crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/main.rs, crates/ignition-cli/src/render.rs</files>
  <action>
1. cli.rs: `Workspace(WorkspaceArgs)` on Commands (`#[command(arg_required_else_help = true)]`, doc: check out a project's resources to a local tree, inspect drift, push guarded changes). `WorkspaceArgs { #[command(subcommand)] command: WorkspaceCommand }`:
   - `Checkout { project: String, target: PathBuf (positional), #[arg(long)] decode_scripts: bool }` — doc on --decode-scripts: decode embedded scripts to editable .py sidecars (nvim-editable; encodes back cleanly).
   - `Status { path: PathBuf (positional, defaults to cwd) }`.
   - `Push { path: PathBuf (positional, defaults to cwd), #[arg(long)] delete: bool }` — --yes is the GLOBAL flag (never redeclared — the globals-once pattern); --delete doc: also delete gateway members removed locally (default: reported, skipped).
2. main.rs dispatch per verb: profile/target resolution → `Session::resolve` (the seam — no second construction) → the 13-03/13-06 action. Status/Push derive the project from the manifest (read first, refuse if foreign) — the user never re-types it. ActionOutput gains Workspace variants (Checkout(actions::workspace::CheckoutOutcome) / Status(...) / Push(...)).
3. render.rs: human mode — checkout: `checked out N members → <target>` + decode-scripts note; status: a stable two-column table (PATH / STATE) ordered by path, STATE from StatusKind (clean rows INCLUDED — agents diff full state), summary line `x local edits, y gateway drift, z conflicts, n untracked`; push: the wrote/deleted/skipped lists. JSON mode: the outcome structs as `data` verbatim. Compact: single-line summaries. All refusal paths ride the standard error envelope.
4. Usage-class guard posture: a missing/foreign manifest on status/push refuses BEFORE Session resolution where the action allows it (mirror the api-call pre-resolve pattern: usage errors have null profile, zero requests) — check what 13-06's read_manifest needs; if the manifest read needs no gateway, do it pre-resolve.
  </action>
  <verify>`cargo build` green; `cargo run -- workspace --help` shows the three verbs; `cargo run -- workspace status /tmp/nonexistent` renders the contract error envelope (exit 2, null profile, no request)</verify>
  <done>The workspace family parses, dispatches through the seam, and renders in all three modes with push-relative status semantics</done>
</task>

<task type="auto">
  <name>Task 2: TUI routes rows + clap-walk coverage + stdout purity coverage (same-task atomicity)</name>
  <files>crates/ignition-tui/src/routes.rs, crates/ignition-cli/tests/tui_coverage.rs, crates/ignition-cli/tests/contract_workspace.rs</files>
  <action>
1. routes.rs: `CliRoute { path: "workspace checkout" | "workspace status" | "workspace push", mapping: Mapping::... }` — these are normal verbs (envelope product); map them the way project diff/sync map (read the existing rows for the project family and follow the same Mapping variant + justification comments).
2. tui_coverage.rs: extend the clap-walk fixtures for the three new nodes (bidirectional — walk stays green proves rows+commands landed together; the pinned OutOfBand set is UNTOUCHED — workspace is not OutOfBand; `edit` joins that set in 13-08, not here).
3. Stdout purity: locate the Phase-8 byte-scan harness (grep tests for the purity harness file — 08-06 shipped it assert-based over the real binary); extend its command sweep to include the workspace verbs (e.g. run `workspace status` against a dead port / missing manifest under max diagnostics and assert ZERO stdout bytes — the envelope refusal goes to stderr). If the harness enumerates commands automatically via clap, verify the new nodes are picked up and add explicit cases for the refusal paths.
4. Start contract_workspace.rs: snapbox goldens for `workspace status` human render (synthetic tree fixture + wiremock-served export — the binary tests get the URL from an isolated config pointing at the wiremock, the established pattern) covering: the four row kinds rendering, summary line, --compact and --json variants.
  </action>
  <verify>`cargo test -p ignition-cli --test tui_coverage` green (walk bidirectional); the purity harness run includes workspace verbs and passes; contract_workspace.rs goldens green</verify>
  <done>New clap nodes are coverage-complete (routes rows + walk + purity) and the status render has goldens — the coverage rituals landed ATOMICALLY with the surface</done>
</task>

<task type="auto">
  <name>Task 3: refusal goldens + checkout/push binary contract tests + README workspace section</name>
  <files>crates/ignition-cli/tests/contract_workspace.rs, README.md</files>
  <action>
1. Binary contract tests (assert_cmd + isolated IGNITION_CLI_CONFIG + wiremock, per the established binary-test pattern):
   - push WITHOUT --yes against a synthetic workspace with one local_edit: exit 2 confirmation_required, stderr message = the blast-radius preview text (golden), traffic pin: zero imports.
   - push WITH a conflict fixture (both-diverged member): exit 2, both member paths named in the message, fires even with --yes.
   - checkout refusals: non-empty unrelated target; foreign-manifest dir. --decode-scripts happy path: golden the human output; assert .py sidecars + both manifests in the target.
   - status against a foreign-workspace manifest: the stable refusal message prefix (13-03's).
2. README workspace section (honesty contract, per research Pitfall W3 + planner locks):
   - Scope: project RESOURCES only — tag values never appear in the tree (tags are `ign tags` verbs); the source is strictly the project export.
   - The manifest (`.ign-workspace.json`) is committed; `scripts-manifest.json` + sidecars are gitignored (checkout writes the .gitignore).
   - Status states are push-relative (table: state → what push does).
   - Push: guarded (--yes), conflict refuses even with --yes (manual reconciliation), deletions need --delete, untracked files are never imported, empty selection writes nothing.
   - --decode-scripts round-trip guarantee (byte-exact unedited) with a pointer to the codec contract.
3. Final sweep: `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --check` — all green.
  </action>
  <verify>contract_workspace.rs fully green (goldens + traffic pins); README renders the section (spot-render or lint if configured); full workspace suite + clippy + fmt green</verify>
  <done>The workspace slice is contract-complete end to end: a user can checkout/status/push with goldens proving every refusal and render — SC-1 and SC-2's exclusion clause closed at the binary level</done>
</task>

</tasks>

<verification>
- `cargo test --workspace` green; clippy -D warnings; fmt --check
- tui_coverage walk green with the three new nodes (OutOfBand set UNCHANGED — still exactly ["completions", "api call"]; `edit` is 13-08's atomic landing)
- Stdout purity harness covers the workspace verbs (grep the harness file for the new cases)
- The push-refusal golden's message text matches the 13-06 preview rendering byte-for-byte (the one-gate property holds through the CLI)
- No new exit slugs; no route-bundle change; no new deps
</verification>

<success_criteria>
`ign workspace checkout|status|push` is live, contract-golden, coverage-complete (routes rows, clap walk, stdout purity), and README-honest — SC-1 fully closed and SC-2's tag-exclusion clause proven at the binary level.
</success_criteria>

<output>
After completion, create `.planning/phases/13-composite-engine-workspace-historian-edit/13-07-SUMMARY.md`
</output>
