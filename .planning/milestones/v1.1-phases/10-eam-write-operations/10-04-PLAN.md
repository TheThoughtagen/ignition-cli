---
phase: 10-eam-write-operations
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - crates/ignition-cli/src/cli.rs
  - crates/ignition-cli/src/main.rs
  - crates/ignition-tui/src/state.rs
  - crates/ignition-tui/src/routes.rs
  - crates/ignition-tui/src/update.rs
  - README.md
autonomous: true

must_haves:
  truths:
    - "User can run ign eam task suspend|resume|cancel|modify|delete — without --yes each refuses exit 2 with the blast-radius preview IN the refusal message (target task, agents, controller impact), with --yes each executes"
    - "ign eam task force refuses with the same blast-radius preview in its operation string (EAMW-04 composition)"
    - "The guard stays PRE-WRITE: the preview fetch may read, but zero writes fire without confirmation — refusal envelope has profile null-equivalent honesty and zero mutations"
    - "TUI exposes the new verbs as Dashboard-mapped routes with Confirm modal body = blast-radius preview, and routes↔menu parity CI passes with the new pinned row count"
    - "README documents every new verb, the agent-vs-task reconciliation (no agent-level suspend exists on the wire), and any exit-class notes — README exit table unchanged unless 10-03 added a slug"
  artifacts:
    - path: "crates/ignition-cli/src/cli.rs"
      provides: "EamTaskCommand::Suspend/Resume/Cancel/Modify/Delete variants with args + doc-comments"
      contains: "Suspend"
    - path: "crates/ignition-cli/src/main.rs"
      provides: "Dispatch arms: lifecycle_precheck → resolve → build_blast_radius → require_confirmation(preview line) → action; ActionOutput variants + render arms"
      contains: "build_blast_radius"
    - path: "crates/ignition-tui/src/routes.rs"
      provides: "CliRoute rows for the five new leaves (Screen::Dashboard) + PendingAction variants + parity pin count updated in the SAME change"
      contains: "eam task suspend"
    - path: "README.md"
      provides: "EAM write lifecycle docs incl. the reconciliation note and preview behavior"
      contains: "eam task suspend"
  key_links:
    - from: "main.rs dispatch"
      to: "actions::eam build_blast_radius + require_confirmation"
      via: "preview line embedded in the confirmation operation string — the refusal ITSELF carries the blast radius"
      pattern: "require_confirmation.*preview|preview.*require_confirmation"
    - from: "TUI PendingAction variants"
      to: "main.rs require_confirmation set"
      via: "TUI Confirm gating mirrors EXACTLY the CLI's guarded verb set"
      pattern: "PendingAction"
    - from: "routes.rs pinned parity test"
      to: "clap tree"
      via: "bidirectional route↔menu resolution stays green with the new row count"
      pattern: "parity|pinned"
---

<objective>
Ship the user-facing surface: five guarded `ign eam task` verbs (suspend/resume/cancel/modify/delete) with the two-tier blast-radius guard (pure precheck → preview fetch → confirm-with-preview → execute), force composed onto the same preview, TUI route/PendingAction rows with parity, and the README contract update.

Purpose: This is the phase's visible product — the success criteria are CLI behaviors. The refusal message IS the blast-radius surface for agents (exit 2 + preview text), the confirm prompt is the surface for humans, the TUI Confirm modal is the surface for the cockpit.

Output: Working CLI verbs in all three render modes, TUI rows + parity, README current. No live-gate work (10-05).
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-eam-write-operations/10-LIVE-CAPTURES.md
@.planning/phases/10-eam-write-operations/10-03-SUMMARY.md
@crates/ignition-cli/src/cli.rs
@crates/ignition-cli/src/main.rs
@crates/ignition-tui/src/routes.rs
@crates/ignition-tui/src/state.rs
@crates/ignition-tui/src/update.rs
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI verbs + two-tier guard dispatch for suspend/resume/cancel/modify/delete + force preview composition</name>
  <files>crates/ignition-cli/src/cli.rs, crates/ignition-cli/src/main.rs</files>
  <action>
1. cli.rs — extend `EamTaskCommand` (the `eam task` subcommand family, after Force):
   - `Suspend { name: String }`, `Resume { name: String }`, `Cancel { name: String }` — positional task name, doc-comments carry the controller-gate honesty (eam_not_controller on stock gateways) and the guard note (refused without --yes).
   - `Modify { name: String, #[arg(long)] rename: Option<String>, #[arg(long)] enable: bool, #[arg(long)] disable: bool, #[arg(long, value_name = "MODE")] schedule_mode: Option<String>, #[arg(long, value_name = "K=V")] setting: Vec<String>, #[arg(long, value_name = "PATH")] description: Option<String> }` — enable/disable are flag-pairs (conflicts_with each other; clap-validated), --setting reuses the K=V auto-typing parse (parse_setting) merged into config.settings, --description takes the new description string (rename flag per 10-03's capture-backed decision — if rename proved unsupported on the wire, OMIT the --rename flag entirely and let the README's composite workflow stand). Trim to what 10-03's TaskChange actually supports — the CLI mirrors the action, never invents surface.
   - `Delete { name: String }` — doc-comment notes signature-keyed delete behind the guard.
2. main.rs — dispatch arms following the force shape, with the TWO-TIER flow: (a) pure `lifecycle_precheck` FIRST (exit 2, zero network); (b) `resolve_gateway_api`; (c) `build_blast_radius(&api, "<verb>", &name)` — on error, propagate (a bad task name refuses BEFORE any confirmation prompt with not_found exit 6); (d) `require_confirmation(cli.yes, &preview_operation)` where preview_operation embeds `render_preview_line(&preview)` — the refusal message therefore names task + agents + impact (EAMW-07); (e) call the action. For Modify, the Tier-0 pure stage also validates flag sanity BEFORE resolution (enable+disable conflicts are clap's job; --setting parse via parse_setting BEFORE resolution — malformed K=V is exit 2 with zero network, the tags-write byte-source precedent). For Delete, the blast radius carries the affected-resource facts from the find; for Modify, preview reflects the POST-change impact line (verb string "modify"). ActionOutput gains variants (EamLifecycle(EamLifecycleResult) per action or one EamTaskLifecycle variant keyed by action — match the existing enum's granularity style; EamTaskModify, EamTaskDelete) + render arms in all three modes via the existing render_ok path (core returns Strings; bin owns streams).
3. Force composition: change force's fixed operation string to the preview line — force's arm gains steps (c)+(d) with `build_blast_radius(&api, "force", &name)` (replacing its inline find — the action still finds owner itself; the preview fetch is the guard's read, the action's find is the authoritative re-read). Its ActionOutput already exists; the new `preview` field rides the existing render (additive JSON key).
4. Goldens: snapbox fixtures for suspend refusal (exit 2, preview text present, profile field per the refusal convention) and suspend success (mock via the existing binary-gateway test pattern — config profile → wiremock URI) for at least suspend + delete; programmatic envelope assertions for the rest (the Phase-1 golden discipline: pin every number you fixture, elide only mock URIs).
  </action>
  <verify>`cargo build --workspace` clean; `cargo test -p ignition-cli` green including new goldens; manual smoke against a wiremock or the IgnitionMock: `ign eam task suspend X` without --yes exits 2 and stderr contains the task name + "suspend"; with --yes and a mocked gateway it exits 0 with the lifecycle envelope.</verify>
  <done>Five new verbs + force preview compose through the two-tier guard; refusals are exit 2 pre-write with the blast radius in the message; all three render modes work; goldens prove the refusal and success shapes.</done>
</task>

<task type="auto">
  <name>Task 2: TUI surface — routes rows, PendingAction variants, Confirm modal body = preview, parity pin update</name>
  <files>crates/ignition-tui/src/routes.rs, crates/ignition-tui/src/state.rs, crates/ignition-tui/src/update.rs</files>
  <action>
1. tui/src/routes.rs (the CliRoute registry lives HERE — the TUI crate owns the clap-walk parity): add CliRoute rows for `eam task suspend`, `eam task resume`, `eam task cancel`, `eam task modify`, `eam task delete` (Mapping::Screen(Screen::Dashboard) — the existing eam family rides the dashboard's actions menu; no dedicated screen). Extend the route comment block with the guard note (Confirm-gated per the CLI's --yes set).
2. tui/src/state.rs: PendingAction gains variants for the five verbs (Confirm ≡ --yes — the established per-verb doc-comment style). The Confirm modal's BODY for these actions = the blast-radius preview text (render_preview_line output + the agent/pending lines) — the modal already takes {title, body}; the update.rs path that composes PendingAction → Confirm body strings the preview through. If the TUI's existing ops workers (workers/ops.rs) dispatch PendingActions through Session handles, add the five dispatch arms there (Session-typed — no second client construction, the Phase-8 rule; the worker calls the same actions::eam fns).
3. tui/src/routes.rs + the parity contract: update the PINNED row count (currently 31) to the new total IN THE SAME COMMIT as the rows — route-without-menu or menu-without-route must both fail red if drifted (the 09-08 mechanism). The menu labels resolve through the menu_label seam; the new leaves get labels only if they join the dashboard actions menu form list — follow the existing eam task new/force pattern for how guarded EAM verbs appear (form or menu entry); whichever surface they join, parity must resolve bidirectionally.
4. Build + full TUI test suite green.
  </action>
  <verify>`cargo test --workspace` green — specifically the routes↔menu parity test (updated count) and TUI state tests; `grep -c "eam task" crates/ignition-tui/src/routes.rs` shows the five new rows; the Confirm-body preview wiring is grep-visible (render_preview_line referenced from the TUI crate or its worker layer).</verify>
  <done>All five verbs reachable from the TUI behind Confirm modals whose body is the blast radius; parity contract green at the new pinned count; Session-typed dispatch only.</done>
</task>

<task type="auto">
  <name>Task 3: README agent contract + reconciliation note + full verification pass</name>
  <files>README.md</files>
  <action>
1. README EAM section: document the five new verbs (args, guards, exit behavior on refusal — exit 2 with the preview in the message), the force preview composition, and THE RECONCILIATION NOTE: the EAM wire has NO agent-level suspend/resume — suspend/resume/cancel are TASK-scoped runtime verbs; an "agent" is suspended by suspending its tasks (the blast-radius preview names the affected agents via targetGateways). Quarantined-agent delete, approve-agent, upgrade-agent exist on the wire but are deliberately NOT exposed in v1.1 (deferred scope — note their existence in one line, plan nothing).
2. Exit-class documentation: only if 10-03 landed a new slug (it should NOT have) — README's exit table row + both agreement tests; otherwise verify `readme_exit_table_agreement` still passes untouched (no edit needed — the check IS the verification).
3. Full verification: `cargo fmt --check`, `cargo clippy --workspace -- -D warnings`, `cargo test --workspace`, `cargo build --workspace --no-default-features` (lean build), and the stdout-purity harness (Phase-8 CI) green with the new verbs in the tree.
  </action>
  <verify>README contains "eam task suspend" and the reconciliation note (grep "no agent-level"); `cargo test --workspace` fully green including readme_exit_table_agreement + stdout purity + parity; clippy/fmt clean.</verify>
  <done>README documents the full guarded write surface honestly (including what the wire does NOT offer); the entire suite + contract rituals pass with the new surface landed.</done>
</task>

</tasks>

<verification>
- `ign eam task suspend <name>` without --yes → exit 2, message names the task/agents/impact, ZERO mutation (binary test with a request-counting mock proves zero POSTs on refusal)
- With --yes against a mock → 200 envelope with the lifecycle result
- Parity test green at updated pinned count; Three-Place agreement test untouched and green
- Full workspace suite + fmt + clippy + lean build green
</verification>

<success_criteria>
All EAMW CLI behaviors demonstrable: guarded suspend/resume (SC-1), cancel + force-with-preview (SC-2 + EAMW-04), modify/delete (SC-3), blast-radius preview visible in every refusal BEFORE any write (SC-4). TUI parity holds. README honest. Live verification explicitly NOT in this plan (10-05).
</success_criteria>

<output>
After completion, create `.planning/phases/10-eam-write-operations/10-04-SUMMARY.md`
</output>
