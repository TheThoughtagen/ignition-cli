---
phase: 10-eam-write-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-eam-write-operations/10-LIVE-CAPTURES.md
  - .planning/phases/10-eam-write-operations/10-RIG-NOTES.md
autonomous: true

must_haves:
  truths:
    - "Every lifecycle/mutation wire shape used by 10-02's models is recorded VERBATIM and rig-labeled (8.3.3 + 8.3.6), or its non-capturability is recorded as an honest known limitation"
    - "The 12 research open questions each have a captured answer or a recorded 'could not capture, why' entry"
    - "A 'Decisions locked by captures' section exists and later plans can cite it for every vocabulary/model choice"
  artifacts:
    - path: ".planning/phases/10-eam-write-operations/10-LIVE-CAPTURES.md"
      provides: "Verbatim rig-labeled wire captures: suspend/resume/cancel 204s, PUT echo semantics, DELETE confirm body, signature-mismatch behavior, scheduled/running shape, taskState vocabulary, 404 shapes"
      min_lines: 80
    - path: ".planning/phases/10-eam-write-operations/10-RIG-NOTES.md"
      provides: "Ops log: container names, ports, EAM controller-provisioning recipe (or its honest failure), token NAMES only, teardown proof"
      min_lines: 20
  key_links:
    - from: ".planning/phases/10-eam-write-operations/10-LIVE-CAPTURES.md"
      to: "10-02 client models"
      via: "Decisions-locked-by-captures section consumed by model tasks"
      pattern: "Decisions locked by captures"
    - from: "10-RIG-NOTES.md"
      to: "10-05 live gate"
      via: "EAM controller-mode rig recipe reused for the WHK-gate scratch-task procedure"
      pattern: "controller"
---

<objective>
Capture the EAM task-lifecycle and config-mutation wire truth from live 8.3.3 + 8.3.6 rigs into `10-LIVE-CAPTURES.md` — BEFORE any client/model code is written (the 09-02 capture-first pattern; a locked decision for this phase).

Purpose: The research's mutation semantics are spec-read, not capture-proven (PUT echo behavior, delete `?confirm=` semantics, signature mismatch, rename support, taskState vocabulary). Guessing any of these ships a dishonest wire client. Every shape 10-02 models must be capture-backed or honestly marked uncaptured.

Output: `10-LIVE-CAPTURES.md` (verbatim, rig-labeled, Decisions-locked-by-captures) + `10-RIG-NOTES.md` (ops log), zero code changes.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-eam-write-operations/10-RESEARCH.md
@.planning/phases/09-agent-surface-api-diagnostics/09-RIG-NOTES.md
@.planning/phases/09-agent-surface-api-diagnostics/09-LIVE-CAPTURES.md
@.planning/phases/07-ecosystem-interop-advanced-ops/07-openapi-extract.json
@.planning/debug/eam-create-422.md
@.planning/debug/eam-working-definition.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spin both rigs, headlessly provision EAM controller mode, create a scratch task per rig</name>
  <files>.planning/phases/10-eam-write-operations/10-RIG-NOTES.md</files>
  <action>
Spin two disposable rigs per the proven Phase-9 recipe in 09-RIG-NOTES.md: 8.3.6 on host port 18188 (container `ign-p10-836`), 8.3.3 on 19188 (`ign-p10-833` — compose project shape per the 09 notes). Commission headlessly via the commissioner wire API (bootstrap → eula-accept → start-gateway → poll /StatusPing to plain `{"state":"RUNNING"}`), then headlessly token-provision API tokens (collection "core" + security-properties 403-fix ladder). Stage token keys in a chmod-600 /tmp scratch env sourced per command batch (they do NOT survive between tool calls — the 09-02 lesson); record token NAMES only, never keys.

Then the NEW part: attempt EAM controller-mode provisioning headlessly on each rig:
1. Probe the EAM config landscape first: GET `/data/api/v1/resources/list/com.inductiveautomation.eam/...` families + GET `/data/api/v1/resources/com.inductiveautomation.eam/module-settings` (in the 07 openapi extract) to see what a controller-mode gateway carries.
2. Attempt provisioning: an EAM gateway-network resource (so the gateway acts as EAM controller) — consult the extract + module-settings shape; if the resource type name is not discoverable, record that honestly and try the module-settings PUT route.
3. Create a scratch task definition per rig: array-body POST to `/data/api/v1/resources/com.inductiveautomation.eam/eam-tasks` seeded from `.planning/debug/eam-working-definition.json` (name it `ign-p10-scratch` — config.settings REQUIRED, the 422 trap in eam-create-422.md). Use an `eam_backup` profile, OnDemand schedule.

Fallback ladder (record which rung you landed on in RIG-NOTES): (a) headless controller provisioning works → proceed on both rigs; (b) controller mode cannot be established headlessly on a fresh rig → capture what a stock (non-controller) gateway answers for every probe (the 403 controller-gate shape is itself required evidence — 403 body + message verbatim per rig), and additionally capture success-shape probes against the WHK controller rig IF its env access is available (URL + token via existing IGNITION_LIVE_* conventions from tests/live_gateway.rs); the WHK probes use a `ign-p10-scratch` task created and deleted by the probe, nothing else; (c) neither → record all shapes as uncaptured with reasons, so 10-02 models from the openapi extract with honest provenance. Do NOT spend more than ~30 minutes attempting (b)'s WHK route — if creds are absent from the environment, stop and record.

Teardown ONLY after Task 2's captures are complete (rigs must stay up across both tasks; the task boundary here is provisioning-complete, not rig-down).
  </action>
  <verify>Both rigs answer authed `GET /data/api/v1/gateway-info` 200; per rig: either an authed GET of the scratch task's find record returns 200 with `config.profile.type=="eam_backup"`, OR the RIG-NOTES fallback rung is recorded with the verbatim refusal/error evidence. Token keys never appear in any file (grep for the key material returns nothing).</verify>
  <done>Both rigs live and authenticated; EAM controller state established or honestly recorded as unestablished per rig; scratch task present per the achieved rung; ops log written with names/ports/recipe/fallback-rung; zero secrets persisted.</done>
</task>

<task type="auto">
  <name>Task 2: Run the 12-question probe list per rig → 10-LIVE-CAPTURES.md, then full teardown</name>
  <files>.planning/phases/10-eam-write-operations/10-LIVE-CAPTURES.md, .planning/phases/10-eam-write-operations/10-RIG-NOTES.md</files>
  <action>
Probe per rig (8.3.6 AND 8.3.3, every observation verbatim + rig-labeled + timestamped, using curl with the token header like 09-LIVE-CAPTURES.md did). Probe list (= the research's 12 open questions, in dependency order):

1. **suspend→isSuspended sync**: GET find record (capture `config.profile.isSuspended` + `scheduledTaskState`) → POST `/data/eam/api/v1/eam-tasks/suspend/ign-p10-scratch` (capture exact status + body) → GET find read-back (isSuspended changed? scheduledTaskState changed?). Repeat with resume.
2. **taskState vocabulary + can* truth table**: GET `/data/eam/api/v1/eam-tasks/scheduled/true` and `/scheduled/false` (literal path segments — capture both), idle AND (if achievable) with a task pending/running; capture full `{name, owner, type, execStart, message, repeats, canPause, canResume, canCancel, taskState}` records. For a pending execution, also capture canCancel=true if reachable (force the scratch task first if needed and capture the follow-up scheduled read).
3. **DELETE confirm semantics**: `DELETE /data/api/v1/resources/com.inductiveautomation.eam/eam-tasks/{name}/{signature}` WITHOUT `?confirm=` first (capture the exact status + body — this decides 10-03's confirm policy), then WITH `?confirm=true&collection=eam-tasks` (capture the success 200 body: `{success, changes[{name,type,collection,newSignature}]}` shape).
4. **Signature mismatch**: attempt PUT (array body) and DELETE with a deliberately wrong signature — capture status + body verbatim (this decides whether a classify arm is needed; do NOT decide classification here, just record).
5. **Rename via PUT**: PUT the full record (read via find, single-element array, ORIGINAL signature + collection) with `name` changed — capture whether it renames, creates-new, or 4xx's. Clean up any created duplicate.
6. **PUT echo semantics**: find → clone FULL record → mutate ONE key (e.g. `enabled: false`) → PUT single-element array → capture the response AND a find read-back proving the mutation landed. Then a second PUT where config.settings is REMOVED (partial body) — capture the 422/error if the create-422 trap repeats on modify.
7. **404 shapes on runtime verbs**: POST suspend/{nonexistent-name} — capture status + body (Jetty HTML vs JSON decides 10-03's not-found handling).
8. **scheduled pagination**: capture the empty-list body of scheduled/false on a quiet rig + any pagination/metadata keys.
9. **PUT success response shape**: already in probe 6 — capture `{success, changes[]}` or whatever returns.
10. **problem{message, stacktrace}**: note on every mutation response whether `problem` appeared and when.
11. **Rig drift**: end every probe section with a per-rig status/shape diff note (8.3.3 vs 8.3.6).
12. **scheduleDetails wire form**: find read-back of the scratch task with scheduleMode OnDemand vs (create a second scratch `ign-p10-scratch-sched` if cheap) a Scheduled task — capture `config.profile.scheduleDetails` shape; then delete the second scratch task.

After captures: recreate-or-restore the scratch task state if probes consumed it (no requirement — rigs get torn down), then FULL teardown per the 09-02 discipline: `docker rm -f` both containers (and compose down for the 8.3.3 project), verify zero `ign-p10*` containers/volumes remain, ports 18188/19188 free, shred the /tmp token env (`shred -u` or `rm -P`).

Write 10-LIVE-CAPTURES.md in the 09-LIVE-CAPTURES.md format: header table (rig/version/port/container), one section per probe with verbatim JSON blocks per rig, and end with a **"Decisions locked by captures"** section enumerating: isSuspended sync behavior, taskState/currentState vocabulary (String set with per-element rig provenance), can* flag semantics, delete-without-confirm body → recommended CLI policy (RECOMMENDATION only — 10-03 decides), signature-mismatch status+body, rename semantics, PUT echo/partial semantics, 404 body kind, PUT success shape, scheduleDetails shape, per-rig drift. Also append the provisioning recipe + teardown proof to RIG-NOTES.
  </action>
  <verify>`grep -c "8.3.3" 10-LIVE-CAPTURES.md` and `grep -c "8.3.6" 10-LIVE-CAPTURES.md` both ≥ 12 (every probe section labeled per rig); the Decisions-locked-by-captures section covers all 12 probes (or explicit uncaptured entries); `docker ps -a | grep ign-p10` returns empty; ports 18188/19188 free; token keys absent from the repo (git status clean of stray files, /tmp env shredded).</verify>
  <done>10-LIVE-CAPTURES.md answers (or honestly marks uncapturable) each of the 12 research questions with verbatim rig-labeled evidence; Decisions section is citable by 10-02/10-03; rigs torn down with proof; zero secrets persisted.</done>
</task>

</tasks>

<verification>
- `grep -c "8.3.3" 10-LIVE-CAPTURES.md` ≥ 12 AND `grep -c "8.3.6" 10-LIVE-CAPTURES.md` ≥ 12
- Decisions-locked-by-captures section present and covers: isSuspended sync, taskState vocab, can* flags, delete confirm body, signature mismatch, rename semantics, PUT echo, 404 shape, PUT success shape, scheduleDetails
- `docker ps -a` shows no ign-p10* containers; lsof confirms 18188/19188 free
- `git status` — only the two planning docs added; no token material anywhere
</verification>

<success_criteria>
A later plan can cite 10-LIVE-CAPTURES.md for every wire shape it models, with per-rig provenance, and zero shapes were guessed. Both-rig coverage (or honest limitation records) for all 12 questions. Full teardown proven.
</success_criteria>

<output>
After completion, create `.planning/phases/10-eam-write-operations/10-01-SUMMARY.md`
</output>
