---
phase: 10-eam-write-operations
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ignition-core/tests/live_gateway.rs
  - .planning/phases/10-eam-write-operations/10-LIVE-CAPTURES.md
  - .planning/phases/10-eam-write-operations/10-LIVE-GATE.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "The live gate runs its full scratch lifecycle end-to-end and exits 0 on a disposable EAM controller rig: create → Scheduled+cron flip → suspend with retry → §2 poll-until-vanish → resume → delete → not_found post-proof"
    - "The §2 suspended-vocabulary check polls (~90s deadline) instead of false-failing on the captured transient grace row (taskState=Suspended still listed in scheduled/false immediately post-suspend)"
    - "A mid-test panic inside the tokio test runtime still runs the scratch task's remote cleanup — no 'Cannot start a runtime from within a runtime', the cleanup request provably hits the mock"
    - "10-LIVE-CAPTURES.md §2 records the grace-period row shape with UAT-rig provenance (2026-09-10)"
    - "10-LIVE-GATE.md §5 carries the verbatim passing run record; §6 records the disposable-controller-rig substitution (UAT-recorded decision) and closes SC-5 on that basis"
  artifacts:
    - path: "crates/ignition-core/tests/live_gateway.rs"
      provides: "poll-until-vanish §2 step + in-runtime-safe ScratchTaskGuard::drop + non-ignored wiremock unwind-mechanism test"
      contains: "spawn_blocking"
    - path: ".planning/phases/10-eam-write-operations/10-LIVE-CAPTURES.md"
      provides: "§2 vocabulary extension: transient grace row (taskState=Suspended in scheduled/false, vanished ~48s)"
      contains: "grace"
    - path: ".planning/phases/10-eam-write-operations/10-LIVE-GATE.md"
      provides: "§5 run record (verbatim gate output) + §6 SC-5 verdict update"
      contains: "run record"
  key_links:
    - from: "crates/ignition-core/tests/live_gateway.rs (§2 step)"
      to: "api.eam_tasks_scheduled(false) polled in a deadline loop"
      via: "poll loop, 10s interval, ~90s deadline, grace-row tolerant"
      pattern: "eam_tasks_scheduled"
    - from: "crates/ignition-core/tests/live_gateway.rs (ScratchTaskGuard::drop)"
      to: "Handle::try_current → spawn_blocking → fresh current_thread runtime + block_on cleanup"
      via: "blocking-pool route that is joined at runtime drop (legal off the async context)"
      pattern: "spawn_blocking"
    - from: "live_gateway.rs (new non-ignored test)"
      to: "wiremock find+delete mocks"
      via: "catch_unwind drop inside the runtime, then poll wiremock received requests"
      pattern: "wiremock"
---

<objective>
Close UAT gap 2 (test 12): the SC-5 live gate aborted at its own §2 assertion — a single-shot absence check raced the captured ~48s suspend-vanish latency and hit a never-captured grace-period row (taskState=Suspended still in scheduled/false). Secondary: ScratchTaskGuard::drop could not clean up when dropped during unwind inside the test runtime ("Cannot start a runtime from within a runtime").

Purpose: the guarded EAM writes are live-proven through suspend only; the gate must complete its full lifecycle (resume → delete → not_found) so SC-5 closes on a passing run, and a failed run must never strand scratch tasks on the rig.

Output: fixed gate (poll-until-vanish + in-runtime-safe Drop guard), extended §2 vocabulary record, a passing live-gate run recorded in 10-LIVE-GATE.md §5, teardown clean.

DECISION HONORED (10-UAT.md test 12, recorded): disposable-controller-rig substitution supersedes 10-05's WHK-only constraint — the user stated WHK has no EAM. The live re-run rides a disposable rig per 10-RIG-NOTES.md.
</objective>

<execution_context>
@/Users/pmannion/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-eam-write-operations/10-UAT.md
@.planning/phases/10-eam-write-operations/10-LIVE-CAPTURES.md
@.planning/phases/10-eam-write-operations/10-LIVE-GATE.md
@.planning/phases/10-eam-write-operations/10-RIG-NOTES.md
@crates/ignition-core/tests/live_gateway.rs
@crates/ignition-core/src/client/eam.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: §2 poll-until-vanish + grace-row tolerance, vocabulary record extended</name>
  <files>crates/ignition-core/tests/live_gateway.rs, .planning/phases/10-eam-write-operations/10-LIVE-CAPTURES.md, .planning/phases/10-eam-write-operations/10-LIVE-GATE.md</files>
  <action>
In `crates/ignition-core/tests/live_gateway.rs`, replace the single-shot §2 step (current lines ~825-836: one `eam_tasks_scheduled(false)` read + absence assert) with a poll-until-vanish loop modeled on the existing resume-reappear loop (lines ~853-865):
  - Poll every 10s with a ~90s deadline (capture evidence: vanish observed at ~48s — 10-LIVE-CAPTURES §2; 90s gives headroom without dragging).
  - While the scratch row is present: if `row.task_state == "Suspended"`, `eprintln!` the grace-row observation ("grace-period row: taskState=Suspended still listed in scheduled/false — transient, UAT rig 2026-09-10") and keep polling. Any other taskState on the scratch row: log it and keep polling too (deadline decides, not the first read).
  - Panic only at deadline, with diagnostics: whether a grace row was seen, and the last observed taskState.
  - Keep the success `eprintln!` (mention vanish latency if cheaply measurable).
  - The row model is `EamScheduledTask` with `task_state: String` (crates/ignition-core/src/client/eam.rs:266-305).

Extend `10-LIVE-CAPTURES.md` §2: after the "Observed taskState set" paragraph (line ~158), add the grace-period row record — immediately post-suspend the scratch row appeared in scheduled/false with `taskState:"Suspended"` (never seen in the original 12-probe captures), transient, vanished within the ~48s window; provenance: UAT rig 8.3.6, 2026-09-10 (10-UAT.md test 12). State the consumer implication: suspended tasks leave scheduled/false EVENTUALLY, not immediately — checks must poll.

Update `10-LIVE-GATE.md` §3 step 6 wording to match the implemented behavior (poll-until-vanish, ~90s deadline, grace-row tolerant) — §3 claims to describe the committed test and must stay truthful.
  </action>
  <verify>cargo test -p ignition-core --test live_gateway compiles clean (the lifecycle test stays #[ignore]); `grep -n "grace" .planning/phases/10-eam-write-operations/10-LIVE-CAPTURES.md` finds the new record; §3 step 6 names the poll window.</verify>
  <done>§2 absence check is a deadline-bounded poll tolerating the captured grace row; the captures doc records the row shape with provenance; the gate-protocol doc matches the code.</done>
</task>

<task type="auto">
  <name>Task 2: ScratchTaskGuard::drop works inside the test runtime + wiremock unwind proof</name>
  <files>crates/ignition-core/tests/live_gateway.rs</files>
  <action>
Restructure `ScratchTaskGuard::drop` (current lines ~631-680) so cleanup works when the guard is dropped during unwind INSIDE the test runtime (the root cause: `block_on` from within a runtime panics "Cannot start a runtime from within a runtime"; catch_unwind swallows the panic but the cleanup never runs):
  - Extract the cleanup future into a shared async fn (find → derive signature → delete → honest eprintln logs) used by every path.
  - If `tokio::runtime::Handle::try_current()` succeeds (dropped inside a runtime — the #[tokio::test] unwind case): route cleanup through `handle.spawn_blocking(move || { build a fresh current_thread runtime; block_on(cleanup_future) })`. Use spawn_blocking, NOT `tokio::spawn`: a spawned async task may never be polled after unwind (runtime shutdown cancels pending tasks), while the blocking pool is JOINED at runtime drop — and a spawn_blocking thread is not an async context, so a fresh runtime + block_on is legal there.
  - Else (no ambient runtime): keep the existing fresh-current-thread-runtime path verbatim.
  - Keep catch_unwind + the disarmed fast path + all existing eprintln log wording style.

Add a NON-ignored mechanism test in the same file proving the fix: `#[tokio::test]` that (1) starts a wiremock server mocking the eam_task_find route (200 record with a signature — copy the fixture shape from the existing wiremock tests in crates/ignition-core/src/client/eam.rs) and the delete route (200 `success:true`), (2) builds a ScratchTaskGuard pointed at the wiremock URL, (3) inside `std::panic::catch_unwind(AssertUnwindSafe(...))` opens a scope owning the guard and panics — the guard drops mid-unwind inside the runtime, exactly the failure shape from the UAT gate run, (4) after catching, polls the wiremock received requests (e.g. 1s-interval loop, ~10s cap) and asserts the cleanup find + delete requests ARRIVED. This is the request-level proof that the nested-runtime panic is gone.
  </action>
  <verify>cargo test -p ignition-core --test live_gateway (non-ignored only) passes: the new unwind test runs green, the ignored lifecycle test still compiles; cargo clippy -p ignition-core clean.</verify>
  <done>A guard dropped during unwind inside the test runtime performs its remote cleanup (requests proven at the mock), while the standalone path is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Live gate re-run on a disposable controller rig + record the run</name>
  <files>.planning/phases/10-eam-write-operations/10-LIVE-GATE.md</files>
  <action>
Provision (or reuse) a disposable EAM controller rig and run the fixed gate end-to-end:
  1. Pre-flight: `docker ps -a` for an existing UAT rig (container `ign-uat-836`, port 18188) — if it is RUNNING and answers `GET /StatusPing`, reuse it; if gone, re-provision per 10-RIG-NOTES.md: `docker run -d --name ign-uat-836 -p 18188:8088 -e ACCEPT_EULA=Y -e GATEWAY_ADMIN_PASSWORD=password -e IGNITION_EDITION=standard inductiveautomation/ignition:8.3.6` → headless commissioning (GET /bootstrap → POST /post-step eula-accept → POST /post-step start-gateway → poll /StatusPing) → provision a token via the /tmp/ign-p9-rigs/provision_token.sh recipe (recreate the script from RIG-NOTES if /tmp was cleared) → EAM controller flip (singleton GET → clone → installMode:"Controller" → array PUT; verify scheduled/false answers 200 `{"items":[]...}`).
  2. Stage the token OUTSIDE the repo (chmod 600 /tmp env file), source it in the same shell batch as the run — token KEYS never enter the repo (RIG-NOTES hygiene).
  3. Run: `IGNITION_LIVE_URL=http://localhost:18188 IGNITION_LIVE_TOKEN=<name:key> cargo test -p ignition-core --test live_gateway live_eam_write_lifecycle -- --ignored --nocapture`. Expect exit 0 through the FULL lifecycle — including the new §2 poll (the grace row may appear; the poll tolerates it) — through resume, delete, and the not_found post-proof.
  4. If a step fails live: capture the verbatim output, fix NOTHING by guesswork — record drift in 10-LIVE-GATE.md §4 (the §4 contract names wire-shape fixes as follow-up gap work) and stop. One honest retry of a transient-only failure is allowed; log both attempts.
  5. Append the verbatim run record to 10-LIVE-GATE.md §5 (per-step outcomes + timestamps + the §2 poll result). Update §6: SC-5 live-verified on the disposable controller rig per the UAT-recorded substitution decision (user-stated WHK has no EAM); note the substitution supersedes 10-05's WHK-only constraint.
  6. Teardown: `docker rm -f -v ign-uat-836`; shred the token env file; verify ports free and zero `ign-uat*` / `ign-live-scratch*` leftovers (the gate's own delete + the Drop guard cover the scratch task — confirm `ign eam` find answers 404 before teardown).
  </action>
  <verify>The cargo test command exits 0 with the full step log (create → flip → suspend → §2 poll-vanish → resume → delete → not_found); 10-LIVE-GATE.md §5 contains the run record and §6 the verdict; `docker ps -a | grep ign-uat` is empty afterward.</verify>
  <done>The live gate completes its full scratch lifecycle on a real controller and SC-5 closes on a recorded passing run — no stranded scratch task, no leaked token material.</done>
</task>

</tasks>

<verification>
- `cargo test -p ignition-core --test live_gateway` — the non-ignored unwind test passes; the ignored gate compiles.
- `cargo clippy -p ignition-core` clean.
- Live run (Task 3) exits 0 through the complete lifecycle, including the new §2 poll.
- 10-LIVE-CAPTURES.md §2 grace-row record present with provenance; 10-LIVE-GATE.md §5 populated, §6 updated, §3 truthful.
- Rig torn down; no scratch task, container, or token material left behind.
</verification>

<success_criteria>
The UAT gap "Live gate completes the full scratch lifecycle: create → flip → suspend → §2 vocabulary check → resume → delete → not_found post-proof" is closed: the gate passes end-to-end on a real disposable controller with the poll-based §2 check and the runtime-safe Drop guard, and the run is recorded per the gate's own contract.
</success_criteria>

<output>
After completion, create `.planning/phases/10-eam-write-operations/10-06-SUMMARY.md`
</output>
